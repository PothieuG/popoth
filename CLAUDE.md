# CLAUDE.md — Popoth

> Guide à charger en début de chaque session Claude Code sur ce repo. Garde-le à jour si une convention change.

## 1. Projet

**Popoth** : PWA francophone de gestion financière personnelle et en groupe. Domaines clés : budgets estimés, dépenses réelles, économies cumulées, tirelire commune, récap mensuel, transferts inter-budgets.

Prod hébergée sur Supabase (`jzmppreybwabaeycvasz`). Audit complet 2026-04 dans [docs/audit/](docs/audit/) (score 47/100 avant Sprint 0, ~51 après Sprint 0, ~58 après Sprint DB, ~62-65 après Sprint Refactor, ~70 après Sprint Hardening, ~73 après Sprint Polish, ~75 après Sprint Audit-Triggers, ~76 après Sprint Audit-Functions-v2, ~77 après Sprint Cleanup-Legacy, ~78 après Sprint Polish-CI, ~79 après Sprint Hygiene-CI, ~80 après Sprint Code-CI, ~81 après Sprint DX-Verify, ~82 après Sprint Stabilize-Deps + Sprint Align-PackageJson, ~83 après Sprint Lint-Baseline-Cleanup, ~85 après Sprint Lint-Followups, ~86 après Sprint Hygiène-Code, ~87 après Sprint Hygiène-Code-v2, ~89 après Sprint Refactor-Architecture, ~90 après Sprint Refactor-Architecture-v2, ~91 après Sprint Refactor-Architecture-v3, ~92 après Sprint Refactor-Architecture-v4, ~93 après Sprint Refactor-Architecture-v5, ~94 après Sprint 1 (Tooling), ~95 après Sprint 1.5 (React Hooks v7 refactor + TanStack Query), ~96.5 après Sprint 2 + Sprint 2-followup, ~97 après Sprint 2-followup-v2 (useGroups invalidation cascade + AuthContext disable narrowing), ~97.5 après Sprint 2-followup-v3 (FK ON DELETE SET NULL pinned + AuthContext useReducer migration), ~98 après Sprint 2-followup-v4 (authReducer pure-unit tests + Context memoize), ~98.2 après Sprint 2-followup-v5 (useAuth.ts dead code purge), ~98.2 stable après Sprint Audit-Closeout (closeout administratif du plan d'action 2026-04-29), ~98.2 stable après Sprint Cleanup-I8 / Lot 1 (filet logger + strip prod), ~98.2 stable après Lot 3 (middleware + expense-allocation, 7 sites), ~98.2 stable après Lot 4a (app/api/groups/**, 22 sites → 11/11 triage Modéré), ~98.2 stable après Lot 4b (app/api/monthly-recap/{...9 routes simples}, 132 sites → 113/19 triage agressif, lint baseline 990 → 819), ~98.2 stable après Lot 4c (app/api/{profile,savings/data,bank-balance}, 52 sites → 43/9 triage strict, lint baseline 819 → 783), ~98.2 stable après Lot 4d (app/api/savings/transfer, 38 sites → 32/6 triage strict avec 3 cleanup-attempts CRITIQUES préservés, lint baseline 783 → 751), ~98.2 stable après Lot 4e (lib/api/finance/, 12 fichiers, 152 sites → 119/33 triage strict avec 3 cleanup-attempts CRITIQUES + 1 fallback 200-on-error préservés, lint baseline 751 → 618), ~98.2 stable après Lot 5 (couche client : 30 fichiers components+hooks+contexts+pages, 193 sites → 133/60 triage strict avec 5 cleanup-attempts CRITIQUES + 1 boot-path PWA préservés, lint baseline 618 → 490), ~98.2 stable après Lot 5b (3 fichiers serveur orphelins : auth/session + recover triage strict + status-test DELETE, 16 sites → 8 DROP / 4 KEEP / 4 file-deleted avec 2 cleanup-attempts CRITIQUES préservés — auth/session L56 Supabase auth réussi mais JWT session fail + recover L306 rollback partiel snapshot peut rester actif, lint baseline 490 → 482), **~98.2 stable après Lot 5c (8 fichiers libs server-side foundationnels indépendants I4/I5 : lib/{auth,session,session-server,supabase-client,database-snapshot,monthly-recap-calculations}.ts + app/auth/confirm/route.ts + DELETE testSupabaseConnection() dead code, 45 sites distincts (audit a sous-compté monthly-recap à 13 vs 19 réels, 2 outer catches manqués) → 23 KEEP+migrate / 18 DROP triage Modéré 56/44 avec **1 cleanup-attempt CRITIQUE préservé** (app/auth/confirm L46 OTP réussi mais data.user manquant) + **5 audit-trail CRITIQUES préservés** (database-snapshot L169-173 multi-table snapshot foundational pour recovery), lint baseline 482 → 461)**, **~98.2 stable après Lot 5d (6 fichiers `app/api/debug/**` toutes gardées par `blockInProduction()` 404 prod : financial + group-financial + remaining-to-live + reset-all + reset-budgets + retrigger-recap, **57 sites annoncés / 64 statements réels** — audit a sous-compté financial+group-financial blocs summary multi-line 9-10 lignes en 1 site chacun → recount précis = 17+17+3+4+17+6 = 64 → **45 DROP / 12 KEEP+migrate / 7 statements multi-line dans 2 blocs summary** triage Strict 79/21 avec **0 cleanup-attempt CRITIQUE** (routes atomiques Supabase, pas de rollback) + 6 outer catches préservés (binding `error.message` utilisé dans returns 500) + 5 inline DB error discriminants (reset-budgets) + 1 inline warn rule c silently-swallowed (retrigger-recap snapshot deactivation), `lib/debug-guard.ts` confirmé clean 0 sites (glob future-proof seulement), lint baseline 461 → 406)\*\*, **~99 après Sprint Refactor-I4** (chantier majeur 2026-05-11 : god file `lib/financial-calculations.ts` 1069 LOC supprimé et splitté en 8 modules sous `lib/finance/` ; scope-cast `as unknown as SupabaseClient` 2 → 1 ; Lot 2 console-cleanup officiellement clos par I4 lint 406 → 307 −99 warnings ; 24 nouveaux tests dont 19 promus depuis caractérisation + 5 unit-mock snapshot dispatcher ; fail-soft contracts R1 snapshot Promise<boolean> + R2 EMPTY_FINANCIAL_DATA fallback préservés verbatim). Carte du schéma à jour dans [docs/db/SCHEMA.md](docs/db/SCHEMA.md) (inclut l'inventaire complet des triggers prod).

## 2. Stack

- **Next.js 16.1.6** App Router, Turbopack en build, **webpack en dev** (`pnpm dev` → `next dev --webpack`)
- **React 19.1.1** + **TanStack Query 5.100.9** pour le data-fetching (provider mounted dans [app/layout.tsx](app/layout.tsx) ; tous les hooks fetcher migrés au Sprint 1.5)
- **TypeScript 5** strict + `noUncheckedIndexedAccess` + `verbatimModuleSyntax` (imports type-only obligatoires)
- **Tailwind 3** + **shadcn/ui** (variant new-york)
- **Supabase** (`@supabase/supabase-js@^2.57.4`) — PostgreSQL + Auth
- **pnpm 9.15.5** (verrouillé via `packageManager` + `engines.pnpm >=9.0.0` depuis Sprint Align-PackageJson / P2), Node ≥ 20.10.0 (`engines.node` + [`.nvmrc`](.nvmrc) pinned `20` LTS major depuis Sprint Align-PackageJson / P2)
- **Vitest 4.1.5** pour les tests unitaires (Sprint 0 — environnement `node`)
- **eslint-config-next 16.2.6** + **eslint 9.39.4** (Sprint 1 — upgrade depuis 15.0.0 + eslint v8). Native flat config natif (les exports `eslint-config-next/core-web-vitals` + `eslint-config-next/typescript` retournent directement des `Linter.Config[]`, plus de `FlatCompat`). Voir [eslint.config.mjs](eslint.config.mjs).

## 3. Commandes

| Commande                                               | Effet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm dev`                                             | Serveur dev Next.js (webpack)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `pnpm build`                                           | Build prod (Turbopack)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `pnpm start`                                           | Serveur prod                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `pnpm typecheck`                                       | `tsc --noEmit` (BLOQUANT — `ignoreBuildErrors` retiré en C1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `pnpm lint:check`                                      | ESLint sans `--fix`. **BLOQUANT** depuis Sprint Lint-Baseline-Cleanup — la baseline 125 errors + 11 warnings a été nettoyée à zéro et `code-checks.yml` exige désormais exit 0 sur tout PR / push `cleanup`. Tout nouveau `: any`, `no-unused-vars`, ou warning `exhaustive-deps` non-justifié sort la PR rouge.                                                                                                                                                                                                                                                            |
| `pnpm lint`                                            | ESLint avec `--fix` (auto-fixes les rares cas auto-fixables — `prefer-const`, etc. — exit 0 attendu sur la baseline propre).                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `pnpm lint:fix`                                        | Alias de `pnpm lint` (Sprint Align-PackageJson / P2 — ajouté pour conformité au template canonique `lint`/`lint:fix`/`lint:check`).                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `pnpm format`                                          | Prettier `--write` sur tout le repo (Sprint 1). Idempotent ; à utiliser avant un commit volumineux ou après un changement large de fichiers config.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `pnpm format:check`                                    | Prettier `--check` sans écriture. **BLOQUANT** en CI depuis Sprint 1 — `code-checks.yml` exige exit 0 sur tout PR / push `cleanup`. Le hook `pre-commit` (lint-staged) prévient localement la majorité des régressions.                                                                                                                                                                                                                                                                                                                                                     |
| `pnpm ci`                                              | Chaîne code-side : `typecheck` + `lint:check` + `format:check` + `test:run` + `build` (Sprint Align-PackageJson / P2 + Sprint 1). Exit 0 attendu. À invoquer via `pnpm run ci` (le bareword `pnpm ci` invoque le verb npm `ci` non implémenté par pnpm).                                                                                                                                                                                                                                                                                                                    |
| `pnpm test`                                            | Vitest watch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `pnpm test:run`                                        | Vitest single run (CI)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `pnpm test:coverage`                                   | Vitest single run avec rapport de couverture v8 (Sprint Templates-Triage). `@vitest/coverage-v8` est installé depuis Sprint 1.5. Exit 0 attendu, écrit dans `coverage/` (gitignored). Utile avant/après les chantiers I4 / I5 / Zod / console.log pour mesurer un signal de qualité. **Pas dans `pnpm verify` ni `pnpm ci`** — coverage est ad-hoc, pas un gate.                                                                                                                                                                                                            |
| `pnpm db:types`                                        | Régénère `lib/database.types.ts` depuis le schéma prod (Sprint DB / D6). Le script écrit lui-même dans le fichier — appeler **sans redirection** (`pnpm db:types`, pas `pnpm db:types > ...`). Sprint Polish-CI / D1 a déplacé le `>` à l'intérieur du script pour ne plus capturer l'en-tête wrapper pnpm. **Sprint Code-CI / F2** a aligné le flag sur `--project-id jzmppreybwabaeycvasz` (au lieu de `--linked`) — fonctionne dans un fresh clone sans `supabase link` préalable, output byte-identique.                                                                |
| `pnpm db:diff`                                         | Wrapper `supabase db diff` (Sprint Align-PackageJson / P2 — ajouté pour conformité au template). Génère un diff schéma local↔remote. Fonctionne uniquement si un `supabase link` a été fait. **Pas dans le workflow réel** : le repo utilise `node scripts/export-schema.mjs` + `pnpm db:check-drift` à la place (pattern API Management sans Docker).                                                                                                                                                                                                                      |
| `pnpm db:reset`                                        | Wrapper `supabase db reset` (Sprint Align-PackageJson / P2 — ajouté pour conformité au template). **⚠️ Nécessite Docker** pour la stack Supabase locale. Le repo n'utilise pas Docker, donc ce script échouera dans l'environnement courant. Reste en place pour les forks qui auraient une stack locale.                                                                                                                                                                                                                                                                   |
| `pnpm db:check-drift`                                  | Compare prod ↔ baseline `20260101000000_remote_schema.sql`. Exit 0 = clean, 1 = drift (Sprint Refactor / R4). Idempotent face aux CRLF Windows depuis Sprint Polish-CI / D2 (normalisation `\r\n → \n` avant `stripTimestamp`, sinon le regex JS ne consommait pas le `\r` et l'offset générait un faux positif global).                                                                                                                                                                                                                                                    |
| `pnpm db:check-rpcs`                                   | Vérifie via `pg_proc` que les 4 RPC C3 (`update_piggy_bank_amount`, `update_bank_balance`, `update_budget_cumulated_savings`, `transfer_from_piggy_to_budget`) existent en prod. Exit 0 = présentes, 1 = manquantes (Sprint Hardening / H4)                                                                                                                                                                                                                                                                                                                                 |
| `pnpm db:check-functions`                              | Vérifie la présence des 4 fonctions trigger custom A2 (liste hardcodée). Exit 0 = présentes, 1 = manquantes (Sprint Audit-Triggers / A3)                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `pnpm db:check-types-fresh`                            | Vérifie que `lib/database.types.ts` correspond à ce que `supabase gen types --project-id <ref>` produirait à l'instant T. Exit 0 = synchro, 1 = stale + diff sur stdout, 2 = fatal (Sprint Hygiene-CI / E2). Comble le trou que `db:check-drift` ne couvre pas (drift schéma SQL ≠ drift types générés). Utilise `--project-id` (pattern partagé avec `db:types` depuis Sprint Code-CI / F2) pour fonctionner dans un fresh clone / CI sans `supabase link` préalable — output byte-identique à `--linked`. À lancer après chaque migration touchant une table/colonne/RPC. |
| `pnpm db:audit-functions`                              | **Audit générique** : liste TOUTES les `public.*` fonctions de `pg_proc`, vérifie que chacune est versionnée dans `supabase/migrations/`. Exit 0 = toutes versionnées, 1 = au moins une orpheline (Sprint Audit-Functions-v2 / B1). À lancer après chaque migration touchant une fonction PL/pgSQL.                                                                                                                                                                                                                                                                         |
| `pnpm db:audit-objects`                                | **Audit générique étendu** : énumère 5 catégories d'objets `public.*` (functions, composite types, enums, domains, operators) via `pg_catalog` et vérifie que chacun a un `CREATE` dans `supabase/migrations/`. Exit 0 = clean, 1 = orphelin (Sprint Cleanup-Legacy / C2). Filtre les row-types implicites de `CREATE TABLE`. À lancer après toute migration ajoutant un `CREATE TYPE` / `CREATE DOMAIN` / `CREATE OPERATOR`. **Pas dans le cron**.                                                                                                                         |
| `pnpm verify`                                          | **Meta-script sanity sweep** : enchaîne `typecheck` + `test:run` + les 6 `db:*` checks (drift, rpcs, functions, types-fresh, audit-functions, audit-objects) avec **fail-fast** (`&&`). Exit 0 = tout vert, sinon code de la première étape qui fail (les suivantes ne sont pas spawnées). À lancer après chaque sprint pour clôturer la sanity sweep en une commande. ~36s en local. Sprint DX-Verify / G1. **Outil dev local — pas de wirage CI** (le PR-time gate F1 + cron weekly H5 couvrent déjà CI side).                                                            |
| `pnpm supabase ...`                                    | Supabase CLI (lié à `jzmppreybwabaeycvasz`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `node scripts/export-schema.mjs <out.sql>`             | Snapshot du schéma prod via API Management (sans Docker)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `node scripts/apply-sql.mjs <file.sql>`                | Applique un fichier SQL (write OU read-only SELECT) via API Management                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `node scripts/apply-sql.mjs scripts/list-triggers.sql` | Inventaire trigger prod (Sprint Polish T5). Output JSON UTF-16 sur stdout — rediriger via `> file.json` puis lire (PowerShell encode en UTF-16 LE par défaut)                                                                                                                                                                                                                                                                                                                                                                                                               |

### Hooks Git (Husky)

Sprint Lint-Followups / item 3 a installé `husky@^9.1.7` + un seul hook **pre-push** ([.husky/pre-push](.husky/pre-push)) qui lance `pnpm lint:check && pnpm typecheck` avec **fail-fast**. Sprint 1 a ajouté un hook **pre-commit** ([.husky/pre-commit](.husky/pre-commit)) qui lance `pnpm lint-staged` — prettier `--write` + eslint `--fix` sur les seuls fichiers staged (config dans [package.json](package.json) → `lint-staged`). Première ligne de défense locale (rapide, fichiers staged uniquement) ; le pre-push reste le filet final full-tree avant l'arrivée du PR gate `code-checks.yml`. Bypass d'urgence via `git commit --no-verify` / `git push --no-verify` toujours possible. Le `prepare` script dans [package.json](package.json) ré-arme automatiquement `.husky/_/` à chaque `pnpm install` — note : si le hook ne fire pas après un fresh clone, lancer `pnpm exec husky` manuellement (le `prepare` lifecycle peut ne pas s'exécuter selon le flag `--ignore-scripts`). commitlint **non installé** (Sprint 1 décidé de skip — convention CLAUDE.md §6 documentée par confiance).

Tests gated (env var requise pour s'exécuter, sinon `describe.skipIf` skippe) :

- `SUPABASE_RPC_CONCURRENCY_TESTS=1 pnpm test:run` — couverture RPC concurrence (D9)
- `SUPABASE_RLS_TESTS=1 pnpm test:run` — isolation cross-user RLS (D4)
- `SUPABASE_API_TESTS=1 pnpm test:run` — régressions H1/H2/R2 (Sprint Polish T3) sur dashboard aggregates, expenses progress, available cash + couverture wrapper `withAuth`/`withAuthAndProfile` (Sprint Refactor-Architecture-v5, [lib/api/**tests**/with-auth.test.ts](lib/api/__tests__/with-auth.test.ts) — 12 cas : auth, expired payload, overloads static/dynamic, profile shape, isolation parallèle)
- `SUPABASE_TRIGGER_TESTS=1 pnpm test:run` — comportement des 4 fonctions trigger A2 (Sprint Audit-Functions-v2 / B2) — auto-create contributions sur JOIN, recalc sur UPDATE budget, cleanup sur DELETE group, touch `updated_at`
- `SUPABASE_FINANCE_TESTS=1 pnpm test:run` — round-trip `_loadFinancialData` profile+group (Sprint Refactor-I4 follow-up, [lib/finance/**tests**/financial-data.test.ts](lib/finance/__tests__/financial-data.test.ts) — 6 cas : profile/group golden math + edge cases sans estimés + fail-soft sur UUID inconnu + `saveRavToDatabase` round-trip via `bank_balances.current_remaining_to_live`)

## 4. Structure du repo

```
app/                       # App Router (pages + API routes)
  api/
    debug/                 # 6 routes seed/reset (post Sprint Polish T2) — BLOQUÉES en prod via blockInProduction()
    finance/               # ✅ Sprint Refactor-Architecture v1+v2 — namespace canonique unifié (12 routes)
                           #   summary, rav, budgets (POST/PUT/DELETE only), budgets/estimated, incomes,
                           #   income/{real,estimated,progress}, expenses/{real,add-with-logic,preview-breakdown,progress}
                           #   Toutes ré-exportent les handlers depuis lib/api/finance/<route>.ts
                           #   Sprint v2 a supprimé /api/finance/dashboard (0 consumer, 457 LOC dead code)
                           #   et le GET sur /api/finance/budgets (read passe par /budgets/estimated)
    monthly-recap/         # workflow récap mensuel (process-step1 = god route, ne pas refactor)
    savings/transfer/      # transferts budget↔budget et budget↔tirelire
components/                # UI (shadcn/ui sous components/ui/)
  providers/QueryProvider.tsx # ✅ Sprint 1.5 — 'use client' wrapper QueryClientProvider + ReactQueryDevtools (dev only). Mounted outermost in app/layout.tsx
contexts/AuthContext.tsx   # ✅ Sprint Hygiène-Code — split en `AuthUserContext` + `AuthActionsContext` ; hooks `useAuthUser()` / `useAuthActions()`. Sprint 2-followup-v5 — l'aggregator `useAuth()` (qui spread les deux contexts) a été supprimé : 0 consumer en prod. Si jamais un consumer en a besoin, recréer ad-hoc.
hooks/                     # 20 hooks React — ✅ Sprint 1.5 : 11 hooks fetcher migrés sur TanStack Query (useQuery + useMutation)
  useRavValidation.ts      # ✅ Sprint Refactor-Architecture — useMemo pur, validation { blocked, newRav } pour AddTransactionModal
  useStep1Data.ts          # ✅ Sprint 1.5 — useQuery({ queryKey: ['step1-data', context] }) ; { data, loading, error, refresh } shape preserved
  useBudgetProgress.ts     # ✅ Sprint Refactor-Architecture — dedupe state + sync effect → return useMemo direct
  useFinancialData.ts      # ✅ Sprint 1.5 — useQuery natif. Sprint 2 — bridge legacy `triggerFinancialRefresh`/`registerFinancialRefreshCallback` supprimé ; les mutations cross-domain invalident `['financial-summary']` + `['progress-data']` + `['budgets']` via [`invalidateFinancialRefreshes(qc)`](lib/query-client.ts) (single source of truth, Sprint 2-followup). Sprint 2-followup — bloc debug `console.log` ~17 lignes dans queryFn supprimé (dump du payload financier à chaque fetch ; data toujours visible via TanStack Query DevTools si besoin)
  useProgressData.ts       # ✅ Sprint 1.5 — useQuery({ queryKey: ['progress-data', context] }) ; le contextRef pattern C ref site est éliminé natif via queryKey. Sprint 2 — bridge effect supprimé (les mutations cross-domain invalident `['progress-data']` directement)
  useBudgets.ts            # ✅ Sprint 1.5 — useQuery + 3 useMutation (add/update/delete). Sprint 2 — bridge effect supprimé ; les mutations CRUD invalident `['financial-summary']` + `['progress-data']` + `['budgets']` directement via [`invalidateFinancialRefreshes`](lib/query-client.ts) (Sprint 2-followup — extraction du helper local vers single source of truth)
  useIncomes.ts            # ✅ Sprint 1.5 — useQuery + 3 useMutation
  useRealExpenses.ts       # ✅ Sprint 1.5 — useQuery + 3 useMutation (smart-allocation : mutation result peut être null si dépense couverte 100% par piggy/savings)
  useRealIncomes.ts        # ✅ Sprint 1.5 — useQuery + 3 useMutation
  useProfile.ts            # ✅ Sprint 1.5 — useQuery + 2 useMutation (create/update)
  useGroups.ts             # ✅ Sprint 1.5 — useQuery + 5 useMutation (create/update/delete/join/leave) avec optimistic updates via setQueryData
  useExpenseProgress.ts    # ✅ Sprint 1.5 — useQuery (fetcher pur) ; renvoie Record<budgetId, ExpenseProgress>
  useIncomeProgress.ts     # ✅ Sprint 1.5 — derived state via useMemo direct (pas Query, dépend de useRealIncomes)
lib/
lib/
  query-client.ts          # ✅ Sprint 1.5 — createQueryClient() factory (staleTime 30s, refetchOnWindowFocus false, retry 1) ; consommé par components/providers/QueryProvider.tsx
  supabase-server.ts       # client serveur (service_role) — BYPASS RLS
  supabase-client.ts       # client browser (anon key) — soumis à RLS
  database.types.ts        # types Supabase générés (pnpm db:types) — Sprint DB D6 (inclut désormais les 4 RPC C3 depuis le regen Sprint Cleanup-Legacy / C1, augmentation lib/database.ts supprimée en Sprint Polish-CI / D3)
  session.ts               # JWT (jose) pour cookie session
  session-server.ts        # validateSessionToken() — utilisé dans toutes les routes API
  debug-guard.ts           # blockInProduction() pour /api/debug/*
  expense-allocation.ts    # calculateBreakdown + applyAllocation (lecture, RPC à l'écriture)
  # NOTE: lib/financial-calculations.ts (1069 LOC god file) supprimé au Sprint Refactor-I4 —
  # tout le code a migré sous lib/finance/ (cf. ci-dessous). 18 importers ont basculé vers @/lib/finance.
  logger.ts                # ✅ Sprint Cleanup-I8 / Lot 1 — logger général level-aware Edge-safe (LOG_LEVEL gated, défaut warn prod / debug dev). Boundary `console.*` du repo ; tout le reste appelle logger.error/warn/info/debug
  financial-logger.ts      # logger domain-specific (LogContext, FinancialLogger class) — 1 consumer (lib/api/finance/income-real.ts), alignement avec lib/logger.ts deferred I4
  database-snapshot.ts     # createFullDatabaseSnapshot — utilise SnapshotPayloadV2 (Sprint Polish T4)
  recap-snapshot.types.ts  # ✅ SnapshotPayload v1/v2 discriminated union + isSnapshotV2() (Sprint Polish T4)
  recap/
    check-status.ts        # ✅ Sprint Refactor-Architecture — checkRecapStatus(userId, context) Edge-safe ; importé par middleware.ts ET app/api/monthly-recap/status/route.ts (au lieu d'un fetch HTTP self-call)
  api/                     # ✅ Sprint Refactor-Architecture v1+v2 — handlers extraits, ré-exportés par app/api/finance/**/route.ts
    with-auth.ts           # ✅ Sprint Refactor-Architecture-v3+v4+v5 — withAuth(handler) + withAuthAndProfile(handler) higher-order helpers utilisés par 34 modules (12 finance + 21 Volet C + process-step1 depuis v5). Profile shape étendu en v4 à { id, group_id, first_name, last_name }. Signature étendue avec 2 overloads en v5 : (a) static-route signature sans routeContext, (b) dynamic-route signature avec generic `<TParams>` et routeContext NON-optionnel — élimine le `routeContext!` dans groups/[id]/** sans casser la cohabitation static. Tests gated `SUPABASE_API_TESTS=1` dans [lib/api/__tests__/with-auth.test.ts](lib/api/__tests__/with-auth.test.ts) (12 cas, Sprint v5).
    finance/               # 12 modules : summary, rav, budgets (POST/PUT/DELETE), budgets-estimated, incomes, income-{real,estimated,progress}, expenses-{real,add-with-logic,preview-breakdown,progress}
    __tests__/             # ✅ Sprint Refactor-Architecture-v5
      with-auth.test.ts    # gated SUPABASE_API_TESTS=1 — 12 cas withAuth + withAuthAndProfile (auth, expired payload, overloads, profile shape, isolation)
  constants/               # ✅ Sprint Hygiène-Code — magic numbers extraits
    auth.ts                # SESSION_EXPIRATION_SECONDS (3600), SESSION_EXPIRATION_JOSE ('1h'), SESSION_REFRESH_INTERVAL_MS (50min), AUTH_CHECK_INTERVAL_MS (5min)
    finance.ts             # ROUNDING_TOLERANCE (0.01) — utilisé dans process-step1 (6 sites)
  finance/                 # ✅ Sprint 0 C3 (RPC atomiques) + Sprint Refactor-I4 (split god file 1069 LOC)
    # Sprint 0 / C3 — atomic RPC helpers (single-call + retry-safe DB writes)
    context.ts             # ContextFilter type discriminé { profile_id } | { group_id } + asContextFilter() + resolveContextIds()
    piggy-bank.ts          # updatePiggyBank, transferFromPiggyToBudget
    bank-balance.ts        # updateBankBalance
    budget-savings.ts      # updateBudgetCumulatedSavings
    # Sprint Refactor-I4 — modules extraits de l'ex-god file lib/financial-calculations.ts
    types.ts               # FinancialData, BudgetSavings interfaces
    constants.ts           # EMPTY_FINANCIAL_DATA (frozen, fallback fail-soft pour get*FinancialData)
    calc-rtl.ts            # 5 helpers PURS : calculateAvailableCash, calculateRemainingToLive{Profile,Group}, calculateBudgetSavings, calculateBudgetDeficit (no I/O)
    income-compensation.ts # calculateIncomeCompensation(filter: ContextFilter) — unifie les 95%-identiques profile/group
    rav-persistence.ts     # saveRavToDatabase + getRavFromDatabase (lecture/écriture RAV en bank_balances)
    financial-data.ts      # _loadFinancialData(filter, opts) factorisé + 2 wrappers getProfileFinancialData / getGroupFinancialData
    budget-savings-detail.ts # getBudgetSavingsDetail (profile-only, breakdown par budget)
    snapshots.ts           # saveRemainingToLiveSnapshot({profileId?, groupId?, reason}) — fail-soft Promise<boolean>, dispatcher + private inserter via ContextFilter
    index.ts               # barrel re-export — consumers : import { ... } from '@/lib/finance'
    __tests__/             # Sprint DB + Sprint Refactor-I4
      rpc-concurrency.test.ts  # gated SUPABASE_RPC_CONCURRENCY_TESTS=1
      rls-isolation.test.ts    # gated SUPABASE_RLS_TESTS=1
      calc-rtl.test.ts         # ✅ I4 — 19 cas pure-unit non-gated, formules RAV/budget/cash
      snapshots.test.ts        # ✅ I4 — 5 cas mocked supabase non-gated, validation + R1 fail-soft contract
  __tests__/               # ✅ Sprint Polish T3
    api-regressions.test.ts  # gated SUPABASE_API_TESTS=1 — H1/H2/R2 regressions
scripts/                   # Sprint DB outils API Management (sans Docker)
  export-schema.mjs        # snapshot prod schema → SQL baseline (⚠️ filtre trigger buggy, cf. Sprint Audit-Triggers v6)
  apply-sql.mjs            # applique un .sql via API Management (drift recovery, ou SELECT lecture seule)
  check-drift.mjs          # backend de pnpm db:check-drift
  check-rpcs.mjs           # backend de pnpm db:check-rpcs
  list-triggers.sql        # ✅ Sprint Polish T5 — SELECT pg_trigger pour inventaire
supabase/
  config.toml              # CLI config (lié au projet distant)
  migrations/              # ✅ baseline + RLS + perf + dedup (Sprint DB) + dedup R3 + recursive policy R6 + overdraft H3
    20260101000000_remote_schema.sql           # baseline hand-curated (D5) — ⚠️ -- (no user triggers) due to filter bug v6
    20260506000000_create_finance_rpcs.sql     # 4 RPC C3 — NE PAS MODIFIER
    20260507000000_enable_rls_piggy_bank.sql   # D1
    20260507000001_fix_group_contributions_policy.sql  # D2
    20260507000002_fix_remaining_to_live_insert.sql    # D3
    20260508000000_add_piggy_bank_indexes.sql  # D7
    20260508000001_add_piggy_bank_constraints.sql  # D8
    20260509000000_dedupe_profiles_policies.sql    # D10
    20260510000000_dedupe_indexes_constraints.sql  # R3
    20260510000001_drop_recursive_profiles_policy.sql  # R6 — fix infinite-recursion sur profiles SELECT
    20260511000000_align_bank_balance_overdraft.sql    # H3 — RAISE EXCEPTION dans update_bank_balance
docs/audit/                # Audit complet codebase 2026-04
  00-executive-summary.md  # vue d'ensemble + score
  06-action-plan.md        # plan multi-sprint
  RLS-FINDINGS.md          # snapshot RLS pré-Sprint DB (les 3 failles sont closes)
  POST-MORTEM-C3-DRIFT.md  # post-mortem du drift schema_migrations ↔ pg_proc (R0)
  07-deep-dive-*.md        # playbooks par chantier
docs/db/                   # ✅ Sprint DB / D11 + inventaire triggers Sprint Polish T5
  SCHEMA.md                # carte des tables, RPC, indexes, FK, hot path, triggers (post T5)
prompts/                   # prompts Claude Code par chantier
  prompt-00-executive-summary.md     # Sprint 0 (livré)
  prompt-00-executive-summary-v2.md  # Sprint DB (livré)
  prompt-00-executive-summary-v3.md  # Sprint Refactor (livré)
  prompt-00-executive-summary-v4.md  # Sprint Hardening (livré)
  prompt-00-executive-summary-v5.md  # Sprint Polish (livré 2026-05-07)
  prompt-00-executive-summary-v6.md  # Sprint Audit-Triggers (à exécuter)
```

## 5. Architecture critique

- **2 clients Supabase** :
  - `supabase-server.ts` (service_role, bypass RLS) — utilisé par TOUTES les routes API. Les failles RLS ne s'exploitent PAS depuis ce client.
  - `supabase-client.ts` (anon key, soumis à RLS) — utilisé côté browser via les hooks. **C'est par ici que les failles RLS sont exploitables** (cf. RLS-FINDINGS.md).
- **Workflow recap mensuel** : `app/api/monthly-recap/{initialize,step1-data,process-step1,step2-data,balance,auto-balance,accumulate-piggy-bank,transfer,recover,refresh,resume,update-step,complete}/route.ts`. Le cœur algorithmique est dans `process-step1/route.ts` (>700 LOC) — **ne pas extraire** (chantier I5 séparé). Depuis Sprint v5, la route est wrap par `withAuthAndProfile` (auth header seulement) — l'algorithme métier dans le `try { ... }` reste verbatim et hors scope.
- **Allocation des dépenses** : ordre de priorité tirelire → économies budget → budget restant, codé dans [lib/expense-allocation.ts:calculateBreakdown](lib/expense-allocation.ts). L'écriture passe **toujours** par les helpers `lib/finance/*` (RPC atomiques).
- **Auth** : JWT custom signé via `jose` (pas Supabase Auth direct). Cookie `session` validé par `validateSessionToken(request)` dans chaque route API.
- **Globals partagés** dans `app/api/monthly-recap/complete/route.ts` (`global.carryoverUpdates`, etc.) — déclarés via `declare global` au top du fichier. Ne pas étendre ce pattern aux autres routes.
- **Distinction calculs finance** : [lib/contribution-calculator.ts](lib/contribution-calculator.ts) (budget-allocation, salary-proportional split d'un group budget, consumer = `components/profile/ProfileSettingsCard.tsx`) ≠ [lib/finance/income-compensation.ts](lib/finance/income-compensation.ts) (income aggregation, alimente le RAV via `_loadFinancialData`). Les noms sont voisins mais les domaines sont orthogonaux — l'un répond "que doit cotiser chaque membre ?", l'autre "quel est le total des revenus à ajouter au RAV ?". Le module `contribution-calculator` est pure-sync sans I/O ; `income-compensation` est async + Supabase.

## 6. Conventions

### API

- Format réponse : **`{ data: T } | { error: string }`** sur toutes les routes.
- Statut auth invalide : `401` + `{ error: 'Session invalide' }`.
- Statut debug-route en prod : `404` (pas 403, pour ne pas révéler l'existence).
- Pattern obligatoire dans une route :
  ```ts
  export async function POST(request: NextRequest) {
    const blocked = blockInProduction() // SI route /api/debug/*
    if (blocked) return blocked
    try {
      const sessionData = await validateSessionToken(request)
      if (!sessionData?.userId)
        return NextResponse.json({ error: 'Session invalide' }, { status: 401 })
      // ...
    } catch (error) {
      console.error('...', error)
      return NextResponse.json({ error: 'Erreur interne du serveur' }, { status: 500 })
    }
  }
  ```

### TypeScript

- `verbatimModuleSyntax` actif → **`import type` obligatoire** pour les types.
- `noUncheckedIndexedAccess` actif → `arr[i]` est `T | undefined`. Toujours narrow avant d'utiliser.
- Erreurs catch : `error: unknown` par défaut → narrow via `error instanceof Error ? error.message : String(error)`. **Préférer `} catch {` (sans binding) si la valeur n'est pas utilisée** — TS 4.4+ supporte le binding optionnel et c'est plus propre que `_error` (l'ESLint actuelle ne reconnaît pas le préfixe `_` pour `no-unused-vars`).
- Préférer `as unknown as T` plutôt que `as any` lorsqu'un cast est inévitable.
- **Aucun `any`** dans le codebase depuis Sprint Lint-Baseline-Cleanup (livré 2026-05-08). 69 sites `: any` existants ont été typés strict, et `pnpm lint:check` est désormais bloquant en CI (`code-checks.yml`) — toute nouvelle PR avec un `: any` sort rouge.
- **Pour les payloads Supabase Insert/Update** : utiliser `Database['public']['Tables']['<table>']['Insert' | 'Update']` depuis [lib/database.types.ts](lib/database.types.ts). Pattern installé partout pendant Phase 4.2 du Sprint Lint-Baseline-Cleanup. Exemples : [app/api/finances/budgets/estimated/route.ts](app/api/finances/budgets/estimated/route.ts), [app/api/finances/expenses/real/route.ts](app/api/finances/expenses/real/route.ts).
- **Pour les retours `getProfileFinancialData` / `getGroupFinancialData`** : importer le type via `import { ..., type FinancialData } from '@/lib/financial-calculations'` (Sprint Lint-Baseline-Cleanup Phase 4.2).
- **Pour les hooks return types** : préférer ré-exporter les interfaces internes (`export interface EstimatedBudget {...}`) plutôt que dupliquer en consumer. Pattern installé sur [hooks/useBudgets.ts](hooks/useBudgets.ts), [hooks/useIncomes.ts](hooks/useIncomes.ts), [hooks/useRealExpenses.ts](hooks/useRealExpenses.ts), [hooks/useRealIncomes.ts](hooks/useRealIncomes.ts).
- Pour les blobs JSONB côté DB (`Json` dans `lib/database.types.ts`) : définir un type discriminé applicatif et caster `as unknown as Json` au seul boundary insert. Pattern : [lib/recap-snapshot.types.ts](lib/recap-snapshot.types.ts).
- Compteur `as unknown as SupabaseClient` à 1 (le god file I5 `process-step1`) depuis le Sprint Refactor-I4 (l'ex-`lib/financial-calculations.ts` a été supprimé). **Ne pas en ajouter** — débugger le typage à la place. Sprint Polish T2 a vérifié qu'aucune autre route n'en a besoin.

### ESLint suppressions (justified disable pattern)

- Sprint Lint-Baseline-Cleanup a établi le pattern : pour les rares cas où un disable est légitime, utiliser `// eslint-disable-next-line <rule> -- <raison explicite>` (le double-tiret + raison est conventionnel, parsable, et review-friendly). Exemples installés :
  - **`react-hooks/exhaustive-deps`** sur les fetchers mount-only ou context-change-only : le fetcher inline est intentionnellement recréé à chaque render ; ajouter la dep créerait un loop. Pattern dans [hooks/useFinancialData.ts](hooks/useFinancialData.ts), [hooks/useBankBalance.ts](hooks/useBankBalance.ts), [contexts/AuthContext.tsx](contexts/AuthContext.tsx), [components/dashboard/SavingsDistributionDrawer.tsx](components/dashboard/SavingsDistributionDrawer.tsx), etc.
  - **`@next/next/no-img-element`** sur [components/ui/UserAvatar.tsx](components/ui/UserAvatar.tsx) : avatars Supabase Storage avec hosts arbitraires ; migration `next/image` requiert d'auditer `next.config remotePatterns` au préalable (chantier séparé).
  - **`react-hooks/set-state-in-effect`** (Sprint 1.5) — depuis Sprint 2 il ne reste qu'1 disable, sur [contexts/AuthContext.tsx](contexts/AuthContext.tsx) (`initializeAuth()` async pipeline appelle setState dans la continuation, pas dans le body de l'effet). ProfileSettingsCard a été splitté en outer + inner Sprint 2 ; le sync effect form-state-from-prop disparaît au profit d'un lazy `useState(() => profile.foo)` dans l'inner — le disable est retiré. **Important runtime behavior** (découvert Sprint 2-followup-v2) : **la règle report au call site (l'expression qui appelle la fonction contenant les setStates), PAS aux setState call sites eux-mêmes**. Conséquence : un block disable wrappant l'intérieur d'une fonction async appelée depuis un effet est reporté comme "Unused eslint-disable directive". Le bon scoping est `// eslint-disable-next-line react-hooks/set-state-in-effect -- <raison>` sur la ligne de l'appel (e.g. `initializeAuth()` dans le body de l'effet). Pour un setState directement dans le body de l'effet (cas Sprint 1.5 typique), block-style autour de l'effet reste valide. Pour un setState fired dans un async pipeline appelé depuis l'effet, next-line sur la ligne de l'appel.
- **Ne PAS utiliser `// eslint-disable-next-line` sans raison** : le code review doit pouvoir évaluer si le disable est encore valide ou rotté.
- **Ne PAS utiliser `// eslint-disable` (sans `-next-line`)** au top du fichier — c'est trop invasif. Si un fichier a 5+ disables, il y a probablement un bug structurel à fixer ailleurs.

### Format / Prettier (Sprint 1)

- Config : [.prettierrc.json](.prettierrc.json) — `semi: false`, `singleQuote: true`, `trailingComma: 'all'`, `printWidth: 100`, `tabWidth: 2`, `arrowParens: 'always'`, `endOfLine: 'lf'`, plugin `prettier-plugin-tailwindcss` (reorder déterministe des classes Tailwind).
- Ignore : [.prettierignore](.prettierignore) — `node_modules`, `.next`, `out`, `dist`, `build`, `coverage`, `public/sw.js`, `public/workbox-*.js`, `pnpm-lock.yaml`, `lib/database.types.ts` (auto-gen), `next-env.d.ts` (auto-gen par `next build`).
- `pnpm format:check` est **BLOQUANT** en CI (`code-checks.yml`) — toute PR avec un fichier mal formaté sort rouge. Le hook `pre-commit` (lint-staged) prévient localement.
- **Ne PAS lancer** `pnpm format` (= `prettier --write .`) dans une PR feature — le diff est mécanique mais massif (Sprint 1 a généré 162 files / +6935/−5470 LOC). Garder `format` pour la maintenance config (e.g. après modif `.prettierrc.json`). Pour un commit normal : `lint-staged` formate les fichiers staged automatiquement au commit.
- **ESLint v9 + eslint-config-next 16** depuis Sprint 1 : flat configs natifs ([eslint-config-next/core-web-vitals](node_modules/eslint-config-next/dist/core-web-vitals.js) + [eslint-config-next/typescript](node_modules/eslint-config-next/dist/typescript.js)) — **pas de FlatCompat** (déclenche un bug circular-reference avec eslint-plugin-react v7 sous v9). Si tu vois `TypeError: Converting circular structure to JSON` dans une nouvelle config ESLint, c'est ça : utiliser les flat exports natifs.
- **eslint-plugin-react-hooks v7** (shipped via eslint-config-next 16) introduit deux nouvelles règles : `react-hooks/set-state-in-effect` et `react-hooks/refs`. Sprint 1 a downgradé les deux à `warn` car 26 sites pré-existants violent ces règles (refacto = sprint séparé). **Ne PAS introduire de nouveau site** : le compteur est gelé à 24 + 1 = 25 ; tout ajout doit être justifié via `// eslint-disable-next-line react-hooks/set-state-in-effect -- <raison>` ou refactoré. Le warn ne fail pas la CI mais c'est de la dette traquée.

### Logs (Sprint Cleanup-I8 / Lot 1 + Lot 3 + Lot 4a + Lot 4b + Lot 4c + Lot 4d + Lot 4e)

- Logger central : [lib/logger.ts](lib/logger.ts). 4 niveaux exposés — `logger.error`, `logger.warn`, `logger.info`, `logger.debug` — chacun signature `(msg: string, ...rest: unknown[])` (drop-in mécanique pour les call sites `console.log`).
- Niveau actif gated via `LOG_LEVEL` (`error | warn | info | debug`). Défaut : `warn` en prod, `debug` en dev. Cached à module load (Edge-safe, pas de lookup `process.env` par appel).
- Strip prod automatique : [next.config.js](next.config.js) `compiler.removeConsole` SWC supprime tous les `console.log/info/debug` du code applicatif au build (`exclude: ['error', 'warn']` les conserve). N'affecte ni `node_modules` ni le runtime Next.js.
- ESLint global : `'no-console': ['warn', { allow: ['warn', 'error'] }]` ([eslint.config.mjs:29](eslint.config.mjs#L29)) — tout nouveau `console.log` génère un warning, mais reste autorisé en transit (le strip prod le rendra inerte). Pour le code que tu écris : préfère `logger.debug/info` ; `console.warn`/`console.error` directs restent acceptables ad-hoc mais `logger.warn`/`logger.error` est plus cohérent.
- **Règle d'or de triage** (installée Sprint Cleanup-I8 / Lot 4a) : pour tout `console.*` rencontré (existant ou nouveau), se poser la question — "est-ce que quelqu'un (toi, dans 6 mois, devant une prod en panne) lira ce log ?". Si non → **SUPPRIMER**. Si oui mais sans cas concret → **SUPPRIMER** aussi (YAGNI ; on ré-instrumente quand le bug arrive). Décliné par pattern : (a) `console.error('Error in METHOD /api/...:', error)` dans un `try/catch` qui converti en `NextResponse.json({error}, {status: 500})` → **SUPPRIMER** (Next.js capture déjà l'exception avec stack trace côté Vercel ; le custom message ajoute juste le route name déjà dans la stack) ; (b) DB error qui discrimine une branche métier non-évidente (`'Error fetching contributions:'` vs `'Error recalculating contributions:'`) → **KEEP+migrate** vers `logger.error` (le code/details Supabase n'apparaît qu'ici, grep-able si bug futur) ; (c) erreur silencieusement avalée (le catch retourne 200 ou un fallback comme `{groups: []}`) → **KEEP+migrate** (sans le log, le serveur perd l'info qu'une op DB a fail) ; (d) cleanup-attempt critique (e.g. groupe créé puis profile join fail → cleanup `delete groups`) → **KEEP+migrate** (path métier non-trivial qui mérite trace si jamais ça arrive). Pour les `} catch (error) {` dont l'erreur est ainsi déliée du log : passer à `} catch {` (TS 4.4+, CLAUDE.md §6 convention) plutôt que `_error` qui leverait `unused-imports/no-unused-vars`.
- **Per-file ESLint override** depuis Lot 3 (étendu Lot 4a + Lot 4b + Lot 4c + Lot 4d + Lot 4e + Lot 5 + Lot 5b + Lot 5c + Lot 5d) : un bloc ajouté APRÈS le bloc principal d'`eslint.config.mjs` escalade `no-console: 'error'` sur le scope migré : 3 fichiers explicites (`middleware.ts`, `lib/expense-allocation.ts`, `lib/logger.ts`) + 17 globs/paths (`app/api/debug/**` Lot 5d — couvre les 6 routes migrées + future-proof + `lib/debug-guard.ts` Lot 5d future-proof 0 site aujourd'hui) + 15 entrées préexistantes (`app/api/auth/**` Lot 5b — couvre `session/route.ts` migré + `confirm/route.ts` 0 site future-proof ; `app/api/groups/**` qui couvre les 5 fichiers du domaine + toute future route ; `app/api/monthly-recap/{status,refresh,resume,initialize,step1-data,step2-data,accumulate-piggy-bank,transfer,update-step,recover}/**` Lot 4b + Lot 5b (recover ajouté à la brace expansion, 10 routes couvertes) — brace expansion explicite **volontairement** pas un glob global `app/api/monthly-recap/**` qui escaladerait les 4 routes hors scope (`complete`, `balance`, `auto-balance`, `process-step1`, ~334 sites encore non-migrés ; `status-test` deleted en Lot 5b) en lint error et casserait `pnpm lint:check` ; `app/api/profile/**` Lot 4c ; `app/api/savings/**` Lot 4d — **swap depuis `app/api/savings/data/**`** maintenant que `savings/transfer`est migré, le domaine`savings/_`est 100% couvert (cas concret de la règle "100% couverture domaine → glob global" ci-dessous, future-proof : toute future route savings auto-protégée) ;`app/api/bank-balance/**`Lot 4c ;`lib/api/finance/**`Lot 4e — couvre les 12 modules handlers du namespace canonique unifié (Sprint Refactor-Architecture-v3) ;`app/api/finance/**` Lot 4e — **future-proof, 0 site aujourd'hui** car les `route.ts` du namespace sont des thin re-exports `export { GET, POST } from '@/lib/api/finance/...'`, mais protège contre tout futur écart route.ts qui inlinerait du code ; **`components/**`** Lot 5 — couvre les 12 components migrés + future-proof toute future addition ; **`hooks/**`** Lot 5 — couvre les 12 hooks migrés + future-proof ; **`contexts/**`** Lot 5 — couvre AuthContext + future-proof ; **5 paths app/{dashboard,monthly-recap,inscription,reset-password,forgot-password}/page.tsx** Lot 5 — paths individuels parce que les autres pages App Router (`app/page.tsx`, `app/settings/page.tsx`, `app/connexion/page.tsx`, `app/group-dashboard/page.tsx`) sont déjà clean sans console.\* mais ne sont PAS escaladées en `error` car d'autres pages futures pourraient nécessiter un disable explicit). Pattern flat config = last-match wins. **Inclure `lib/logger.ts` dans la liste est intentionnel\*\* : le `/_ eslint-disable no-console \*/`au top du fichier reste source de vérité (le module est la frontière), mais l'override durcit le contrat si le disable est jamais retiré accidentellement. Quand un nouveau domaine est migré, préférer le glob (e.g.`app/api/profile/\*\*`) une fois qu'on a >2 fichiers ; sinon ajouter à la liste explicite. Quand un domaine partiellement migré atteint 100% de couverture, swap la brace expansion pour un glob global (réduit le bruit de la config).
- **Tests pure-unit du logger** ([lib/**tests**/logger.test.ts](lib/__tests__/logger.test.ts), Lot 3 follow-up) : 11 cas non-gated qui pin la level-filtering logic + `LOG_LEVEL` env handling + format `[level]` + rest-spread + non-utilisation de `console.log`. **Gotcha** : `lib/logger.ts` cache `currentLevel` à module load via `getCurrentLevel()`, donc chaque cas stub `LOG_LEVEL` + `NODE_ENV` AVANT `vi.resetModules()` + `await import('@/lib/logger')`. Si tu refactor `lib/logger.ts`, ces tests régression-guardent contre un drift de signature ou de comportement.
- **Migration progressive** : ~64 `console.*` restants après Sprint Refactor-I4 (Lot 2 clos par I4, ~95 sites supprimés dans l'ex-`lib/financial-calculations.ts` ; new top 5 : `process-step1/route.ts` 120, `complete/route.ts` 85, `auto-balance/route.ts` 66, `balance/route.ts` 63, `financial-logger.ts` ~60. Le top reste dominé par le god file I5 + les 4 routes monthly-recap stateful + `financial-logger.ts` dont l'alignement est addressable maintenant ; `status-test` deleted en Lot 5b, `testSupabaseConnection` deleted en Lot 5c, `financial-calculations.ts` deleted en I4). **Lot 1** = filet (logger + strip prod), **Lot 3** = première migration (middleware + expense-allocation, 7 sites), **Lot 4a** = première salve API avec triage (`app/api/groups/**`, 22 sites → 11 supprimés + 11 migrés, ratio 50/50 "ménage avant migration"), **Lot 4b** = deuxième salve API (`app/api/monthly-recap/{...9 routes simples}/**`, 132 sites distincts → 113 supprimés + 19 migrés, ratio 86%/14% "triage agressif sur dump-debug"), **Lot 4c** = troisième salve API (`app/api/{profile,savings/data,bank-balance}/**`, 52 sites → 43 supprimés + 9 migrés, ratio 83%/17% "triage strict avec drop critique de 3 PII surfaces — first_name/last_name/salary dans profile, solde bancaire dans bank-balance"), **Lot 4d** = quatrième salve API single-file (`app/api/savings/transfer/route.ts`, 38 sites → 32 supprimés + 6 migrés, ratio 84%/16% "triage strict avec **3 cleanup-attempts CRITIQUES préservés** — L123/L322/L338 rollback-impossible logs qui fire seulement si la DB est laissée dans un état inconsistant ; Lot 4d a aussi documenté que la pre-audit grep -c surcomptait à cause des template literals multi-ligne, 45 estimés vs 38 réels"), **Lot 4e** = cinquième salve la plus large (`lib/api/finance/**`, 12 fichiers, 152 sites distincts vs 190 grep-counted — multi-line console.log inflated le compteur, pattern miroir Lot 4b/4d ; → 119 supprimés + 33 migrés, ratio 78%/22% "triage strict avec **3 cleanup-attempts CRITIQUES préservés** — `expenses-add-with-logic.ts:216` rollback piggy debit + `:229` rollback savings debit dans smart-allocation tirelire→savings→budget + `expenses-real.ts:431` rollback allocation reverse on DELETE silently-swallowed ; **+ 1 fallback 200-on-error préservé** sur `summary.ts:115` → logger.warn (Sprint Refactor-Architecture-v3 explicit choice 200-with-default-data, sémantique 200-on-error pas error) ; **+ ~10 snapshot failures** `'⚠️ Échec sauvegarde snapshot (non critique)'` dans incomes/budgets/income-real/expenses-real → logger.warn per §6 règle d'or rule c silently-swallowed"), **Lot 5** = sixième salve, plus gros périmètre du chantier (couche client complète : 30 fichiers = 12 components + 12 hooks + 1 context + 5 pages, 193 sites distincts vs 199 grep-counted ; → 133 supprimés + 60 migrés, ratio 69%/31% "triage strict avec **5 cleanup-attempts CRITIQUES préservés** — `SavingsDistributionDrawer.tsx:171` POST /savings/transfer fail + `useMonthlyRecap.ts:84` /monthly-recap/transfer fail + `:115` /auto-balance fail + `:157` /complete fail + `useGroups.ts:145+168` join/leave cross-mutation cascade fail (financial state stale risk si client cascade fail après server commit) ; **+ 1 boot-path PWA préservé** sur `ServiceWorkerRegistration.tsx:18` → logger.error (silent par design, log nécessaire pour tickets support 'offline ne marche pas') ; **+ 4 dev-guarded sites** dans useProfile/useGroupContributions/useGroupSearch migrés vers logger.debug avec drop des `if (process.env.NODE_ENV === 'development')` (logger gère le gating via LOG_LEVEL, décision Q3 plan) ; **+ 18/19 DROP massif sur `app/dashboard/page.tsx`** useEffect verbose state dump RAV/économies/revenus/dépenses en € + PII profile.first_name/last_name à chaque render ; **+ AuthContext L144 logger.warn** auth check error filter 401 préservé"), **Lot 5b** = sixième bis salve (3 fichiers serveur orphelins hors-scope Lot 5/4b : `app/api/auth/session/route.ts` + `app/api/monthly-recap/recover/route.ts` + `app/api/monthly-recap/status-test/route.ts`, 16 sites distincts → 8 supprimés + 4 migrés + 4 file-deleted, ratio 67/33 sur les 12 sites triagés "triage strict avec **2 cleanup-attempts CRITIQUES préservés** — `auth/session/route.ts:56` Supabase auth réussi mais JWT session fail → état inconsistant grep-able + `recover/route.ts:306` recovery rollback partiel → snapshot peut rester actif ; **+ status-test DELETE** confirmed dead code 0 consumer cross-codebase, mock data hardcodé recovery via git revert ; **+ recover L288 logger.warn** silently swallowed snapshot deactivation fail + L404 logger.error GET catch silently propagated ; lint baseline 490 → 482"), **Lot 5c** = sixième ter salve (8 fichiers libs server-side foundationnels indépendants I4/I5 : `lib/{auth,session,session-server,supabase-client,database-snapshot,monthly-recap-calculations}.ts` + `app/auth/confirm/route.ts` + `lib/api/with-auth.ts` future-proof glob, 45 sites distincts — **audit a sous-compté `monthly-recap-calculations` à 13 vs 19 réels**, 2 outer catches manqués L154/L365, pattern leçon "ne pas trust un audit single-pass sur des fichiers >300 LOC" ; → 18 supprimés + 23 migrés + 3 file-internal-deleted (`testSupabaseConnection()` dead code 0 callsite cross-codebase, recovery via git revert), ratio 56/44 sur les 41 sites triagés "triage Modéré KEEP-heavy parce que `monthly-recap-calculations` et `database-snapshot` sont audit-trail heavy plutôt que flow-log heavy — 8 success markers avec metrics sur monthly-recap + 5 snapshot audit trail sur database-snapshot tous → logger.info" avec **1 cleanup-attempt CRITIQUE préservé** : `app/auth/confirm/route.ts:46` OTP verification réussie mais `data.user` manquant (edge case non-évident, mérite trace si récurrent) ; **+ 5 audit-trail CRITIQUES préservés** : `database-snapshot.ts:169-173` × 5 statements logger.info (snapshot ID + mois + total records + per-table counts — foundational pour audit recovery si rollback nécessaire post-process-step1) ; **+ `lib/api/with-auth.ts` confirmé 0 site** (wrapper canonique reste clean depuis création v3+v4+v5, ESLint glob ajouté future-proof) ; **+ 2 KEEP+logger.warn** `lib/auth.ts:194/224` 401-filtered silently-swallowed unexpected errors ; **+ 1 KEEP+logger.debug** `lib/session-server.ts:114` session validation success per-request gated dev-only ; lint baseline 482 → 461 −21 warnings"), **Lot 5d** = septième salve optionnelle (6 fichiers `app/api/debug/**` toutes gardées par `blockInProduction()` 404 prod : `financial/route.ts` + `group-financial/route.ts` + `remaining-to-live/route.ts` + `reset-all/route.ts` + `reset-budgets/route.ts` + `retrigger-recap/route.ts` ; 57 sites annoncés / 64 statements réels — **audit a sous-compté financial+group-financial blocs summary multi-line 9-10 lignes en 1 site chacun → recount précis** = 17+17+3+4+17+6 = 64 statements distincts ; → 45 supprimés + 12 migrés + 7 statements multi-line collapsé Prettier après 1 statement DROP, ratio 79/21 sur les 57 statements logiques "triage Strict cohérent Lot 4c/4d/4e/5/5b avec **0 cleanup-attempt CRITIQUE confirmé** (routes atomiques Supabase, pas de rollback) + **6 outer catches préservés** (binding `error.message` utilisé dans returns 500 — non-normalisable en `} catch {`) + **5 inline DB error discriminants** dans `reset-budgets/route.ts` (suppression transferts/dépenses, fetch budgets, insertion dépenses, désactivation snapshots — 4 inline + 1 outer) + **1 inline warn rule c silently-swallowed** dans `retrigger-recap/route.ts:84` (snapshot deactivation fail → la route continue, pas blocking)" avec **`lib/debug-guard.ts` confirmé clean 0 sites** (module guard 9 LOC, glob future-proof seulement) ; **PII test acceptable DROP** sur `reset-budgets` lignes 115-117 et 161-164 (montants € + noms budgets seed test, pas user-PII) ; lint baseline 461 → 406 −55 warnings (dépasse fourchette estimée −10-30 du plan : majorité des sites sont `console.log` flow logs warn-counted, pas `console.error` allow-listés)"). **Lot 2 = clos par Sprint Refactor-I4 (2026-05-11)** — la migration `lib/financial-calculations.ts → lib/finance/*` a interleavé le Lot 2 cleanup pendant l'extraction (commits #5, #6, #7, #8 du chantier I4). 112 sites sources → ~13 sites en `lib/finance/` tous via `logger.*`. Lint baseline 406 → 307 (−99 warnings). Le glob `'lib/finance/**'` a été ajouté au bloc per-file `no-console: 'error'` au commit #10 du chantier I4, donc toute régression future sort la PR rouge. Le Lot 6 (sweep final + activation globale `no-console: 'error'` dans le bloc principal d'`eslint.config.mjs` + ajout des 4 routes monthly-recap stateful `complete`/`balance`/`auto-balance`/`process-step1` après que I5 ait extrait leur logique métier ~334 sites) migrera par opportunité au fil des PRs touchant chaque fichier ; ne PAS faire un gros commit "remove all console.log". **Pour le Lot 6, reprendre la règle d'or de triage de Lot 4a/4b/4c/4d/4e/5/5b/5c/5d** (cf. bullet ci-dessus) : ménage avant migration, pas drop-in mécanique. `lib/financial-logger.ts` (289 LOC, 1 consumer = `income-real.ts`, ~60 sites `console.*` non-gated) est désormais addressable indépendamment de I4 — voir prompt v2 pour le sprint d'alignement (Option α/β/γ) à arbitrer.

### Naming

- DB : **snake_case** (`profile_id`, `cumulated_savings`, `bank_balances`).
- TS : **camelCase** (`profileId`, `cumulatedSavings`, `bankBalances`).
- Migrations Supabase : `<YYYYMMDDHHMMSS>_<verb>_<scope>.sql`.

### Git

- **Default branch GitHub : `cleanup`** depuis Sprint Hygiene-CI / E3 (`main` n'avait jamais reçu les workflows YAML, donc le cron weekly ne fired pas en mode `schedule` ni `workflow_dispatch`). `main` reste figé à 3 commits derrière les sprints livrés. Si on un jour décide de release prod, options : (a) renommer `cleanup → main`, (b) merger `cleanup → main` via grosse PR, (c) status quo. Pas d'urgence — le filet CI tourne depuis cleanup.
- Branches feature depuis `cleanup` (la default actuelle).
- **Conventional Commits** : `fix:`, `feat:`, `chore:`, `docs:`, `perf:`, `test:`. Préfixer le scope quand pertinent : `fix(api/debug)`, `chore(supabase)`.
- **Un commit par item** dans les sprints multi-items (cf. Sprint 0 : 5 commits + 1 follow-up).
- **Toujours créer un nouveau commit**, jamais `--amend` un commit publié.
- **JAMAIS** `--no-verify`, `--no-gpg-sign`, ou `git push --force` sans demande explicite.

## 7. Sécurité — état des lieux

### ✅ Fait (Sprint 0)

- `typescript.ignoreBuildErrors` retiré (C1).
- Routes `/api/debug/*` (×20) bloquées en prod via `blockInProduction()` (C2).
- Pattern SELECT-then-UPDATE remplacé par 4 RPC atomiques sur `piggy_bank.amount`, `bank_balances.balance`, `estimated_budgets.cumulated_savings` (C3, 14 sites migrés).
- Audit RLS exécuté + documenté (C4).
- README UTF-8 (C5).

### ✅ Fait (Sprint DB — livré 2026-05-07)

- D1 — RLS activée sur `piggy_bank` + policies owner-or-group-member.
- D2 — `group_contributions` policy ouverte remplacée par membre-de-groupe.
- D3 — `remaining_to_live_snapshots.INSERT` restreint à `service_role`.
- D4 — Tests d'isolation RLS gated (`SUPABASE_RLS_TESTS=1`).
- D5 — Schéma prod versionné en baseline backdaté (`20260101000000_remote_schema.sql`) via API Management (Docker absent).
- D6 — `lib/database.types.ts` généré + augmenté à l'origine dans `lib/database.ts` avec les 4 RPC C3 service-role-only. Wirage `<Database>` aux clients livré au Sprint Refactor / R2. **Augmentation supprimée en Sprint Polish-CI / D3** : depuis le regen Sprint Cleanup-Legacy / C1 (`--linked` expose les RPC service-role-only), les 4 RPC apparaissent dans les types générés et l'augmentation était devenue no-op.
- D7 — `piggy_bank` indexé (2 partial unique indexes par owner).
- D8 — `piggy_bank` contraint (amount ≥ 0, owner XOR).
- D9 — Tests concurrence RPC (4/4 verts, 100× parallèles convergent).
- D10 — Policy SELECT redondante sur `profiles` supprimée.
- D11 — [docs/db/SCHEMA.md](docs/db/SCHEMA.md) ajouté.

### ✅ Fait (Sprint Refactor — livré 2026-05-07)

- R0 — post-mortem du drift C3 documenté ([docs/audit/POST-MORTEM-C3-DRIFT.md](docs/audit/POST-MORTEM-C3-DRIFT.md)).
- R1 — 11 routes `app/api/debug/populate-*` cassées supprimées (~2 800 LOC).
- R2 — `createClient<Database>(...)` activé sur `lib/supabase-server.ts` + `lib/supabase-client.ts` + fixtures Vitest. **17 routes scope-cast** `as unknown as SupabaseClient` (dette H1 Sprint Hardening). Bug réel corrigé : `current_savings` → `cumulated_savings` dans `app/api/finances/expenses/progress/route.ts`.
- R3 — Migration `20260510000000_dedupe_indexes_constraints.sql` : 6 indexes, 1 FK, 3 CHECKs dupliqués droppés + le CHECK NULL-hole `budget_transfers_different_budgets`. Le baseline a aussi rattrapé D7/D8 (piggy_bank constraints/indexes) et D10 (policy profiles) qui n'avaient jamais été ré-exportés.
- R4 — `pnpm db:check-drift` ([scripts/check-drift.mjs](scripts/check-drift.mjs)) — compare prod ↔ baseline, exit 0 = clean, 1 = drift.
- R6 — Tests RLS D2 (group_contributions cross-membre) + D3 (remaining_to_live_snapshots INSERT rejet authenticated) implémentés. **Bug critique découvert** : la SELECT policy `Group members can see each other` sur `profiles` se référençait elle-même → `42P17 infinite recursion` sur toute lecture anon traversant `profiles`. Corrigé via `20260510000001_drop_recursive_profiles_policy.sql`.

### ✅ Fait (Sprint Hardening — livré 2026-05-07)

- H4 — `pnpm db:check-rpcs` ([scripts/check-rpcs.mjs](scripts/check-rpcs.mjs)) : vérifie via `pg_proc` la présence des 4 RPC C3 en prod. Comble le trou que `db:check-drift` ne couvre pas (les RPCs sont volontairement exclues du baseline).
- H2 — Retrait des reads sur `financial_snapshots`, table fantôme jamais créée (absente du schéma prod, absente du baseline, jamais dans l'historique git). Erreurs silencieusement avalées dans `lib/database-snapshot.ts` et `app/api/finances/dashboard/route.ts`. Le dashboard retournait toujours 0 pour `total_real_income` et `total_real_expenses` à cause de fallbacks morts sur cette table.
- H1 — Unwind des 17 scope-casts `as unknown as SupabaseClient` héritage de R2. **3 commits** : 6 singletons (bank-balance, budgets, incomes, groups/{search,contributions,[id]/members}), 3 aggregators (groups, profile, finances/dashboard), 7 routes recap. Compteur final : 5 occurrences (les 4 god/debug + R0 cleanup). **2 bugs réels corrigés** : (a) `calculate_available_cash` était une RPC fantôme dans dashboard (le résultat retombait toujours sur 0 avec `|| 0`), remplacée par `remainingToLiveData.availableBalance`. (b) `resume/route.ts` passait `profile.group_id` au lieu de `contextId` à `getGroupFinancialData`.
- H3 — Migration `20260511000000_align_bank_balance_overdraft.sql` : aligne `update_bank_balance` sur `update_piggy_bank_amount` en levant une exception explicite si `new_balance < 0` au lieu de laisser la CHECK constraint répondre. Test gated `bank_balances_balance_check` ajouté à `rpc-concurrency.test.ts`. **À appliquer manuellement** via `node scripts/apply-sql.mjs supabase/migrations/20260511000000_align_bank_balance_overdraft.sql` puis re-exporter le baseline.

### ✅ Fait (Sprint Polish — livré 2026-05-07)

- T2 — 3 debug routes (`quick-test`, `recap-data`, `test-balance`) supprimées (-1224 LOC). Compteur `as unknown as SupabaseClient` 5 → 2 (les 2 god files restants).
- T1 — `total_real_income` / `total_real_expenses` ne sont plus hardcodés à 0 dans [app/api/finances/dashboard/route.ts](app/api/finances/dashboard/route.ts). Branchés sur `remainingToLiveData.totalReal*` qui était déjà calculé par `getProfileFinancialData()` / `getGroupFinancialData()` mais ignoré.
- T4 — [lib/recap-snapshot.types.ts](lib/recap-snapshot.types.ts) : `SnapshotPayload = SnapshotPayloadV1 | SnapshotPayloadV2` discriminé sur `snapshot_version`, prédicat `isSnapshotV2()`. recover/route.ts ne caste plus `as any`. database-snapshot.ts caste `as unknown as Json` au seul boundary insert.
- T3 — [lib/**tests**/api-regressions.test.ts](lib/__tests__/api-regressions.test.ts) : 3 tests gated `SUPABASE_API_TESTS=1` couvrant les régressions H1/H2/R2 (cumulated*savings, total_real*\*, availableBalance). Pattern dynamic-import + cleanup cascade calé sur `rpc-concurrency.test.ts`.
- T5 — [scripts/list-triggers.sql](scripts/list-triggers.sql) + section "Inventory" dans [docs/db/SCHEMA.md](docs/db/SCHEMA.md). **2 trouvailles** documentées non-fixées (Sprint Audit-Triggers v6) : (a) le filtre `LIKE 'public.%'` du baseline exporter ne matche jamais → 6 triggers `public.*` invisibles dans le baseline ; (b) 3 fonctions trigger non-versionnées en prod (`trigger_group_budget_change`, `cleanup_group_contributions`, `trigger_recalculate_contributions`).

### ✅ Fait (Sprint Audit-Triggers — livré 2026-05-07)

- A1 — Fix du filtre trigger dans [scripts/export-schema.mjs](scripts/export-schema.mjs) : JOIN explicite `pg_class`+`pg_namespace` (le `tgrelid::regclass::text LIKE 'public.%'` précédent ne matchait jamais quand `public` est dans le `search_path`). Baseline ré-exporté contient désormais les 6 `CREATE TRIGGER` `public.*`. `pnpm db:check-drift` détecte maintenant la suppression d'un trigger.
- A2 — Migration [20260512000000_capture_trigger_functions.sql](supabase/migrations/20260512000000_capture_trigger_functions.sql) : capture verbatim de **5 fonctions PL/pgSQL** non-versionnées (`update_updated_at_column`, `calculate_group_contributions`, `trigger_group_budget_change`, `cleanup_group_contributions`, `trigger_recalculate_contributions`). `calculate_group_contributions` (~80 LOC, core métier) découverte en cours de route via `PERFORM` depuis 2 des 3 triggers — capturée pour ne pas laisser un trou caché. Migration appliquée via `apply-sql.mjs` (idempotent `CREATE OR REPLACE`) + `supabase migration repair --status applied 20260512000000`. Pas de `db push` (les fonctions existent déjà en prod). Helper [scripts/dump-functions.sql](scripts/dump-functions.sql) committé pour audits futurs.
- A3 — `pnpm db:check-functions` ([scripts/check-trigger-functions.mjs](scripts/check-trigger-functions.mjs)) : pendant `db:check-rpcs` pour les 4 fonctions custom (exclut `update_updated_at_column` canonique pour éviter false positives). Comble le trou que `db:check-drift` ne couvre pas (le baseline ne capture pas les bodies de fonction).
- A4 — [.github/workflows/db-drift-check.yml](.github/workflows/db-drift-check.yml) étendu : `db:check-drift` + `db:check-rpcs` + `db:check-functions` weekly cron + on-demand.

### ✅ Fait (Sprint Audit-Functions-v2 — livré 2026-05-07)

- B1 — `pnpm db:audit-functions` ([scripts/audit-functions.mjs](scripts/audit-functions.mjs)) : audit générique `pg_proc` ↔ migrations. Pendant que `db:check-functions` pin une liste hardcodée de 4 noms, B1 enumère TOUTES les `public.*` fonctions et regrep les migrations. **Trouvaille au premier run** : 4 fonctions legacy non-versionnées en prod (zéro callsite app), capturées verbatim dans [supabase/migrations/20260513000000_capture_legacy_functions.sql](supabase/migrations/20260513000000_capture_legacy_functions.sql) — `check_column_exists`, `create_recap_snapshot` (remplacée par `lib/database-snapshot.ts` en T4), `final_verification`, `is_monthly_recap_required`. Workflow A2 réappliqué (apply-sql + migration repair + re-export baseline). Audit termine maintenant à 13/13 versionnées.
- B2 — [lib/**tests**/trigger-behavior.test.ts](lib/__tests__/trigger-behavior.test.ts) gated `SUPABASE_TRIGGER_TESTS=1` : 4 cas couvrant `trigger_recalculate_contributions` (auto-create on JOIN), `trigger_group_budget_change` (recalc on UPDATE budget), `cleanup_group_contributions` (cascade DELETE group), `update_updated_at_column` (touch). Ferme le trou que `db:check-functions` ne couvre pas — un stub `BEGIN RETURN NEW; END;` accidentel passerait check-functions au vert.
- B3 — [.github/workflows/db-drift-pr.yml](.github/workflows/db-drift-pr.yml) : drift gate PR-time sur paths `supabase/migrations/**` + `scripts/check-*.mjs` + `scripts/export-schema.mjs` + `scripts/dump-functions.sql`. Run `db:check-drift` + `db:check-rpcs` + `db:check-functions`. Ferme la fenêtre 6 jours du cron weekly.

### ✅ Fait (Sprint Cleanup-Legacy — livré 2026-05-07)

- C1 — DROP des 4 fonctions legacy capturées en B1 ([supabase/migrations/20260514000000_drop_legacy_functions.sql](supabase/migrations/20260514000000_drop_legacy_functions.sql)) : `check_column_exists`, `create_recap_snapshot`, `final_verification`, `is_monthly_recap_required`. Confirmées dead code (0 callsite app, 0 référence GitHub Actions, 0 cron Supabase / webhook / Edge Function). Workflow capture-then-drop appliqué (apply-sql.mjs + migration repair + re-export baseline). `pnpm db:audit-functions` passe à 9 fonctions versionnées (vs 13). Recovery path : re-apply de la migration de capture `20260513000000` (CREATE OR REPLACE idempotent). **Effets de bord** du `pnpm db:types` régénéré : (a) FK `budget_transfers_from_budget_id_fkey` dédupliquée disparaît des types (catch-up post R3 jamais reflété en types), (b) les 4 RPC C3 apparaissent désormais dans les `Functions` générées (supabase gen types --linked les expose maintenant alors que leur GRANT EXECUTE est `service_role` only) — l'augmentation `lib/database.ts` devient effectivement no-op (intersection narrow `string|undefined`). Fix collatéral [lib/finance/context.ts](lib/finance/context.ts) : `ContextIds` passe de `string|null` à `string|undefined` pour rester compatible (sémantique runtime préservée — Postgres traite `null` et missing-with-default identiquement quand la fonction a `DEFAULT NULL`).
- C2 — `pnpm db:audit-objects` ([scripts/audit-db-objects.mjs](scripts/audit-db-objects.mjs)) : généralise B1 à 5 catégories `pg_catalog` (functions, composite types, enums, domains, operators). Filtre les row-types implicites via `NOT EXISTS pg_class.relkind IN ('r','v','m','f','p')`. Premier run clean : 9 fonctions versionnées + 0 entrée dans les 4 autres catégories (le schéma n'a pas de custom types/enums/operators aujourd'hui). Pas dans cron ni PR-time gate (heavier, ad-hoc).
- C3 — Validation end-to-end du PR-time gate B3 + cron weekly H5/A4. **2 vrais bugs surfacés** au premier run de la PR test : (a) `pnpm/action-setup@v4` refuse de choisir entre `with: version: 9.15.5` et le champ `packageManager` de package.json (même version, mais l'action voit l'integrity hash comme "plus spécifique") → cascade `pnpm: command not found` sur tous les steps ; (b) secret `SUPABASE_ACCESS_TOKEN` perdu lors du rename `Popoth_App_Claude` → `popoth`. Fixes : suppression de `with: version` dans les 2 YAML ([.github/workflows/db-drift-check.yml](.github/workflows/db-drift-check.yml) + [.github/workflows/db-drift-pr.yml](.github/workflows/db-drift-pr.yml)) + ré-ajout du secret. Validation seconde itération : `db:check-drift` exit 1 sur drift volontaire, `db:check-rpcs` + `db:check-functions` exit 0 grâce à `if: always()`. **Le gate weekly n'a JAMAIS tourné depuis B3** — c'est la première fois qu'on a une vraie boucle de feedback CI sur ce repo.

### ✅ Fait (Sprint Polish-CI — livré 2026-05-07)

- D1 — `pnpm db:types` ne pollue plus le fichier généré quand on l'invoque normalement. Le fix déplace le `>` à l'intérieur du script ([package.json:14](package.json#L14)) : le wrapper banner pnpm va sur la stdout du terminal (visible à l'utilisateur), seul l'output du binaire `supabase gen types` atteint `lib/database.types.ts`. **Faux pas en cours de route** : tenté d'abord `pnpm --silent exec supabase gen types ...` au niveau script — ça ne marche pas car le banner est imprimé par le pnpm OUTER (celui qui spawn le script), avant même d'évaluer la valeur du script. `--silent` doit être passé par l'appelant, pas par l'appelé. **Doc à jour** : on appelle désormais `pnpm db:types` sans redirection.
- D2 — `pnpm db:check-drift` idempotent face à CRLF Windows ([scripts/check-drift.mjs:83-91](scripts/check-drift.mjs#L83)). **Root cause subtile** (pas juste byte-mismatch) : en regex JS, `.` ne matche pas `\r` (line terminator). La regex `TIMESTAMP_LINE = /^-- Generated by ... .+$/` échoue sur une ligne CRLF parce que `.+` n'avale pas le `\r` final. Conséquence : la timestamp line N'EST PAS strippée du fichier committé (CRLF), MAIS l'est du in-memory (LF) — les deux side ne s'alignent plus, offset d'une ligne, diff intégralement spurious. Fix : normaliser `\r\n → \n` AVANT `stripTimestamp`, pas après. CI Linux non affectée.
- D3 — Augmentation `lib/database.ts` (47 LOC) supprimée. **Origine** : Sprint DB / D6 avait créé l'augmentation parce que les 4 RPC C3 sont `GRANT EXECUTE TO service_role` only et n'apparaissaient pas dans `pnpm db:types`. Sprint Cleanup-Legacy / C1 a régénéré les types via `--linked` (qui les expose) → l'augmentation devient un intersection no-op (elargit `string|undefined` → `string|null|undefined`, mais aucun caller ne passe `null`). **6 imports migrés** vers `@/lib/database.types` (4 tests + 2 prod : `lib/supabase-{client,server}.ts` qui utilisaient `from './database'` relatif — la première exploration les a manqués, surfacé par le typecheck après le `git rm`). Recovery : recréer le fichier depuis le parent du commit `731cded`.
- D4 — Path filter du PR-time gate étendu ([.github/workflows/db-drift-pr.yml:14-21](.github/workflows/db-drift-pr.yml#L14)). Ajout : les 2 YAML eux-mêmes (self-monitoring — sans ça, casser le gate ne le redéclenche pas pour le détecter, cas C3 redux) + `scripts/audit-*.mjs` (B1/C2, pour cohérence avec `check-*.mjs`).
- D5 — **À OBSERVER MANUELLEMENT** : aller sur https://github.com/PothieuG/popoth/actions/workflows/db-drift-check.yml, **Run workflow** sur `cleanup`, vérifier les 4 steps verts (pnpm/action-setup, db:check-drift, db:check-rpcs, db:check-functions) et que "Open issue on failure" ne se déclenche pas. C3 a fixé le YAML mais aucun run cron n'a encore été observé exit 0 — `workflow_dispatch` est le moyen le plus rapide de lever ce doute sans attendre lundi.
- D6 — GH Actions Node.js 24 migration **déférée** (warning depuis fin 2025 sur `actions/checkout@v4`, `setup-node@v4`, `pnpm/action-setup@v4`). Stratégie : laisser auto-upgrade arriver le 2 juin 2026, observer, ajuster si breakage. Documenté dans roadmap §11.

### ✅ Fait (Sprint Hygiene-CI — livré 2026-05-07)

- E1 — `.gitattributes` ajouté à la racine ([./.gitattributes](.gitattributes)) avec `* text=auto eol=lf` + binaires explicites. Élimine le warning `LF will be replaced by CRLF` sur chaque commit Windows et obsolète le fix Sprint Polish-CI / D2 en steady state (le working tree est désormais LF côté policy, plus juste côté storage). **Renormalize a été un no-op** : `git ls-files --eol` confirme que repo + working tree étaient déjà LF (`i/lf w/lf`) — la storage était bonne, ce qu'il manquait c'était la déclaration explicite pour que tous les clones (Mac/Linux/Windows) convergent sur LF dans le working tree. Le `replace(/\r\n/g, '\n')` dans `scripts/check-drift.mjs` reste comme défense en profondeur si un dev a `core.autocrlf=true` localement.
- E2 — `pnpm db:check-types-fresh` ([scripts/check-types-fresh.mjs](scripts/check-types-fresh.mjs)) : compare `lib/database.types.ts` ↔ ce que `supabase gen types typescript --linked --schema public` produit à l'instant T contre prod. Comble le trou que `db:check-drift` ne couvre pas (drift schéma SQL ≠ drift types générés). Pattern miroir de `check-drift.mjs` : `unifiedDiff()` line-by-line, normalisation CRLF→LF + trimEnd des deux côtés, `process.exitCode` (consistant avec sibling scripts), exit 0/1/2. **Windows quirk** : `child_process.spawnSync` avec `shell: true` requis pour résoudre le `.cmd` shim de pnpm — args hardcodés (zero injection surface). Wirage CI : step ajouté à **db-drift-pr.yml ET db-drift-check.yml** avec `if: always()` ; path filter du PR-time gate étendu à `lib/database.types.ts`. **Verif end-to-end** : exit 0 sur tree clean, exit 1 avec diff résumé sur édition manuelle, exit 2 sur token absent.
- E3 — **✅ Validé end-to-end** après 2 fixes hotfix sur `cleanup` (5d8292a). Le premier `workflow_dispatch` a surfacé 2 vrais bugs cachés depuis la création des workflows (H5/A4) — exactement le pattern C3 où la première vraie boucle de feedback CI révèle ce que la default branch couvre. **Bugs trouvés et fixés** :
  - **(a) workflow_dispatch indisponible** : le bouton "Run workflow" ne s'affiche que si la default branch GitHub a le fichier YAML. `main` n'a JAMAIS reçu les workflows (cleanup est 64 commits ahead). Fix : swap default branch `main → cleanup` via Settings → Branches. Avant ça, le cron weekly schedulé depuis H5 n'avait littéralement aucun moyen de tourner — il était silencieusement no-op depuis sa création. Action future : quand cleanup sera mergé dans main, re-swap si on veut main comme trunk.
  - **(b) `db:check-types-fresh` fail en CI** : `Cannot find project ref. Have you run supabase link?`. Root cause : `supabase gen types --linked` lit un fichier d'état local créé par `supabase link` — fonctionne sur la machine du dev (où le link a été fait une fois) mais pas dans un fresh checkout CI. Fix : passer le détecteur à `--project-id <ref>` (pattern miroir de `check-rpcs.mjs:19`, `SUPABASE_PROJECT_REF` env ou fallback hardcodé `jzmppreybwabaeycvasz`, validé `/^[a-z]{20}$/` avant splice shell). Output byte-identique à `--linked` contre le même projet (vérifié), donc `pnpm db:types` reste sur `--linked` (plus rapide localement, pas de ref à respecifier).
  - **(c) Issue creation 403** : `Resource not accessible by integration` sur le step `Open issue on failure`. Root cause : le GITHUB_TOKEN par défaut a uniquement `contents:read` / `metadata:read` / `packages:read`. Créer une issue requiert `issues:write`. Sans bloc `permissions:` explicite, le workflow hérite des read-only defaults. Fix : ajouter `permissions: { contents: read, issues: write }` au job level (pas workflow level — plus conservateur). Note : ce step n'aurait JAMAIS eu la chance de fonctionner depuis H5 — c'est le bug le plus sournois car il ne se déclenche qu'en cas de failure (et les autres checks étaient verts à chaque exec PR). Le bug (a) cachait le bug (c).
  - **Re-dispatch après fixes** : 5 steps verts (pnpm/action-setup, db:check-drift, db:check-rpcs, db:check-functions, db:check-types-fresh), "Open issue on failure" skipped (if: failure() non-déclenché). Filet CI désormais réellement opérationnel pour la première fois. Cron lundi 08:00 UTC tournera vraiment.

### ✅ Fait (Sprint Code-CI — livré 2026-05-07)

- F1 — [.github/workflows/code-checks.yml](.github/workflows/code-checks.yml) ajouté : PR-time gate `pnpm typecheck` + `pnpm test:run` (avec `if: always()` sur le test step pour qu'il fire même si typecheck échoue) sur tout PR touchant code TS/config/deps. **Premier filet code-side** après 6 sprints DB-side. Pattern miroir [.github/workflows/db-drift-pr.yml](.github/workflows/db-drift-pr.yml) — pas de `with: version` sur `pnpm/action-setup@v4` (leçon Sprint Cleanup-Legacy / C3 — conflit avec `packageManager`), pas de bloc `permissions:` (workflow ne crée pas d'issue, default `contents:read` suffit). **Verif end-to-end full PR test ceremony** (matérialisation directe de la leçon E3 : ne jamais déclarer un workflow livré sans l'avoir vu fire) : (a) branche `test-f1-typecheck` avec `const _: number = 'string'` dans [lib/finance/piggy-bank.ts](lib/finance/piggy-bank.ts) → CI rouge `error TS2322` exit 2, revert pushé alors PR encore ouverte → CI vert observé ; (b) branche `test-f1-test` avec `expect(true).toBe(false)` dans [lib/debug-guard.test.ts](lib/debug-guard.test.ts) → step "TypeScript check" vert, step "Vitest single run" rouge `AssertionError` exit 1 (valide `if: always()`), revert vert localement (CI re-fire pas observé : push après PR fermée — pattern attendu, pas un bug). Path filter sanity non explicitement testée — trustée par les patterns GH Actions natifs (`paths:` est config-driven, bien testé en amont).
- F2 — `pnpm db:types` aligné sur `--project-id jzmppreybwabaeycvasz --schema public` (au lieu de `--linked --schema public`). Cohérence avec `db:check-types-fresh` post Sprint Hygiene-CI / E2 hotfix. **Élimine la dépendance à `supabase link`** pour les fresh clones — un dev qui clone le repo et tape `pnpm db:types` ne se prend plus l'erreur `Cannot find project ref. Have you run supabase link?` que le runner CI prenait avant E2. Output byte-identique à `--linked` contre le même projet (vérifié au commit : `git diff lib/database.types.ts` empty après `pnpm db:types`). Le ref est hardcodé dans `package.json` (pas une env var) car (a) public, visible dans `lib/supabase-{client,server}.ts` URLs, pas un secret ; (b) shell expansion brittle sur Windows ; (c) git diff rend explicite tout futur swap vers staging.
- F3#1 — Local git remote renommé `Popoth_App_Claude.git → popoth.git` (drop la redirection GitHub post Sprint Cleanup-Legacy / C3, smoke test `git fetch` réussi). Cosmetic, pas un commit (modif `.git/config` local).
- F3#2 — README.md typo corrigé : ligne 112 disait `db:check-types-fresh` utilise `--linked`, c'était `--project-id` depuis hotfix Sprint Hygiene-CI / E2. Folded dans le commit closeout.
- F3#3 — **À OBSERVER PASSIVEMENT** : confirmer que le run cron du lundi suivant 08:00 UTC est vert sur https://github.com/PothieuG/popoth/actions/workflows/db-drift-check.yml. Sprint Hygiene-CI / E3 a validé un `workflow_dispatch` mais pas encore un vrai `schedule`. Si le run hebdo bug, c'est probablement un nouveau bug cron-only (improbable mais pas zéro).
- **Observation Node.js 20 deprecation** : les 2 runs CI de la verif F1 ont fait remonter le warning `Node.js 20 actions are deprecated... forced to Node.js 24 by default starting June 2nd, 2026` sur `actions/checkout@v4`, `actions/setup-node@v4`, `pnpm/action-setup@v4`. C'est exactement le défer Sprint Polish-CI / D6 — on est ~1 mois avant l'échéance. Stratégie inchangée : laisser auto-upgrade arriver le 2 juin 2026, observer, ajuster si breakage.

### ✅ Fait (Sprint Lint-Followups — livré 2026-05-08)

- Item 1 — bug réel `recover.ts` v1/v2 type mismatch fermé. `recoveryResults.bank_balance` et `.piggy_bank` typés strict `boolean` (au lieu de `boolean | number`). Le path v2 brachait sur `data.length` (numérique, falsy si 0) tandis que v1 assignait `true` — divergence silencieuse pour tout consumer faisant `=== true`. Fix : split `CountKey` en `CountKey + BooleanKey + ResultKey`, branche dans `restoreTable` ([app/api/monthly-recap/recover/route.ts](app/api/monthly-recap/recover/route.ts:160-167) + ligne 234). 3 régressions ajoutées dans [lib/**tests**/api-regressions.test.ts](lib/__tests__/api-regressions.test.ts) gated `SUPABASE_API_TESTS=1` couvrant v1, v2 vide, v2 avec rows — toutes assertent `toStrictEqual(boolean)`. Pattern dynamic-import + cleanup cascade calé sur les 3 tests T3 existants.
- Item 2 — **31 → 0 vulnerabilities** triagées via `pnpm audit`. Direct upgrades : `next 16.1.6 → 16.2.6` (closes 4 next CVEs : DoS server components, request smuggling, postponed resume DoS, HMR CSRF) + `postcss 8.5.6 → 8.5.14` (XSS via `</style>`). Transitives via `pnpm.overrides` ([package.json](package.json)) sur 12 packages : `minimatch@3.x→3.1.4` + `9.x→9.0.7` (3 ReDoS CVEs sur 291 paths), `flatted→3.4.2`, `picomatch@2.x→2.3.2` + `4.x→4.0.4`, `brace-expansion@1.x→1.1.13` + `2.x→2.0.3`, `ajv@6→6.14.0`, `js-yaml→4.1.1`, `yaml@2→2.8.3`, `playwright→1.55.1`, `serialize-javascript→7.0.5`, `lodash→4.18.0`, `glob@10→10.5.0`, `postcss→8.5.10` (couvre next bundled). Aucun des 3 `ignore` rules existants (tailwindcss majors, supabase-js ≥2.105, eslint-config-next ≥16) n'a bloqué un security PR — les 31 CVEs étaient toutes ailleurs. `pnpm audit` reporte désormais "No known vulnerabilities found".
- Item 3 — hook Husky `pre-push` ajouté ([.husky/pre-push](.husky/pre-push)) lançant `pnpm lint:check && pnpm typecheck` avec fail-fast. `husky@^9.1.7` en devDep + `prepare` script. Première ligne de défense locale avant le PR gate `code-checks.yml` — un `: any` injecté sort exit 1 sans push. Bypass `git push --no-verify` reste fonctionnel. Sprint 1 étendra avec `lint-staged` + commitlint.

### ✅ Fait (Sprint Hygiène-Code — livré 2026-05-08)

- **Inventaire pré-sprint a invalidé 4 des 8 objectifs du prompt source** ([prompts/prompt-02-code-quality.md](prompts/prompt-02-code-quality.md), audit 2026-04). Stale post-Lint-Baseline-Cleanup : `: any` count = 0 (pas 57), 5/5 catches loguent déjà via `console.error`, patterns `array[key]` checkent l'objet (safe), 100% des `key` listes utilisent `key={item.id}`. Décision user : scope Medium = 4 chantiers réels + `useAuth()` rétro-compat.
- **Chantier 1** — magic numbers extraits dans [lib/constants/auth.ts](lib/constants/auth.ts) (`SESSION_EXPIRATION_SECONDS=3600`, `SESSION_EXPIRATION_JOSE='1h'`, `SESSION_REFRESH_INTERVAL_MS=50*60*1000`, `AUTH_CHECK_INTERVAL_MS=5*60*1000`) + [lib/constants/finance.ts](lib/constants/finance.ts) (`ROUNDING_TOLERANCE=0.01`). 13 substitutions : 2× [lib/session.ts](lib/session.ts), 2× [lib/session-server.ts](lib/session-server.ts), 2× [contexts/AuthContext.tsx](contexts/AuthContext.tsx), 6× [app/api/monthly-recap/process-step1/route.ts](app/api/monthly-recap/process-step1/route.ts). **Le `45` "magic" du prompt n'a jamais existé** — c'était un line-number dans middleware.ts (la ligne 45 contient `return response`). Le vrai TTL est `'1h'` / `3600` dans `lib/session.ts`.
- **Chantier 2** — dead code : `getRemainingToLiveHistory` + `getGroupRemainingToLiveHistory` (78 LOC, 0 callsites, vérifié grep) + interface `RemainingToLiveSnapshot` (devenue dead avec) supprimées de [lib/financial-calculations.ts](lib/financial-calculations.ts) (~95 LOC total). Comment scope-cast ajusté pour ne plus citer `RemainingToLiveSnapshot` comme exemple. **Pas de bloc "SUPPRIMÉ" exécutable** comme prétendait l'audit — juste 4 commentaires informatifs (lignes 211, 240, 485, 679 actuelles) qui documentent une décision métier ("ce calcul a été retiré du flux à la demande utilisateur") et qui restent utiles. Recovery : revert du commit.
- **Chantier 3** — split `AuthContext` ([contexts/AuthContext.tsx](contexts/AuthContext.tsx)) en `AuthUserContext` (`{ user, loading, error, isLoggedIn }`) + `AuthActionsContext` (`{ login, register, logout, clearError, refreshUserSession }`). Nouveaux hooks `useAuthUser()` / `useAuthActions()`. `useAuth()` préservé comme **agrégateur rétro-compatible** (`{ ...userCtx, ...actionsCtx }`) — les 5 consumers existants (via `hooks/useAuth.ts` qui spread `...authContext`) ne sont pas migrés (scope Medium, pas Full). Détails techniques : `useState` → `useRef` pour les 2 intervals (refresh + auth-check) — élimine la re-render churn quand un interval start/stop. `userRef` ajouté pour que `startAuthCheck` lise le user courant sans listing-as-dep. Public handlers wrappés en `useCallback` avec deps stables. eslint-disable du init effect retiré (les helpers sont maintenant tous stables via `useCallback([])`).
- **Chantier 4** — 5 modals lazy-loadés dans [components/dashboard/PlanningDrawer.tsx](components/dashboard/PlanningDrawer.tsx) via `next/dynamic` + `{ ssr: false }` : `AddBudgetDialog`, `AddIncomeDialog`, `EditBudgetDialog`, `EditIncomeDialog`, `ConfirmationDialog`. Aucun loading skeleton (latence négligeable sur action utilisateur). Réduction du bundle initial dashboard. **Hors scope** mais identifié : `AddTransactionModal` (491 LOC, importé statiquement par `app/dashboard/page.tsx` + `app/group-dashboard/page.tsx`) + `SavingsDistributionDrawer` (540 LOC, transitif via `SavingsDrawer`) + `PlanningDrawer` lui-même (importé par `FinancialIndicators`) — couverts par Sprint Hygiène-Code-v2 ([prompts/prompt-02-code-quality-v2.md](prompts/prompt-02-code-quality-v2.md)).
- **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` exit 0, `pnpm test:run` 4 passed / 21 skipped (gated tests sans env vars), `pnpm build` exit 0 (57/57 routes). Smoke browser confirmé manuellement par user. Livré 2026-05-08, 4 commits sur `cleanup` (`a2e0b18` magic numbers / `b3d7be6` dead code / `6844089` AuthContext split / `58620b9` lazy modals), score estimé ~86/100.

### ✅ Fait (Sprint Hygiène-Code-v2 — livré 2026-05-08)

- **Item 1** — perf benefit du split AuthContext (Sprint Hygiène-Code chantier 3) actualisé au niveau consumer. [hooks/useAuth.ts](hooks/useAuth.ts) refactoré : `useRequireAuth()` / `useRequireGuest()` subscribent désormais à `useAuthUser()` only (au lieu de l'agrégateur via `useAuthContext`) ; `useLogin()` / `useRegister()` splittent leur read en `useAuthUser()` (pour `error`) + `useAuthActions()` (pour `login` / `register` / `clearError`) ; nouveau hook `useLogoutAndRedirect()` exporté pour les single-concern consumers, subscribe à `useAuthActions()` only. `useAuth()` agrégateur préservé pour rétro-compat (subscribe both, returned object inchangé incluant les utilities `redirectToLogin` / `redirectAfterLogin` / `requireAuth` / `requireGuest` / `sessionExpiring` / `isGuest` / `hasUser` / `userEmail` / `userId`). 4 page consumers migrés : `app/page.tsx` → `useAuthUser()`, `app/dashboard/page.tsx` + `app/group-dashboard/page.tsx` + `app/settings/page.tsx` → `useLogoutAndRedirect()`. `app/connexion/page.tsx` consumer-side inchangé (utilise `useLogin()` + `useRequireGuest()` du wrapper, le bénéfice perf flow par le refactor des internals).
- **Item 2** — extension du lazy-load (Sprint Hygiène-Code chantier 4) à 3 modals/drawers outer-level totalisant ≈1.7k LOC. `AddTransactionModal` (491 LOC, importé par `app/dashboard/page.tsx` + `app/group-dashboard/page.tsx`) lazy-loadé via `dynamic(() => import(...), { ssr: false })`. `PlanningDrawer` (688 LOC) lazy-loadé au niveau [components/dashboard/FinancialIndicators.tsx](components/dashboard/FinancialIndicators.tsx) — préserve les 5 chunks internes du sprint v1 (additionnels). `SavingsDistributionDrawer` (540 LOC) lazy-loadé à l'intérieur de [components/dashboard/SavingsDrawer.tsx](components/dashboard/SavingsDrawer.tsx) (le wrapper de 29 LOC reste static — lazy-loader le wrapper serait du cargo cult). Pattern miroir de `PlanningDrawer.tsx:10-14` (sprint v1).
- **Item 3** — header `> ⚠️ Note 2026-05-08` ajouté en tête de [docs/audit/02-code-quality.md](docs/audit/02-code-quality.md). Le snapshot 2026-04 n'est pas réécrit (préservation historique) ; le bandeau redirige vers CLAUDE.md §11 (la roadmap maintenue) et liste les 5 sections devenues stales post-Sprints Lint-Baseline-Cleanup / Lint-Followups / Hygiène-Code (magic numbers, AuthContext split, dynamic modals, `: any` count, silent errors / fragile `array[key]`).
- **Plan / pré-sprint** : Phase 1 inventaire via 2 Explore agents en parallèle a confirmé le mapping consumers ↔ contexts du prompt v2 et a surfacé une trouvaille : `useRegister()` n'a aucun consumer (la page inscription utilise `supabase.auth.signUp` directement, pas le hook). Décision : refactor `useRegister()` pour cohérence avec `useLogin()` (effort marginal, garde la surface API stable) plutôt que delete dead code (eût été un changement scope-creep).
- **Verif end-to-end** : `pnpm typecheck` + `pnpm lint:check` + `pnpm test:run` (4 passed / 21 skipped, gated) + `pnpm build` exit 0 (57/57 routes) — réexécutés après chaque item. Smoke browser non-fait dans cette session (deferred à la verif user).
- **Plan dans** `C:\Users\gille\.claude\plans\sprint-hygi-ne-code-staged-pizza.md`. Livré 2026-05-08, 3 commits code + 1 closeout sur `cleanup` (`53a9c97` refactor auth / `91ed481` lazy-load modals / `a62f9d6` audit doc header / closeout). Score estimé ~87/100.

### ✅ Fait (Sprint Refactor-Architecture — livré 2026-05-08)

- **Scope arbitré Phase 1** : 5 chantiers sur 6 du prompt source ([prompts/prompt-03-architecture.md](prompts/prompt-03-architecture.md)). Le Context local PlanningDrawer a été **skippé** (pattern Sprint Hygiène-Code) après que l'inventaire a montré que l'audit était stale : `context` est lu **une seule fois** au top de [components/dashboard/PlanningDrawer.tsx:25](components/dashboard/PlanningDrawer.tsx) pour alimenter 4 hooks, jamais re-passé aux dialogs enfants. Les 3 props vraiment "drilled" (`totalBudgets`, `totalIncomesWithSalary`, `onPlanningChange`) ne traversent qu'1 niveau vers 4 dialogs. Un Context aurait remplacé ces 3 props par un provider+hook — gain marginal, churn pour rien.
- **Chantier 1** — middleware self-call → import direct. [middleware.ts:65-74](middleware.ts) faisait un `fetch(${baseUrl}/api/monthly-recap/status?...)` synchrone bloquant en Edge runtime. Extraction de la logique métier en [lib/recap/check-status.ts](lib/recap/check-status.ts) exportant `checkRecapStatus(userId, context: 'profile' | 'group')`. Edge-safe vérifié Phase 1 : `lib/supabase-server.ts` n'importe que `@supabase/supabase-js` + `process.env`, et `validateSessionToken` (`lib/session-server.ts`) utilise `jose` + cookie parsing manuel — pas de Node stdlib. La route [app/api/monthly-recap/status/route.ts](app/api/monthly-recap/status/route.ts) devient un thin wrapper qui valide la session, parse le contexte et délègue à `checkRecapStatus`. Le middleware appelle directement la fonction. Response shape de la route préservée à l'identique (pas de breaking change). Pattern d'erreur : `RecapStatusError` avec codes `PROFILE_NOT_FOUND` / `NO_GROUP` — la route mappe `404` / `400`, le middleware avale silencieusement `NO_GROUP` (l'utilisateur sans groupe n'est pas concerné par le récap groupe). Commit `35c86e7`.
- **Chantier 2** — namespace `/api/finance/*` unifié + aliases rétro-compat avec header `Deprecation: true`. **Volet le plus lourd** : 13 routes existantes (9 sous `/api/finances/`, 2 sous `/api/financial/`, 2 standalone `/api/budgets` + `/api/incomes`), 29 fetch URLs dans 7 hooks + 1 component, 3913 LOC à déplacer. Pattern d'extraction : chaque handler atterrit dans `lib/api/finance/<route>.ts` (13 modules). Les nouvelles routes [app/api/finance/\*\*/route.ts](app/api/finance/) re-exportent `GET/POST/PUT/DELETE` depuis ces lib (1-ligne `export { GET, POST } from '@/lib/api/finance/dashboard'`). Les anciennes routes wrappent les handlers via [lib/api/with-deprecation.ts](lib/api/with-deprecation.ts) qui ajoute `response.headers.set('Deprecation', 'true')`. **2 ambiguïtés découvertes en Phase 1, préservées telles quelles (zero-risk)** : (a) `/api/finances/dashboard` (GET, POST) ≠ `/api/financial/dashboard` (GET) — handlers différents, le second devient `/api/finance/summary` pour distinguer ; (b) `/api/budgets` et `/api/finances/budgets/estimated` cohabitent sous `/api/finance/budgets` et `/api/finance/budgets/estimated` (Next.js App Router permet la cohabitation file vs folder). Le cleanup de ces ambiguïtés est documenté dans [prompts/prompt-03-architecture-v2.md](prompts/prompt-03-architecture-v2.md). Commit `2e00339` (48 fichiers, +4065/-3939). **Verif** : `pnpm build` montre les 13 nouveaux paths sous `/api/finance/` ET les anciens encore en place ; `grep -rn "/api/(finances|financial|budgets|incomes)/" hooks/ components/ app/` ne retourne plus rien (tout migré).
- **Chantier 3** — `useRavValidation` extrait de `AddTransactionModal`. Le calcul du blocage RAV (lines 62-96 de [components/dashboard/AddTransactionModal.tsx](components/dashboard/AddTransactionModal.tsx)) était une IIFE inline sans memoization, recalculée à chaque render. Extraction en hook [hooks/useRavValidation.ts](hooks/useRavValidation.ts) qui prend `{ transactionType, isExceptional, amount, remainingToLive, budgetId, budgetProgress }` et retourne `{ blocked, newRav }` via `useMemo`. Logique inchangée. Commit `3e8d842`.
- **Chantier 4** — `useStep1Data` extrait de `MonthlyRecapStep1`. Le useEffect+fetch direct (lines 72-116 de [components/monthly-recap/MonthlyRecapStep1.tsx](components/monthly-recap/MonthlyRecapStep1.tsx)) devient [hooks/useStep1Data.ts](hooks/useStep1Data.ts) exportant `{ data, loading, error, refresh }`. Le type `Step1Data` (38 LOC) déplacé du composant vers le hook (+ exporté). Le composant subscribe via `const { data: step1Data, loading: isLoading, error, refresh: fetchStep1Data } = useStep1Data(context)` — destructuring + alias pour préserver les noms locaux du composant (zero diff dans le JSX qui utilise `step1Data.x`). Hook custom maison, pas TanStack Query (scope du sprint). Commit `bf76731`.
- **Chantier 5** — `useBudgetProgress` dedup. Le hook avait 3 sources redondantes : `useState<BudgetProgress[]>` (line 74) publié au consumer, `useMemo calculatedProgresses` (lines 137-142, vraie source de vérité), et `useEffect` (lines 145-149) qui copiait l'un dans l'autre à chaque rerender. Pareil pour `loading`/`error` : 2 useState mirroir de `expensesLoading` / `expensesError` de `useRealExpenses`. Drop le state, drop l'effet de sync, retourne directement le `useMemo` renommé `budgetProgresses`. `loading` et `error` remontent directement. Surface API exposée inchangée (consumer-side : `PlanningDrawer.tsx:82` et `BudgetProgressIndicator.tsx` continuent de lire `{ budgetProgresses, loading, error, refreshProgress, getBudgetProgress }`). Commit `3601b28`.
- **Verif end-to-end** : `pnpm typecheck` + `pnpm lint:check` + `pnpm build` exit 0 après chaque commit. Build expose les 13 nouveaux paths `/api/finance/**` + les 13 anciens (alias). Smoke browser non-fait dans cette session (deferred à verif user). Tous les critères de validation du prompt source vérifiés : pas de self-call HTTP dans middleware, `checkRecapStatus` référencé ≥3 fois, plus aucun call à `/api/(finances|financial|budgets|incomes)/` dans hooks/components.
- **Plan dans** `C:\Users\gille\.claude\plans\refactor-architecture-glimmering-parnas.md`. Livré 2026-05-08, 5 commits sur `cleanup` (`35c86e7` middleware / `2e00339` namespace / `3e8d842` ravValidation / `bf76731` step1Data / `3601b28` budgetProgress). Score estimé ~89/100. Suite documentée dans [prompts/prompt-03-architecture-v2.md](prompts/prompt-03-architecture-v2.md) : cleanup des deprecated après 1 sprint d'observation + investigation des 2 ambiguïtés résiduelles (`/api/finance/budgets` GET vs `/api/finance/budgets/estimated` GET ; `/api/finance/dashboard` vs `/api/finance/summary` redondance).

### ⚠️ Drift C3 résolu

Le drift `supabase_migrations.schema_migrations` ↔ `pg_proc` (les 4 RPC C3 marquées appliquées sans exécution du SQL) est documenté dans [docs/audit/POST-MORTEM-C3-DRIFT.md](docs/audit/POST-MORTEM-C3-DRIFT.md). Le filet aujourd'hui :

- `pnpm db:check-drift` pour le drift table/colonne/policy/index.
- `pnpm db:check-rpcs` pour la présence des 4 RPC C3 dans `pg_proc` (livré Sprint Hardening / H4).
- `pnpm db:check-types-fresh` pour la désynchro `lib/database.types.ts` ↔ schéma prod (livré Sprint Hygiene-CI / E2).
- `SUPABASE_RPC_CONCURRENCY_TESTS=1 pnpm test:run` pour vérifier que les RPC fonctionnent réellement sous concurrence (gated).

Voir [docs/audit/RLS-FINDINGS.md](docs/audit/RLS-FINDINGS.md) (état pré-Sprint DB), [prompts/prompt-00-executive-summary-v5.md](prompts/prompt-00-executive-summary-v5.md) (Sprint Polish — livré), et [prompts/prompt-00-executive-summary-v6.md](prompts/prompt-00-executive-summary-v6.md) (Sprint Audit-Triggers — livré).

## 8. À FAIRE / À NE PAS FAIRE

### ✅ À faire

- Pour toute écriture sur `piggy_bank.amount`, `bank_balances.balance`, `estimated_budgets.cumulated_savings` : **utiliser obligatoirement** les helpers `lib/finance/*`. Pas de SELECT-then-UPDATE direct.
- Pour tout nouveau composant qui consomme l'auth : utiliser `useAuthUser()` (state) ou `useAuthActions()` (handlers) — ou les hooks composés `useRequireGuest()` / `useLogin()` / `useLogoutAndRedirect()` dans [hooks/useAuth.ts](hooks/useAuth.ts). Sprint 2-followup-v5 a supprimé l'aggregator `useAuth()` (et `useRequireAuth` / `useRegister` du même fichier) : 0 consumer en prod. Ne PAS le réintroduire sans cas d'usage concret — les deux contexts `AuthUserContext` + `AuthActionsContext` couvrent tous les besoins, et un aggregator force un re-render sur tout changement de l'un ou l'autre.
- Pour toute valeur "magique" (TTL, intervalle, tolérance d'arrondi) : déclarer dans [lib/constants/](lib/constants/) (`auth.ts` / `finance.ts`) avant d'utiliser. Pas de littéraux numériques répétés à plusieurs sites.
- **Pour toute nouvelle route API finance** (Sprint Refactor-Architecture) : créer le handler dans [lib/api/finance/<route>.ts](lib/api/finance/) (named exports `GET`/`POST`/`PUT`/`DELETE`), puis créer [app/api/finance/<path>/route.ts](app/api/finance/) qui ré-exporte. Ce pattern garde les `route.ts` triviaux et permet aux handlers d'être importés ailleurs (tests, autres routes, middleware). Si tu dois renommer une route existante, garder l'ancien chemin comme alias en wrappant le handler dans une fonction qui ajoute `response.headers.set('Deprecation', 'true')` avant return — pendant 1 sprint d'observation, puis supprimer (pattern v1+v2 : `lib/api/with-deprecation.ts` a existé entre Sprint Refactor-Architecture et Sprint Refactor-Architecture-v2 puis a été supprimé après cleanup ; recréer ad-hoc si besoin). Vérifier 0 hit sur les anciens chemins en prod avant le drop final.
- **Pour tout nouveau handler API** (Sprint Refactor-Architecture-v3 + v4 + v5) : utiliser `withAuth(handler)` ou `withAuthAndProfile(handler)` depuis [lib/api/with-auth.ts](lib/api/with-auth.ts). `withAuthAndProfile` fetch `select('id, group_id, first_name, last_name')` (étendu en v4 — first_name/last_name sont free) et passe `{ userId, profile }` au callback. `withAuth` valide la session et passe `{ userId }` — utiliser pour les handlers auth-only OU pour les conditional-fetch handlers qui ne tirent le profil que sur `forGroup`/`context==='group'` (laisser cette fetch lazy dans le body). Le wrapper retourne 401 + `'Session invalide'` (CLAUDE.md §6) ou 404 + `'Profil non trouvé'`. **Pour les routes dynamiques** (`app/api/.../[id]/route.ts`) : utiliser le generic 2nd arg `withAuth<RouteParams>(async (req, ctx, routeContext) => { const { id } = await routeContext.params })` — depuis Sprint v5, l'overload dynamic-route rend `routeContext` non-optionnel quand `TParams` est fourni, donc plus besoin du `!` (le compte est désormais 0 dans `app/api/`). **Pas de try/catch dans le wrapper** — chaque handler garde son `try/catch` route-aware avec `console.error('... /api/X:', error)` (le chantier console.log cleanup les balayera). `summary.ts` et `profile.ts:GET` préservent leurs fallbacks (200-with-default / 200-on-no-profile) dans leur catch interne. **Routes hors scope du wrapper** (à laisser en boilerplate explicite) : `app/api/debug/**` (blockInProduction wrap d'abord), `app/api/auth/**` (créent/rafraîchissent la session, ne la valident pas). Depuis Sprint v5, `app/api/monthly-recap/process-step1/route.ts` est sur `withAuthAndProfile` pour son auth header — l'extraction de la logique métier reste chantier I5 séparé.
- **Pour le middleware ou tout code en Edge runtime** : ne JAMAIS faire de `fetch` self-call HTTP vers une route locale (latence + coût + risque de boucle). Extraire la logique en lib pure et l'importer directement. Pattern installé dans [lib/recap/check-status.ts](lib/recap/check-status.ts) (Sprint Refactor-Architecture chantier 1) — vérifier que les imports transitifs restent Edge-safe (`@supabase/supabase-js` ✅, `jose` ✅, `next/headers` ❌, `node:fs`/`node:path` ❌, `lib/financial-calculations.ts` ❌ trop lourd).
- Pour tout nouveau composant qui consomme un fetch à durée de vie limitée (mount + retry) : utiliser **TanStack Query** (`useQuery` / `useMutation`) — c'est le standard du repo depuis Sprint 1.5. Pattern type : `useQuery({ queryKey: ['<domain>', ...deps], queryFn: async ({ signal }) => fetch(url, { signal }).then(r => r.json()) })`. Pour les CRUD : `useMutation` + `onSuccess: () => queryClient.setQueryData(...)` (optimistic) ou `queryClient.invalidateQueries({ queryKey: [...] })` (refetch). Provider mounted dans [app/layout.tsx](app/layout.tsx). Préserver le `{ data, loading, error, refresh }` shape côté consumer pour minimiser les changements aux appelants. **Cross-domain invalidation** : depuis Sprint 2, le bridge legacy `triggerFinancialRefresh`/`registerFinancialRefreshCallback` est supprimé. Pour qu'une mutation CRUD invalide les vues financières globales, importer `invalidateFinancialRefreshes` depuis [@/lib/query-client](lib/query-client.ts) (single source of truth depuis Sprint 2-followup) et l'invoquer depuis chaque `onSuccess` — il appelle `qc.invalidateQueries` sur les 3 keys impactées (`['financial-summary']`, `['progress-data']`, `['budgets']`). Pattern installé dans useBudgets, useIncomes, useRealExpenses, useRealIncomes, useProfile. Si tu ajoutes une key cross-domain au flux financier, modifier `lib/query-client.ts` est suffisant — tous les hooks consomment le même helper. Pour un hook fetcher pur (lecture seule) sans CRUD : pattern simple [hooks/useStep1Data.ts](hooks/useStep1Data.ts) (~25 LOC). **Pour toute mutation qui change `profile.group_id` ou la membership d'un groupe** (Sprint 2-followup-v2) : invalider AUSSI `['profile']` + `['groups']` en plus de la cascade financière. Le serveur (POST/DELETE `/api/groups/[id]/members`, POST/DELETE `/api/groups`) update `profile.group_id` ; le trigger `profiles_contribution_recalc` recalcule les `group_contributions` ; le profil cache devient stale sans invalidation. Voir [hooks/useGroups.ts](hooks/useGroups.ts) (5 mutations create/update/delete/join/leave) pour le pattern. `updateGroup` n'invalide les financials QUE si `'monthly_budget_estimate' in updates` (mirror du fire conditionnel du trigger `groups_budget_contribution_recalc`).
- Pour les modal forms qui mirror un prop dans le state local (`useEffect(() => setForm(...editing))`) : utiliser le pattern **`key={editing.id}` + `useState(() => ...editing.foo)` lazy init** (Sprint 1.5 standard). Parent doit conditionnellement render `{isOpen && editing && <Modal key={editing.id} ... />}` pour que React unmount/remount cleanly à chaque ouverture. Pattern dans [components/dashboard/EditBudgetDialog.tsx](components/dashboard/EditBudgetDialog.tsx), [components/dashboard/EditIncomeDialog.tsx](components/dashboard/EditIncomeDialog.tsx), [components/dashboard/EditTransactionModal.tsx](components/dashboard/EditTransactionModal.tsx), [components/dashboard/AddTransactionModal.tsx](components/dashboard/AddTransactionModal.tsx) ; parents [components/dashboard/PlanningDrawer.tsx](components/dashboard/PlanningDrawer.tsx), [app/dashboard/page.tsx](app/dashboard/page.tsx), [app/group-dashboard/page.tsx](app/group-dashboard/page.tsx).
- **Pour tout hook qui mirror un calcul `useMemo` dans un `useState`** : c'est probablement de la duplication. Le `useMemo` est déjà la source de vérité, retourner-le directement et drop le state + l'effet de sync. Pattern leçon Sprint Refactor-Architecture chantier 5 sur [hooks/useBudgetProgress.ts](hooks/useBudgetProgress.ts).
- **Pour tout reducer / state machine `useReducer`** (Sprint 2-followup-v4) : extraire le reducer + ses types (`State`, `Action`, `initialState`) dans un module dédié sans `'use client'` (pattern : [contexts/auth-reducer.ts](contexts/auth-reducer.ts)). Ça donne une cible pure-unit testable sans setup React (`@testing-library/react` non requis), pattern miroir [lib/**tests**/auth-reducer.test.ts](lib/__tests__/auth-reducer.test.ts) — 1 cas par discriminant + 1 cas runtime invalid action via `as Action` + 1 identity guard `result !== input`. Inclure un `default` branch avec `const _: never = action` pour l'exhaustiveness compile-time.
- **Pour tout Context value alimenté par `useReducer` (ou tout state qui change identity à chaque dispatch)** (Sprint 2-followup-v4) : wrapper la value prop en `useMemo` avec deps slice-by-slice. Sans memoize, le littéral object est recréé à chaque render → tous les consumers re-render même si leur slice ne change pas, défaisant le bénéfice du split contexts (pattern Sprint Hygiène-Code). Voir [contexts/AuthContext.tsx](contexts/AuthContext.tsx) `userValue` / `actionsValue`. Si les actions sont déjà `useCallback`, le memoize de l'`actionsValue` est essentiellement no-op après le premier render mais signale "stable" et protège contre futurs ajouts non-mémoizés.
- Pour toute nouvelle route `/api/debug/*` : importer + appeler `blockInProduction()` en première instruction.
- Pour tout consommateur de `recap_snapshots.snapshot_data` : utiliser les types [lib/recap-snapshot.types.ts](lib/recap-snapshot.types.ts) (`SnapshotPayload` + `isSnapshotV2()`). Pas de `as any`.
- Lire `RLS-FINDINGS.md` + [docs/db/SCHEMA.md](docs/db/SCHEMA.md) avant d'ajouter une nouvelle table, une nouvelle policy, ou un nouveau trigger.
- Lancer `pnpm typecheck && pnpm test:run` après chaque modif significative.
- Lancer `pnpm lint:check` avant push (ou laisser le push gate `code-checks.yml` le faire) — exit 0 attendu depuis Sprint Lint-Baseline-Cleanup. Tout nouveau `: any`, var inutilisée, ou warning `react-hooks/exhaustive-deps` non-justifié sort la PR rouge. Pour les rares cas où un disable est légitime : `// eslint-disable-next-line <rule> -- <raison>` avec la raison explicite (pattern Phase 5 du sprint).
- Pour les requêtes hors de l'app (audit, migration, debug schéma) : préférer l'API Management `POST /v1/projects/{ref}/database/query` (sans Docker) plutôt que `psql` ou `db pull`. `scripts/export-schema.mjs` et `scripts/apply-sql.mjs` exposent ce pattern.
- Pour toute nouvelle RPC : `SECURITY DEFINER` + `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE TO service_role` + `SET search_path = public`. **Suivre la migration de** `NOTIFY pgrst, 'reload schema';` pour forcer le rafraîchissement du cache PostgREST (sinon `.rpc()` lève "Could not find the function in the schema cache" — leçon Sprint DB).
- Pour toute nouvelle fonction trigger ou modification d'une existante : versionner dans une migration dédiée (pattern : [supabase/migrations/20260512000000_capture_trigger_functions.sql](supabase/migrations/20260512000000_capture_trigger_functions.sql), `CREATE OR REPLACE FUNCTION`). Le baseline n'inclut PAS les bodies de fonction — c'est volontaire (pattern C3) et `pnpm db:check-functions` tient le filet. Ajouter le nom de la nouvelle fonction à `EXPECTED_FUNCTIONS` dans [scripts/check-trigger-functions.mjs](scripts/check-trigger-functions.mjs) si elle est custom (pas Supabase canonique).
- **Après toute migration touchant une fonction PL/pgSQL** (création, modification, suppression) : lancer `pnpm db:audit-functions` (Sprint Audit-Functions-v2 / B1). C'est l'audit générique qui liste TOUTES les `public.*` fonctions de `pg_proc` et confirme que chacune a un `CREATE FUNCTION` quelque part dans `supabase/migrations/`. Catches le cas A2 redux : une fonction load-bearing en prod sans trace dans le repo. **Pas dans le cron weekly** (plus lourd que `db:check-functions`) — à la main après chaque migration touchant une fonction.
- **Après toute migration ajoutant un `CREATE TYPE` / `CREATE DOMAIN` / `CREATE OPERATOR`** : lancer `pnpm db:audit-objects` (Sprint Cleanup-Legacy / C2). Étend l'audit B1 à 5 catégories `pg_catalog` — composite types, enums, domains, operators en plus des functions. Filtre les row-types implicites de `CREATE TABLE` pour ne pas flooder. **Pas dans le cron** (heavier que les check-\* detectors), à la main post-migration. Si exit 1 : suivre le pattern v7/B1 — capturer verbatim si load-bearing, ou DROP si dead code (cf. workflow capture-then-drop ci-dessous).
- **Pour DROP une fonction (ou tout objet DB) déjà capturée et confirmée dead code** (cas Sprint Cleanup-Legacy / C1) : workflow miroir du capture-then-drop. Pré-requis : la migration de capture existe déjà, vérifier 0 callsite app (Grep), 0 référence GitHub Actions, 0 cron Supabase / webhook / Edge Function (dashboard). Pattern :
  1. Migration `<TS>_drop_*.sql` avec `DROP FUNCTION IF EXISTS <name>(<args>);` (RESTRICT par défaut — ne PAS utiliser CASCADE, pour que Postgres refuse explicitement si une dépendance cachée existe). Inclure `NOTIFY pgrst, 'reload schema';` à la fin.
  2. `node scripts/apply-sql.mjs supabase/migrations/<TS>_drop_*.sql` (HTTP 201 + `[]` = OK ; tout autre code = dépendance détectée, investigate).
  3. `pnpm supabase migration repair --status applied <TS>` (obligatoire — sinon prochain `db push` retentera = drift C3 redux, et l'objet n'existant plus en prod le SQL retentera et échouera).
  4. `node scripts/export-schema.mjs supabase/migrations/20260101000000_remote_schema.sql` puis `pnpm db:check-drift` → exit 0.
  5. Régénérer types : `pnpm db:types` (Sprint Polish-CI / D1 a fixé l'inclusion du wrapper pnpm dans le fichier ; Sprint Code-CI / F2 a aligné sur `--project-id` pour fonctionner sans `supabase link`). Verifier `pnpm typecheck` exit 0.
  6. Recovery path : si on doit recréer l'objet, le body verbatim est dans la migration de capture (CREATE OR REPLACE idempotent — re-applicable via apply-sql.mjs).
- **Pour capturer rétroactivement une fonction PL/pgSQL qui existe déjà en prod** (cas Sprint Audit-Triggers / A2) : workflow strict, **NE PAS** faire `supabase db push` (collision sur la fonction existante). Pattern :
  1. Dump le body via [scripts/dump-functions.sql](scripts/dump-functions.sql) (étendre la liste si besoin) → `node scripts/apply-sql.mjs scripts/dump-functions.sql > tmp\\functions.json`.
  2. Coller le `def` (déjà sous forme `CREATE OR REPLACE FUNCTION` en PG 14+, vérifier) dans une migration `<TS>_capture_*.sql`. Préserver verbatim (LANGUAGE, SECURITY, SET search_path) — ne PAS ajouter `REVOKE/GRANT` boilerplate sur des fonctions trigger-only.
  3. `node scripts/apply-sql.mjs supabase/migrations/<TS>_capture_*.sql` (idempotent grâce à `CREATE OR REPLACE`).
  4. `pnpm supabase migration repair --status applied <TS>` (sinon prochain `db push` retentera = drift C3 redux).
  5. `node scripts/export-schema.mjs supabase/migrations/20260101000000_remote_schema.sql` puis `pnpm db:check-drift` → exit 0.
- Push gate prod : `pnpm supabase db push --dry-run` → STOP confirmation utilisateur → `db push` → re-audit Management API → commit.
- Régénérer les types après changement de schéma : `pnpm db:types` (sans redirection — le script s'écrit lui-même dans `lib/database.types.ts` depuis Sprint Polish-CI / D1). Les RPC service-role-only sont incluses depuis le passage à `--linked` (Sprint Cleanup-Legacy / C1) puis `--project-id` (Sprint Code-CI / F2 — élimine la dépendance à `supabase link`) — pas besoin de réaugmenter. Vérifier ensuite que le commit des types matche prod via `pnpm db:check-types-fresh` (Sprint Hygiene-CI / E2) — exit 0 = synchro, 1 = il faut re-`pnpm db:types` + commit. Le PR-time gate l'exécute aussi sur tout PR touchant `lib/database.types.ts` ou `supabase/migrations/**`.
- Après chaque migration non-triviale : lancer `pnpm db:check-drift`. Si exit 1, re-exporter le baseline via `node scripts/export-schema.mjs supabase/migrations/20260101000000_remote_schema.sql` et committer (sinon le détecteur reste rouge et on retombe dans la trap C3).
- **Quand une PR Dependabot est mergée** (workflow appris au follow-up Sprint DX-Verify, 2026-05-07 ; filet CI fermé Sprint Stabilize-Deps / S2) :
  1. `git pull origin cleanup` puis `pnpm install` pour aligner les modules locaux sur le nouveau lockfile.
  2. **`pnpm verify`** (Sprint DX-Verify / G1) — exit 0 attendu. **Depuis Sprint Stabilize-Deps / S2**, [.github/workflows/code-checks.yml](.github/workflows/code-checks.yml) re-tourne aussi sur `push: branches: [cleanup]`, donc une régression typecheck/test post-merge sort en CI rouge sans intervention. Mais le verify local reste utile pour itérer rapidement et couvrir les `db:*` checks (que le push gate ne lance pas).
  3. **Démarrer `pnpm dev`** et hit `curl http://localhost:3000/` au moins une fois — le typecheck + tests ne couvrent pas les régressions runtime/compile-CSS (e.g. react/react-dom version mismatch, tailwindcss v4 PostCSS plugin missing) qui ne se voient qu'au premier render. Le filet CI S2 capture la régression typecheck mais pas le runtime CSS — le smoke local reste nécessaire.
  4. **Si une PR Dependabot a cassé quelque chose** : préférer le **fix-forward** (`pnpm update <pkg>@<version>` + commit `revert: re-pin <pkg> to <version>`) plutôt que `git revert -m 1 <merge>`. Les merges Dependabot enchaînés touchent presque toujours le même lockfile → conflits sur `git revert -m 1` quasi-garantis. Pattern appliqué pour react (7989ed2) et supabase (3e37015) au follow-up DX-Verify.
  5. **Au moindre doute sur un major bump qui re-cassera au prochain scan** : ajouter un `ignore` dans [.github/dependabot.yml](.github/dependabot.yml) (3 patterns existants à reproduire — Sprint Stabilize-Deps / S1) :
     - `update-types: ["version-update:semver-major"]` pour bloquer tous les majors d'un package (cas tailwindcss).
     - `versions: [">=X.Y.Z"]` pour bloquer une plage précise après laquelle un breaking change a été introduit (cas supabase-js >=2.105).
     - `versions: [">=N.0.0"]` pour pinner sur un major existant (cas eslint-config-next ≥16).
  6. **⚠️ Interaction `ignore` ↔ Dependabot security updates** (GitHub Advanced Security activé 2026-05-07) : par défaut, **les `versions: [...]` rules bloquent AUSSI les security PRs**, pas seulement les version updates. En revanche, **les `update-types: ["version-update:*"]` rules ne bloquent que les version updates**, donc les security PRs passent toujours. Conséquence pratique :
     - Tailwind 4.x.x security CVE → security PR auto-créée (rule `update-types` n'affecte que version updates) ✅
     - Supabase-js ≥2.105 security CVE → security PR **bloquée** par notre `versions: ">=2.105.0"` ❌
     - eslint-config-next ≥16 security CVE → security PR **bloquée** par notre `versions: ">=16.0.0"` ❌
     - Si un CVE critique tombe sur supabase-js 2.105+ ou eslint-config-next 16+, l'alerte Dependabot **arrivera quand même** (Dependabot alerts est désactivé par les `ignore` rules uniquement si on ajoute `dismissed-types`). Action manuelle requise : retirer temporairement le `ignore` rule pour laisser le security PR être créée, puis re-mettre.

### ❌ À ne pas faire

- ❌ **Ne pas réintroduire** un fichier `lib/financial-calculations.ts` — la dépouille god file (1069 LOC) a été splittée en 8 modules sous [lib/finance/](lib/finance/) au Sprint Refactor-I4. Tout nouveau code finance va dans un module `lib/finance/` ad-hoc, exposé via le barrel [lib/finance/index.ts](lib/finance/index.ts).
- ❌ **Ne pas refactorer** l'algo `process-step1` (chantier I5 séparé) — uniquement remplacer les paires SELECT-then-UPDATE par RPC.
- ❌ **Ne pas modifier** [supabase/migrations/20260506000000_create_finance_rpcs.sql](supabase/migrations/20260506000000_create_finance_rpcs.sql). Pour corriger une RPC : `CREATE OR REPLACE` dans une nouvelle migration.
- ❌ **Pas de `any`** dans le nouveau code. Préférer type guards.
- ❌ **Pas de `console.log` ajouté** dans le nouveau code — utiliser `logger.debug/info` depuis [lib/logger.ts](lib/logger.ts) (cf. §6 Conventions / Logs). Le strip prod SWC les supprime quand même au build, mais ESLint flag les nouveaux sites en `warn`. `console.warn`/`console.error` directs restent autorisés ad-hoc (allowlist ESLint global), mais préférer `logger.warn`/`logger.error` pour la cohérence. **Pour les fichiers déjà migrés** (`middleware.ts`, `lib/expense-allocation.ts`, `lib/logger.ts` post-Lot 3), un per-file override escalade `no-console: 'error'` — toute regression sort la PR rouge. Compteur `console.log` legacy : **~985** (post-Sprint Cleanup-I8 / Lot 3) — migration progressive par les Lots 2 / 4-6 au fil des PRs.
- ❌ **Ne pas mocker la DB** dans les tests d'intégration — utiliser Supabase local ou staging.
- ❌ **Ne pas commiter** de secret. `supabase/.gitignore` exclut `.temp/` et `.env.local`. **`.claude/settings.local.json` est désormais gitignored** (follow-up Sprint DX-Verify, commit 21859e4) — il accumulait des allow rules Bash contenant des secrets en clair (`SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD`) à chaque approbation. Le fichier reste local par dev. Si tu approuves une commande contenant un secret en argument, le secret va dans cette allowlist locale et **doit y rester** — vérification : `git ls-files .claude/` ne doit jamais le retourner.
- ❌ **Ne pas upgrader** `eslint-config-next` 15→16 maintenant (Sprint 1 séparé).
- ❌ **Ne pas réactiver** `typescript.ignoreBuildErrors`.
- ❌ **Ne pas écrire** de docs `.md` sans demande explicite (sauf RLS-FINDINGS, CLAUDE.md, prompts/ qui sont demandés).
- ❌ **Ne pas écrire** la phrase littérale `eslint-disable-next-line` dans un commentaire de documentation qui n'est PAS un disable directive (cas vu en Sprint 2-followup-v3) — ESLint la parse comme une tentative de disable d'une rule nommée "directive." (ou ce qui suit la phrase) et lève `error : Definition for rule 'directive.' was not found`. Reformuler en "lint suppression comment", "directive eslint-disable", ou simplement "lint exception".
- ❌ **Ne pas ajouter** un trigger ou un handler-side cleanup pour cascader la nullification d'une FK avant d'avoir vérifié si la FK a déjà `ON DELETE SET NULL` / `ON DELETE CASCADE` — c'est le mécanisme idiomatique Postgres et il fire automatiquement. Cas vu Sprint 2-followup-v3 / Item 1 : un trigger `BEFORE DELETE` deployed pour null `profiles.group_id` était redondant avec `profiles_group_id_fkey ON DELETE SET NULL` (baseline schema line 235), capture-then-drop dans le même sprint. Vérifier d'abord via `pg_constraint` ou en lisant [supabase/migrations/20260101000000_remote_schema.sql](supabase/migrations/20260101000000_remote_schema.sql) section "ADD CONSTRAINT ... FOREIGN KEY".

## 9. Tests

- Framework : **Vitest** (`vitest.config.ts` à la racine, env `node`, alias `@/` → racine, charge `.env.local` automatiquement via parser inline depuis Sprint DB).
- Convention : tests à côté du code, suffixe `.test.ts`. Exemples : `lib/debug-guard.test.ts`, `lib/finance/__tests__/rpc-concurrency.test.ts`.
- **CI auto-run** : depuis Sprint Code-CI / F1, [.github/workflows/code-checks.yml](.github/workflows/code-checks.yml) lance `pnpm typecheck` + `pnpm test:run` (avec `if: always()` sur le test step) sur tout PR touchant `**/*.ts` + configs. **Étendu Sprint Stabilize-Deps / S2** : le workflow tourne aussi sur `push: branches: [cleanup]` pour valider l'état post-merge (le merge UI d'une PR Dependabot ne re-trigger pas les workflows `pull_request:`). Aucune action manuelle requise — un dev qui pousse une régression TS ou un test cassé voit la PR rouge en <1min, et un merge UI qui casserait `cleanup` voit le push rouge dans la même fenêtre.
- Pour tester `process.env` : `vi.stubEnv('NODE_ENV', 'production')` + `vi.unstubAllEnvs()` (NODE_ENV est readonly avec les types Next).
- Tests d'intégration DB (Sprint DB / D9) : `lib/finance/__tests__/rpc-concurrency.test.ts` couvre 4 scénarios — 100×updatePiggyBank, drainage à zéro, transferFromPiggyToBudget, alternance ±1. Gated `SUPABASE_RPC_CONCURRENCY_TESTS=1`.
- Tests régression API (Sprint Polish / T3) : `lib/__tests__/api-regressions.test.ts` couvre 3 bugs surfacés en H1/H2/R2 (cumulated*savings round-trip, total_real*\*, availableBalance). Gated `SUPABASE_API_TESTS=1`.
- Tests comportement trigger (Sprint Audit-Functions-v2 / B2) : [lib/**tests**/trigger-behavior.test.ts](lib/__tests__/trigger-behavior.test.ts) couvre les 4 fonctions trigger custom A2 — `trigger_recalculate_contributions` (auto-create on JOIN), `trigger_group_budget_change` (recalc on UPDATE), `cleanup_group_contributions` (cascade DELETE), `update_updated_at_column` (touch). Gated `SUPABASE_TRIGGER_TESTS=1`. Catches le cas où une `CREATE OR REPLACE FUNCTION ... AS 'BEGIN RETURN NEW; END;'` accidentelle ferait passer `db:check-functions` au vert tout en cassant la chaîne trigger → fonction → INSERT.
- Tests `_loadFinancialData` round-trip (Sprint Refactor-I4 follow-up) : [lib/finance/**tests**/financial-data.test.ts](lib/finance/__tests__/financial-data.test.ts) couvre 6 cas — profile golden math (salary-add, exceptional incomes/expenses, deficits = 0), group golden math (trigger contribution sanity-check + 7 `FinancialData` fields), profile/group sans estimés (availableBalance = bankBalance, RAV = 0), fail-soft sur UUIDs inconnus (returns `EMPTY_FINANCIAL_DATA` shape, never throws), `saveRavToDatabase` round-trip via `bank_balances.current_remaining_to_live`. Gated `SUPABASE_FINANCE_TESTS=1`. Le seed full-fixture orchestre profile + group + group_contributions (trigger-driven) ; le 2e `describe` exerce les chemins minimaux sans déclencher de trigger.
- Tests wrapper auth (Sprint Refactor-Architecture-v5) : [lib/api/**tests**/with-auth.test.ts](lib/api/__tests__/with-auth.test.ts) couvre `withAuth` + `withAuthAndProfile` — 12 cas répartis en 3 describes : withAuth (valid session, no cookie, invalid JWT, expired payload, static-route overload, dynamic-route overload), withAuthAndProfile (valid + complete shape regression-guard, no cookie, no profile → 404, dynamic + profile), isolation (wrapper does NOT catch handler errors, parallel invocations don't cross userId contexts). Gated `SUPABASE_API_TESTS=1` (réutilise le même bucket que api-regressions). Pattern dynamic-import + cleanup cascade calé sur api-regressions.test.ts. **Forge un token expiré** via `encrypt({ ..., expiresAt: pastTime })` direct — la JWT signature reste valide (jose's `setExpirationTime('1h')` est indépendant), mais `validateSessionToken` check la propriété `expiresAt` du payload (lib/session-server.ts:108) — couverture du chemin "session expirée" sans waiter 1h.
- **Tests pure-unit non-gated** (Sprint 2-followup) : [lib/**tests**/query-client.test.ts](lib/__tests__/query-client.test.ts) regression-guard la cascade de `invalidateFinancialRefreshes(qc)` via `vi.spyOn(qc, 'invalidateQueries')` + `toHaveBeenNthCalledWith` × 3 (pin l'ordre + la liste des 3 keys). Pas de Supabase, pas d'env var, pas de dynamic-import-in-beforeAll : `import { describe, it, expect, vi } from 'vitest'` direct (les globals vitest **ne sont PAS auto-imported** dans ce repo — `vitest.config.ts` n'a pas `globals: true`, et tous les tests existants importent explicitement). Pour tout nouveau helper qui n'a pas de dépendance Supabase, suivre ce pattern (rapide, déterministe, exécute en CI sans secrets).
- **Pattern import dynamique pour gated tests** : si un test importe `lib/finance/*` (qui transitivement charge `lib/supabase-server.ts` créant un client à l'eval du module), faire l'`await import('@/lib/...')` à l'intérieur de `beforeAll` pour que le module ne se charge PAS quand le suite est skipped sans env vars. Pattern visible dans `rpc-concurrency.test.ts` et `api-regressions.test.ts`.
- **chunked(...)** helper dans `rpc-concurrency.test.ts` : batch les appels parallèles en groupes de 10 pour rester sous le pool undici default per-origin de Node fetch.
- **Cleanup en cascade obligatoire** dans `afterAll` : tables FK → profiles sans `ON DELETE CASCADE` doivent être nettoyées explicitement avant `auth.admin.deleteUser`. Pattern dans `api-regressions.test.ts:afterAll`. Sans ça, prod accumule des comptes test orphelins.
- **Sanity sweep one-stop** : `pnpm verify` (Sprint DX-Verify / G1) enchaîne `typecheck` + `test:run` + les 6 `db:*` checks avec fail-fast. À lancer après chaque modif significative ou à la clôture de chaque sprint — remplace l'invocation manuelle des 8 commandes individuelles. ~36s en local. Skip-friendly : si un test est gated derrière `SUPABASE_*_TESTS=1` non set, il skip et `verify` continue.

## 10. Variables d'environnement

`.env.local` (gitignored) doit contenir :

```
NEXT_PUBLIC_SUPABASE_URL=https://jzmppreybwabaeycvasz.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<...>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<...>
SUPABASE_SERVICE_ROLE_KEY=<...>     # utilisé par lib/supabase-server.ts
JWT_SECRET_KEY=<...>                 # utilisé par lib/session.ts
LOG_LEVEL=debug                      # optionnel — error|warn|info|debug, défaut warn en prod / debug en dev (Sprint Cleanup-I8)
```

Pour les opérations CLI Supabase qui requièrent l'access token + DB password :

```
SUPABASE_ACCESS_TOKEN=sbp_...        # https://supabase.com/dashboard/account/tokens
SUPABASE_DB_PASSWORD=...             # Project Settings > Database > Reset password si oublié
```

Ces deux derniers sont à passer en variables inline (`SUPABASE_ACCESS_TOKEN=... pnpm supabase ...`) ou persistés au niveau User env (`[Environment]::SetEnvironmentVariable(...)`), **jamais** committés dans un fichier.

> 🚨 **Règle absolue (sécurité)** : Claude **ne doit JAMAIS lire la valeur** de `SUPABASE_ACCESS_TOKEN` ni `SUPABASE_DB_PASSWORD` (ni de tout autre secret du `.env.local`).
>
> - ❌ **Interdit** : `Write-Output $env:SUPABASE_ACCESS_TOKEN`, `echo $env:SUPABASE_DB_PASSWORD`, `Get-Content .env.local`, ou toute commande qui rend la valeur visible dans le transcript.
> - ✅ **Autorisé** : test de présence binaire (`if ($env:SUPABASE_ACCESS_TOKEN) { "OK" } else { "MISSING" }`) — ne révèle pas la valeur.
> - ✅ **Autorisé** : laisser les scripts (`apply-sql.mjs`, `export-schema.mjs`, `check-rpcs.mjs`) lire `process.env.SUPABASE_ACCESS_TOKEN` en interne — la valeur ne transite pas par stdout/stderr.
> - **Si une commande échoue avec "TOKEN_MISSING"** : demander à l'utilisateur de le set lui-même via `[Environment]::SetEnvironmentVariable(...)` puis redémarrer Claude Code. **Ne jamais** lui demander de coller le secret dans le chat.

## 11. Roadmap (à jour 2026-05-09)

- ✅ **Sprint 0** (`cleanup` branch) : C1–C5 + follow-up RLS audit (livré)
- ✅ **Sprint DB** ([prompt-00-executive-summary-v2.md](prompts/prompt-00-executive-summary-v2.md)) : D1–D11 livré 2026-05-07, 5 commits (`39e56f8 → 55d1606`), score ~58/100
- ✅ **Sprint Refactor** ([prompt-00-executive-summary-v3.md](prompts/prompt-00-executive-summary-v3.md)) : R0 post-mortem + R1 routes debug + R2 wirage `<Database>` (avec scope-cast à dérouler en H1) + R3 dedup schéma + R4 drift detection + R6 tests RLS D2/D3 + drop policy récursive profiles. R5 (overdraft) reporté en H3. Livré 2026-05-07, 6 commits (`5efacfe → ab58db2`), score estimé ~62-65/100
- ✅ **Sprint Hardening** ([prompt-00-executive-summary-v4.md](prompts/prompt-00-executive-summary-v4.md)) : H1 unwind 17 scope-casts (5 restants tous god/debug) + H2 ghost table `financial_snapshots` + H3 overdraft `bank_balances` (RAISE EXCEPTION dans la RPC) + H4 `pnpm db:check-rpcs` + H5 GH Actions cron drift + H6 trigger forensics. **3 bugs réels surfacés et fixés** : `current_savings`/`cumulated_savings` (R2), RPC fantôme `calculate_available_cash` dans dashboard, `total_real_income`/`total_real_expenses` qui retombent toujours sur 0 (T1 Sprint Polish corrigera ce dernier). Livré 2026-05-07, 9 commits (`858b243 → 5d65922`), score estimé ~70/100
- ✅ **Sprint Polish** ([prompt-00-executive-summary-v5.md](prompts/prompt-00-executive-summary-v5.md)) : T1 dashboard aggregates fix + T2 delete 3 debug routes + T3 regression tests gated `SUPABASE_API_TESTS=1` + T4 `SnapshotPayload` discriminé + T5 trigger inventory + 2 trouvailles documentées. Livré 2026-05-07, 6 commits (`be6af8e → c54fb7f`), score estimé ~73/100
- ✅ **Sprint Audit-Triggers** ([prompt-00-executive-summary-v6.md](prompts/prompt-00-executive-summary-v6.md)) : A1 fix filtre baseline trigger + A2 capture des **5** fonctions PL/pgSQL non-versionnées (les 3 du prompt + `update_updated_at_column` canonique + `calculate_group_contributions` découverte en cours via `PERFORM`) + A3 `pnpm db:check-functions` (pin 4 custom) + A4 CI extension. Livré 2026-05-07, 6 commits (`f747e98 → b5c1cfb`), score estimé ~75/100
- ✅ **Sprint Audit-Functions-v2** : B1 audit générique `db:audit-functions` + capture de **4 fonctions legacy** non-versionnées trouvées au premier run (`check_column_exists`, `create_recap_snapshot`, `final_verification`, `is_monthly_recap_required` — toutes dead code, zéro callsite app) + B2 tests comportement trigger gated `SUPABASE_TRIGGER_TESTS=1` (4/4 verts) + B3 PR-time drift gate. Livré 2026-05-07, 3 commits (`eed2f1e → 46a5df2`), score estimé ~76/100
- ✅ **Sprint Cleanup-Legacy** : C1 DROP des 4 fonctions legacy capturées en B1 (workflow capture-then-drop, recovery via re-apply de la migration de capture) — `pnpm db:audit-functions` passe à 9 fonctions versionnées + fix collatéral `lib/finance/context.ts` `string|null → string|undefined` (regen types reflète les RPC C3 désormais visibles via `--linked`). C2 `pnpm db:audit-objects` étend l'audit générique à 5 catégories `pg_catalog` (functions, types, enums, domains, operators) — premier run clean. C3 validation end-to-end PR-time gate B3 + cron weekly H5/A4 → **2 vrais bugs surfacés** : (a) `pnpm/action-setup@v4` conflit avec `packageManager` package.json (cron weekly n'a JAMAIS tourné depuis B3), (b) secret `SUPABASE_ACCESS_TOKEN` perdu lors du rename `Popoth_App_Claude → popoth`. Fix YAML appliqué + secret ré-ajouté + validation seconde itération réussie (drift exit 1, autres exit 0). Livré 2026-05-07, 3 commits (`6482c57 → c17d2a1`) sur `cleanup`, score estimé ~77/100
- ✅ **Sprint Polish-CI** ([prompt-00-executive-summary-v9.md](prompts/prompt-00-executive-summary-v9.md)) : D1 `pnpm db:types` redirect déplacé à l'intérieur du script (le wrapper pnpm allait dans le fichier généré ; `--silent` au niveau script ne suffit pas car le banner est imprimé par le pnpm OUTER avant le spawn — l'unique angle d'attaque était d'embarquer le `>` dans la valeur du script). D2 fix `check-drift.mjs` normalise CRLF→LF AVANT `stripTimestamp` (root cause subtile : `.+$` en regex JS ne consomme pas `\r`, donc la timestamp line n'était pas filtrée du fichier committé alors qu'elle est absente de la version in-memory → offset d'une ligne → diff intégral spurious). D3 `lib/database.ts` augmentation supprimée (47 LOC), 6 imports migrés vers `@/lib/database.types` (incluant 2 fichiers prod `supabase-{client,server}.ts` que la première exploration avait manqué — usage `from './database'` relatif). D4 path filter du PR-time gate étendu aux 2 YAML eux-mêmes + `audit-*.mjs`. **D5 cron weekly à observer manuellement** (workflow_dispatch). D6 Node.js 24 migration déférée. Livré 2026-05-07, 4 commits code + closeout sur `cleanup`, score estimé ~78/100
- ✅ **Sprint Hygiene-CI** ([prompt-00-executive-summary-v10.md](prompts/prompt-00-executive-summary-v10.md)) : E1 `.gitattributes` ajouté avec `* text=auto eol=lf` (élimine le warning `LF will be replaced by CRLF` sur chaque commit Windows ; **renormalize a été un no-op** car repo + working tree étaient déjà LF en storage — il manquait juste la déclaration explicite pour que tous les clones convergent côté policy ; le fix D2 reste comme défense en profondeur). E2 `pnpm db:check-types-fresh` ([scripts/check-types-fresh.mjs](scripts/check-types-fresh.mjs)) : détecte un `lib/database.types.ts` désynchronisé par rapport à prod via `supabase gen types --project-id <ref>` + line-by-line diff. Wirage : step ajouté à db-drift-pr.yml ET db-drift-check.yml (`if: always()`) ; path filter du PR-time gate étendu à `lib/database.types.ts`. **Verif end-to-end E2** : exit 0 clean, 1 sur édition manuelle avec diff résumé, 2 sur token absent. E3 validation `workflow_dispatch` du cron weekly → **3 vrais bugs surfacés** (workflow invisible côté UI car main n'a aucun YAML, `--linked` fail en CI sans `supabase link` préalable, 403 sur issue creation faute de `permissions: issues:write`) tous fixés par swap default branch `main → cleanup` + commit `5d8292a`. C'est exactement le pattern Sprint Cleanup-Legacy / C3 — première vraie boucle de feedback CI révèle ce que la default branch couvrait silencieusement depuis Sprint Hardening / H5. **Default branch GitHub** est désormais `cleanup` (le local git remote URL utilise encore `Popoth_App_Claude.git`, GitHub redirige). Livré 2026-05-07, 5 commits sur `cleanup` (`2174e41 → 7092773`), score estimé ~79/100
- ✅ **Sprint Code-CI** ([prompt-00-executive-summary-v11.md](prompts/prompt-00-executive-summary-v11.md)) : F1 [.github/workflows/code-checks.yml](.github/workflows/code-checks.yml) ajouté (PR-time gate `pnpm typecheck` + `pnpm test:run` avec `if: always()`, premier filet code-side après 6 sprints DB-side, validé end-to-end via 2 PR tests — typecheck rouge sur TS error puis green ; test rouge sur AssertionError puis vert local). F2 `pnpm db:types` aligné sur `--project-id jzmppreybwabaeycvasz` (cohérence avec `db:check-types-fresh` post E2 hotfix, élimine la dépendance à `supabase link` pour les fresh clones, output byte-identique à `--linked`). F3#1 git remote local renommé `popoth.git`. F3#2 README l.112 typo `--linked` → `--project-id` corrigé (folded dans closeout). F3#3 cron lundi 08:00 UTC à observer passivement post-sprint. **Observation collatérale** : warning Node.js 20 deprecation observé sur les 2 runs CI de la verif F1 (~1 mois avant échéance 2 juin 2026 — défer D6 inchangé). Pas de migration DB. Livré 2026-05-07, 3 commits sur `cleanup` (F2 / F1 / closeout), score estimé ~80/100 (premier vrai franchissement du seuil 80).
- ✅ **Sprint DX-Verify** ([prompt-00-executive-summary-v12.md](prompts/prompt-00-executive-summary-v12.md)) : G1 [package.json](package.json) — script `pnpm verify` ajouté (chaîne `typecheck` + `test:run` + 6 `db:*` checks avec `&&` fail-fast, ~36s en local). G2 [.github/dependabot.yml](.github/dependabot.yml) ajouté : auto-PR weekly lundi 08:00 Europe/Paris pour npm (groups radix-ui / supabase / eslint / vitest) et github-actions. **G3 deferred to post-G2 observation**. Livré 2026-05-07, 3 commits sur `cleanup` (G1 / G2 / closeout), score estimé ~81/100.
- ✅ **Sprint DX-Verify follow-up** (livré 2026-05-07, **non documenté dans un prompt** — réaction directe à la 1re vague Dependabot) : la wave a embarqué 10 merges. **3 cassures** surfacées via `pnpm verify` + `pnpm dev` HTTP 500 :
  - **(a) tailwindcss ^3.4.1 → ^4.2.4** (`6b23ea3` + dup `3caefee`) : PostCSS plugin déplacé en package séparé `@tailwindcss/postcss`, `@tailwind` directives remplacées par `@import "tailwindcss"`. Compile-time error `app/globals.css`.
  - **(b) react ^19.1.1 → ^19.2.6 alone** (`81902b8` "multi") : Dependabot a bumpé `react` sans `react-dom` → runtime error "Incompatible React versions" sur tout render. Le PR-time gate ne le voyait pas (invariant React = runtime-only).
  - **(c) @supabase/supabase-js ^2.57.4 → ^2.105.3** (`e6f973d`) : `RejectExcessProperties` strict typing introduit en v2.x → 5 typechecks cassent dans `app/api/monthly-recap/*` sur le code legacy avec `[x: string]: any` index signatures.
  - **6 commits revert** (`f3cd8ae → 8d2a509`) : revert tailwind propre + 2 fix-forward repins (react, supabase — `git revert -m 1` impossible car merges intermédiaires touchent le même lockfile) + group `react-stack` dans dependabot.yml + untrack `.claude/settings.local.json` (security collateral, embarquait des secrets en clair) + re-sync `next-env.d.ts` au path Next 16.2.6.
  - **Conservé** du wave : Next 16.2.6 (patch), @radix-ui groupé, **4 gh actions v6** (ferme le défer Node 24 G3 sans coût).
  - **Trous identifiés** (à fermer dans Sprint Stabilize-Deps v13) : (i) sans `ignore` rules, Dependabot va re-proposer les 3 mêmes versions au prochain scan ; (ii) `code-checks.yml` ne fire que sur `pull_request:` — pas sur `push:` à cleanup, donc rien ne valide l'état post-merge automatiquement (c'est le user qui a tapé `pnpm verify` localement et trouvé le supabase break).
- ✅ **Sprint Stabilize-Deps** ([prompt-00-executive-summary-v13.md](prompts/prompt-00-executive-summary-v13.md)) : S1 `ignore` rules dans [.github/dependabot.yml](.github/dependabot.yml) (tailwindcss `update-types: semver-major` + @supabase/supabase-js `versions: ">=2.105.0"` + eslint-config-next `versions: ">=16.0.0"`) — stoppe l'hémorragie de revert au prochain scan Dependabot pour les 3 patterns surfacés au follow-up DX-Verify. S2 [.github/workflows/code-checks.yml](.github/workflows/code-checks.yml) étendu à `push: branches: [cleanup]` (paths alignés au `pull_request:` filter, incluant `next.config.*` / `eslint.config.*` / `.eslintrc*`) — ferme le trou post-merge où GitHub fast-forward la default sans re-trigger `pull_request:` (la classe de bug qui a fait passer 3 cassures Dependabot en silence). S3 doc Dependabot triage workflow mise à jour dans §8 (filet CI désormais en place) + §9 (mention push-time gate). Skip POST-MORTEM-DEPENDABOT-WAVE.md séparé (cargo cult risk — leçons déjà capturées dans §8/§11). Livré 2026-05-07, 3 commits sur `cleanup`, score estimé ~82/100. **Activation collatérale GitHub Advanced Security** (settings → Code security) : Dependency graph + Dependabot alerts + Dependabot security updates + Grouped security updates ON ; Private vulnerability reporting et Automatic dependency submission OFF (zéro valeur pour repo solo / pnpm full-lockfile). À surveiller au prochain scan Dependabot lundi 2026-05-11 08:00 Europe/Paris : (a) confirmer qu'aucune PR n'apparaît pour les 3 patterns ignored S1, (b) confirmer que le push de S2 trigger bien `code-checks.yml` sur `push:` event, (c) observer l'arrivée des premières CVE alerts sur `@supabase/supabase-js@2.57.4` ou `tailwindcss@3.4.1` qui peuvent maintenant générer des security PRs auto.
- ✅ **Sprint Align-PackageJson** ([prompts/prompt-01-inventory.md](prompts/prompt-01-inventory.md), périmètre triagé 2026-05-07) : P1 retrait `@anthropic-ai/sdk` (0 callsite confirmé via grep `from '@anthropic-ai/sdk'`, `require`, `[Aa]nthropic`, `ANTHROPIC_API_KEY`) + déplacement `autoprefixer` `dependencies` → `devDependencies` (build-time only via `postcss.config.js`, smoke test `pnpm dev` + HTTP 200 sur `/` confirme PostCSS résout encore). P2 ajout 4 scripts (`lint:fix` alias de `lint`, `db:diff` wrapper supabase, `db:reset` wrapper supabase nécessite Docker, `ci` chaîne typecheck+lint:check+test:run+build) + section `engines` (`node >=20.10.0`, `pnpm >=9.0.0`) + création [.nvmrc](.nvmrc) (`20` LTS major) + fix typo `.env.example` (`hereJ` → `here`). **Périmètre réduit vs prompt original** : le prompt v1 listait 11 items, 6 ont été skip après triage avec utilisateur (eslint upgrade Sprint 1, retrait `--webpack` choix volontaire CLAUDE.md §2, scripts Prettier/Husky/Playwright Sprint 1/2, NEXT_PUBLIC_APP_URL/LOG_LEVEL pas de consommateur, README déjà OK 349 lignes UTF-8 — l'audit `01-inventory.md` était obsolète sur ce point). **Surface secondaire identifiée** : `pnpm lint:check` retourne 125 errors + 11 warnings pré-existantes (~80×`no-explicit-any`, ~25×`no-unused-vars`, ~6×`prefer-const`, ~6 React hooks deps warnings). Aucun workflow CI ne lance `lint:check` — la dette est réelle mais invisible. Documentée comme nouveau chantier `lint-baseline-cleanup` (cf. [prompts/prompt-01-inventory-v2.md](prompts/prompt-01-inventory-v2.md)). Livré 2026-05-07, 2 commits sur `cleanup` (`7b5cd98 → 4d1621c`), score estimé ~82/100 (+0, surface dette visible, score n'évolue pas).
- ✅ **Sprint Lint-Baseline-Cleanup** : 136 problems (125 errors + 11 warnings) → 0. Phase 1 prefer-const auto-fix (7 sites) + Phase 2 unused-vars cleanup (43 sites, surface 2 dead-code blocks deletable in `lib/financial-calculations.ts` god file: pure filter+reduce calcs with no callers, plus dead `setCookie` helper and dead `spentThisMonth` recalc) + Phase 3 JSX entity escapes (15 sites, French apostrophes + 1 quote pair) + Phase 4 `any` typing strictement (69 sites): financial-logger boundary swap to `unknown` + structural error types (DatabaseErrorLike, ErrorLike) ; finances/_ + monthly-recap/_ routes typed via `Database['public']['Tables'][...]['Insert'|'Update']` from [lib/database.types.ts](lib/database.types.ts) ; god files I4/I5 untouched except for boundary types (`FinancialData` for return values, `Record<string, unknown>` for `details` field) ; PlanningDrawer + dashboard editingTransaction typed via re-exported `EstimatedBudget` / `EstimatedIncome` / `RealExpense` / `RealIncome`; cloneElement casts via `ReactElement<Props>` instead of `as any`. Phase 5 exhaustive-deps (10 warnings): all suppressed with `// eslint-disable-next-line ... -- <raison>` since each is a documented mount-only or context-change-only fetcher (adding deps would loop). Phase 6 isolated: textarea empty interface → type alias ; UserAvatar `<img>` kept with reasoned disable (Supabase Storage remote hosts, next/image migration deferred). **Phase 7 CI gate**: `pnpm lint:check` step added to [.github/workflows/code-checks.yml](.github/workflows/code-checks.yml) with `if: always()` (mirrors typecheck step), validated end-to-end via test-lint-gate PR (deliberate `: any` injection → ESLint step rouge as expected). **Phase 8 closeout**: `pnpm verify` exit 0, `pnpm run ci` exit 0 (first clean run since Sprint Align-PackageJson / P2 added the script — note `pnpm ci` calls the npm verb not implemented by pnpm, must use `pnpm run ci`), `pnpm dev` HTTP 200 on `/`, 307 redirect on protected routes, 200 on auth pages — no React loop warnings introduced by Phase 5. **Real bugs surfaced** (not fixed, documented as separate items): (a) `recoveryResults.bank_balance` and `.piggy_bank` in [app/api/monthly-recap/recover/route.ts](app/api/monthly-recap/recover/route.ts) silently mismatch types between v1 and v2 paths (boolean vs number) — typed as `boolean | number` to preserve existing behavior, real fix is a separate sprint ; (b) inferred `parseFloat(number)` no-ops in expenses/progress, replaced by `Number()` for type compatibility. Livré 2026-05-08, 9 commits sur `cleanup` (Phase 1-7 + closeout). Score estimé ~83/100.
- ✅ **Sprint Lint-Followups** ([prompts/prompt-01-inventory-v3.md](prompts/prompt-01-inventory-v3.md)) : Item 1 fix `recover.ts` v1/v2 type mismatch — `bank_balance` / `piggy_bank` normalisés sur strict `boolean` partout (split `CountKey + BooleanKey` dans `restoreTable`, branche conditionnelle ligne 234) + 3 régressions gated `SUPABASE_API_TESTS=1` couvrant v1, v2 vide, v2 avec rows. Item 2 triage Dependabot **31 → 0** : direct upgrades `next 16.1.6 → 16.2.6` (4 next CVEs) + `postcss 8.5.6 → 8.5.14` (XSS), 12 `pnpm.overrides` pour transitives (minimatch 3+9, flatted, picomatch 2+4, brace-expansion 1+2, ajv 6, js-yaml, yaml 2, playwright, serialize-javascript, lodash, glob 10, postcss). `pnpm audit` exit 0. Aucun `ignore` rule existant n'a bloqué un security PR — les 31 CVEs étaient toutes ailleurs que supabase-js ≥2.105 / eslint-config-next ≥16. Item 3 hook Husky `pre-push` ([.husky/pre-push](.husky/pre-push)) lance `pnpm lint:check && pnpm typecheck` fail-fast — première gate locale alignée sur `code-checks.yml`. Livré 2026-05-08, 4 commits sur `cleanup` (`0f4c676` recover fix + tests / `67525d4` husky / `9e0e938` deps triage / closeout). Score estimé ~85/100.
- ✅ **Sprint Hygiène-Code** (livré 2026-05-08, plan dans `C:\Users\gille\.claude\plans\hygi-ne-du-code-playful-yao.md`) : 4 chantiers en scope **Medium**, 4/8 objectifs du prompt source résolus silencieusement par les sprints Lint-\* précédents (`: any` count 0, silent catches loggés, fragile array[key] safe, key listes stables). Chantier 1 : magic numbers extraits dans [lib/constants/auth.ts](lib/constants/auth.ts) + [lib/constants/finance.ts](lib/constants/finance.ts), 13 substitutions dans 4 fichiers. Chantier 2 : suppression `getRemainingToLiveHistory` + `getGroupRemainingToLiveHistory` + interface `RemainingToLiveSnapshot` (dead, 0 callsites, –95 LOC). Chantier 3 : split `AuthContext` en `AuthUserContext` + `AuthActionsContext` ; nouveaux hooks `useAuthUser()` / `useAuthActions()` ; `useAuth()` rétro-compat via aggregateur ; `useState` intervals → `useRef`, `userRef` pour closure-free reads, public handlers en `useCallback`. Chantier 4 : 5 modals dans `PlanningDrawer` lazy-loadés via `next/dynamic` + `ssr: false` (AddBudgetDialog, AddIncomeDialog, EditBudgetDialog, EditIncomeDialog, ConfirmationDialog). Verif `pnpm typecheck` + `lint:check` + `test:run` + `build` exit 0, smoke browser user-confirmed. Livré 2026-05-08, 4 commits sur `cleanup` (`a2e0b18` → `58620b9`), score estimé ~86/100. Follow-ups documentés dans [prompts/prompt-02-code-quality-v2.md](prompts/prompt-02-code-quality-v2.md) (migration consumers AuthContext + lazy-load AddTransactionModal/SavingsDistributionDrawer + closeout audit `02-code-quality.md`).
- ✅ **Sprint Hygiène-Code-v2** ([prompts/prompt-02-code-quality-v2.md](prompts/prompt-02-code-quality-v2.md), plan dans `C:\Users\gille\.claude\plans\sprint-hygi-ne-code-staged-pizza.md`) : 3 items indépendants. (1) Migration des 4 single-concern consumers (`app/page.tsx` → `useAuthUser()` ; `dashboard` + `group-dashboard` + `settings` → nouveau hook `useLogoutAndRedirect()`) + refactor des internals de `useRequireAuth` / `useRequireGuest` / `useLogin` / `useRegister` pour qu'ils subscribent à la slice la plus narrow (auth state ou actions seulement, plus l'agrégateur). `useAuth()` agrégateur préservé inchangé pour rétro-compat. `app/connexion/page.tsx` consumer-side inchangé. (2) Lazy-load 3 modals outer-level via `next/dynamic` + `{ ssr: false }` : `AddTransactionModal` (491 LOC, 2 sites), `PlanningDrawer` (688 LOC, au niveau `FinancialIndicators`), `SavingsDistributionDrawer` (540 LOC, à l'intérieur du wrapper `SavingsDrawer` qui reste static). (3) Header stale-content sur [docs/audit/02-code-quality.md](docs/audit/02-code-quality.md). Trouvaille au pré-sprint : `useRegister()` n'a aucun consumer (inscription page utilise supabase.auth.signUp direct). Verif `pnpm typecheck` + `lint:check` + `test:run` + `build` exit 0. Livré 2026-05-08, 3 commits code + 1 closeout sur `cleanup` (`53a9c97` → closeout). Score estimé ~87/100.
- ✅ **Sprint Refactor-Architecture** ([prompts/prompt-03-architecture.md](prompts/prompt-03-architecture.md), plan dans `C:\Users\gille\.claude\plans\refactor-architecture-glimmering-parnas.md`) : 5 chantiers sur 6 du prompt source (PlanningDrawer Context skippé après inventaire — audit stale, `context` ne traverse qu'1 niveau). Chantier 1 : middleware self-call HTTP → import direct via [lib/recap/check-status.ts](lib/recap/check-status.ts) Edge-safe. Chantier 2 : namespace `/api/finance/*` unifié (13 routes) avec aliases rétro-compat taggés `Deprecation: true` via [lib/api/with-deprecation.ts](lib/api/with-deprecation.ts) + handlers extraits dans [lib/api/finance/](lib/api/finance/). Chantier 3 : [hooks/useRavValidation.ts](hooks/useRavValidation.ts) extrait de l'IIFE inline de `AddTransactionModal`. Chantier 4 : [hooks/useStep1Data.ts](hooks/useStep1Data.ts) extrait de `MonthlyRecapStep1` (custom maison, pas TanStack Query). Chantier 5 : [hooks/useBudgetProgress.ts](hooks/useBudgetProgress.ts) dedup state+effect+memo → return useMemo direct. **2 ambiguïtés résiduelles préservées** (zero-risk migration) : `/api/finance/budgets` GET vs `/api/finance/budgets/estimated` GET et `/api/finance/dashboard` vs `/api/finance/summary`. Cleanup et résolution dans [prompts/prompt-03-architecture-v2.md](prompts/prompt-03-architecture-v2.md). Verif `pnpm typecheck` + `lint:check` + `build` exit 0 après chaque commit. Livré 2026-05-08, 5 commits sur `cleanup` (`35c86e7` middleware / `2e00339` namespace / `3e8d842` ravValidation / `bf76731` step1Data / `3601b28` budgetProgress). Score estimé ~89/100.
- ✅ **Sprint Refactor-Architecture-v2** ([prompts/prompt-03-architecture-v2.md](prompts/prompt-03-architecture-v2.md), plan dans `C:\Users\gille\.claude\plans\prompt-sprint-peaceful-yao.md`) : 4 commits code + closeout. **Volet A** (cleanup) — 13 thin-wrappers supprimés (`app/api/finances/**`, `app/api/financial/**`, `app/api/budgets/`, `app/api/incomes/` — 101 LOC) + helper [`lib/api/with-deprecation.ts`](lib/api/with-deprecation.ts) supprimé (12 LOC). Period of observation OK (user a confirmé 0 hit prod sur les anciens paths). **B.1** GET sur `/api/finance/budgets` supprimé (0 consumer applicatif — `useBudgets` lit via `/budgets/estimated`) ; POST/PUT/DELETE conservés. **B.2** `/api/finance/dashboard` supprimé entièrement (0 consumer, 457 LOC dead code, type `FinancialDashboardData` jamais importé externally) ; B.3 (duplication dashboard.ts/summary.ts) résolu en side-effect. **Volet C** skipped (pas de plan de rename pour les autres surfaces). Doc [docs/api/README.md](docs/api/README.md) mise à jour (suppression bloc d'observation, exemple `withDeprecation`, ligne `dashboard` table, note `budgets GET candidat à suppression`). Verif `pnpm typecheck` + `lint:check` + `build` exit 0 après chaque commit ; build expose 12 paths sous `/api/finance/` (était 13 avec dashboard). Livré 2026-05-08, 4 commits sur `cleanup` (`0b1e44b` Volet A / `df923d1` helper / `d151742` B.1 / `3d9591b` B.2). Score estimé ~90/100.
- ✅ **Sprint Refactor-Architecture-v3** ([prompts/prompt-03-architecture-v3.md](prompts/prompt-03-architecture-v3.md), plan dans `C:\Users\gille\.claude\plans\prompt-sprint-logical-sloth.md`) : extraction du boilerplate auth + profile dans les 12 modules `lib/api/finance/*.ts` via [lib/api/with-auth.ts](lib/api/with-auth.ts). **Phase 1 a invalidé l'hypothèse uniforme du prompt source** : split 4 always-fetch (budgets, incomes, rav, summary) vs 8 conditional-fetch (le reste — fetch profil seulement quand `forGroup`/`context==='group'`), messages d'erreur drifts (`'Non autorisé'` 5× / `'Non authentifié'` 6× / aucun `'Session invalide'` malgré CLAUDE.md §6). User a arbitré : (a) 2 helpers `withAuth` (auth seule) + `withAuthAndProfile` (auth + profile), (b) harmoniser sur `'Session invalide'` per CLAUDE.md §6, (c) finance-only — Volet C (monthly-recap/groups/profile/savings, +61 callsites) déféré Sprint v4 après période d'observation. **Wrapper sans try/catch outer** : chaque handler garde son `try/catch` route-aware avec `console.error` route-spécifique (sera balayé par le chantier console.log cleanup). Préserve aussi le fallback 200-with-default-data de `summary.ts` (le catch interne reste à l'intérieur du callback). **summary.ts edge case** : retourne 200 avec données par défaut sur catch (UX choice — ne pas casser le dashboard sur erreur transitoire), inchangé. **profile.id remplacé par userId** dans les bodies (équivalents par construction puisque le wrapper query `eq('id', userId)`). **0 callsite de `validateSessionToken` restant dans `lib/api/finance/`**, **0 occurrence de `'Non autorisé'` ou `'Non authentifié'`**. Verif `pnpm typecheck` + `lint:check` + `test:run` (4 passed / 21 skipped) + `build` (12 paths `/api/finance/**` intacts) exit 0. LOC delta brut : −290 LOC sur 28 handlers + helper +71 LOC = **net −219 LOC**. Livré 2026-05-08, 5 commits code + closeout sur `cleanup` (`644f1b3` helper / `bc439f3` rav pilot / `6c712f7` budgets+incomes+summary / `5b971c6` income-_ / `02eb148` budgets-estimated+expenses-_). Score estimé ~91/100. Volet C reporté Sprint v4 ; le wrapper donne aussi un boundary clean au chantier Zod rollout.
- ✅ **Sprint Refactor-Architecture-v4** ([prompts/prompt-03-architecture-v4.md](prompts/prompt-03-architecture-v4.md), plan dans `C:\Users\gille\.claude\plans\prompt-sprint-iterative-thunder.md`) : Volet C — extension du pattern `withAuth(AndProfile)` aux 21 routes hors finance. **Phase 1 surface scan a surfacé 6 patterns distincts de profile-fetch** (vs 2 patterns dans v3 finance) : `select('id, group_id)` minimal, `select('id, group_id, first_name, last_name')` (8 routes utilisent `user_name` dans le response), `select('*')` (profile GET seul), `select('group_id')` conditional (bank-balance), `select('id')` redondant (groups/search), aucun (auth-only). User a arbitré 4 décisions : (a) **étendre la default `withAuthAndProfile` à `'id, group_id, first_name, last_name'`** plutôt que créer un 3e helper, (b) profile GET reste sur `withAuth` + manual `select('*')` pour préserver le 200-on-no-profile fallback, (c) **étendre le wrapper avec un generic optionnel `routeContext` pour les routes dynamiques** (groups/[id], groups/[id]/members) plutôt que dupliquer en `withAuthDynamic`, (d) skip debug routes (low-value, blockedInProduction, ~12 callsites éphémères). **42 callsites migrés sur 21 routes** (audit prompt initial sous-estimait à ~52 — bank-balance manquait, groupes [id]/members double-comptés). Découpage en 7 commits : helper extension / savings + bank-balance / profile / groups static / groups dynamic / monthly-recap simple (9 routes) / monthly-recap stateful (4 routes — complete avec 4 globals carryover, recover avec dispatch v1/v2, balance avec setTimeout 500ms, auto-balance avec algo 3-phase — handler bodies tous préservés verbatim). **Edge cases vérifiés sans regression** : (1) global.carryoverUpdates et al. dans complete — wrapper closure ne shadow pas, writes globaux restent visibles ; (2) recover v1/v2 dispatch dans `restoreTable()` deep — wrapper ne touche pas, les 3 tests gated `SUPABASE_API_TESTS=1` doivent rester verts ; (3) profile GET 200-on-no-profile préservé via `withAuth` + manual fetch ; (4) bank-balance conditional fetch lazy-in-body comme les 8 conditional-fetch finance handlers de v3 ; (5) groups/[id]/members DELETE joined query split en 2 calls (wrapper-supplied profile + separate groups fetch). **Negative greps clean** : `validateSessionToken` ne reste que dans `process-step1` (excluded) et debug routes (excluded) ; `'Non autoris'`/`'Non authentifi'` ne restent que dans 2 debug files. Verif `pnpm typecheck` + `lint:check` + `test:run` (4 passed / 21 skipped) + `pnpm build` exit 0 après chaque commit ; build expose 12 paths `/api/finance/**` + tous les 21 paths Volet C intacts. LOC delta brut : −819 LOC sur les 21 routes + helper +8 LOC = **net ~−811 LOC**. Livré 2026-05-08, 7 commits code + closeout sur `cleanup` (`b16cca3` helper / `0d931af` savings+bank / `5a35436` profile / `e931f6e` groups static / `044593a` groups dynamic / `f753d7d` recap simple / `e680514` recap stateful). Score estimé ~92/100. **Architecture finale** : 33 modules sur le wrapper (12 finance + 21 hors finance), seules `process-step1`, debug routes, et auth routes restent boilerplate. Le pattern est maintenant le standard du repo.
- ✅ **Sprint Refactor-Architecture-v5** ([prompts/prompt-03-architecture-v5.md](prompts/prompt-03-architecture-v5.md), plan dans `C:\Users\gille\.claude\plans\prompt-sprint-calm-kitten.md`) : hardening du wrapper auth canonique installé en v3+v4. **3 gaps** fermés + 1 leftover v4. Phase 1 a confirmé : 0 test sur le wrapper avant ce sprint, 5 callsites `routeContext!.params` (groups/[id]/route.ts PUT/DELETE + groups/[id]/members/route.ts GET/POST/DELETE), `validateSessionToken` direct dans process-step1 hors debug routes. User a arbitré 3 décisions (toutes les "Recommended") : (a) Option A overloads TypeScript pour la signature dynamic-route, (b) inclure le wrap auth de process-step1, (c) tests gated `SUPABASE_API_TESTS=1` avec real `auth.users` + JWT. **Commit 1** ([lib/api/with-auth.ts](lib/api/with-auth.ts)) : 2 overloads par helper (static-route sans routeContext, dynamic-route avec generic `<TParams>` non-optional) + JSDoc complète (description, @param, @returns, @example x3 patterns) + drop des 5 `routeContext!` dans `app/api/groups/[id]/**`. **Trade-off** : impl signature opaque (`any`) annotée avec eslint-disable + raison — required-vs-optional routeContext ne se réconcilient pas dans une signature typée unique, mais les overloads enforce les types au call site (verifs : test 5 = static, test 6 = dynamic). **Commit 2** ([app/api/monthly-recap/process-step1/route.ts](app/api/monthly-recap/process-step1/route.ts)) : 38-line auth header wrapped en `withAuthAndProfile` ; le >700 LOC métier body reste verbatim pour I5. **Negative invariant établi** : `rg "validateSessionToken" app/api/` ne match plus que les 6 debug routes (auth/\* utilise sa propre logique session). Note : drift `'Profil utilisateur non trouvé'` → `'Profil non trouvé'` accepté (cohérent v3+v4 standardization). **Commit 3** ([lib/api/**tests**/with-auth.test.ts](lib/api/__tests__/with-auth.test.ts), 298 LOC) : 12 cas gated couvrant withAuth (6 : valid session, no cookie, invalid JWT, expired payload via custom expiresAt past + JWT signature still valid, static-route overload, dynamic-route overload), withAuthAndProfile (4 : valid + complete shape regression-guard, no cookie, no profile → 404, dynamic + profile), isolation (2 : wrapper does NOT catch handler errors, parallel invocations don't cross userId contexts). Pattern dynamic-import-in-beforeAll + cleanup cascade calé sur api-regressions.test.ts. Verif : `SUPABASE_API_TESTS=1 pnpm test:run` 22 passed (12 nouveaux + 10 existants), default `pnpm test:run` 4 passed / 33 skipped (gated). `pnpm typecheck` + `lint:check` + `build` exit 0 après chaque commit. LOC delta brut : +298 tests + ~+90 JSDoc/overloads −34 process-step1 boilerplate −5 `!` deletions = **net ~+349 LOC** (premier sprint avec delta positif depuis 4 sprints — coverage > refactor). Livré 2026-05-09, 4 commits sur `cleanup` (`186d526` overloads+JSDoc / `7bd7575` process-step1 wrap / `290b921` tests / closeout). Score estimé ~93/100.
- ✅ **Sprint 1** ([prompt/prompt-04-tooling-dx.md](prompt/prompt-04-tooling-dx.md), plan dans `C:\Users\gille\.claude\plans\sprint-1-soft-lovelace.md`) : industrialisation tooling, 7 commits + closeout. **Phase 1 inventaire a invalidé la moitié du prompt source** (audit 2026-04 stale post-sprints intermédiaires) : `.nvmrc` + `engines` + Husky pre-push + 3 GitHub Actions workflows + Vitest + `.gitattributes` + `prepare: husky` étaient déjà en place. Vrais gaps fermés : Prettier complet, ESLint v8→v9 + eslint-config-next 15→16, lint-staged + pre-commit hook, `.editorconfig`, `.vscode/` versionné, PR template, `format:check` en CI. **Out of scope** : commitlint (skip per arbitration — convention CLAUDE.md §6 documentée par confiance), nouveau workflow `ci.yml` séparé (extension de `code-checks.yml` préférée — pas de secrets Supabase requis). **Commit 1** ([57d026b](#)) Prettier config + scripts. **Commit 2** ([4fc2fb7](#)) format pass : 162 fichiers, +6935/−5470 lines (mécanique, no semi / single-quote / 100col / lf / tailwind class reorder). **Commit 3** ([5d5d882](#)) ESLint v9 + Next 16 — bascule sur les flat configs natifs `eslint-config-next/{core-web-vitals,typescript}` (FlatCompat circular-reference bug sous v9 résolu par cette migration). Final post-upgrade : 0 errors, 1039 warnings (1013 `no-console` + 26 nouvelles règles `react-hooks` v7 — `set-state-in-effect` et `refs` — downgradées à `warn` car au-dessus du seuil d'abort 20 sites ; refacto séparée). **Commit 4** ([1cabae4](#)) lint-staged@^17 + `.husky/pre-commit` (`pnpm lint-staged`) — companion du pre-push, vérif end-to-end avec un `const   x   :string="hello"` réécrit en `const x: string = 'hello'` au moment du commit. **Note pre-push intervention** : après `pnpm install` du hook `prepare: husky`, le `.husky/_/` n'avait pas été initialisé automatiquement — `pnpm exec husky` manuel nécessaire pour armer les hooks (à savoir pour les fresh clones). **Commit 5** ([50e4d2e](#)) format:check step ajouté à `code-checks.yml` avec `if: always()` (mirroir typecheck/lint/test). **Commit 6** ([02e86f4](#)) `.editorconfig` + `.github/pull_request_template.md` (FR) + `.vscode/{settings,extensions}.json` + `.gitignore` selective `.vscode/*` + whitelist. **Commit 7** ([5ede0d5](#)) `pnpm run ci` étendu à `typecheck && lint:check && format:check && test:run && build` + `next-env.d.ts` ajouté à `.prettierignore` (auto-régénéré par `next build`). Verif end-to-end : `pnpm run ci` exit 0 (56/56 routes built). Livré 2026-05-09, 7 commits sur `cleanup` (`57d026b → 5ede0d5`), score estimé ~94/100. **Suite** : la dette `react-hooks/set-state-in-effect` (24 sites) + `react-hooks/refs` (1 site) reste warn-only — refactor à programmer en sprint séparé. La dette `no-console` (1013 sites) reste warn-only — chantier console.log cleanup déjà roadmappé.
- ✅ **Sprint 1.5 — React Hooks v7 anti-patterns refactor** ([prompt/prompt-04-tooling-dx-v2.md](prompt/prompt-04-tooling-dx-v2.md), plan dans `C:\Users\gille\.claude\plans\sprint-1-5-peaceful-glade.md`) : refacto des 26 sites flagués par les règles `react-hooks/set-state-in-effect` (25) + `react-hooks/refs` (1) introduites par eslint-plugin-react-hooks v7 (shipped via eslint-config-next 16 au Sprint 1). User a arbitré 3 décisions Phase 1 : (a) TanStack Query pour pattern B fetcher hooks (vs `useFetchOnMount` helper), (b) `key` prop + `useState(() => init)` lazy init pour pattern A modals, (c) full refactor scope avec disables documentés pour les 2 false positives. **14 commits** sur `cleanup` :
  - **Phase 1** ([00f64a5](#)) install `@tanstack/react-query@5.100.9` + `@tanstack/react-query-devtools` + [lib/query-client.ts](lib/query-client.ts) singleton (staleTime 30s, refetchOnWindowFocus false, retry 1) + [components/providers/QueryProvider.tsx](components/providers/QueryProvider.tsx) wrap outermost dans [app/layout.tsx](app/layout.tsx). Devtools mounted only in dev.
  - **Phase 2** (7 commits) — migration des **11 hooks fetcher** vers `useQuery` / `useMutation` : useStep1Data, useFinancialData (bridge `triggerFinancialRefresh`/`registerFinancialRefreshCallback` préservé pour les 5 hooks CRUD encore non-migrés à ce moment), useProfile + useGroups, useExpenseProgress + useIncomeProgress (le second dedup state mirror, pas Query), useProgressData (closes le pattern C ref site `contextRef.current = context` via `queryKey: [context]` natif), useIncomes + useRealIncomes, useBudgets + useRealExpenses. Bridge final : 4 hooks (useFinancialData, useProgressData, useBudgets) registent un callback qui invalide leur propre query — les 5 hooks CRUD continuent d'appeler `triggerFinancialRefresh()` après mutation (chaîne préservée verbatim).
  - **Phase 3** (4 commits) — pattern A modals + components : (a) **4 form modals** (EditBudget/Income/Transaction + AddTransaction) avec `key={editing.id}` + parent conditional render `{isOpen && editing && <Modal ... />}` + lazy `useState(() => editing.foo)` ; (b) **side-effect setState moved** : monthly-recap/page.tsx context derived inline (drop state mirror), SavingsDistributionDrawer fetch → useQuery + redundant reset effect supprimé ; (c) UserAvatar `imageKey` state remplacé par `<img key={profile.avatar_url}>`, ExpenseBreakdownPreview migré à useQuery, ProfileSettingsCard documented disable (form init from async profile load) ; (d) MonthlyRecapStep2 fetch → useQuery + redundant reset effect supprimé, AuthContext documented disable (async init pipeline).
  - **Phase 4** ([f6e1683](#)) escalation des 2 rules à `error` (drop downgrade block dans [eslint.config.mjs](eslint.config.mjs)).
  - **Phase 5** closeout CLAUDE.md.
  - **Bridge legacy callback registry préservé** : `triggerFinancialRefresh()` / `registerFinancialRefreshCallback()` exports dans `useFinancialData` restent (back-compat) mais maintenant les handlers font `queryClient.invalidateQueries({ queryKey: ['financial-summary'] })` — Query owns the cache lifecycle. Eventually deletable mais low-value cleanup.
  - **2 disables documented** (false positives) : ProfileSettingsCard (form state mirrors async profile load), AuthContext (`initializeAuth()` async pipeline calls setState in continuation, not in effect body). Les autres 24 sites refactorés sans disable.
  - **Verif end-to-end** : `pnpm typecheck` + `pnpm lint:check` (exit 0, **0 errors / 1012 warnings** down from 1039 = 26 fewer warnings + 1 unused-disable supprimée) + `pnpm build` (57/57 routes) après chaque commit. Smoke browser **deferred to user verification**. Livré 2026-05-09, 14 commits sur `cleanup`, score estimé ~95/100. **Pattern Sprint 1.5 → standard du repo** : pour tout nouveau hook fetcher, utiliser `useQuery`/`useMutation` ; pour modal form-state-from-prop, utiliser `key` + lazy `useState(() => ...)`.
- ✅ **Sprint 2 — TanStack Query bridge cleanup + Sprint 1.5 followups** ([prompt/prompt-04-tooling-dx-v3.md](prompt/prompt-04-tooling-dx-v3.md), plan dans `C:\Users\gille\.claude\plans\sprint-2-rosy-wozniak.md`) : 6 commits code + closeout. **Phase 2 — bridge cleanup** : remplacement des 13 callsites de `triggerFinancialRefresh()` (3 par hook CRUD : useBudgets, useIncomes, useRealIncomes, useRealExpenses + 1 dans useProfile) par un helper local `invalidateFinancialRefreshes(qc)` qui appelle `queryClient.invalidateQueries` sur les 3 keys impactées (`['financial-summary']`, `['progress-data']`, `['budgets']`). Suppression des 3 bridge effects (useFinancialData, useProgressData, useBudgets) et des exports `triggerFinancialRefresh` + `registerFinancialRefreshCallback` + Set `financialRefreshCallbacks` dans [hooks/useFinancialData.ts](hooks/useFinancialData.ts). Le helper est dupliqué dans 5 hooks (~3 lignes × 5 = 15 LOC) — choix volontaire vs un module partagé : aucune logique métier, évite un nouveau module qui devra évoluer avec le temps. **Phase 3 — modal cleanup** : retrait des 4 `if (!isOpen) return null` dead code post-Sprint 1.5 (parents font `{isOpen && <Modal />}`). Découverte en cours : le prop `isOpen` lui-même est devenu inert (destructuré jamais lu) → 4 nouveaux warnings `unused-imports/no-unused-vars`. Décision : drop le prop entièrement (interface + destructure) et update les 5 callsites consumers (dashboard, group-dashboard, PlanningDrawer × 2, dashboard EditTransaction). Scope élargi vs plan original mais cohérent — on retirait du dead code, pas du nettoyage cosmetic. **Phase 4 — ProfileSettingsCard split** : refactor en outer `ProfileSettingsCard` (data-fetching + loading skeleton) + inner `ProfileSettingsForm` (`profile: ProfileData` non-null prop, lazy `useState(() => profile.first_name ?? '')` etc.). Le sync effect form-state-from-prop disparaît + l'`eslint-disable react-hooks/set-state-in-effect` block-level avec. Compteur disables `set-state-in-effect` : 2 → 1 (il reste AuthContext, hors scope). **commitlint skip** confirmé per arbitrage user (cohérent avec Sprint 1). **Verif end-to-end** : `pnpm typecheck` + `pnpm lint:check` (exit 0, **0 errors / 1009 warnings** down from 1012 = 3 fewer = 3 console.log retirés du bridge legacy) + `pnpm test:run` (4 passed / 33 skipped) + `pnpm build` (56/56 routes) après chaque commit. Smoke browser **deferred to user verification**. Negative grep clean : `triggerFinancialRefresh|registerFinancialRefreshCallback` 0 hits dans `app/`, `components/`, `hooks/`, `lib/`. Livré 2026-05-09, 6 commits code + closeout sur `cleanup` (`2d58cb6` useBudgets+useRealExpenses / `4d9f986` useIncomes+useRealIncomes / `47e8495` useProfile / `ed709ff` drop bridge + exports / `9bb8b38` modals isOpen / `f3913d0` ProfileSettingsCard split / closeout). Score estimé ~95/100 → ~96/100.
- ✅ **Sprint 2-followup** ([prompt/prompt-04-tooling-dx-v4.md](prompt/prompt-04-tooling-dx-v4.md), plan dans `C:\Users\gille\.claude\plans\prompt-sprint-zippy-glacier.md`) : 3 cleanup chirurgicaux post-Sprint 2. **Commit 1** ([877fe89](#)) extraction du helper `invalidateFinancialRefreshes(qc)` (5 copies locales identiques) vers [lib/query-client.ts](lib/query-client.ts) en single source of truth ; les 5 hooks (useBudgets, useIncomes, useRealIncomes, useRealExpenses, useProfile) importent désormais depuis `@/lib/query-client`. Si la liste des keys cross-domain doit évoluer, 1 site à toucher au lieu de 5. **Commit 2** ([5e07605](#)) test unit non-gated [lib/**tests**/query-client.test.ts](lib/__tests__/query-client.test.ts) — `vi.spyOn(qc, 'invalidateQueries')` + `toHaveBeenNthCalledWith` × 3 pour pin l'ordre et la liste des 3 keys ; régression-guard contre un futur drift (key ajoutée, retirée, ou réordonnée). 5 passed / 33 skipped (was 4 / 33). **Commit 3** ([0f2ea5b](#)) strip du bloc debug 17 `console.log` (lignes 62-81) dans le queryFn de [hooks/useFinancialData.ts](hooks/useFinancialData.ts) — dump verbose du payload (🏠💰📊) à chaque fetch, désormais inutile car les data sont visibles via TanStack Query DevTools (mounted Sprint 1.5). **Lint baseline** : 1009 → 991 warnings (−18 — légèrement au-delà des −11 estimés au plan parce que la `console.log` multi-ligne 75-77 et un wrap soft-line on row 76 ont été comptés séparément par ESLint). Verif `pnpm typecheck` + `pnpm lint:check` + `pnpm test:run` + `pnpm format:check` + `pnpm build` exit 0 après chaque commit. Smoke browser **deferred to user verification**. Negative greps : `function invalidateFinancialRefreshes` dans `hooks/` 0 hits, `🏠` dans `hooks/` 0 hits. Livré 2026-05-09, 3 commits code + closeout sur `cleanup` (`877fe89` extraction / `5e07605` test / `0f2ea5b` console strip / closeout). Score estimé ~96 → ~96.5/100.
- ✅ **Sprint 2-followup-v2** ([prompt/prompt-04-tooling-dx-v5.md](prompt/prompt-04-tooling-dx-v5.md) si capturé, plan dans `C:\Users\gille\.claude\plans\prompt-sprint-mossy-donut.md`) : 2 gaps fermés. **Commit 1** ([3af2920](#)) [hooks/useGroups.ts](hooks/useGroups.ts) — les 5 mutations (`createGroup`/`updateGroup`/`deleteGroup`/`joinGroup`/`leaveGroup`) invalident désormais correctement les caches cross-domain. Pour create/delete/join/leave : `queryClient.invalidateQueries(['profile'])` + `invalidateFinancialRefreshes(queryClient)` ajoutés (cascade vers `['financial-summary']`, `['progress-data']`, `['budgets']`). Pour update : invalidation financière conditionnelle `if ('monthly_budget_estimate' in updates)` qui mirror le fire conditionnel du trigger `groups_budget_contribution_recalc` côté serveur — group rename a zéro impact financier. Optimistic `setQueryData` calls préservés. Ferme un bug de fraîcheur silencieux : avant ce commit, l'utilisateur qui join/leave un groupe voyait stale data sur `/group-dashboard` jusqu'au prochain `staleTime` (30s) ou un F5 manuel. **Commit 2** ([6bdcfb1](#)) [contexts/AuthContext.tsx](contexts/AuthContext.tsx) — block disable `react-hooks/set-state-in-effect` qui wrappait le `useEffect` (L260-268) remplacé par un single next-line disable sur la ligne `initializeAuth()` à l'intérieur de l'effet. **Découverte runtime** (vs plan initial) : la rule report au call site (L263, l'appel `initializeAuth()`), PAS aux setStates inside the function body — j'ai d'abord essayé de scoper le block disable au body de `initializeAuth` mais lint l'a flag comme "Unused eslint-disable directive" (le rule ne trace pas dans le call graph). Conclusion : la disable la plus chirurgical pour ce rule est next-line sur le call site, pas wrap-around-function-body. Compteur disables `set-state-in-effect` : 1 → 1 (relocalisé, scope strictement narrower : seul `initializeAuth()` est masqué ; tout futur setState ajouté dans le body de l'effet ou dans `stopTokenRefresh`/`stopAuthCheck` sera flagué). **Server-side observation** (résolu Sprint 2-followup-v3) : la note out-of-scope ici prétendait que `DELETE /api/groups/[id]` ne reset pas `profiles.group_id`. Investigation v3 a trouvé que le FK `profiles_group_id_fkey ON DELETE SET NULL` (baseline schema line 235) le fait automatiquement — le bug n'existait pas. Sprint v3 a néanmoins pinned ce comportement avec un test gated `SUPABASE_TRIGGER_TESTS=1` regression-guard. Verif end-to-end : `pnpm typecheck` + `pnpm lint:check` (exit 0, **0 errors / 991 warnings** stable) + `pnpm test:run` (5 passed / 33 skipped) + `pnpm format:check` exit 0. Smoke browser **deferred to user verification** (flow join/leave/create/delete sans F5 + AuthContext login/logout). Livré 2026-05-10, 2 commits code + closeout sur `cleanup` (`3af2920` useGroups / `6bdcfb1` AuthContext / closeout). Score estimé ~96.5/100 → ~97/100.
- ✅ **Sprint 2-followup-v3** ([prompt/prompt-04-tooling-dx-v4.md](prompt/prompt-04-tooling-dx-v4.md), plan dans `C:\Users\gille\.claude\plans\prompt-sprint-smooth-abelson.md`) : 2 items + pivot Item 1. **Item 1 (pivot)** — investigation a invalidé l'observation v2 : `profiles_group_id_fkey ON DELETE SET NULL` (baseline schema line 235, vérifié direct via `pg_constraint`) gère déjà le nullage de `profiles.group_id` quand un group est deleté. Le trigger BEFORE DELETE proposé (`groups_aaa_cleanup_members` + `cleanup_group_members_on_delete()`) a été deployé puis droppé via capture-then-drop dans le même sprint — recovery via re-apply de `20260515000000_add_group_members_cleanup_trigger.sql`. Net DB delta : 2 migrations historiques (add + drop), 0 fonction added, état final identique au pré-sprint. Test gated `SUPABASE_TRIGGER_TESTS=1` ajouté en Case 5 dans [lib/**tests**/trigger-behavior.test.ts](lib/__tests__/trigger-behavior.test.ts) qui regression-guard le FK SET NULL behavior (5 trigger tests passants). **Item 2** — [contexts/AuthContext.tsx](contexts/AuthContext.tsx) migré de 3 `useState` (user, loading, error) à 1 `useReducer`. La règle `react-hooks/set-state-in-effect` (eslint-plugin-react-hooks v7) tracks uniquement les setters `useState` via `setStateCallSites` WeakMap registered pendant la destructuration `useState` — `dispatch` de `useReducer` est exempt. **Compteur disables `set-state-in-effect` : 1 → 0 repo-wide** (le single next-line disable sur `initializeAuth()` éliminé). Reducer surface : 10 actions (INIT_START/SUCCESS/ERROR, AUTH_REQUEST/SUCCESS/FAILURE, LOGOUT_START/LOGOUT, REGISTER_SUCCESS, CLEAR_ERROR, SET_USER) avec exhaustiveness check via `never` default. Consumer impact zero — les 25 callsites lisent user/loading/error individuellement via `useAuthUser()`/`useAuthActions()`/`useAuth()`, jamais le state object entier. Bundled tweak `.prettierignore` : ajout `supabase/.temp/` (créé par `supabase link` localement, gitignored mais Prettier le scannait quand même → format:check fail local). **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` exit 0 (**0 errors / 991 warnings stable**), `pnpm test:run` (5 passed / 34 skipped, +1 skip from new gated case), `pnpm format:check` exit 0, `pnpm build` 56/56 routes, `SUPABASE_TRIGGER_TESTS=1 pnpm test:run lib/__tests__/trigger-behavior.test.ts` 5/5 passed. **Smoke browser deferred to user** (flow create+delete group avec 2 members confirme FK SET NULL côté serveur ; flow login/logout/refresh post-Item 2). **Erreur évitée en chemin** : initial comment top-of-file utilisait la phrase littérale "eslint-disable-next-line directive" qui ESLint a parsé comme une tentative de disable d'une rule nommée "directive." → reformulé en "lint suppression comment". Cette classe d'erreur est typique pour tout commentaire qui mentionne le mécanisme eslint-disable sans en être un. Livré 2026-05-10, 5 commits sur `cleanup` (`8a9432f` add trigger / `40dbe87` baseline+allowlist / `04ae011` drop redundant / `2749509` FK regression test / `d2d5a9f` AuthContext useReducer / closeout). Score estimé ~97 → ~97.5/100.
- ✅ **Sprint 2-followup-v4** ([prompt/prompt-04-tooling-dx-v4.md](prompt/prompt-04-tooling-dx-v4.md), plan dans `C:\Users\gille\.claude\plans\prompt-sprint-jaunty-bee.md`) : 2 items post-Sprint v3 useReducer migration. **Commit 1** ([5b451e0](#)) extraction du reducer (types `AuthState` / `AuthAction` + `initialAuthState` + `authReducer`, ~58 LOC) de [contexts/AuthContext.tsx](contexts/AuthContext.tsx) vers nouveau module dédié [contexts/auth-reducer.ts](contexts/auth-reducer.ts) — pure function, pas de `'use client'`, pas de runtime React (juste import `type AuthUser`). Le commentaire de tête expliquant le pourquoi v3 reste dans AuthContext.tsx où il informe le lecteur du Provider. Décision Option B (extract) vs Option A (re-export) : extraction préférée parce qu'expose mieux la testabilité et clarifie la responsabilité (mirror du pattern Sprint Hygiène-Code split contexts). **Commit 2** ([dcbee66](#)) [lib/**tests**/auth-reducer.test.ts](lib/__tests__/auth-reducer.test.ts) — 14 cas pure-unit non-gated (1 sanity check `initialAuthState` + 12 cas transition + 1 cas runtime invalid action via cast `as AuthAction`). Pattern miroir [lib/**tests**/query-client.test.ts](lib/__tests__/query-client.test.ts) (Sprint 2-followup) : `import { describe, it, expect } from 'vitest'` direct, pas de Supabase, pas de dynamic-import-in-beforeAll. Identity guard sur 1 cas (`result !== input`) pin l'invariant useReducer requires-new-reference. Compteur tests non-gated : 5 → 19 (les 14 nouveaux + 5 existants). Le suite couvre les 11 transitions (LOGOUT_START + LOGOUT séparés intentionnellement dans le reducer pour préserver le `loading: true` flag entre les deux dispatches), la préservation de l'utilisateur au `AUTH_FAILURE` (login fail ≠ logout), et que le `default` branch du reducer ne crash pas runtime (le `never` exhaustiveness est compile-time). **Commit 3** ([99b4844](#)) [contexts/AuthContext.tsx](contexts/AuthContext.tsx) — `userValue` et `actionsValue` wrappés en `useMemo` avec deps slice-by-slice. **Pourquoi** : avec `useReducer`, le state object identity change à chaque dispatch — un littéral `{ user, loading, error, isLoggedIn }` créé à chaque render forçait tous les consumers de `useAuthUser()` à re-render même quand leur slice n'avait pas bougé (e.g. `useLogin()` qui ne lit que `error` re-render sur dispatch `INIT_START` flip loading). Avec `useMemo` deps `[user, loading, error, isLoggedIn]`, `useAuthUser()` consumers ne re-render que quand l'une des 4 slices a réellement changé. Memo est strictement additif — même valeur, identité plus stable. `actionsValue` essentiellement no-op après le premier render (les 5 actions sont déjà `useCallback`) mais le wrap signale "stable" et protège contre futurs ajouts. **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` exit 0 (**0 errors / 991 warnings stable**), `pnpm test:run` (19 passed / 34 skipped, was 5 / 34), `pnpm format:check` exit 0, `pnpm build` 56/56 routes, `pnpm verify` exit 0 (8 steps : typecheck + test:run + 6 db:\* checks). **Smoke browser deferred to user** (login/logout/refresh + flows lourds avec un seul des 2 contexts subscribed). Livré 2026-05-10, 3 commits code + closeout sur `cleanup` (`5b451e0` extract / `dcbee66` tests / `99b4844` memoize / closeout). Score estimé ~97.5 → ~98/100.
- ✅ **Sprint 2-followup-v5** ([prompt/prompt-04-tooling-dx-v8.md](prompt/prompt-04-tooling-dx-v8.md), plan dans `C:\Users\gille\.claude\plans\prompt-sprint-ticklish-boole.md`) : dead-code purge sur [hooks/useAuth.ts](hooks/useAuth.ts) + [contexts/AuthContext.tsx](contexts/AuthContext.tsx). **Phase 1 inventaire a invalidé la prémisse du prompt source** (memoize les 5 utilities du retour d'`useAuth()` aggregator + arbitrer `useRegister` delete vs keep + arbitrer consolidation `logoutAndRedirect` dupliqué) : `useAuth()` aggregator, `useRequireAuth()`, et `useRegister()` ont **0 consumers** dans `app/` ou `components/`. Les 4 imports applicatifs depuis `@/hooks/useAuth` n'utilisent que `useLogin` (connexion), `useRequireGuest` (connexion), et `useLogoutAndRedirect` × 3 (dashboard, group-dashboard, settings). User a arbitré "Dead code purge" plutôt que polir les 3 hooks dont aucun consumer ne tirera de bénéfice. **Commit 1** ([f804de7](#)) drop dans [hooks/useAuth.ts](hooks/useAuth.ts) du `useAuth()` aggregator (5 utilities `redirectToLogin`/`redirectAfterLogin`/`logoutAndRedirect` aggregator-version/`requireAuth`/`requireGuest` + `sessionExpiring` derived state + `checkSessionExpiry` `useEffect` + `useRequireAuth()` + `useRegister()`) ; et drop dans [contexts/AuthContext.tsx](contexts/AuthContext.tsx) du `useAuth(): AuthUserValue & AuthActionsValue` intersection helper (8 LOC, son seul consumer était l'aggregator dropped). **Commit 2** ([commit closeout](#)) ce closeout. **Trois hooks conservés** dans [hooks/useAuth.ts](hooks/useAuth.ts) : `useRequireGuest`, `useLogin`, `useLogoutAndRedirect` (les 3 réellement consommés). Net : 203 LOC → 75 LOC dans `hooks/useAuth.ts`, −132 / +1 LOC commit. **Conséquence convention §8** : ne PAS réintroduire un aggregator `useAuth()` sans cas d'usage concret — les deux contexts `AuthUserContext` + `AuthActionsContext` couvrent tous les besoins, et un aggregator force un re-render sur tout changement de l'un ou l'autre, défaisant le bénéfice du split (Sprint Hygiène-Code) + memoize (Sprint v4). **Pourquoi le prompt source était stale** : le prompt v8 supposait un retour total memoizable d'`useAuth()`, mais Sprint Hygiène-Code-v2 (livré 2026-05-08) avait déjà migré tous les consumers vers les hooks granulaires (`useAuthUser`/`useAuthActions`/`useLogoutAndRedirect`) — fait silencieusement non-reflété dans le prompt source v8. Pattern à retenir : avant de planifier une optimisation, `grep` les consumers réels — un wrapper sans consumers est une opportunité de delete, pas de polish. **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` exit 0 (**0 errors / 991 warnings stable**), `pnpm test:run` (19 passed / 34 skipped, inchangé), `pnpm format:check` exit 0, `pnpm build` 56/56 routes. **Smoke browser deferred to user** (login/logout flows + redirect after-login + 3 pages avec `useLogoutAndRedirect`). Livré 2026-05-10, 1 commit code + closeout sur `cleanup` (`f804de7` purge / closeout). Score estimé ~98 → ~98.2/100.
- ✅ **Sprint Templates-Triage** ([prompt/prompt-05-templates.md](prompt/prompt-05-templates.md), plan dans `C:\Users\gille\.claude\plans\cr-ation-des-fichiers-fluffy-haven.md`) : closeout d'un prompt issu de l'audit 2026-04 demandant la création mécanique de 21 fichiers config / doc à partir des templates de `doc2/audit/05-templates.md`. **Phase 1 inventaire a invalidé 16/21 items déjà livrés** par les sprints postérieurs (CLAUDE.md, README.md UTF-8, .editorconfig, .nvmrc=`20`, .prettierrc.json + .prettierignore, .env.example, package.json complet avec `engines`+`lint-staged`+husky, eslint.config.mjs flat config, .husky/pre-commit + pre-push, code-checks.yml + db-drift-pr.yml + db-drift-check.yml en lieu de `ci.yml` monolithique, pull_request_template.md, .vscode/{settings,extensions}.json, .gitignore avec règles `.vscode/`, vitest.config.ts). **9 items écartés** au triage : 4 doublons (`lib/logger.ts` vs `financial-logger.ts`, `docs/ARCHITECTURE.md` vs CLAUDE.md, `ci.yml` monolithique vs split actuel, commitlint skip Sprint 1) + 4 cargo cult (`Makefile`, `vitest.setup.ts` split, `test:watch`/`test:e2e`, `NEXT_PUBLIC_APP_URL`/`LOG_LEVEL` non-consommés vérifié par grep) + 1 prématuré (ADR bootstrap meta sans follow-up concret). **1 item livré** : `test:coverage` script dans [package.json:15](package.json:15) — `@vitest/coverage-v8@^4.1.5` était déjà dans devDependencies (Sprint 1.5) mais jamais exposé via script. Verifié end-to-end (`pnpm test:coverage` exit 0, 95.65% statements / 100% branches sur les 19 tests non-gated, écrit dans `coverage/` déjà gitignored). **Pas dans `pnpm verify` ni `pnpm ci`** — coverage est ad-hoc, pas un gate. **Pattern miroir** des Sprints Hygiène-Code (4/8 objectifs invalidés au pré-sprint) et Align-PackageJson (6/11 items skip après triage) : pour tout prompt issu de l'audit 2026-04, l'inventaire pré-sprint est obligatoire — la moitié-aux-trois-quarts des items est typiquement déjà livrée silencieusement par les sprints intermédiaires. Livré 2026-05-10, 1 commit sur `cleanup` (`45312fb` test:coverage / closeout). Score : ~98.2 → ~98.2/100 (1-line script ajouté ne bouge pas le score, mais le triage explicite ferme le ticket).
- ✅ **Sprint Audit-Closeout** ([prompt/prompt-04-tooling-dx-v3.md](prompt/prompt-04-tooling-dx-v3.md) — prompt "préparation du suivi" hérité de `doc2/audit/06-action-plan.md`, plan dans `C:\Users\gille\.claude\plans\pr-paration-du-suivi-snazzy-narwhal.md`) : closeout administratif du plan d'action 2026-04-29 (audit historique, score 47/100 baseline). **Phase 1 inventaire a invalidé 4/6 livrables demandés** par le prompt source — `scripts/audit-snapshot.sh` doublon de `pnpm verify` + Bash hostile sous Windows ; `baseline 2026-04-29` date révolue (capturer un snapshot au 2026-05-10 ne donne pas le "before" recherché) ; `PROGRESS.md` redondant avec CLAUDE.md §11 (la roadmap vivante) ; `issues-to-create.md` redondant avec les ⏭️ dans §11 et les prompts dédiés `prompt-04-tooling-dx-v*` / `prompt-07-deep-dive-*`. **Trouvaille collatérale** : `docs/audit/` n'existe plus dans le filesystem (working tree montre tous les fichiers en `D`) — contenu migré vers `doc2/audit/` (untracked, `?? doc2/`). Le prompt référence `docs/audit/06-action-plan.md` ; le vrai chemin est `doc2/audit/06-action-plan.md`. **2 livrables réels exécutés** : (1) [doc2/audit/AUDIT-RESOLUTIONS.md](doc2/audit/AUDIT-RESOLUTIONS.md) — tableau cross-reference one-shot mappant les 41 items C/I/R/N du plan d'action à leur résolution. Bilan : **27/41 done (65.9 %)**, 3 partial (I8 console.log 1331→986, R6 split components partiel via lazy-load, R14 PR template only — commitlint refusé), 12 deferred (I4/I5/I6 chantiers, R9-R11/R13, N1-N10 nice-to-have), 2 refused (R2 `--webpack` per CLAUDE.md §2, commitlint dans R14 per Sprint 1). C'est l'axe orthogonal à CLAUDE.md §11 (qui est sprint-par-sprint chronologique) — utile pour le lecteur qui arrive par l'audit historique et veut savoir "C3 race piggy_bank, c'est résolu où ?". (2) Migration `next steps.txt` (10 lignes plates produit) → [next-steps.md](next-steps.md) à la racine — items P1-P10 structurés (titre + domaine + description). Closes R15 du plan d'action. **Pattern miroir des Sprints Templates-Triage (1/21 items livré, 16 invalidés au triage) et Hygiène-Code (4/8 invalidés)** : pour tout prompt issu de l'audit 2026-04, le triage explicite ferme le ticket sans cargo cult. **Convention CLAUDE.md §8 implicite confirmée** : la roadmap vivante reste §11 ; ne PAS créer un PROGRESS.md item-par-item parallèle. 0 code touché, 0 typecheck/lint touché. Verif via `git status --short` + `git log --oneline -3`. Livré 2026-05-10, 2 commits sur `cleanup` (`fff13cb` audit resolutions / `0fcfab5` next-steps migration / closeout). Score : ~98.2 → ~98.2/100 stable (closeout administratif).
- ✅ **Sprint Cleanup-I8 / Lot 1** ([prompt/prompt-07-deep-dive-console-log-cleanup.md](prompt/prompt-07-deep-dive-console-log-cleanup.md), plan dans `C:\Users\gille\.claude\plans\cleanup-i8-enumerated-mitten.md`) : pose le filet pour la migration progressive des 986 `console.log` + 328 `console.error` du code applicatif. **Phase 1 inventaire a invalidé un item du prompt source** : la règle ESLint `'no-console': ['warn', { allow: ['warn', 'error'] }]` est déjà en place ([eslint.config.mjs:29](eslint.config.mjs#L29)) depuis Sprint Lint-Baseline-Cleanup — l'item 2 du Lot 1 est un no-op, le commit 2 prévu (`chore(eslint): warn on no-console`) est annulé. Audit collatéral `Grep "console\.log\(.*="` : 50 matches, tous template literals (e.g. `\`✅ Budget ${id}: surplus=${s}€\``), aucun side-effect → strip prod sûr. **Commit 1** ([bcb950f](#)) [lib/logger.ts](lib/logger.ts) (~40 LOC) Edge-safe : 4 niveaux `error/warn/info/debug`, gated par `LOG_LEVEL`env var (cached à module load), défaut`warn`en prod /`debug`en dev. Signature`(msg: string, ...rest: unknown[])`choisie (template 05) — drop-in mécanique pour les futurs migrations. Block`eslint-disable no-console`au top du fichier (le module est la frontière légitime du logging). **Commit 2** ([7419657](#)) [next.config.js](next.config.js)`compiler.removeConsole`gated`NODE_ENV === 'production'`avec`exclude: ['error', 'warn']`. SWC/Turbopack drop tous les `console.log/info/debug`du code applicatif au build ; conserve`error`/`warn`. Vendor (`node_modules`) et Next.js framework runtime non touchés (par design). **Verif end-to-end** : `pnpm typecheck`+`pnpm lint:check`(0 errors / 991 warnings stable) +`pnpm test:run`(19 passed / 34 skipped) +`pnpm build`(56/56 routes) exit 0. Strip vérifié post-build :`Grep "console\.log" .next/server/app/`→ **0 hit** (vs 986 source) ;`Grep "CALCUL CONTRIBUTION"`(string unique source) → 0 hit en prod ;`console.error` user-code (`'❌ [Middleware] Erreur lors...'`, `'Middleware authentication error:'`) → préservé en 4+1 chunks. **Commit 3** doc ([.env.example](.env.example) `LOG_LEVEL=debug`+ CLAUDE.md §6 Logs subsection + §8 update + §10 LOG_LEVEL doc + §11 entry). **Trade-off documenté** : Sprint Templates-Triage (2026-05-10) avait skippé`lib/logger.ts`en notant un "doublon de financial-logger.ts". Lot 1 le crée quand même parce que`financial-logger.ts`(289 LOC, 1 consumer dans`lib/api/finance/income-real.ts`) est domain-specific (FinancialLogger class avec `startOperation`/`success`/`databaseError`/`financialCalculation`/etc., toujours active sans gate `LOG_LEVEL`) — incompatible avec le besoin général. Alignement deferred au refactor I4. **Lots 2-6 hors scope** : migration des 986 `console.log`au fil des PRs (par opportunité), per-file ESLint overrides`no-console: 'error'`sur les fichiers migrés, sweep final + activation globale`no-console: 'error'`. Smoke browser **deferred to user**. Livré 2026-05-10, 3 commits sur `cleanup` (`bcb950f`logger /`7419657` removeConsole / commit 3 doc / closeout). Score : ~98.2 → ~98.2/100 stable (setup, pas d'impact métier).
- ✅ **Sprint Cleanup-I8 / Lot 3 + Lot 1 follow-up** ([prompt/prompt-07-deep-dive-console-log-cleanup-v2.md](prompt/prompt-07-deep-dive-console-log-cleanup-v2.md), plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-sleepy-pebble.md`) : 4 commits code + closeout, 2 items indépendants. **Item 1** ([44906b7](#)) [lib/**tests**/logger.test.ts](lib/__tests__/logger.test.ts) (~150 LOC, 11 cas pure-unit non-gated) — pin la level-filtering logic + `LOG_LEVEL` env handling avant que les Lots 2-6 ne créent des call sites. Pattern miroir [lib/**tests**/auth-reducer.test.ts](lib/__tests__/auth-reducer.test.ts) (Sprint 2-followup-v4). Cas couverts : 2 défauts (debug en dev, warn en prod), 4 LOG*LEVEL explicites, 3 fallbacks (invalid → warn prod, empty → debug dev, case-insensitive INFO), format `[level]` + rest-spread, et un negative-guard que le logger ne touche jamais `console.log` (utilise info/debug/warn/error). **Gotcha handled** : `lib/logger.ts` cache `currentLevel` à module load via `getCurrentLevel()`, donc chaque cas stub `LOG_LEVEL` + `NODE_ENV` AVANT `vi.resetModules()` + dynamic `await import('@/lib/logger')`. **Item 2 — Lot 3** : migration drop-in de 2 fichiers, premier vrai déploiement du logger après le setup Lot 1. **Commit 2** ([34cbd33](#)) [middleware.ts](middleware.ts) — 3 sites : `console.log('📅 [Middleware] Récap mensuel requis...')` L71 → `logger.debug` (gated dev / `LOG_LEVEL=debug` ; flow log fire à chaque request authentifiée sur dashboard, c'est du bruit prod) ; 2 `console.error` L80/L93 → `logger.error` (toujours rendus). Edge runtime stays clean (lib/logger.ts only references console.* + process.env, validé Lot 1). **Commit 3** ([67f48fd](#)) [lib/expense-allocation.ts](lib/expense-allocation.ts) — 4 catch-block `console.error` (L78/L88/L149/L159) → `logger.error`, drop-in mécanique, message verbatim, rest-spread error préservé. **Commit 4** ([2ab696d](#)) [eslint.config.mjs](eslint.config.mjs) — config block ajouté APRÈS le bloc existant (flat config, last-match wins) avec `files: ['middleware.ts', 'lib/expense-allocation.ts', 'lib/logger.ts']` + `no-console: 'error'`. `lib/logger.ts` inclus exprès : le `/_ eslint-disable no-console \*/`au top du fichier reste source de vérité (le module est la frontière) ; l'override durcit le contrat si le disable est jamais retiré accidentellement. **Lint baseline 991 → 990** (−1, pas −7 comme estimé au plan : les 6`console.error`migrés étaient déjà allow-listés par`allow: ['warn', 'error']`donc 0 warning, seul le 1`console.log`du middleware comptait). **Verif end-to-end** :`pnpm typecheck`exit 0,`pnpm lint:check`0 errors / 990 warnings,`pnpm test:run`30 passed (was 19, +11) / 34 skipped (gated inchangés),`pnpm build`56/56 routes exit 0 (aucun warning Edge nouveau). **Smoke browser deferred to user** (GET /dashboard non-loggé → redirect 307 /connexion exerce le middleware ; flow create depense exerce expense-allocation). **Lots 2 (lib/finance, couplé I4) / 4 (routes API) / 5 (composants) / 6 (sweep final + activation globale)** restent à venir par opportunité au fil des PRs. Livré 2026-05-10, 4 commits code + closeout sur`cleanup` (`44906b7`tests /`34cbd33`middleware /`67f48fd`expense-allocation /`2ab696d` eslint / closeout). Score : ~98.2 → ~98.2/100 stable (Lot 3 = dette finie −7 sites, pas de saut métier).
- ⏭️ **Sprint Tailwind-v4** (émergé du Sprint DX-Verify follow-up) : migrer tailwindcss 3 → 4. Auto-migration via `npx @tailwindcss/upgrade`. Audit visuel UI complet (shadcn/ui new-york theme tokens peuvent shifter), regen des CSS variables, possible bump @ducanh2912/next-pwa si compat issue. Fichiers touchés : `tailwind.config.ts` (CSS-first ou JS compat), `postcss.config.js` (nouveau plugin `@tailwindcss/postcss`), `app/globals.css` (`@import "tailwindcss"` au lieu de `@tailwind base/components/utilities`).
- ⏭️ **Sprint Supabase-Strict-Types** (émergé du Sprint DX-Verify follow-up) : refactorer 5 sites `app/api/monthly-recap/*` pour satisfaire `RejectExcessProperties` introduit en `@supabase/supabase-js` 2.105+. Concerne `accumulate-piggy-bank/route.ts:133`, `auto-balance/route.ts:556+588`, `transfer/route.ts:182`, `update-step/route.ts:154`. Implique de typer explicitement les inserts au lieu de spreader des objets `[x: string]: any`. **Couplé avec chantier I5** (extraction logique métier process-step1) qui touche le même domaine — opportunité de bundling.
- ✅ **Sprint Refactor-I4** ([prompt/prompt-07-deep-dive-financial-calculations.md](prompt/prompt-07-deep-dive-financial-calculations.md), plan dans `C:\Users\gille\.claude\plans\refactor-i4-vast-flask.md`) : split du god file `lib/financial-calculations.ts` (1069 LOC, 11 exports, 60 Supabase queries, 112 console.\*) en 8 modules sous `lib/finance/` avec couplage Lot 2 console-cleanup. **9 commits** sur `cleanup` (commit #4 du plan = no-op, `lib/finance/context.ts` existait déjà depuis Sprint 0 / C3) :
  - **Commit 1** ([b0cf57d](#)) `test(finance): characterize calc + RTL + budget helpers before split` — 19 tests pure-unit pinnant les 5 helpers PURS avant extraction.
  - **Commit 2** ([4237dd9](#)) `extract FinancialData/BudgetSavings to lib/finance/types` — 2 interfaces déménagent, god file re-exporte.
  - **Commit 3** ([e02829b](#)) `centralize EMPTY_FINANCIAL_DATA constant` — `Object.freeze`d, callers spread `{ ...EMPTY_FINANCIAL_DATA }` au return site (préserve la sémantique fresh-literal de l'original).
  - **Commit 5** ([336edf6](#)) `extract pure calc-rtl helpers + drop flow logs (I4 + Lot 2)` — 5 helpers extraits, ~25 console.log flow logs DROP, deficit notice migré vers `logger.debug`. Tests caractérisation déménagent vers `lib/finance/__tests__/calc-rtl.test.ts` (rename `R`).
  - **Commit 6** ([bc5d3ef](#)) `unify income-compensation profile+group on ContextFilter` — `calculateIncomeCompensationProfile` (65 LOC) + `calculateIncomeCompensationGroup` (54 LOC) → `calculateIncomeCompensation(filter: ContextFilter)` (60 LOC). Gated tests deferred au commit #8 (amortir l'infra de seed).
  - **Commit 7** ([15ffcdf](#)) `extract rav-persistence + snapshots (I4 + Lot 2)` — 2 modules. **R1 fail-soft contract** sur `saveRemainingToLiveSnapshot` (Promise<boolean>, return false on error, never throw) pinné par 5 cas non-gated dans `lib/finance/__tests__/snapshots.test.ts` (mocked supabase). Cycle import temporaire snapshots ↔ god file via re-export (résout au commit #8).
  - **Commit 8** ([70b0581](#)) `extract financial-data orchestrator + budget-savings-detail` — le plus gros : `getProfileFinancialData` (~280 LOC) + `getGroupFinancialData` (~200 LOC) → `_loadFinancialData(filter)` (180 LOC) + 2 wrappers minces. Cycle résolu (snapshots.ts importe désormais directement de `@/lib/finance/financial-data`). God file passe de 602 LOC → 26 LOC (thin re-export shim).
  - **Commit 9** ([63c84e1](#)) `migrate 18 importers from financial-calculations to lib/finance` — bulk sed sur 18 fichiers (path swap `@/lib/financial-calculations` → `@/lib/finance`) + 1 dynamic import dans `complete/route.ts` + 1 dynamic + type + comment dans `lib/__tests__/api-regressions.test.ts`. Le god-file shim reste comme transient safety net.
  - **Commit 10** (this) `delete legacy financial-calculations.ts + escalate no-console` — `git rm` du shim + ajout `'lib/finance/**'` au bloc per-file `no-console: 'error'` dans [eslint.config.mjs](eslint.config.mjs) (mirror Lot 4e). Lurker dans `lib/monthly-recap-calculations.ts:296` (relative `'./financial-calculations'`) corrigé en `'./finance'`. Mise à jour CLAUDE.md (§4 god-file inventory + §11 cette entrée + scope-cast count §5 + ❌ Ne pas refactorer §8).
  - **Lot 2 console cleanup** appliqué pendant l'extraction (§6 règle d'or) : ~95 sites DROP (flow logs + per-row dumps + summary blocks + outer catch-all redondants) + ~17 KEEP+migrate vers `logger.{error,warn,debug}` (DB-error discriminants + outer fail-soft boundaries + dispatcher validation). Net: 112 → ~13 sites en `lib/finance/` (tous via `logger.*`, jamais `console.*` direct). Lint baseline 406 → ~307 (−99 warnings). **Lots 2 du chantier console.log officiellement clos** par I4.
  - **Outcomes** : `as unknown as SupabaseClient` 2 → 1 (reste le seul I5). Tous les 8 nouveaux fichiers sous `lib/finance/` sont ≤ 230 LOC (max `financial-data.ts` à 229). 5 nouveaux tests non-gated (snapshot dispatcher, +5 vs commit #1 caractérisation préservée 19 tests). Tests gated `SUPABASE_FINANCE_TESTS=1` deferred (le seed ~120 LOC est une infra séparée, à introduire au prochain sprint touchant ces flows si besoin de regression-guarder le 80% diff `_loadFinancialData`). Fail-soft contracts préservés verbatim (R1 snapshot, R2 EMPTY_FINANCIAL_DATA fallback). **process-step1 algorithmic body intact** (R3) — seule sa ligne d'import a changé. **Smoke browser deferred to user** : `/dashboard` + `/group-dashboard` doivent afficher le même RAV qu'avant ; `/monthly-recap` flow exerce `getProfileFinancialData` / `getGroupFinancialData` via initialize/step1-data/step2-data/process-step1 ; mutations finance (add expense, edit budget) exercent le snapshot dispatcher fail-soft. Livré 2026-05-11, 9 commits sur `cleanup`. Score estimé ~98.2 → ~99/100 (god file I4 résolu — chantier majeur).
- ⏭️ **Chantier I5** : extraction logique métier de `app/api/monthly-recap/process-step1/route.ts`
- ✅ **Sprint Cleanup-I8 / Lot 4a** ([prompt/prompt-07-deep-dive-console-log-cleanup-v3.md](prompt/prompt-07-deep-dive-console-log-cleanup-v3.md), plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-glistening-beaver.md`) : première salve API du chantier console.log cleanup avec **philosophie de triage** (vs drop-in mécanique de Lot 3). Scope : `app/api/groups/**`, 5 fichiers, 22 sites. **Phase 1 audit a invalidé l'estimation du prompt source** (qui anticipait du `console.log` debug avec ~15-18 suppressions + ~4-7 migrations) : la réalité est **22 console.error** (zéro console.log), tous dans des paths d'erreur. 3 patterns : Type A DB-error→500 JSON (9 sites), Type B catch-all→500 JSON (10 sites), spéciaux (3 : silently-swallowed L43, stack-trace dump L67 redondant, cleanup-attempt L140 critique). User a arbitré **triage Modéré** : DROP les 10 catch-all redondants avec Next.js exception logs + le stack-trace dump = 11 sites ; KEEP+migrate les 9 Type A (le code/details Supabase n'apparaît qu'ici, grep-able si bug futur) + 2 spéciaux (L43 silent + L140 cleanup) = 11 sites. Ratio final 50/50 ("ménage avant migration"). **Commit 1** ([877504b](#)) static routes (route.ts, search/route.ts, contributions/route.ts) — 12 sites en input → 6 KEEP+migrate, 6 DROP. **Commit 2** ([8de275e](#)) dynamic routes ([id]/route.ts, [id]/members/route.ts wrapped en `withAuth<RouteParams>` + `withAuthAndProfile<RouteParams>` Sprint v5 overload preserved) — 10 sites → 5 KEEP+migrate, 5 DROP. **11 catch blocks** maintenant `} catch {` (sans binding) per CLAUDE.md §6 convention puisque l'`error` n'est plus utilisé après le DROP. **Commit 3** ([84e4e84](#)) [eslint.config.mjs](eslint.config.mjs) glob `'app/api/groups/**'` ajouté à la liste per-file `no-console: 'error'` (préféré à liste explicite : future-proof, toute future route du domaine auto-protégée). **Lint baseline 990 stable** : les 22 console.error étaient déjà allow-listés par `'no-console': ['warn', { allow: ['warn', 'error'] }]`, donc pas de delta lint — le bénéfice n'est pas la dette lint mais : (a) ménage du résidu (11 sites supprimés = signal/bruit amélioré), (b) `logger.error` préfixe `[error]` grep-able + gating LOG_LEVEL future, (c) per-file override durcit le contrat (any future regression sort la PR rouge). **Règle d'or de triage promue dans CLAUDE.md §6 Logs** (voir bullet "Règle d'or de triage") — à reprendre pour les Lots 4b-6 (pas de drop-in mécanique). **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` 0 errors / 990 warnings, `pnpm test:run` 30 passed / 34 skipped inchangé, `pnpm build` 56/56 routes exit 0, negative greps `Grep "console\." app/api/groups/` = 0 hit + `Grep "from '@/lib/logger'" app/api/groups/` = 5 hits (1 par fichier). **Smoke browser deferred to user** (créer/join/leave/delete groupe via /dashboard pour exercer les 5 routes). Livré 2026-05-10, 3 commits code + closeout sur `cleanup` (`877504b` static / `8de275e` dynamic / `84e4e84` eslint glob / closeout). Score : ~98.2 → ~98.2/100 stable (cleanup, pas de saut métier).
- ✅ **Sprint Cleanup-I8 / Lot 4b** ([prompt/prompt-07-deep-dive-console-log-cleanup-v4.md](prompt/prompt-07-deep-dive-console-log-cleanup-v4.md), plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-atomic-minsky.md`) : deuxième salve API du chantier console.log cleanup. Scope : 9 routes simples `app/api/monthly-recap/{status,refresh,resume,initialize,step1-data,step2-data,accumulate-piggy-bank,transfer,update-step}` (les 6 stateful — `complete`/`recover`/`balance`/`auto-balance`/`process-step1`/`status-test` — restent hors scope, couplées I5 ou debug-only). **Phase 1 audit a invalidé l'estimation du prompt source** (qui comptait 203 sites via `grep -c` lignes, gonflé par les `console.log` multi-lignes) : 132 sites distincts (33 flow logs + 95 dump-debug + ~3 warn + ~14 error). 9/9 routes déjà wrappées en `withAuth(AndProfile)` Sprint v4/v5 — pas de boilerplate auth à toucher. User a arbitré **triage agressif** (vs Lot 4a Modéré) parce que la nature des sites est différente : Lot 4a = 100% `console.error` allow-listés (lint baseline stable) ; Lot 4b = majorité `console.log` debug verbeux avec séparateurs `🎯🎯🎯`/`📊📊📊`/`🏦🏦🏦` et dump de payloads PII. Ratio final **86% DROP / 14% KEEP** (113 supprimés, 19 migrés). **Cohérence avec règle d'or** : les 2 `transfersError` silently-swallowed dans `refresh` + `initialize` sont normalisés à `logger.error` (ce que l'audit avait classé DROP par erreur — même pattern que `resume L130` qui était KEEP, donc 19 KEEP au lieu des 17 du plan initial). **Commit 1** ([40e6099](#)) short routes (5 fichiers, ~39 sites → ~31 DROP / ~8 KEEP) : status (4/0), accumulate-piggy-bank (4/3), transfer (7/1), update-step (5/4), resume (7/4). **Commit 2** ([60a8457](#)) medium routes (refresh 11/3, initialize 10/4). **Commit 3** ([1b71f53](#)) heavy debug routes 100% DROP : step1-data (37 sites) + step2-data (29 sites) — séparateurs visuels, dumps RAV/soldes/surplus par budget en PII, `console.log('')` lignes vides. Aucun import logger ajouté dans ces 2 fichiers ; binding `catch (error)` préservé pour le `error instanceof Error ? error.message : ...` qui propage l'erreur métier au client. **Variables destructure unused** (`error: exceptionalError`, `piggyBankError`) supprimées par cohérence après DROP des `warn`. **Commit 4** ([0694534](#)) [eslint.config.mjs](eslint.config.mjs) glob brace expansion `app/api/monthly-recap/{status,...,update-step}/**` (volontairement pas un glob global qui escaladerait les ~347 sites des 6 hors scope). **Sanity-testé** par injection d'un `console.log("test")` temp dans status/route.ts → 1 error ESLint comme attendu. **Lint baseline 990 → 819 (−171 warnings)** — dépasse l'estimation −115 du plan parce que les `console.log` multi-lignes étaient comptés ligne-par-ligne par ESLint (1 warning par ligne d'un single statement multi-line). **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` 0 errors / 819 warnings, `pnpm test:run` 30 passed / 34 skipped inchangé, `pnpm build` exit 0 (`pnpm verify` final — voir commit 5). Negative greps : `Grep "console\." app/api/monthly-recap/{status,refresh,resume,initialize,step1-data,step2-data,accumulate-piggy-bank,transfer,update-step}/` = 0 hit ; `Grep "@/lib/logger" app/api/monthly-recap/{...9 routes}` = 6 fichiers (1 par fichier ayant ≥1 KEEP : accumulate-piggy-bank, transfer, update-step, resume, refresh, initialize ; 0 dans status/step1-data/step2-data). **Top 5 fichiers post-Lot 4b** : `process-step1` 116, `financial-calculations` 95, `complete` 65, `auto-balance` 66, `balance` 62 — `step1-data` (58) et `step2-data` (57) ont quitté le top 5. **Smoke browser deferred to user** (flow `/monthly-recap` exerce status → step1-data → step2-data → update-step → transfer/accumulate-piggy-bank ; `/dashboard` post-récap exerce refresh/resume/initialize). LOC delta : -291 LOC nets sur 9 fichiers (-84 commit 1 / -70 commit 2 / -159 commit 3, +14 lignes import logger). Livré 2026-05-10, 4 commits code + closeout sur `cleanup`. Score : ~98.2 → ~98.2/100 stable (cleanup, pas de saut métier).
- ✅ **Sprint Cleanup-I8 / Lot 4c** ([prompt/prompt-07-deep-dive-console-log-cleanup-v5.md](prompt/prompt-07-deep-dive-console-log-cleanup-v5.md), plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-temporal-octopus.md`) : troisième salve API du chantier console.log cleanup. Scope : 3 routes singulières restantes du périmètre routes API — `app/api/profile/route.ts` (18 sites), `app/api/savings/data/route.ts` (19 sites), `app/api/bank-balance/route.ts` (15 sites) = **52 sites totaux**. **Phase 1 audit Explore** a confirmé exactement l'estimation du prompt (18/19/15) et tranché les 4 pivots : (1) PII confirmée dans profile L98/L150/L224 — `data` post-insert et `updates` PUT contiennent `first_name`/`last_name`/`salary` en clair (DROP critique), (2) bank-balance L158 fait `return 500` immédiat (pas de continuation silencieuse) → DROP redondant avec L146 qui logue déjà checkError code 42P01, (3) bank-balance L100 validation diagnostic (NaN/string/négatif) → KEEP+migrate sur `logger.warn` (valeur reçue grep-able), (4) savings/data L64 confirmé silently-swallowed → KEEP+migrate. 3/3 routes déjà wrappées en `withAuth(AndProfile)` (Sprint Refactor-Architecture-v4) — pas de boilerplate auth. User a arbitré **triage strict** (cohérent avec Lot 4a/4b precedent) sur les flow logs `'🔍 GET /api/profile - userId:', userId` (le `userId` est déjà dans le wrapper context exploitable côté Vercel) → DROP. Ratio final **43 supprimés / 9 migrés** (~83%/17%). **Commit 1** ([6087506](#)) triage console.\* sur 3 fichiers : profile (15 DROP / 3 KEEP+migrate — DB select+insert+update discriminantes par méthode HTTP), savings/data (17 DROP / 2 KEEP+migrate — L42 budgets DB error + L64 piggy_bank silently-swallowed), bank-balance (11 DROP / 4 KEEP+migrate — L50 DB error multi-branche logger.warn / L100 validation diagnostic logger.warn / L146 + L190 DB errors logger.error). 2 catch-blocks normalisés `} catch (error) { → } catch {` (profile PUT, savings/data) ; les 4 autres catches conservent `error` pour propager `error.message` au client via la 500. Imports logger ajoutés au top des 3 fichiers. **Commit 2** ([48556f0](#)) [eslint.config.mjs](eslint.config.mjs) — extension du bloc per-file `no-console: 'error'` avec liste explicite des 3 paths Lot 4c (préférée à un glob `app/api/savings/**` global parce que `savings/transfer` reste hors scope Lot 4d). Sanity test injection `console.log` dans profile/route.ts → 1 error ESLint comme attendu, revert vérifié. **Lint baseline 819 → 783 (−36 warnings)** — aligné avec l'estimation. **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` 0 errors / 783 warnings, `pnpm test:run` 30 passed / 34 skipped inchangé, negative greps : `Grep "console\." app/api/{profile,savings/data,bank-balance}/route.ts` = 0 hit + `Grep "from '@/lib/logger'" app/api/{profile,savings/data,bank-balance}/route.ts` = 3 fichiers (1 par fichier). **Top 5 fichiers post-Lot 4c** inchangé : `process-step1` 120, `financial-calculations` 112, `complete` 85, `auto-balance` 66, `balance` 63. **Smoke browser deferred to user** (`/settings` exerce profile GET/PUT, `/dashboard`/`/group-dashboard` exerce bank-balance GET, `SavingsDistributionDrawer` exerce savings/data GET, POST `/api/bank-balance` avec body invalide doit logger via `logger.warn`). LOC delta brut : −63 LOC + 14 LOC import = net −49 LOC. Livré 2026-05-10, 3 commits sur `cleanup` (`6087506` triage / `48556f0` eslint glob / closeout). Score : ~98.2 → ~98.2/100 stable (cleanup, pas de saut métier).
- ✅ **Sprint Cleanup-I8 / Lot 4d** ([prompt/prompt-07-deep-dive-console-log-cleanup-v6.md](prompt/prompt-07-deep-dive-console-log-cleanup-v6.md) si capturé, plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-cached-valley.md`) : quatrième salve API du chantier console.log cleanup. Scope **single-file** : `app/api/savings/transfer/route.ts` (401 LOC, le plus gros consommateur de `console.*` hors god files I4/I5). **Phase 1 audit Explore a invalidé l'estimation du prompt source** (45 sites attendus, mais grep -c surcomptait à cause des template literals multi-ligne) : **38 sites distincts / 41 statements**. Aucun `console.warn`/`info`/`debug` (tout `log` ou `error`). Structure : POST handler L24-176 (21 sites) + `handlePiggyBankAction` L181-290 (15 sites) + `handleBudgetToPiggyBank` L296-401 (4 sites). Wrapper `withAuthAndProfile` confirmé. User a arbitré **triage Strict** (vs Modéré ou Agressif) cohérent avec Lot 4c (PII context) : ratio final **32 supprimés / 6 migrés** (~84%/~16%) — légèrement plus aggressive que Lot 4c (83/17) parce qu'aucun KEEP non-rollback ne survit (les 3 cleanup-attempts CRITIQUES tirent à eux seuls 50% des KEEP). **Commit 1** ([79cbe8b](#)) triage console._ sur 1 fichier : 32 DROP (13 headers visuels + empty lines `💸💸💸`/`🐷🐷🐷`/`✅✅✅` + 12 PII flow logs avec montants € + budget IDs + user ID + 6 DB errors → 500 sans branche métier discriminante L83/L96/L221/L255/L270/L336 + 1 catch-all outer L173) ; 6 KEEP+migrate vers `logger.error` : L106 `updateFromError` (pre-rollback discriminator), L118 `updateToError` (discriminates rollback branch), **L123 `rollbackError` CLEANUP-ATTEMPT CRITIQUE** (rollback FROM impossible après TO fail → DB inconsistante FROM debited / TO unchanged si fire), L317 `updateError` (piggy update fails après budget debited), **L322 `rollbackErr` CLEANUP-ATTEMPT CRITIQUE** (rollback budget impossible après piggy update fail), **L338 `rollbackErr` CLEANUP-ATTEMPT CRITIQUE** (rollback budget impossible après piggy INSERT fail). 3 catch-blocks normalisés `} catch (error) { → } catch {` (outer POST L148 + piggy update L214 + budget update L289 — leur binding `error` n'est plus utilisé après DROP du log). Bindings préservés sur les 6 KEEP (le binding nourrit le `logger.error(..., error)` rest-spread). LOC : 401 → 355 (−46 LOC). **Commit 2** ([e6e5da9](#)) [eslint.config.mjs](eslint.config.mjs) — **swap** `'app/api/savings/data/**'` pour `'app/api/savings/**'` (Option α). Cas concret de la convention §6 "100% couverture domaine → glob global" maintenant que Lot 4c (savings/data) + Lot 4d (savings/transfer) couvrent tout `savings/_`. Future-proof : toute future route savings auto-protégée. **Sanity test** par injection `console.log('SANITY-TEST')`dans savings/transfer →`pnpm lint:check`exit 1 avec 1 error (752 problems = 751 warnings + 1 error), revert vérifié. **Lint baseline 783 → 751 (−32 warnings)**, légèrement meilleur que l'estimation −18 à −28 du plan parce que les 13 sites visuels/empty`console.log('')`étaient warn-counted alors que le plan attendait que la majorité soit`console.error`allow-listés. **Verif end-to-end** :`pnpm typecheck`exit 0,`pnpm lint:check`0 errors / 751 warnings,`pnpm test:run`30 passed / 34 skipped inchangé,`pnpm build`56/56 routes ✓. Negative greps :`Grep "console\." app/api/savings/transfer/route.ts`= 0 hit ;`Grep "from '@/lib/logger'" app/api/savings/transfer/route.ts`= 1 hit. **Trouvaille collatérale Phase 1** : la pré-estimation 45 sites était inflated, l'audit Explore a redressé à 38 + classifié les 7 KEEP attendus en 6 KEEP réels (l'audit avait listé 7 entries mais la 7e était une note descriptive du tableau, pas un site distinct — leçon à reprendre pour les Lots 4e/5). **Top 5 fichiers post-Lot 4d** inchangé :`process-step1`120,`financial-calculations`112,`complete`85,`auto-balance`66,`balance`63 — Lot 4d a touché 1 fichier hors top. **Smoke browser deferred to user** (drawer transfert savings sur`/dashboard`exerce POST budget→budget ;`action: 'piggy_bank'`exerce`handlePiggyBankAction`; budget→tirelire exerce`handleBudgetToPiggyBank`. Les 3 cleanup-attempts L123/L322/L338 ne sont pas reproductibles manuellement — race condition DB qui fail au milieu d'un transfert atomique — pas bloquant). Livré 2026-05-10, 2 commits code + closeout sur `cleanup` (`79cbe8b`triage /`e6e5da9` eslint glob swap / closeout). Score : ~98.2 → ~98.2/100 stable (cleanup, pas de saut métier).
- ✅ **Sprint Cleanup-I8 / Lot 4e** (plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-synthetic-noodle.md`) : cinquième salve API du chantier console.log cleanup, **plus gros périmètre du chantier hors god files I4/I5**. Scope `lib/api/finance/**` (12 modules handlers du namespace canonique unifié installé Sprint Refactor-Architecture-v3 — les `route.ts` sous `app/api/finance/` étant des thin re-exports `export { GET, POST } from '@/lib/api/finance/...'` avec 0 site `console.*`). Tous les 12 modules déjà wrappés `withAuth(AndProfile)` Sprint v3, pas de boilerplate auth à toucher. **Phase 1 audit Explore × 3 en parallèle** (heavy/medium/light buckets) a redressé la pré-estimation `grep -c` de 190 sites vers **152 statements distincts** (multi-line console.log inflated le compteur, pattern miroir Lot 4b/4d). User a arbitré **triage Strict** (cohérent Lot 4c/4d PII context) + fusion **medium+light en 1 commit** (le bucket light de 13 sites étant trop petit pour mériter un commit séparé) + KEEP+migrate des 9 snapshot failures `'⚠️ Échec sauvegarde snapshot (non critique)'` (silently-swallowed per §6 règle d'or rule c, sémantique 200/201-on-error → logger.warn). Ratio final **119 supprimés / 33 migrés** (~78%/~22%). **Commit 1** ([572e94a](#)) heavy bucket : `expenses-add-with-logic.ts` (21 stmts, **2 cleanup-attempts CRITIQUES** L216 piggy rollback + L229 savings rollback dans smart-allocation tirelire→savings→budget — pattern miroir Lot 4d savings/transfer) + `incomes.ts` (32) + `budgets.ts` (30) + `summary.ts` (20, **L115 → logger.warn** pour préserver la sémantique 200-with-default-data fallback explicit Sprint v3 — 200-on-error pas error). **Commit 2** ([a3217cc](#)) medium+light combinés (49 stmts, 8 fichiers) : `income-real.ts` (13 — **interaction `FinancialLogger` 0 overlap audit-confirmed**, les 10 `console.*` directs sont indépendants des call sites `FinancialLogger.startOperation/success/databaseError/etc.` qui restent intacts pour l'alignement deferred I4) + `expenses-real.ts` (11, **+1 cleanup-attempt CRITIQUE** L431 rollback allocation reverse on DELETE silently-swallowed catch ne re-throw pas) + `income-estimated.ts` (9) + `expenses-progress.ts` (6) + `budgets-estimated.ts` (8) + `rav.ts` (3) + `income-progress.ts` (1) + `expenses-preview-breakdown.ts` (1). **Politique de triage uniforme** appliquée pour résoudre les micro-divergences entre buckets : DB error inside `if (error)` après opération Supabase spécifique → KEEP+migrate logger.error (rule b discrimine non-obvious branch) ; outer catch-all `'Error in METHOD /api/...:'` → DROP (rule a, Vercel capture la stack). **Commit 3** ([7c73fc2](#)) [eslint.config.mjs](eslint.config.mjs) extension du bloc per-file `no-console: 'error'` avec 2 globs : `lib/api/finance/**` (couvre les 12 modules migrés + toute future route handlers) + `app/api/finance/**` (**future-proof, 0 site aujourd'hui** car thin re-exports — protège contre tout futur écart route.ts qui inlinerait du code). **Sanity test** par injection `console.log("SANITY-TEST-LOT-4E")` dans `lib/api/finance/budgets.ts` → `pnpm lint:check` exit 1 avec 619 problems (1 error + 618 warnings), revert vérifié. **Lint baseline 751 → 618 (−133 warnings, dépassement de la fourchette estimée −130-170)**. **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` 0 errors / 618 warnings, `pnpm test:run` 30 passed / 34 skipped inchangé, `pnpm verify` exit 0 (8 stages incluant 6 db:\* checks). Negative greps clean : `Grep "console\." lib/api/finance/` 0 hit, `Grep "from '@/lib/logger'" lib/api/finance/` retourne **9 fichiers** (tous ceux ayant ≥1 KEEP : expenses-add-with-logic, incomes, budgets, summary, income-real, expenses-real, income-estimated, budgets-estimated, rav ; les 3 sans KEEP — expenses-progress, income-progress, expenses-preview-breakdown — n'importent pas le logger). **Trouvaille collatérale Phase 1** : l'audit medium bucket a classifié initialement les inner DB-error `console.error` comme DROP (cohérent avec son interprétation locale) tandis que l'audit heavy bucket les classifiait KEEP+migrate ; ma politique uniforme dans le plan a aligné sur la version règle d'or-stricte (KEEP+migrate inner discriminators), donnant +9 KEEP supplémentaires côté `income-estimated`/`budgets-estimated` vs le rapport audit initial. **Top 5 fichiers post-Lot 4e** inchangé : `process-step1` 120, `financial-calculations` 112, `complete` 85, `auto-balance` 66, `balance` 63 — Lot 4e a touché 12 fichiers tous hors top, le top reste dominé par les god files I4/I5 et les 6 routes monthly-recap stateful hors scope. **Smoke browser deferred to user** (`/dashboard` exerce summary/budgets-estimated/expenses-progress/income-progress/rav ; planning drawer exerce budgets/incomes/income-estimated ; AddTransactionModal exerce expenses-real+expenses-add-with-logic+expenses-preview-breakdown ; AddIncomeDialog exerce income-real ; cleanup-attempts L216/L229/L431 non reproductibles manuellement — race DB au milieu d'op atomique). LOC delta brut : −165 LOC commit 1 + −81 LOC commit 2 + +2 LOC commit 3 = **net −244 LOC**. Livré 2026-05-10, 3 commits code + closeout sur `cleanup` (`572e94a` heavy / `a3217cc` light-medium / `7c73fc2` eslint glob / closeout). Score : ~98.2 → ~98.2/100 stable (cleanup, pas de saut métier).
- ✅ **Sprint Cleanup-I8 / Lot 5** ([prompt/prompt-07-deep-dive-console-log-cleanup-v6.md](prompt/prompt-07-deep-dive-console-log-cleanup-v6.md) si capturé, plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-fancy-duckling.md`) : sixième salve du chantier console.log cleanup, **plus gros périmètre du chantier hors god files I4/I5**. Scope : toute la couche client = 12 components UI + 12 hooks + 1 context + 5 pages App Router = **30 fichiers**. **Phase 1 audit Explore × 3 en parallèle** (heavy/medium/light) + 1 grep verification a redressé l'estimation initiale 199 → **193 sites distincts** (multi-line console.log inflated marginal vs Lots 4b/4d/4e parce que le code client a moins de console.log multi-lignes décoratifs) + a surfacé 5 fichiers manquants vs prompt source (3 hooks : useAuth/useGroupSearch/useGroupMembers + 2 pages : reset-password/forgot-password). User a arbitré 3 décisions : (a) **Option B découpage par taille** (Heavy/Medium/Light cohérent Lot 4b/4d/4e), (b) **Stratégie α broad globs ESLint** (`components/**`+`hooks/**`+`contexts/**` + 5 paths page.tsx — 100% couverture vérifiée), (c) **logger.debug + drop guard** pour les 4 sites dev-guarded `if (process.env.NODE_ENV === 'development')` (préserve l'intent dev-only via LOG_LEVEL gating). Ratio final **133 supprimés / 60 migrés** (~69%/~31%, légèrement plus KEEP-heavy que Lot 4e 78/22 parce que le code client a moins de flow-log décoratifs et plus de catch silently-swallowed ; cohérent triage Strict). **Commit 1** ([f1a1619](#)) Heavy bucket (3 monthly-recap components, 69 sites → 68 DROP + 1 KEEP) : MonthlyRecapFlow.tsx 31 DROP + 1 KEEP+migrate `logger.warn` L110 gap_residuel partiel (discrimine la branche "équilibrage partiel" non-évidente du return code) ; MonthlyRecapStep1.tsx 20 DROP (useEffect debug pure 15 lignes supprimé entièrement, import useEffect retiré) ; MonthlyRecapStep2.tsx 17 DROP (queryFn banner 📊📊📊 + dump RAV/surplus/déficit dépouillé) + 3 catch normalisés `} catch (error) → } catch`. Aucun cleanup-attempt côté client (recovery server-side). Lint 618 → 541. **Commit 2** ([e6e6bbf](#)) Medium bucket (8 fichiers, 67 sites → 42 DROP + 25 KEEP, **5 cleanup-attempts CRITIQUES préservés annotés in-line**) : SavingsDistributionDrawer L171 (POST /savings/transfer fail peut laisser DB partiellement débitée, no toast UX) ; useMonthlyRecap L84 (recap transfer fail) + L115 (auto-balance fail) + L157 (recap completion fail — partial finalization risk) ; useGroups L145 + L168 (join/leave cross-mutation cascade fail si invalidate ['profile']/['financial-*'] échoue après server commit, financial state stale). 5 hooks CRUD (useRealIncomes, useRealExpenses, useIncomes, useBudgets, useGroups) : drop des mutationFn redundant errors (re-thrown puis re-loggés dans onError) + migrate les onError silently-swallowed (no toast UX) vers logger.error. TransactionTabsComponent : drop flow logs deletion lifecycle 🗑️/✅/🔄 + 2 KEEP+logger.warn pour silent deletion failures. Lint 541 → 510. **Commit 3** ([e474410](#)) Light bucket (19 fichiers, 57 sites → 23 DROP + 34 KEEP) : ServiceWorkerRegistration L18 boot path PWA CRITICAL non-négociable ; AuthContext L144 logger.warn (auth check error filter 401 préservé) + 4 logger.error (logout/refresh/init/manual refresh) ; 4 dev-guarded sites migrés vers logger.debug + drop guard ; useBankBalance L34 logger.warn (validation diagnostic, branche 500+missing-table fallback légit) ; **app/dashboard/page.tsx 18/19 DROP massif** (useEffect verbose state dump RAV/économies/revenus/dépenses en € à chaque render + PII profile.first_name/last_name — 1 KEEP L68 logger.error onProfileError) ; app/monthly-recap/page.tsx 5/5 DROP (2 lignes commentées + 3 success traces redundantes UI) ; reset-password + forgot-password + inscription auth flow errors → logger.error. Lint 510 → 490. **Commit 4** ([a388511](#)) [eslint.config.mjs](eslint.config.mjs) extension du bloc per-file `no-console: 'error'` avec 8 entrées Lot 5 : `components/**` + `hooks/**` + `contexts/**` + 5 paths app/{dashboard,monthly-recap,inscription,reset-password,forgot-password}/page.tsx. **Sanity test** par injection `console.log('SANITY-LOT5')` dans hooks/useBudgets.ts → `pnpm lint:check` exit 1 avec 491 problems (1 error + 490 warnings), revert vérifié exit 0. **Lint baseline 618 → 490 (−128 warnings cumulés sur les 3 commits, dépasse la fourchette estimée −120-140)**. **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` 0 errors / 490 warnings, `pnpm test:run` 30 passed / 34 skipped inchangé, `pnpm build` 56/56 routes exit 0, `pnpm verify` exit 0 (8 stages). `pnpm format:check` 2 issues pré-existantes hors scope (doc2/AUDIT-RESOLUTIONS.md untracked + next.config.js). Negative greps clean : `Grep "console\." components/ hooks/ contexts/ app/{dashboard,monthly-recap,inscription,reset-password,forgot-password}/` 0 hit. **Smoke browser deferred to user** (flow login/logout exerce AuthContext + useAuth ; /dashboard exerce 6 components + 4 hooks + dashboard page ; /monthly-recap exerce flow complet MonthlyRecapFlow→Step1→Step2 + useMonthlyRecap ; /settings exerce ProfileSettingsCard + AvatarUpload ; /inscription + /reset-password + /forgot-password chacune leur page ; cleanup-attempts non reproductibles manuellement — race conditions DB côté serveur). LOC delta brut : −129 LOC commit 1 + −23 LOC commit 2 + −52 LOC commit 3 + +8 LOC commit 4 = **net −196 LOC**. Livré 2026-05-10, 4 commits code + closeout sur `cleanup` (`f1a1619` heavy / `e6e6bbf` medium / `e474410` light / `a388511` eslint glob / closeout). Score : ~98.2 → ~98.2/100 stable (cleanup, pas de saut métier).
- ✅ **Sprint Cleanup-I8 / Lot 5b** ([prompt/prompt-07-deep-dive-console-log-cleanup-v9.md](prompt/prompt-07-deep-dive-console-log-cleanup-v9.md) si capturé, plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-soft-hamster.md`) : sixième bis salve, **petite salve consécutive Lot 5** qui ramasse 3 fichiers serveur orphelins du périmètre Lot 5/4b. Scope : `app/api/auth/session/route.ts` (3 sites, hors-scope Lot 5 server-side) + `app/api/monthly-recap/recover/route.ts` (9 sites, hors-scope Lot 4b stateful exclu de la brace expansion) + `app/api/monthly-recap/status-test/route.ts` (4 sites, candidat DELETE confirmé). **Phase 1 audit Explore × 1** a confirmé exactement les 16 sites distincts (vs grep -c, pas d'inflation multi-line) + a tranché : (a) `status-test` confirmé **DEAD CODE** par grep cross-codebase (`/api/monthly-recap/status-test` → 0 hit dans `app/`, `components/`, `hooks/`, `contexts/`, `lib/`, `middleware.ts`), mock data hardcodé `testUserId`/`hasExistingRecap = false`/`test_mode: true`, 0 query DB → DELETE entirely. User a arbitré 3 décisions (toutes "Recommended" du brief) : (Q1) **DELETE entirely** status-test, (Q2) **triage Strict 6/3 sur recover** (cohérent Lot 5 strict 69/31 vs audit conservative 2/7), (Q3) **KEEP+migrate logger.error CRITIQUE sur auth/session L56** (Supabase auth réussi mais JWT session fail → état inconsistant grep-able si bug futur, vs audit DROP rule a). Découpage Option α : 1 commit triage + 1 commit ESLint + 1 closeout = **3 commits**. Ratio final triage **8 supprimés / 4 migrés** (~67/33 sur les 12 sites triagés, cohérent triage Strict). **Commit 1** ([d7196b9](#)) triage console.\* sur 3 fichiers : auth/session (1 KEEP+migrate L56 logger.error CLEANUP-ATTEMPT CRITIQUE + 2 DROP L97/L129 outer catch-all rule a, 2 catches normalisés `} catch {`) + recover (3 KEEP+migrate dont **L306 logger.error CLEANUP-ATTEMPT CRITIQUE** "recovery rollback partiel → snapshot peut rester actif" + L288 logger.warn silently swallowed deactivation + L404 logger.error GET catch propagated, 6 DROP L88/L89/L100 flow logs verbose + L291/L292 success traces + L316 outer catch-all, 1 catch normalisé) + status-test DELETE FILE (~64 LOC). Imports logger ajoutés au top des 2 fichiers ayant ≥1 KEEP. **Commit 2** ([e740762](#)) [eslint.config.mjs](eslint.config.mjs) — extension du bloc per-file `no-console: 'error'` avec 2 nouveaux paths : `app/api/auth/**` (couvre `session/route.ts` + `confirm/route.ts` 0 site future-proof) + `recover` ajouté à la brace expansion `app/api/monthly-recap/{...,recover}/**` (10 routes couvertes vs 9). **Sanity test** par injection `console.log('SANITY-LOT5B-AUTH/RECOVER')` → `pnpm lint:check` exit 1 avec **2 errors** (484 problems = 482 warnings + 2 errors), revert vérifié exit 0. **Lint baseline 490 → 482 (−8 warnings)** — dans la fourchette estimée −5-15 du plan (tous les KEEP sont `console.error` allow-listés warn-side, donc le delta vient des DROP `console.log` flow + DROP de status-test). **Verif end-to-end** : `pnpm typecheck` exit 0 (avec rm -rf .next/ préalable — cache Next.js validator stale référençait le route deleted), `pnpm lint:check` 0 errors / 482 warnings, `pnpm test:run` 30 passed / 34 skipped inchangé, `pnpm build` 55/55 routes (vs 56 — status-test removed), `pnpm verify` exit 0. Negative greps clean : `Grep "console\." app/api/auth/ app/api/monthly-recap/recover/` 0 hit ; `Grep "from '@/lib/logger'" ...` 2 fichiers (1 par fichier ayant ≥1 KEEP) ; `Glob app/api/monthly-recap/status-test/route.ts` 0 hit (deleted). **Top 5 fichiers post-Lot 5b** inchangé : `process-step1` 120, `financial-calculations` 112, `complete` 85, `auto-balance` 66, `balance` 63 — Lot 5b a touché 3 fichiers tous hors top, et status-test (4 sites) deleted. **Smoke browser deferred to user** (POST `/api/auth/session` exerce login/logout/refresh ; GET `/api/auth/session` exerce status check via middleware ; recovery flow + 2 cleanup-attempts L56/L306 non reproductibles manuellement — race DB en prod). LOC delta brut : −83 LOC commit 1 + +1 LOC commit 2 = **net −82 LOC**. Livré 2026-05-10, 2 commits code + closeout sur `cleanup` (`d7196b9` triage / `e740762` eslint glob / closeout). Score : ~98.2 → ~98.2/100 stable (cleanup, pas de saut métier).
- ✅ **Sprint Cleanup-I8 / Lot 5c** (plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-ethereal-brooks.md`) : sixième ter salve, **petites libs server-side indépendantes des chantiers I4/I5**. Scope : `lib/{auth,session,session-server,supabase-client,database-snapshot,monthly-recap-calculations}.ts` + `app/auth/confirm/route.ts` + `lib/api/with-auth.ts` future-proof glob = **8 fichiers**. **Phase 1 audit Explore × 1** a confirmé l'indépendance I4/I5 de `monthly-recap-calculations.ts` (0 import depuis `app/api/monthly-recap/{complete,balance,auto-balance,process-step1}/`) + a surfacé 3 trouvailles : (a) **`testSupabaseConnection()` dead code** — 0 callsite cross-codebase, mirror Lot 5b status-test pattern ; (b) `lib/api/with-auth.ts` **0 console.\* site** (wrapper canonique reste clean depuis création v3+v4+v5) ; (c) audit a sous-compté `lib/monthly-recap-calculations.ts` à 13 statements vs **19 réels** — 2 outer catches L154/L365 manqués, leçon "ne pas trust un audit single-pass sur des fichiers >300 LOC". User a arbitré 2 décisions (toutes "Recommended") : (Q3) **DELETE testSupabaseConnection() entirely** (mirror Lot 5b dead-code precedent) ; (Q4) **Option β** = 2 commits triage par domaine (auth helpers vs snapshot+recap) + 1 ESLint + 1 closeout = **4 commits**. **Total réel : 45 sites distincts** (19 monthly-recap-calculations + 9 auth + 8 database-snapshot + 6 auth/confirm + 5 session-server + 3 supabase-client deleted + 1 session). Ratio final triage **23 KEEP+migrate / 18 DROP** (~56/44, triage Modéré KEEP-heavy parce que monthly-recap et database-snapshot sont audit-trail heavy plutôt que flow-log heavy — 13 success markers avec metrics dont les 5 audit-trail CRITIQUES `database-snapshot.ts:169-173` préservés via logger.info). **Commit 1** ([9d93a2b](#)) auth domain (5 fichiers, 24 sites — `lib/auth.ts` 9 sites : 2 KEEP+migrate logger.warn L194/L224 filter-401 silently-swallowed + 7 DROP outer catches client-side, 6 catches normalisés `} catch {` ; `lib/session.ts` 1 site DROP outer catch, catch normalisé ; `lib/session-server.ts` 5 sites : 1 KEEP+logger.debug L114 audit per-request gated dev-only + 4 DROP (3 early-exit + 1 outer catch), catch normalisé ; `lib/supabase-client.ts` DELETE `testSupabaseConnection()` 17 LOC dead code recovery git revert ; `app/auth/confirm/route.ts` 6 sites : 4 KEEP+migrate logger.{debug,warn,error,error,info} dont **L46 logger.error CLEANUP-ATTEMPT CRITIQUE** OTP réussi mais data.user manquant — edge case non-évident, mérite trace si récurrent + 1 DROP outer catch, catch normalisé). **Commit 2** ([947229d](#)) snapshot+recap (2 fichiers, 27 sites — `lib/monthly-recap-calculations.ts` 19 sites : 6 KEEP+logger.error DB error discriminators + 7 KEEP+logger.info audit trail metrics + 6 DROP outer catch-all L122/154/222/331/365/402, 6 catches normalisés ; `lib/database-snapshot.ts` 8 sites : 1 KEEP+logger.warn L75 non-blocking DB warnings loop + 1 KEEP+logger.error L163 insertError discriminator + **5 KEEP+logger.info L169-173 AUDIT-TRAIL CRITIQUE multi-table snapshot foundational pour recovery audit** + 1 DROP outer catch, binding `err` préservé pour `err.message` du return). **Commit 3** ([3ce5a5d](#)) [eslint.config.mjs](eslint.config.mjs) extension du bloc per-file `no-console: 'error'` avec 8 nouvelles entrées : `lib/auth.ts`, `lib/session.ts`, `lib/session-server.ts`, `lib/supabase-client.ts`, `lib/database-snapshot.ts`, `lib/monthly-recap-calculations.ts`, `lib/api/with-auth.ts` (future-proof 0 site mais wrapper foundational pour 34 modules), `app/auth/**` (couvre confirm/route.ts + auth-code-error/page.tsx future-proof). **Sanity test** par injection `console.log('SANITY-LOT5C')` dans `lib/auth.ts` → `pnpm lint:check` exit 1 avec 462 problems (1 error + 461 warnings), revert vérifié exit 0. **Lint baseline 482 → 461 (−21 warnings, exact match plan estimate)**. **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` 0 errors / 461 warnings, `pnpm test:run` 30 passed / 34 skipped inchangé, `pnpm format:check` 2 issues pré-existantes hors scope (doc2/AUDIT-RESOLUTIONS.md + next.config.js), `pnpm build` 55/55 routes exit 0. Negative greps clean : `Grep "console\." lib/auth.ts lib/session.ts lib/session-server.ts lib/supabase-client.ts lib/database-snapshot.ts lib/monthly-recap-calculations.ts app/auth/` 0 hit ; `Grep "from '@/lib/logger'" ...` 5 fichiers (lib/auth + lib/session-server + lib/database-snapshot + lib/monthly-recap-calculations + app/auth/confirm/route.ts) ; `Grep "testSupabaseConnection"` 0 hit. **Top 5 fichiers post-Lot 5c** inchangé : `process-step1` 120, `financial-calculations` 112, `complete` 85, `auto-balance` 66, `balance` 63 — Lot 5c a touché 8 fichiers tous hors top, le top reste dominé par les god files I4/I5 et les 4 routes monthly-recap stateful hors scope. **Smoke browser deferred to user** (`/connexion` exerce `signInWithPassword` + AuthContext init ; logout `/settings` exerce `signOut` ; refresh session 50min interval exerce `refreshSession` ; `/auth/confirm?token_hash=...&type=...` exerce app/auth/confirm/route.ts via email Supabase ; snapshot creation `/monthly-recap` flow exerce `lib/database-snapshot.ts` audit-trail L169-173 visibles via `LOG_LEVEL=info` ; cleanup-attempt L46 OTP no-user non reproductible manuellement). **Trouvaille collatérale hors scope** : `lib/auth.ts` exporte `signUp`/`resetPassword`/`updatePassword` qui n'ont **aucun consumer dans app/** (vérifié grep) — dead code, à séparer dans un dead-code purge sprint, pas dans console cleanup. LOC delta brut : −33 LOC commit 1 + −5 LOC commit 2 (29 ins + 34 del) + 8 LOC commit 3 = **net −30 LOC**. Livré 2026-05-10, 3 commits code + closeout sur `cleanup` (`9d93a2b` auth domain / `947229d` snapshot+recap / `3ce5a5d` eslint glob / closeout). Score : ~98.2 → ~98.2/100 stable (cleanup, pas de saut métier).
- ✅ **Sprint Cleanup-I8 / Lot 5d** (plan dans `C:\Users\gille\.claude\plans\sprint-cleanup-i8-dapper-galaxy.md`) : septième salve **optionnelle** du chantier console.log cleanup — ferme le dernier domaine `app/api/` non-couplé I5. Scope : 6 routes `app/api/debug/**` toutes gardées par `blockInProduction()` (404 prod). **Phase 1 audit Explore × 1** a redressé l'estimation initiale ~68 sites du brief vs **57 sites annoncés / 64 statements réels** (audit a sous-compté financial+group-financial blocs summary multi-line 9-10 lignes en 1 site chacun → recount précis 17+17+3+4+17+6 = 64 statements distincts). User a arbitré 2 décisions ("Recommended") : (Q1) **triage Strict 67/33** (cohérent Lot 4c/4d/4e/5/5b/5c — préserver les DB-error discriminants pour grep en dev même si blockInProduction rend les logs invisibles en prod) ; (Q2) **Option α** = 1 commit triage + 1 ESLint + 1 closeout = **3 commits** (scope homogène). Confirmé Phase 1 : `lib/debug-guard.ts` clean 0 sites (glob future-proof seulement). Ratio final triage **45 supprimés / 12 migrés** (~79/21, plus DROP-heavy que strict typique parce que 2 fichiers — financial.ts + group-financial.ts — sont 95% flow logs sans DB-error inline). **Commit 1** ([a5e6897](#)) triage console.\* sur 6 fichiers : financial (16 DROP + 1 KEEP outer catch logger.error) + group-financial (16 DROP + 1 KEEP outer catch) + remaining-to-live (2 DROP + 1 KEEP outer catch) + reset-all (3 DROP + 1 KEEP outer catch) + reset-budgets (12 DROP + 5 KEEP : 4 inline DB error discriminants suppression-transferts/dépenses/fetch-budgets/insertion-dépenses + 1 outer catch ; PII test acceptable DROP sur lignes 115-117 montants € + noms budgets seed-test) + retrigger-recap (3 DROP + 3 KEEP : 1 inline DB error + 1 inline warn rule c silently-swallowed L84 snapshot deactivation fail + 1 outer catch). **0 cleanup-attempt CRITIQUE** (routes atomiques Supabase, pas de rollback transactionnel multi-table). **6 outer catches binding préservé** (`error.message` utilisé dans returns 500 — non-normalisable en `} catch {`). Imports logger ajoutés aux 6 fichiers. Pre-commit hook (lint-staged) a mécaniquement collapsed 1 statement multi-line dans reset-budgets (Prettier soft-wrap). **Commit 2** ([bb3d8da](#)) [eslint.config.mjs](eslint.config.mjs) extension du bloc per-file `no-console: 'error'` avec 2 nouvelles entrées : `app/api/debug/**` (couvre les 6 routes migrées + future-proof) + `lib/debug-guard.ts` (future-proof 0 site aujourd'hui — module guard 9 LOC). **Sanity test** par injection `console.log('SANITY-LOT5D')` dans `app/api/debug/financial/route.ts` → `pnpm lint:check` exit 1 avec 1 error (407 problems = 1 error + 406 warnings), revert vérifié exit 0 (406 problems / 0 errors). **Lint baseline 461 → 406 (−55 warnings, dépasse fourchette estimée −10-30 du plan)** parce que la majorité des sites étaient `console.log` flow logs warn-counted, pas `console.error` allow-listés (qui resteraient invisibles à la baseline). **Verif end-to-end** : `pnpm typecheck` exit 0, `pnpm lint:check` 0 errors / 406 warnings, `pnpm test:run` 30 passed / 34 skipped inchangé, `pnpm format:check` 2 issues pré-existantes hors scope (doc2/AUDIT-RESOLUTIONS.md + next.config.js — Lot 5/5b/5c precedent), `pnpm build` 55/55 routes exit 0. Negative greps clean : `Grep "console\." app/api/debug/` 0 hit ; `Grep "from '@/lib/logger'" app/api/debug/` 6 fichiers (1 par fichier). **Top 5 fichiers post-Lot 5d** inchangé : `process-step1` 120, `financial-calculations` 112, `complete` 85, `auto-balance` 66, `balance` 63 — Lot 5d a touché 6 fichiers tous hors top, le top reste dominé par les god files I4/I5 et les 4 routes monthly-recap stateful hors scope. **Smoke browser deferred to user** (routes accessibles uniquement en dev `NODE_ENV !== 'production'` — cf. `blockInProduction()` ; en prod toutes retournent 404). **Trouvailles collatérales hors scope Phase 1** : `remaining-to-live.ts` 254 LOC pour seul 3 sites (candidat orpheline cross-codebase) ; `financial.ts` + `group-financial.ts` quasi-dupliqués (148/152 LOC, structure identique sauf scope user vs groupe — candidat refactor consolidation). **Pour le prochain prompt sprint dead-code purge** (couvrant aussi `lib/auth.ts` exports orphelins surfacés Lot 5c). LOC delta brut : −80 LOC commit 1 + +2 LOC commit 2 = **net −78 LOC**. Livré 2026-05-10, 2 commits code + closeout sur `cleanup` (`a5e6897` triage / `bb3d8da` eslint glob / closeout). Score : ~98.2 → ~98.2/100 stable (cleanup, pas de saut métier).
- ✅ **Sprint Audit-Closeout C2** ([prompt/prompt-07-deep-dive-debug-routes.md](prompt/prompt-07-deep-dive-debug-routes.md), plan dans `C:\Users\gille\.claude\plans\fix-c2-velvety-locket.md`) : triage closeout du prompt "Fix C2 — sécuriser les 20 routes `/api/debug/*`" issu de l'audit 2026-04. **Phase 1 inventaire a invalidé 9/10 items** du prompt déjà livrés silencieusement par les sprints intermédiaires : helper `lib/debug-guard.ts` (Sprint 0 / C2), application sur les 20 routes (Sprint 0 / C2 sur la cohorte d'origine, puis R1 -11 `populate-*` + T2 -3 `quick-test`/`recap-data`/`test-balance` + Lot 5b -1 `status-test` → 6 routes survivantes toutes guardées), tests unitaires ([lib/debug-guard.test.ts](lib/debug-guard.test.ts) 4 cas prod/dev/test/undefined), convention CLAUDE.md §6+§8, ESLint glob `app/api/debug/**` no-console (Lot 5d). **5 items résiduels refusés au triage** avec raisons documentées : (a) override `ENABLE_DEBUG_ROUTES` (Option C du deep-dive) — **aucun consumer**, pas d'env staging documentée, le deep-dive flag lui-même "Risque humain : oublier de la mettre à `false` en prod", anti-pattern CLAUDE.md "don't design for hypothetical" ; (b) doc `.env.example` du flag — découle du refus (a) ; (c) smoke test `scripts/test-debug-blocked.ts` — coût 60-120s par PR (build prod + start + curl × 6 + kill), marginal benefit pour une surface qui **rétrécit** (R1 -11, T2 -3, Lot 5b -1, net -15 depuis Sprint 0), la convention §6 + le code review en PR couvrent le cas "nouvelle route sans guard" ; (d) job CI `debug-routes-smoke` — découle du refus (c) ; (e) Phase 2 migration CLI des `populate-*` — **moot**, les 13 routes ont été supprimées en R1. Negative grep `Grep "ENABLE_DEBUG_ROUTES"` retourne 2 hits (le prompt + l'audit doc), 0 dans le code applicatif — preuve qu'on n'a pas introduit le footgun. **Aucun changement de code** : seul livrable = header `⚠️ Prompt triagé 2026-05-10 — STALE, ne pas exécuter` ajouté en tête du prompt + cette entrée CLAUDE.md. **Pattern miroir** des sprints triage récents : Templates-Triage 1/21 livré, Audit-Closeout 2/6 livrés, Hygiène-Code 4/8 invalidés — pour tout prompt issu de l'audit 2026-04, l'inventaire pré-sprint est obligatoire. Livré 2026-05-10, 1 commit closeout sur `cleanup`. Score : ~98.2 → ~98.2/100 stable (closeout administratif, 0 LOC métier).
- ⏭️ **Chantier console.log cleanup — Lots 2 / 6** : Lot 1 (filet logger + strip prod) + Lot 3 (middleware + expense-allocation, 7 sites) + Lot 4a (app/api/groups/\*\*, 22 sites → 11/11 via triage Modéré) + Lot 4b (app/api/monthly-recap/{...9 routes simples}, 132 sites → 113/19 via triage agressif) + Lot 4c (app/api/{profile,savings/data,bank-balance}, 52 sites → 43/9 via triage strict) + Lot 4d (app/api/savings/transfer, 38 sites → 32/6 via triage strict avec 3 cleanup-attempts CRITIQUES préservés) + Lot 4e (lib/api/finance/, 12 fichiers, 152 sites → 119/33 via triage strict avec 3 cleanup-attempts CRITIQUES + 1 fallback 200-on-error préservés) + Lot 5 (couche client : 30 fichiers, 193 sites → 133/60 via triage strict avec 5 cleanup-attempts CRITIQUES + 1 boot-path PWA + 4 dev-guarded migrés debug) + Lot 5b (3 fichiers serveur orphelins : auth/session + recover + status-test DELETE, 16 sites → 8/4/4 deleted via triage strict avec 2 cleanup-attempts CRITIQUES préservés) + Lot 5c (8 fichiers libs server-side foundationnels indépendants I4/I5 + DELETE testSupabaseConnection, 45 sites → 23 KEEP / 18 DROP / 3 file-internal-deleted via triage Modéré avec 1 cleanup-attempt CRITIQUE + 5 audit-trail CRITIQUES préservés, lint 482 → 461) + Lot 5d (`app/api/debug/**` 6 fichiers blockedInProduction, 57 sites → 45 DROP / 12 KEEP via triage Strict avec 0 cleanup-attempt CRITIQUE, lint 461 → 406) livrés ; reste ~176 `console.*` à migrer vers `lib/logger.ts` (cf. §6 Logs + règle d'or de triage). Migration progressive par opportunité — pas de gros commit "remove all". Lot 2 (`lib/finance/*` ~4 fichiers helpers RPC atomiques) couplé I4 ; Lot 6 = sweep final + activation globale `no-console: 'error'` dans le bloc principal d'`eslint.config.mjs` + ajout des routes monthly-recap stateful (`complete`, `balance`, `auto-balance`, `process-step1` ~334 sites) après que I5 ait extrait leur logique métier + ajout `lib/financial-calculations.ts` 112 sites + `lib/financial-logger.ts` après alignement I4.
- ⏭️ **Chantier Zod rollout** : validation runtime des inputs API (cf. `docs/audit/07-deep-dive-zod-rollout.md`)
