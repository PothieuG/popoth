# Règles opérationnelles — Modals & UI (suite de §5 de [operational-rules.md](operational-rules.md))

> Extraction 2026-05-20 (Sprint Drawer-Slide-Fix-And-Header-Harmonize) — la sous-section "Modals & UI" de §5 de [operational-rules.md](operational-rules.md) est déplacée ici suite au franchissement du cap [size-policy](../guardrails/size-policy.md). Le reste de §5 (Séquences non-atomiques, God-files, Idempotency, RAV formula, budget_transfers, recover route + 5 tables, Tests gated, Tables owner-row, Colonnes mirror, Forbidden absolus) reste dans le fichier principal. Étendu au fil des sprints suivants (Fix-Dropdown-PointerEvents-Auto, Enrich-Delete-Confirmation, Modal-Uniformize/Polish/Dropdown-Portal, etc.) — voir [size-policy.md](../guardrails/size-policy.md) §4 pour le décompte des sub-règles ❌.

## Modals & UI

- ❌ **NE PAS** créer de nouveau modal en raw `<div className="fixed inset-0 ...">` — utiliser `<Dialog>` + `<DialogContent>` (Sprint Zod-Rollout v8). 12 surfaces v8 migrées, 12 tests focus-trap regression-guards.
- ❌ **NE PAS** réintroduire un raw `<button onClick> ... <svg path d="M6 18L18 6M6 6l12 12">...</svg></button>` pour le close X d'un modal → utiliser `<ModalCloseX>` (Sprint v10).
- ❌ **NE PAS** réintroduire un wizard single-step `AddTransactionModal` — le wizard 2-step (Step 1 type / Step 2 budgétée-exceptionnelle / Step 3 fields, income skips Step 2) est requis pour P6.
- ❌ **NE PAS** réintroduire le pattern cascade-aggressive piggy→savings→budget dans `calculateBreakdown` — P4 strict default → budget priorité 1, savings cascade UNIQUEMENT si overflow, piggy JAMAIS auto-débitée.
- ❌ **NE PAS** utiliser `window.location.href = '/<route>'` pour naviguer vers une sous-vue qui peut cohabiter dans un drawer/menu déjà ouvert (cas vu Sprint Refactor-Settings-Drawer 2026-05-18 : ancien drawer paramètres → bouton "Gestion du groupe" → `window.location.href = '/settings'` provoquait un full reload + bug intermittent 1/2 sur `history.back()`). Pattern correct : swap horizontal in-place via `[view, setView] = useState<'main'|'sub'>('main')` + 2 panels `absolute inset-0` + `translate-x-{-full,0,full}` + container `overflow-hidden`. Référence : [components/settings/SettingsDrawer.tsx](../../components/settings/SettingsDrawer.tsx).
- ❌ **NE PAS** mettre un loading overlay fullscreen `fixed inset-0 bg-black/50` dans un drawer/sub-panel — préférer **spinner inline** sur les boutons submit + **snackbar non-bloquante z-[60]** sur success (pattern `ProfileSettingsCard.tsx:266-275`). Pour le loading initial d'un fetch dans un panel : skeleton `animate-pulse` léger localisé sur la Card concernée (pattern `ProfileSettingsCard.tsx:33-44`).
- ❌ **NE PAS** imbriquer des `<Card>` (Card racine → Card interne avec actions inline → 2-3 Cards frères) dans un panel de drawer qui doit rester épuré type iOS Settings. Pattern correct (Sprint Rework-Group-Management 2026-05-19, sur [components/settings/GroupManagementPanel.tsx](../../components/settings/GroupManagementPanel.tsx)) : **sections plates** `<section className="space-y-N">` empilées dans un container `space-y-6 overflow-y-auto`, **`<dl>` flat label/valeur** avec séparateurs subtils `border-b border-gray-100 py-2`, **CTA prominent en haut** (style menu-item iOS gradient bleu-indigo, miroir `SettingsDrawer.tsx:92-127`). La Card chrome avec ombre + bordure n'apporte rien dans un panel déjà délimité par le drawer.
- ❌ **NE PAS** placer un bouton d'action destructive ("Quitter", "Supprimer le compte", "Se déconnecter") inline dans le content d'un panel — utiliser un **footer pinned bottom** (`<div className="border-t border-gray-200 p-4">` après le `flex-1 overflow-y-auto`) avec `<Button>` full-width orange ou red. Pattern miroir `SettingsDrawer.tsx:146-154` "Se déconnecter" rouge ; `GroupManagementPanel.tsx` footer "Quitter le groupe" orange (warning, réversible). Le footer slot rend l'action prévisible peu importe la longueur du scroll content.
- ❌ **NE PAS** afficher un bouton actif pour une action que le backend va refuser (false-affordance UX). Pattern correct (Sprint Rework-Group-Management 2026-05-19) : **encart d'info ambré** (`border-amber-200 bg-amber-50 text-amber-800`) au-dessus du bouton expliquant la règle + **bouton désactivé** (`disabled` + `aria-disabled` + `disabled:cursor-not-allowed`) + **handler court-circuité** (defense-in-depth contre click programmatique) + **règle backend matchée** (vérifier `/api/...` retourne 403 dans le même scénario). Exemple : creator avec membres ne peut pas quitter le groupe — encart explique, bouton greyé, handler return early, backend DELETE renvoie 403.
- ❌ **NE PAS** toujours-mounter un composant lourd (qui fait des `useQuery` / `fetch`) à l'intérieur d'un drawer/modal masqué par défaut via CSS (translate off-screen, opacity 0, display visible-mais-pas-vu) — lazy-mount via state flag `hasBeenOpened` initialisé à false, set à true à la 1re ouverture. Pattern "adjust state during render" React 19 (`if (isOpen && !hasBeenOpened) setHasBeenOpened(true)` directement dans le body, PAS dans un `useEffect` car ESLint `react-hooks/set-state-in-effect` refuse `setState` synchrone dans effect). Précédent Sprint Lazy-Mount-GroupManagementPanel 2026-05-20 : `<SettingsDrawer>` rendait toujours `<GroupManagementPanel>` même drawer fermé (juste `-translate-x-full`), `useGroups()` du panel firait `GET /api/groups` à chaque mount dashboard, jamais consommé. Bénéfice prod : 1 fetch en moins par mount. Pattern miroir applicable à tout panel "slot" d'un drawer (sub-views horizontales, modals d'édition). Référence : [components/settings/SettingsDrawer.tsx:34-39](../../components/settings/SettingsDrawer.tsx).
- ❌ **NE PAS** overrider des utilitaires Tailwind v4 `tw-animate-css` qui ciblent la **même CSS custom property** (`--tw-enter-*` / `--tw-exit-*` posés par `slide-in-from-*`, `slide-out-to-*`, `zoom-in/out-*`, `fade-in/out-*`, `spin-in/out-*`, `blur-in/out-*`) sans `!` postfix sur les classes d'override. Tailwind v4 émet les utilités triées alphabétiquement dans le CSS compilé, la cascade prend la dernière assignation : l'override silencieusement perd l'ordre HTML. Précédent Sprint Drawer-Slide-Fix-And-Header-Harmonize 2026-05-20 : `<DialogContent>` base (`slide-in-from-top-[48%]` + `slide-in-from-left-1/2` + `zoom-in-95`) battait `DRAWER_CONTENT_CLASSES` override (`slide-in-from-bottom` + `zoom-in-100`) sur 3 vars → drawer animait en diagonale top-left + zoom au lieu de slider depuis le bas pendant 6 jours. Fix : `slide-in-from-bottom!` / `zoom-in-100!` / `[--tw-enter-translate-x:0]!` (arbitrary CSS var pour neutraliser l'axe non-overridé). Pattern à appliquer à toute future migration shadcn/Radix primitive (Dialog, Popover, Tooltip, DropdownMenu, Toast) ou drawer `vaul`. Header drawer harmonisé sur le template `<SavingsDistributionDrawer>` (icône `h-10` + svg `h-5` + titre `text-xl` + bg `{color}-50/30` + close `<ModalCloseX variant="circle" h-10 w-10>`) — drag handle décoratif supprimé sur fullscreen drawer sans drag-to-dismiss réel. Référence : [components/ui/drawer-content-classes.ts](../../components/ui/drawer-content-classes.ts).

- ❌ **NE PAS** créer un nouveau modal sans utiliser [`MODAL_CONTENT_CLASSES`](../../components/ui/modal-content-classes.ts) comme `className` de `<DialogContent>` (Sprint Modal-Uniformize + Modal-Polish 2026-05-21). Le constant définit `bottom-auto! flex max-h-[85vh] flex-col gap-0 overflow-hidden rounded-2xl border-0 p-0 shadow-xl sm:max-w-md sm:rounded-2xl`. Le `bottom-auto!` est **critique** car Radix DialogContent base applique `fixed inset-4` (bottom:16) + `top-[50%]`. Avec top:50% ET bottom:16 tous deux actifs, le CSS calcule height = 50vh − 16 sur mobile (formule des fixed-positioned avec top+bottom définis), donc la modal était bloquée à ~50% du viewport indépendamment de `max-h-[85vh]` (qui n'est qu'un cap, pas un force). Le `!` postfix Tailwind v4 garantit la cascade winner sur la classe `bottom-4` de la base. Override de width via `cn(MODAL_CONTENT_CLASSES, 'sm:max-w-2xl')` quand un modal nécessite plus de largeur (seul `GroupMembersWithContributionsModal` aujourd'hui).

- ❌ **NE PAS** utiliser `flex-1` sur un wrapper form/body à l'intérieur d'un `<DialogContent>` MODAL_CONTENT_CLASSES — utiliser **`flex-auto` + `min-h-0`** (Sprint Modal-Polish 2026-05-21). Raison : `flex-1` = `flex: 1 1 0%` (basis 0%) → l'item ne contribue pas à la hauteur intrinsèque du parent height-auto, donc le body collapse à 0 et tout le contenu scrolle dans un body minuscule. `flex-auto` = `flex: 1 1 auto` (basis = content size) → l'item prend la taille de son contenu en preferred size puis grow/shrink dans l'espace disponible. `min-h-0` explicite est requis sur les flex items shrinkable (l'auto min-height empêche shrink en-dessous du min-content, ce qui peut bloquer le scroll interne). Pattern modal complet :

  ```tsx
  <DialogContent className={MODAL_CONTENT_CLASSES}>
    <div className="shrink-0 border-b border-gray-200 px-6 py-4">…header…</div>
    <form className="flex min-h-0 flex-auto flex-col overflow-hidden" onSubmit={…}>
      <div className="min-h-0 flex-auto space-y-4 overflow-y-auto px-6 py-4">…body…</div>
      <div className="shrink-0 border-t border-gray-200 px-6 py-4">…footer (submit btn)…</div>
    </form>
  </DialogContent>
  ```

- ❌ **NE PAS** mixer les paddings dans un même modal (ex: header `p-6` + body `px-6 py-4` + footer `px-6 py-3`) — uniformiser sur **`px-6 py-4`** (16 vertical, 24 horizontal) sur header / body / footer (Sprint Modal-Polish 2026-05-21). User feedback : "Certaines modals ont un padding en bas trop large". Cause : combinaison `p-6` body (24 bottom) + `px-6 py-4` footer (16 top) = 40px gap entre last field et buttons + 16px footer bottom = ~56px de bottom area, ressenti comme oversized. **Retirer aussi le `bg-gray-50`** des footers — laisser juste `border-t border-gray-200` pour la séparation. Le sticky footer (form wrappe body+footer en deux divs séparés) est appliqué partout, pas un single scrollable form avec buttons inline (ces derniers scrollent avec le contenu, mauvais UX pour les forms longs).

- ❌ **NE PAS** mettre un bouton "Retour" texte inline en haut du body d'un step de wizard (`<button>...<svg arrow-left/>Retour</button>` au-dessus de la summary chip) — utiliser un **back button iOS-style intégré au header**, top-left, icon-only chevron, rond (Sprint Modal-Polish 2026-05-21). Pattern :

  ```tsx
  <div className="flex shrink-0 items-center gap-2 border-b border-gray-200 px-4 py-3">
    {isFirstStep ? (
      <div className="h-9 w-9 shrink-0" />  {/* placeholder, preserve centrage du titre */}
    ) : (
      <button
        type="button"
        onClick={handleBack}
        disabled={isSubmitting}
        aria-label="Retour à l'étape précédente"
        className="inline-flex h-9 w-9 shrink-0 items-center justify-center rounded-full text-gray-600 transition-colors hover:bg-gray-100 hover:text-gray-900 disabled:opacity-50"
      >
        <svg className="h-5 w-5" .../>chevron-left</svg>
      </button>
    )}
    <DialogTitle asChild>
      <h2 className="flex-1 text-center text-base font-semibold text-gray-900">{stepTitle}</h2>
    </DialogTitle>
    <ModalCloseX onClose={handleClose} disabled={isSubmitting} variant="ghost" className="h-9 w-9" />
  </div>
  ```

  Le titre `text-base font-semibold flex-1 text-center` reste centré entre back (left) et close (right). Si futur RTL needed, swap left/right via classes logical (start/end).

- ❌ **NE PAS** changer le wizardStep sans setter un `stepAnimDir: 'forward' | 'backward'` AVANT le `setWizardStep` — sinon l'animation entre steps part dans la mauvaise direction (Sprint Modal-Polish 2026-05-21). Pattern :

  ```tsx
  const [stepAnimDir, setStepAnimDir] = useState<'forward' | 'backward'>('forward')

  const goNext = () => {
    setStepAnimDir('forward')
    setWizardStep('next')
  }
  const goBack = () => {
    setStepAnimDir('backward')
    setWizardStep('prev')
  }

  // In render — `key={'step-XXX'}` ESSENTIEL pour forcer remount + re-trigger animation :
  {
    wizardStep === 'step-X' && (
      <div
        key="step-X"
        className={cn(
          'min-h-0 flex-auto space-y-4 overflow-y-auto px-6 py-4',
          'animate-in fade-in duration-200',
          stepAnimDir === 'forward' ? 'slide-in-from-right-4' : 'slide-in-from-left-4',
        )}
      >
        …content…
      </div>
    )
  }
  ```

  Pas d'animation de sortie (l'ancien step disparaît instantanément). Si futur besoin riche, migrer vers `framer-motion` `AnimatePresence`. Sans `key`, React réutilise le même div pour les différents steps et l'`animate-in` ne re-déclenche pas.

- ❌ **NE PAS** rendre un dropdown menu/popover en `<div className="absolute z-50 mt-1">` à l'intérieur d'un modal — le menu sera **clipped** par 3 cascades (Sprint Modal-Dropdown-Portal 2026-05-21) : (a) `overflow-hidden` sur DialogContent ; (b) `overflow-y-auto` sur le body scrollable ; (c) `transform: translateY(-50%)` sur DialogContent base (Radix) qui crée un **containing-block pour les descendants `position: fixed`**, donc même fixed positioning ne s'échappe pas du subtree. **Fix obligatoire** : portal vers `document.body` via `React.createPortal()` + `position: fixed` avec coordonnées calculées via `getBoundingClientRect()` sur le button trigger, + `z-[100]` (au-dessus du z-50 Radix) + reposition sur events `scroll` (capture phase via `addEventListener('scroll', update, true)` pour capter les scrolls internes des ancêtres) + `resize`. Pattern de référence : [components/ui/CustomDropdown.tsx](../../components/ui/CustomDropdown.tsx).

  **Sub-règle anti-Radix-close** : si le menu portaled est rendu dans un contexte où Radix Dialog est open, **stopPropagation sur `onPointerDown` + `onMouseDown`** du wrapper du menu. Raison : Radix DismissableLayer écoute `pointerdown` en bubble phase sur `document` pour détecter "clic outside". Si le menu est portaled dans body (hors subtree de DialogContent), le clic sur une option remonte jusqu'à document et Radix le voit "outside" → ferme le dialog. stopPropagation sur le menu coupe le bubble avant qu'il n'atteigne document.

  **Sub-règle max-height** : la max-height d'un menu portaled = `window.innerHeight - buttonRect.bottom - bottomMargin - 4` (avec `bottomMargin = window.innerHeight * 0.1` = 10% margin du bas du viewport, per design system). Plancher `Math.max(_, 120)` pour rester utilisable quand le button est très bas. Le bas du menu ne descend jamais en-dessous de 90% du viewport. Pas de fallback "open above" pour l'instant — ajouter `if spaceBelow < threshold && spaceAbove > spaceBelow → position bottom au lieu de top` si futur user report.

  **Sub-règle click-outside avec portal** : 2 refs nécessaires (`buttonRef` + `menuRef`) car le menu est dans un subtree DOM séparé. Le handler `document.addEventListener('mousedown', ...)` doit vérifier `.contains(target)` sur les DEUX refs avant de close. Sans ça, cliquer dans le menu portaled est considéré "outside du button container" et ferme le menu juste avant l'onClick de l'option.

  **Sub-règle pointer-events: auto OBLIGATOIRE** (Sprint Fix-Dropdown-PointerEvents-Auto 2026-05-21) : tout élément portaled dans `document.body` pendant qu'un Radix Dialog est ouvert DOIT déclarer explicitement `style={{ pointerEvents: 'auto' }}` (ou la classe Tailwind `pointer-events-auto`) sur son wrapper racine. Raison vérifiée dans le source `@radix-ui/react-dismissable-layer/dist/index.mjs:72-73` : à l'ouverture d'un layer, Radix exécute `ownerDocument.body.style.pointerEvents = "none"` (line 73), puis ré-active explicitement `pointerEvents: "auto"` sur sa propre DialogContent (line 104). Tout autre portail enfant de body hérite **effectivement** du `pointer-events: none` malgré la spec CSS qui dit le contraire (comportement Chromium/WebKit observé : les clicks traversent le portail vers le DialogContent qui est au-dessous, l'option button du dropdown n'est jamais hit-target). Symptôme : utilisateur voit le menu, click sur une option, mais le `target` du `mousedown` au niveau document est l'`INPUT` du form en-dessous, pas le `BUTTON` de l'option. Diagnostic via instrumentation `console.warn` éphémère sur le handler click-outside. Fix : ajouter `pointerEvents: 'auto'` dans le `style` inline du wrapper portaled (`createPortal(<div ref={menuRef} style={{ position: 'fixed', pointerEvents: 'auto', ...menuPos }}>...</div>, document.body)`). Pattern miroir applicable à tout futur popover/tooltip/menu/sub-modal portaled. Précédent : Sprint Modal-Dropdown-Portal 2026-05-21 a installé le portal+stopPropagation mais a oublié `pointer-events: auto` → bug user-visible 1 jour plus tard signalé "impossible de sélectionner un budget dans la modal d'ajout de dépense". Le `onPointerDown + e.preventDefault()` sur l'option button (commit `e597ea8`) ne suffisait pas car les events n'atteignaient même pas le button. Référence : [components/ui/CustomDropdown.tsx:248-258](../../components/ui/CustomDropdown.tsx).

- ❌ **NE PAS** utiliser `window.location.href = '/<route>'` pour la nav SPA entre 2 pages soeurs partageant un layout (Sprint Fix-Dashboards-Navbar-Switch 2026-05-20). Le hard reload détruit le QueryClient cache, perd le state React, re-mount tout le layout (header + footer + drawer disparaissent puis re-rendent), re-déclenche le `proxy.ts checkRecapStatus` (2 SELECTs Supabase = 200-500ms). Pattern correct : `useRouter().push('/route')` (next/navigation) pour soft navigation. Pour les redirections programmatiques post-effect (ex: redirect si !profile.group_id), `router.replace()` + `useRef(false)` guard pour empêcher double-fire :

  ```tsx
  const router = useRouter()
  const redirected = useRef(false)
  useEffect(() => {
    if (!isLoading && profile && !profile.group_id && !redirected.current) {
      redirected.current = true
      router.replace('/dashboard')
    }
  }, [isLoading, profile, router])
  ```

  Précédent : `app/dashboard/page.tsx:289` + `app/group-dashboard/page.tsx:86+198` faisaient 3× `window.location.href` → symptômes user "1 fois sur 2 ne marche pas, reload total, lent". Fix Sprint Fix-Dashboards-Navbar-Switch : extraction `<BottomNav>` partagé avec `router.push()` + soft nav via route group `app/(dashboards)/`. Le seul usage `window.location.href` toléré reste les redirections explicites (logout, OAuth callback) où le full reload est désiré pour vider le state.

- ❌ **NE PAS** retourner `if (isLoading) return <div className="flex min-h-screen items-center justify-center ...">...</div>` depuis une page enfant d'un layout (Sprint Fix-Dashboards-Navbar-Switch 2026-05-20). Ce return remplace **tout** le content rendu par le layout — le header + le footer + le drawer **disparaissent** pendant le loading, ce qui rompt la persistence du chrome et fait flasher l'UI (root cause "loader plein écran qui masque la navbar" du sprint). Pattern correct : retourner `<CentralLoader message="..." />` ([components/ui/CentralLoader.tsx](../../components/ui/CentralLoader.tsx)) qui est `flex flex-1 items-center justify-center` (PAS `min-h-screen`, PAS `fixed inset-0`). Le loader s'inscrit naturellement dans le `<main>` du layout parent (qui est `flex-1`), et le header/footer du layout restent rendus. Triple cascade évitée : (a) loader plein écran masque chrome, (b) hard reload détruit chrome aussi, (c) re-mount cascade lente.

- ❌ **NE PAS** dupliquer navbar/header/footer/drawer dans chaque page d'un set de pages soeurs partageant la même chrome — utiliser un **layout partagé via route group** Next.js App Router (Sprint Fix-Dashboards-Navbar-Switch 2026-05-20). Convention : dossier `(<group-name>)` au sein de `app/` n'affecte PAS les URLs publiques mais permet à Next.js de réutiliser le layout entre les navigations soeurs **sans re-mount** du header/footer/drawer. Précédent : `app/dashboard/page.tsx` (381 LOC, incluait `<nav>` + `<footer>` + `<SettingsDrawer>` + `<AddTransactionModal>` + state `isMenuOpen` + state `isAddTransactionModalOpen`) et `app/group-dashboard/page.tsx` (281 LOC, miroir avec navbar/footer dupliqués). Fix : `git mv app/dashboard app/(dashboards)/dashboard` + `git mv app/group-dashboard app/(dashboards)/group-dashboard` + `app/(dashboards)/layout.tsx` qui owne le chrome. Pages allégées à 143 + 124 LOC (juste main content + page-scoped state). Le `context: 'profile' | 'group'` est déduit dans le layout via `usePathname()`. Hooks (useProfile, useBankBalance, useFinancialData) dédupés par TanStack Query entre layout et pages — pas de double fetch.

- ❌ **NE PAS** caller `setState` dans un `useEffect` synchronisé sur un changement de `pathname` (ou autre router state) — l'ESLint rule `react-hooks/set-state-in-effect` (active sur ce repo) refuse le pattern. Utiliser le pattern **"adjust state during render"** (React 19, [react.dev/learn/you-might-not-need-an-effect](https://react.dev/learn/you-might-not-need-an-effect)) :

  ```tsx
  const pathname = usePathname()
  const [prevPathname, setPrevPathname] = useState(pathname)
  if (pathname !== prevPathname) {
    setPrevPathname(pathname)
    setIsModalOpen(false) // ou autre reset état router-dépendant
  }
  ```

  Le check `pathname !== prevPathname` garantit la terminaison de la boucle (1 re-render). Pattern miroir `SettingsDrawer.tsx:38-41` (`hasBeenOpened` flag). Précédent Sprint Fix-Dashboards-Navbar-Switch 2026-05-20 : `useEffect(() => setIsAddTransactionModalOpen(false), [pathname])` dans `app/(dashboards)/layout.tsx` rejeté par lint baseline, migré vers le pattern adjust-during-render.

- ❌ **NE PAS** rappeler `checkRecapStatus()` dans `proxy.ts` à chaque navigation protégée sans cookie cache — la fonction fait 2 SELECTs Supabase synchrones (profiles + monthly_recaps) coûtant 200-500ms par nav (Sprint Fix-Dashboards-Navbar-Switch 2026-05-20). Pattern : cookie `recap-ok-{context}-{month}-{year}` TTL 5min, posé uniquement quand `status.required === false`, scope `httpOnly + sameSite:'lax' + path:'/'`. Le `month-year` dans la clé garantit l'invalidation naturelle au changement de mois (le 1er du nouveau mois, le cookie absent re-déclenche le check, qui voit qu'il manque le recap → redirect /monthly-recap comme avant). Le cas `required:true` ne pose PAS le cookie — sinon l'user redirigé vers /monthly-recap puis cliquant "Personnel" serait coincé sans re-check. Référence : [proxy.ts:70-99](../../proxy.ts).

- ❌ **NE PAS** consommer un hook TanStack Query en n'écoutant que `isLoading` — `isLoading` est `true` uniquement pendant le **premier fetch sans cache**, mais reste `false` pendant tout **refetch background** (post-mutation, post-`invalidateQueries`, post-switch de contexte). Le composant continue d'afficher les données précédentes pendant 200-500ms sans aucun indicateur visuel → l'utilisateur croit que rien ne se passe. **Pattern correct** (Sprint Skeleton-Refetch-Loaders 2026-05-21) : (a) le hook expose `isFetching` au return en plus de `isLoading` (déstructuration `useQuery`) ; (b) le composant consume `isLoading || isFetching` ; (c) la valeur affichée est remplacée par un `<Skeleton>` ([components/ui/skeleton.tsx](../../components/ui/skeleton.tsx)) aux mêmes dimensions visuelles (`h-{N} w-{N}` matchant le texte/montant). Cards conservent leur background color (la dernière valeur connue dicte vert/rouge), seul le chiffre devient skeleton. Pour les listes : remplacer la liste entière par 3-5 `<Skeleton>` rows aux mêmes hauteurs (`h-14` pour une transaction row, `h-16` pour une budget row). Choix "skeleton remplace" (vs "dim + spinner top-right" ou "badge Actualisation...") fait pour minimiser l'ambiguïté UX : skeleton = "ces données ne sont pas à jour, attends". 11 hooks fetcher étendus (`useFinancialData`, `useProfile`, `useGroupContributions`, `useBudgets`, `useIncomes`, `useRealExpenses`, `useRealIncomes`, `useBudgetProgress`, `useIncomeProgress`, `useGroups`, + SavingsDistributionDrawer local `useQuery`), 10+ consumers migrés. Le pattern hook ressemble à : `return { data, loading: isLoading, isFetching, ...mutations }` (backward-compat `loading` préservé). `useBankBalance` et `useGroupMembers` sont legacy `useState` (sprint dédié pour migrer TanStack) → leur `isLoading` actuel couvre déjà initial + post-action (pas de distinction technique nécessaire).

- ❌ **NE PAS** afficher un bouton submit `disabled={isSubmitting}` avec juste un texte qui change ("Création..." / "Transfert..." / "Enregistrement...") sans **spinner inline** (Sprint Skeleton-Refetch-Loaders 2026-05-21). Pattern hétérogène hérité : EditBudgetDialog / EditTransactionModal avaient déjà `<svg animate-spin>` inline ; CreateGroupForm / ProfileSettingsCard / GroupManagementPanel.{join,leave,search} / MonthlyRecapStep1+Step2 avaient juste un disabled+texte. **Pattern correct** : `<InlineSpinner>` ([components/ui/InlineSpinner.tsx](../../components/ui/InlineSpinner.tsx)) wrapper sur `lucide-react`'s `Loader2` avec 2 variants `sm` (h-4 w-4 = défaut bouton) / `md` (h-5 w-5). Code typique : `{isSubmitting && <InlineSpinner className="mr-1.5" />}{isSubmitting ? 'Création...' : 'Créer'}`. Ne pas hardcoder `<Loader2 className="...animate-spin..." />` inline (verbose + churn si l'utilité primitive évolue). Bénéfice : 1 seul vocabulaire de "bouton en cours de soumission" sur tous les boutons d'action async de l'app.

- ❌ **NE PAS** afficher un `<CentralLoader>` global pour le `financialLoading` initial d'un dashboard (`if (financialLoading) return <CentralLoader ...>`) — le `<FinancialIndicators>` gère son propre skeleton interne via la prop `isFetching` (Sprint Skeleton-Refetch-Loaders 2026-05-21). Le CentralLoader plein écran remplace tout le content du `<main>` → l'utilisateur perd la structure visuelle (les cards RAV/balance/savings disparaissent puis re-apparaissent). Pattern correct : laisser passer le rendering, le composant `<FinancialIndicators isFetching={financialFetching}>` rend ses 3 cards avec skeleton sur les chiffres pendant le fetch. Le CentralLoader reste pertinent pour le `if (isLoading)` du profile (premier mount sans aucune structure à montrer) et pour les redirections transitoires (`if (!profile?.group_id)` avant `router.replace`). Cas vu : Sprint Fix-Dashboards-Navbar-Switch avait déjà retiré le `min-h-screen` du CentralLoader pour préserver header/footer ; ce sprint pousse plus loin en retirant l'usage global pour le RAV.

- ❌ **NE PAS** afficher une modal de confirmation de suppression d'une transaction financière (dépense, revenu) avec un message générique "Êtes-vous sûr ?" — utiliser la prop `details?: ReactNode` de `<ConfirmationDialog>` pour rendre un **breakdown 3-col par source impactée** : `[label truncate flex-1] [montant font-semibold color] [→ new balance text-xs text-gray-500]` (Sprint Enrich-Delete-Confirmation 2026-05-21, sur [TransactionListItem.tsx](../../components/dashboard/TransactionListItem.tsx)). Composition selon le type :
  - **Expense budgétée** : 1-4 lignes selon ce qui est > 0 : `Budget « {name} » {budgetPortion}€ → {newAvailable}/{estimated}` (blue-600) / `Économies « {name} » {amount_from_budget_savings}€ → {newSavings}€` (emerald-600) / `Tirelire {amount_from_piggy_bank}€ → {newPiggy}€` (purple-600) / `Reste à vivre {ravRecovered}€ → {newRav}€` (emerald-600). Le `budgetPortion = amount_from_budget - ravRecovered` et `ravRecovered = max(0, spentBefore-est) - max(0, spentAfter-est)` (diff deficit) — split la valeur stockée entre la portion qui revient au budget pool et la portion qui rétablit le RAV.
  - **Expense exceptionnelle** : 1 ligne `Reste à vivre {amount}€ → {currentRav + amount}€` (emerald-600).
  - **Income exceptionnel** : 1 ligne `Reste à vivre -{amount}€ → {currentRav - amount}€` (red-600), title `Cette suppression diminue :`.
  - **Income régulier avec contexte** : 1 ligne avec delta `max(cumulReal-amount, est) - max(cumulReal, est)` (≤0) ; si delta=0 affiche "ne sera pas affecté".

  Le label de la source SAVINGS doit **toujours inclure le nom du budget** (`Économies « {budgetName} »`) — pas juste "Économies" générique — pour clarifier d'où vient/où retourne l'argent. Le suffixe `→ new balance` rassure l'utilisateur sur l'état post-action avant qu'il ne confirme ; pattern miroir des "Nouveau" lines de `RemainingToLivePreview`. Quand le contexte parent n'est pas fourni (`budgetSnapshot === null` car budget supprimé, ou `currentRemainingToLive === null` car loading), fallback gracieux SANS le suffixe `→` (la breakdown reste utile, juste sans le nouveau solde). Helper `formatAmountCompact(amount)` strip `,00` pour whole-euros (regex `/[,.]\d{2}(\s*€)/, '$1'` robuste aux variations locale ICU jsdom). Parent doit câbler 3 hooks `useFinancialData(context)` + `useBudgets(context)` + `useProgressData(context, period)` (tous déjà cachés ailleurs sur dashboard via TanStack Query, dedupe gratuit) puis pass `currentRemainingToLive` + `budgetSnapshot: {cumulatedSavings, estimatedAmount, spentAmount}` par row. Ne pas inliner les fetch dans le row component (anti-pattern N+1 fetches dans une liste).

- ❌ **NE PAS** mettre de couleur sur les chiffres dans la section "Après opération" de `<ExpenseBreakdownPreview>` (Sprint Expense-Preview-Posé-Layout 2026-05-21) — toutes les balances de cette section sont en `text-gray-900` (noir neutre). La couleur sur les MONTANTS est **réservée à la section "Impact de la dépense"** : rouge (`text-red-600`) avec `-` natif Intl pour les débits, vert (`text-green-600`) avec `+` explicite préfixé pour les crédits/refunds. Pas de rouge-sur-overflow sur le Budget recap — le ratio `dépensé/estimé` communique le dépassement directement (`250€/200€` parle de lui-même). Les LABELS gardent leur couleur d'entité (Budget=orange, Économies=violet, Tirelire=pink, RAV=bleu) dans LES DEUX sections, mais seuls les chiffres de l'Impact sont colorés par signe. UX user explicite : "je ne veux des couleurs sur les chiffres que dans la section impact". Référence : [components/dashboard/ExpenseBreakdownPreview.tsx](../../components/dashboard/ExpenseBreakdownPreview.tsx) helpers `signedAmountForImpact` (impact) vs `BalanceRow`/`BudgetRecapRow` (recap = noir).

- ❌ **NE PAS** rendre `<ExpenseBreakdownPreview>` en mode EDIT quand le montant n'a pas changé (Sprint Expense-Preview-Posé-Layout 2026-05-21) — pas de fetch API, pas de skeleton, pas de bandeau. Gate dans le parent `EditTransactionModal` : `Math.round(previewSafe * 100) !== Math.round(transaction.amount * 100)` (comparaison en cents pour éviter les imprécisions flottantes). Le composant n'étant pas monté, `useQuery` ne fire pas → 0 latence + 0 coût réseau à l'ouverture, ré-apparait dès la 1ère frappe sur le champ montant. UX user "je veux ne pas charger/calculer le recap si je ne touche à rien". Pattern à généraliser pour tout futur aperçu d'opération à effet de bord en mode édition : `if (newValue !== existingValue) <Preview ...>`. En mode ADD (pas d'existing), la preview s'affiche toujours dès `amount > 0`. Référence : [components/dashboard/EditTransactionModal.tsx:418](../../components/dashboard/EditTransactionModal.tsx).

- ❌ **NE PAS** réintroduire un toggle UI « Utiliser les économies » (ou équivalent) dans `<AddTransactionModal>` (Sprint Auto-Use-Savings 2026-05-21). Le `use_savings: true` est envoyé par défaut à l'API + `useSavingsToggle: true` aux helpers `calculateBreakdown` / `useRavValidation` / `<ExpenseBreakdownPreview>`. UX user explicite : "j'aimerais enlever la possibilité de choisir si on veut utiliser des économies dans l'ajout de dépenses. Je veux que ça soit automatiquement fait par défaut". L'algo P5 (savings first, budget second avec cascade overflow) est désormais le **seul** mode disponible côté UI add. Le paramètre `useSavingsToggle` reste exposé côté `calculateBreakdown` pour compatibilité ascendante (Phase 2 cross-budget cascade pourrait l'utiliser) mais aucun consumer UI n'opt-out à ce jour. Référence : [components/dashboard/AddTransactionModal.tsx:91-95](../../components/dashboard/AddTransactionModal.tsx).

- ❌ **NE PAS** rendre la ligne Budget de la section IMPACT sur un montant absolu `-new.from_budget` (Sprint Impact-Lines-Polish 2026-05-21). La ligne Budget impact doit être **delta-based sign-flipped** : `amount = -(newBudgetPoolUsage - existingBudgetPoolUsage)` où `newBudgetPoolUsage = min(budget_spent_after, estimated)` et `existingBudgetPoolUsage = min(budget_spent_before, estimated)`. Si delta = 0 (pool unchanged), masquer la ligne entièrement. Cela permet d'afficher `+X €` vert quand le user réduit une dépense (refund vers le pool budget) au lieu d'afficher uniquement `-X €` sur la nouvelle valeur absolue. Pattern miroir pour la **nouvelle ligne Reste à vivre** (toujours absente avant ce sprint) : `amount = -ravDelta` où `ravDelta = newOverflow - currentOverflow`. Tirelire/Économies restent en valeur absolue (`-new.from_X` red, masquées si 0) — asymétrie intentionnelle entre "ressources tirées" (savings/piggy) et "destinations impactées" (budget/RAV). Référence : [components/dashboard/ExpenseBreakdownPreview.tsx](../../components/dashboard/ExpenseBreakdownPreview.tsx) variables `budgetPoolDelta` + `ravDelta`.

- ❌ **NE PAS** réintroduire le breakdown 3-col historique (label / montant coloré / `→ new balance text-xs text-gray-500`) dans la modal de confirmation suppression `<TransactionListItem>` (Sprint Delete-Confirmation-Recap-Reuse 2026-05-21, raffiné Recap-Compact-And-Uniform 2026-05-22). La modal doit utiliser le panel partagé `<AfterOperationPanel compact>` de [components/dashboard/recap-rows.tsx](../../components/dashboard/recap-rows.tsx) — `compact={true}` est obligatoire dans ce contexte (header "Après opération" masqué pour rester discret puisque le dialog a déjà un title + message qui rendent le header redondant). Le `text-left` sur la root du panel coupe l'héritage `text-center` propagé par `<ConfirmationDialog>` (le wrapper `<div className="p-6 text-center sm:p-6">` centrait tout son subtree, y compris les labels du panel). Pattern : `if (budgetSnapshot) <AfterOperationPanel compact>...lignes Tirelire/Économies/Budget/RAV post-delete...</AfterOperationPanel>`. Si `budgetSnapshot` non fourni (budget supprimé en cascade), retourner `undefined` → ConfirmationDialog rend message standard sans détails. UX user explicite : "au moment de la suppression, au lieu de ligne explicative, on pourrait reprendre l'encart 'après opération' tel quel" + (2026-05-22) "fais en sorte que l'encart soit un peu plus discret et petit, et aligne le text verticalement à gauche". Les balances sont en noir (`text-gray-900`, pas de couleur green/red selon signe) — couleur réservée aux LABELS d'entité (Budget=orange, Économies=violet, Tirelire=pink, RAV=blue). Référence : [components/dashboard/TransactionListItem.tsx::buildExpenseDeleteDetails](../../components/dashboard/TransactionListItem.tsx).

- ❌ **NE PAS** réintroduire les thèmes colorés sémantiques (orange/vert/rouge) sur les containers des recap panels des modals `AddBudgetDialog` / `EditBudgetDialog` / `AddIncomeDialog` / `EditIncomeDialog` (Sprint Recap-Compact-And-Uniform 2026-05-22). Tous les 4 modals doivent utiliser le panel bleu uniforme `rounded-lg border-blue-200 bg-blue-50/50 p-4 space-y-3`, en miroir d'`<ExpenseBreakdownPreview>`. Structure interne : (1) header texte sémantique (`<p className="text-sm font-medium text-gray-700">Calcul de la balance :</p>` ou similaire) ; (2) section input rows en `<div className="space-y-1 text-sm">` avec lignes `<div className="flex items-baseline justify-between gap-2"><span className="text-gray-700">{label}</span><span className="shrink-0 font-semibold text-gray-900">{amount}</span></div>` ; (3) divider `<div className="flex items-center gap-2 pt-1"><div className="h-px flex-1 bg-blue-200" /><span className="text-xs font-medium tracking-wide text-gray-500 uppercase">Résultat</span><div className="h-px flex-1 bg-blue-200" /></div>` ; (4) ligne result avec couleur sémantique UNIQUEMENT sur le chiffre (red-600 si négatif ou overflow, gray-900 sinon). UX user : "Uniformise les styles des encarts pour les modals d'ajout de budget, de revenus etc... Partout où il y a un recap j'aimerais que tu uniformises le style avec ce qu'on a fait pour les ajout/modification de dépense." Les anciennes signalétiques (orange-50/orange-200 thème budget, green-50/green-200 thème revenu, red-50/red-200 thème overflow) sont droppées au profit d'une signalétique localisée sur la ligne result.

- ❌ **NE PAS** rendre les `details` d'un `<ConfirmationDialog>` de suppression sans un header explicatif **"Après suppression :"** (ou équivalent contextuel) au-dessus du contenu (Sprint Delete-Header-And-Income-Polish 2026-05-22). Le user voyait l'encart mais ne savait pas ce qu'il représentait — UX user : "j'aimerais qu'il soit marqué quelque part (je pense en haut de l'encart) un truc disant que c'est ce à quoi ça va ressembler". Pattern : wrapper `<div className="space-y-1.5 text-left"><p className="text-sm font-medium text-gray-700">Après suppression :</p>{innerContent}</div>` où innerContent est soit un `<AfterOperationPanel compact>` (TransactionListItem), soit une phrase texte (PlanningDrawer budget savings transfer ou estimated income recap). Référence : [components/dashboard/TransactionListItem.tsx::buildDeleteDetails](../../components/dashboard/TransactionListItem.tsx) + [components/dashboard/PlanningDrawer.tsx::ConfirmationDialog](../../components/dashboard/PlanningDrawer.tsx).

- ❌ **NE PAS** réintroduire le `sourceLine` "Revenu lié à **'xxx'**." dans la modal de confirmation suppression d'un revenu (TransactionListItem::buildIncomeDeleteDetails) — Sprint Delete-Header-And-Income-Polish 2026-05-22. La ligne ajoutait du bruit visuel sans valeur (le nom du revenu apparaît déjà dans le message principal de la dialog `"Êtes-vous sûr de vouloir supprimer "X" ..."`. UX user : "supprime la phrase 'Revenu lié à 'xxx'. Sois plus concis. Utilise un code couleur si tu parles du reste à vivre". Pour les phrases fallback (text-only, pas de panel) : color "reste à vivre" inline via `<span className="font-medium text-blue-600">reste à vivre</span>` — cohérent avec le `text-blue-600` de l'EntityLabel `<EntityLabel type="rav" />` du panel.

- ❌ **NE PAS** rendre `<EditTransactionModal>` sans aperçu post-action pour les revenus (réguliers OU exceptionnels) ni pour les dépenses exceptionnelles (Sprint Delete-Header-And-Income-Polish 2026-05-22). Avant ce sprint, seules les dépenses budgétées avaient un aperçu (`<ExpenseBreakdownPreview>`). Maintenant : pour `transactionType === 'income'` ou `(transactionType === 'expense' && isOriginallyExceptional)`, rendre `<RemainingToLivePreview>` avec `existingAmount={transaction.amount}` (back-out de la transaction existante avant le compute). Gating identique à l'expense preview : `previewSafe > 0 && transaction && Math.round(previewSafe * 100) !== Math.round(transaction.amount * 100)`. Pattern à généraliser pour tout composant de preview qui supporte ADD et EDIT : prop optional `existingAmount?: number` (default 0) pour mode ADD invariant, set en EDIT pour back-out. UX user : "Je n'ai pas d'encart de recap quand je modifie un revenu ajouté (qu'il soit exceptionnel ou non), crée en un".

- ❌ **NE PAS** créer un `<form>` modal sans `onKeyDown={preventEnterSubmit}` ([lib/forms/](../../lib/forms/prevent-enter-submit.ts), Sprint Modal-Forms-Block-Enter-Submit 2026-05-21) — sinon Enter mobile submit le form HTML5. Passe-droit textarea/button/a.
