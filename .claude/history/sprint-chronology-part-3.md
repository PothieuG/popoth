# Précédents Sprint — chronologie résumée (Part 3)

> Suite de [sprint-chronology-part-2.md](sprint-chronology-part-2.md) (Part 2 proche du cap 39.5k au 2026-05-29 — la colonne "Pattern" paddée à ~1900 chars faisait qu'une ligne supplémentaire = +~2000 chars). Split chronologique préemptif (size-policy §7). **Fichier actif** pour logguer les sprints postérieurs au 2026-05-29.
>
> Append-only : 1 nouvelle ligne par sprint installant un pattern réutilisable. Granularité différente des `roadmap-detailed-NN-*.md` (verbatim closeouts). Ici : 1 row = 1 invariant/pattern actionable.

## Sprints installant un pattern réutilisable

| Sprint                          | Date       | Pattern installé                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Référence §11                                                                |
| ------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| Housekeeping-Deps-Format-Triage | 2026-05-29 | **Hygiène repo post-Part 38/39** (5 commits). (1) Glob lint-staged `*.{mjs,cjs,js}` ajouté → ferme le trou où les scripts Node dérivaient (seul `format:check` global les attrapait, jamais le pre-commit). (2) 4 vulns Dependabot **transitives** épinglées via `pnpm.overrides` : `fast-uri ^3.1.2` (2 CVE high), `@babel/plugin-transform-modules-systemjs ^7.29.4` (high), `ws@8 ^8.20.1` (moderate) — `pnpm audit` 0. (3) Fix assertion gated `financial-data-with-projects` case 2 : RAV groupe **inchangé** post-projet (PÉ-12 propage `monthly_allocation` dans `monthly_budget_estimate` → la contribution +50 annule le budget +50) au lieu de `−50`. (4) CLAUDE.md §9 réconcilié sur §5.5 (846/242). (5) `sprint-chronology-part-3.md` créé (split préemptif, part-2 saturée). Validé contre dev : gated `financial-data-with-projects` 4/4 + `add-exceptional-expense-with-piggy` 8/8.                                                                                                                                                                                                                                                                                                                                                                  | CLAUDE.md §6                                                                 |
| Security-RLS-Monthly-Recaps     | 2026-06-03 | **RLS sur table server-only + cause racine event-trigger + guard `db:check-rls`.** Finding linter Supabase `rls_disabled_in_public` (EXTERNAL/ERROR) : `monthly_recaps` seule table publique sans RLS, grants DML `anon`/`authenticated` → lecture/écriture de tous les récaps via PostgREST avec l'anon key publique. Fix : `ENABLE ROW LEVEL SECURITY` **sans policy** (table 100 % service_role, deny-all anon correct ; policies owner-scoped requises seulement si lue par le client browser, cf. D1 `piggy_bank`). Cause racine : event trigger `ensure_rls`→`rls_auto_enable` (auto-enable RLS au `CREATE TABLE`) **absent en prod**, présent en dev → restauré (`20260609000001`). Guard `scripts/check-rls.mjs` câblé dans `verify` (asserte « 0 table publique sans RLS », là où `db:check-drift` est aveugle si la table manque de RLS dans prod ET baseline). Event triggers invisibles à `export-schema.mjs` (`pg_event_trigger` ≠ `pg_trigger`). Validé end-to-end via probe jetable sur dev.                                                                                                                                                                                                                                                         | CLAUDE.md §7 / [Part 40](roadmap-detailed-40-security-rls-monthly-recaps.md) |
| Perf-Group-Members-Rav-Lazy     | 2026-09-10 | **Sortir un calcul par membre du chemin critique plutôt que l'approximer.** Le dashboard groupe était visiblement plus lent que le perso : §13 de `_loadFinancialData` appelait `getProfileFinancialData(membre)` pour chaque membre (9 lectures + 1 écriture chacun) afin d'hydrater `meta.groupMembersRav`. Mesuré au mock du plan de requêtes (30 ms/RTT simulés) : perso 9 req / 2 RTT, groupe 2 membres 27 req / 4 RTT, groupe 5 membres 54 req / 4 RTT. Or ce champ n'alimente que l'encart « RAV actuel → projeté » des modals du `PlanningDrawer` — invisible au premier rendu. Fix : `loadGroupMembersRav` + `GET /api/finance/group-members-rav`, fetché par le drawer avec `enabled: isOpen && context === 'group'` (le `enabled` EST le gain : le drawer est monté fermé dès le premier rendu). Formule strictement inchangée — la simplifiée `salaire − budgets − contribution` reste proscrite (dérive, Group-RAV-Recap 2026-05-27). Après : groupe = perso = 9 req / 2 RTT quel que soit le nombre de membres. Aussi : `useFinancialData()` nu sur `/dashboard` → `'profile'` (queryKey `null` ≠ `'profile'` ⇒ 2 `GET /summary` par chargement) ; `meta.groupMembersPersonalRavTotal` supprimé (0 consommateur, Path B).                             | CLAUDE.md §8 / [Part 42](roadmap-detailed-42-perf-group-dashboard.md)        |
| Perf-Toggle-Targeted-Refresh    | 2026-09-10 | **Une mutation n'invalide que ce qu'elle change ; un drawer ne force jamais un `refetch()` à l'ouverture.** Retour user après les 3 passes Part 42 : « valider une dépense (appui long) est long, les budgets se rechargent ». Le code du chemin groupe est symétrique du perso (vérifié serveur / DB / proxy / client) ; ce qui était lent, c'est l'APRÈS-geste, dans les 2 contextes : `onSettled` des 4 toggles appelait `invalidateFinancialRefreshes` (11 keys → ~12 appels d'API, liste refetchée → skeleton) pour un geste qui ne change que `bank_balances.balance`. Fix : `applyBankBalanceToCache` / `applyContributionPairToCache` écrivent le solde renvoyé par la RPC dans `['bank-balance']` + `['financial-summary']`, 0 refetch nominal, `invalidateBalanceViews` (2 keys) en 409/erreur. `PlanningDrawer` / `SavingsDistributionDrawer` : plus de `refetch()` forcé à l'ouverture (5 relances, dont les 2 listes de transactions). `useGroupMembers` → TanStack (cache entre bascules). `GET /api/groups{,/contributions}` : 2 lectures série → parallèle. `scripts/perf-probe.mjs` : première mesure en millisecondes (médiane/max par endpoint, vague parallèle, région Vercel). Pin : « exactement 1 fetch sur le chemin nominal d'un toggle ». | CLAUDE.md §8 / [Part 42 §10](roadmap-detailed-42-perf-group-dashboard.md)    |
| Fix-Avatar-Payload              | 2026-09-11 | **Une plainte de perf commence par une mesure réelle (HAR / `perf-probe.mjs`), pas par le code.** HAR prod de la bascule Groupe : 11 appels < 400 ms en `fra1`, et `GET /api/finance/expenses/real?group=true` en **500 après 14,7 s** (retry → ~30 s de skeleton). Cause : `AvatarUpload` stockait la photo brute en base64 dans `profiles.avatar_url` (3,7 Mo pour un membre) et les 8 JOIN `created_by(…, avatar_url)` des listes la répétaient dans chaque ligne. Fix : JOIN sans `avatar_url` (avatar résolu client via `useGroupMembers` → prop `creatorAvatarUrl`), redimensionnement client 256 px (`lib/avatar-image.ts`), borne Zod `AVATAR_URL_MAX_CHARS`, `scripts/shrink-avatars.mjs` pour l'existant. Le code était symétrique perso/groupe ; la donnée non.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | CLAUDE.md §8 / [Part 42 §11](roadmap-detailed-42-perf-group-dashboard.md)    |

Pour la chronologie complète des sprints, voir CLAUDE.md §11 (index des parts `.claude/history/roadmap-detailed-NN-*.md`) + [Part 1](sprint-chronology.md) et [Part 2](sprint-chronology-part-2.md).
