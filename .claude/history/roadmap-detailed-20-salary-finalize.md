# Roadmap détaillé — Part 20 : Endpoints-Salary-Update-And-Finalize-V3

> Chronologie des sprints livrés à partir de 2026-05-26 (suite de [roadmap-detailed-19-endpoints-start-status.md](roadmap-detailed-19-endpoints-start-status.md)). Split préemptif pour rester sous le cap 39.5k chars/fichier — la Part 19 plafonnait à ~37.3k post-sprint 07 négatif, ajouter sprint 08 verbatim aurait franchi la limite.

## Sprints

- ✅ **Sprint Endpoints-Salary-Update-And-Finalize-V3** (sprint 08/17 Monthly Recap V3, livré 2026-05-26). 2 endpoints (`update-salaries` écran 4 + `complete` écran 5) + 2 helpers orchestrateurs (`actions-salary.ts` + `actions-finalize.ts`) + 2 nouvelles RPCs PG composites + 11 unit tests + 18 tests gated intégration. Ferme la boucle end-to-end du wizard recap : après les actions positives/négatives, le user met à jour les salaires puis "Retourne au dashboard" → snapshot appliqué aux budgets carryover + transactions validées DELETE + non-validées flaggées carried-over + recap marquée completed.

  **Architecture installée** :

  **(1) Migration `20260526000000_create_recap_finalize_rpcs.sql`** (2 RPCs, ~200 LOC SQL, SECURITY DEFINER + SET search_path = public + REVOKE PUBLIC + GRANT service_role + NOTIFY pgrst en bas) :
  - **`finalize_recap_apply_snapshot(p_recap_id uuid, p_snapshot jsonb) RETURNS json`** : itère `jsonb_each_text(p_snapshot)`, pour chaque pair (budget_id, amount) `UPDATE estimated_budgets SET carryover_spent_amount = COALESCE(.,0) + amount::numeric, carryover_applied_date = now() WHERE id = budget_id::uuid`. Collecte chaque `FOUND` dans un JSONB array. Retourne `{ applied: [{ budget_id, amount }] }`. Snapshot vide ou null → `{ applied: [] }`. Budget id inexistant → silent no-op (UPDATE matches 0 rows). Premier consumer applicatif du couple `estimated_budgets.carryover_spent_amount` + `carryover_applied_date` (les colonnes existaient dans `lib/database.types.ts` sans aucune lecture/écriture côté TS jusqu'à ce sprint).

  - **`process_recap_transactions(p_recap_id uuid, p_profile_id uuid DEFAULT NULL, p_group_id uuid DEFAULT NULL) RETURNS json`** : guard `p_profile_id XOR p_group_id` (RAISE EXCEPTION si les deux NULL ou les deux non-NULL). 4 CTEs séquentielles dans le même statement-block :
    - `WITH deleted AS (DELETE FROM real_expenses WHERE applied_to_balance_at IS NOT NULL AND is_carried_over = false AND (p_profile_id IS NULL OR profile_id = p_profile_id) AND (p_group_id IS NULL OR group_id = p_group_id) RETURNING 1) SELECT count(*) INTO v_deleted_expenses FROM deleted;`
    - Symétrique pour `real_income_entries` → `v_deleted_incomes`.
    - `WITH updated AS (UPDATE real_expenses SET is_carried_over = true, carried_from_recap_id = p_recap_id WHERE applied_to_balance_at IS NULL AND is_carried_over = false AND (...context...) RETURNING 1) SELECT count(*) INTO v_carried_expenses FROM updated;`
    - Symétrique pour `real_income_entries` → `v_carried_incomes`.

    Retourne `{ deleted_expenses, deleted_incomes, carried_expenses, carried_incomes }`. Le filtre `is_carried_over = false` est **critique** : sans lui, les rows déjà carried-over depuis un recap antérieur seraient re-flaggées (overwrite de `carried_from_recap_id`) ou DELETE'd (si l'user les avait re-validées tardivement). Test gated #10 (`prior-carried-untouched`) verrouille ce comportement.

  - **`DEFAULT NULL` sur p_profile_id + p_group_id** : choix délibéré pour que le codegen Supabase marque les 2 params `?: string` côté TS (au lieu de `string` strict). Permet d'appeler `.rpc('process_recap_transactions', { p_recap_id, p_profile_id: profile.id })` sans devoir passer `p_group_id: null` (rejeté par le type). La logique SQL utilise `IS NULL OR equality` donc l'omission TS ↔ NULL PG fonctionne. Pattern miroir de `start_monthly_recap` (sprint 05).

  - EXPECTED_RPCS **14 → 16** dans [scripts/check-rpcs.mjs](../../scripts/check-rpcs.mjs). Functions DB versionnées **21 → 23** dans pg_proc (audit `pnpm db:audit-functions`).

  **(2) Helper `lib/recap/actions-salary.ts`** (~115 LOC) :
  - **`fetchGroupMemberIds(groupId): Promise<Set<string>>`** : SELECT `id` FROM profiles WHERE group_id = X, retourne un Set. Server-side authority — jamais faire confiance au body client pour la liste des profileIds (validation gate critique pour le cas group).

  - **`executeUpdateSalaries({ context, userId, profile, recap, salaries })`** : (a) valide la cible — profile-context exige exactly `salaries.length === 1` + `salaries[0].profileId === userId`, group-context exige que chaque profileId soit dans le Set retourné par fetchGroupMemberIds (sinon `RecapActionError('invalid_target', 400, { invalid: [...] })`). (b) Pour chaque (profileId, salary) → `UPDATE profiles SET salary WHERE id` sequential. (c) Si group → `supabaseServer.rpc('calculate_group_contributions', { group_id_param })` **fail-soft** (les salaires sont déjà UPDATEd ; le trigger sur prochaine mutation de budget resync de toute façon). (d) `UPDATE monthly_recaps SET current_step = 'final_recap' WHERE id`. Throw si UPDATE final échoue (recap stuck sinon). Retourne `{ updated: salaries.length, nextStep: 'final_recap', contributionsRecalculated: boolean }`.

  **(3) Helper `lib/recap/actions-finalize.ts`** (~150 LOC) :
  - **`executeCompleteRecap({ context, profile, recap })`** : orchestrateur 3 étapes.
    - **Étape 1 (fail-soft)** : si `budget_snapshot_data` non-empty (cohésion défensive : `coerceSnapshot(raw)` accepte uniquement les Record<string, number> bien formés, sinon null), appel `finalize_recap_apply_snapshot(p_recap_id, p_snapshot)`. Erreur loggée mais swallowed — outcome.snapshotApplied=null.
    - **Étape 2 (fail-soft)** : appel `process_recap_transactions(p_recap_id, p_profile_id?, p_group_id?)`. Erreur loggée + `transactions={deleted_expenses:0, deleted_incomes:0, carried_expenses:0, carried_incomes:0}`.
    - **Étape 3 (NOT fail-soft)** : `UPDATE monthly_recaps SET completed_at = now().toISOString(), current_step = 'completed' WHERE id`. Throw si erreur — le recap reste open et l'idempotency check du prochain POST `/complete` court-circuite tout duplicate work.

    Retourne `{ recapId, completed: true, snapshotApplied, transactions }`. Type `snapshotApplied: { applied: Array<{ budget_id, amount }> } | null` discrimine empty/erreur vs success non-vide.

  - **`coerceSnapshot(raw: Json): Record<string, number> | null`** : helper privé identique au pattern de `actions-negative.ts::coerceSnapshot`. Filtre les valeurs non-`number` du JSONB (défensive — un snapshot écrit hors-spec serait silently ignoré au lieu de crash).

  **(4) Route `POST /api/monthly-recap/update-salaries/route.ts`** (~70 LOC) : `withAuthAndProfile` + `parseBody(updateSalariesBodySchema)` (schema déjà déclaré sprint 03 : `{ context, salaries: array of { profileId, salary } min(1) }`). Validation chain canonique (héritée des sprints 06/07) : context group sans group_id (400 'Pas de groupe') → `getActiveRecap` null (404 'no_active_recap') → `started_by_profile_id !== userId` (403 'not_initiator') → `current_step ≠ 'salary_update'` (409 'invalid_step' + `currentStep` echoed). Delegate à `executeUpdateSalaries` (catch RecapActionError → status code + extras dans body).

  **(5) Route `POST /api/monthly-recap/complete/route.ts`** (~90 LOC) : `withAuthAndProfile` + `parseBody(completeRecapBodySchema)` (schema sprint 03 : `{ context }` seul). Validation chain canonique + **branche idempotency** :
  - `getActiveRecap` filtre `completed_at IS NULL` (cf. sprint 06 helper). Si null, query directe `monthly_recaps` avec `.match({ profile_id|group_id, recap_month, recap_year }).not('completed_at', 'is', null).maybeSingle()`. Si match → 200 `{ data: { alreadyCompleted: true, recap: { id, completed_at, current_step } } }` (replay safe). Sinon 404 'no_active_recap'.
  - `started_by_profile_id !== userId` → 403 'not_initiator'.
  - `current_step !== 'final_recap'` → 409 'invalid_step' + `currentStep`.
  - Sinon delegate à `executeCompleteRecap` → 200 `{ data: outcome }`.

  **(6) Régénération `lib/database.types.ts`** depuis le projet **dev** (`pnpm supabase gen types typescript --project-id ddehmjucyfgyppfkbddr --schema public`) après application de la migration. Encoding UTF-8 sans BOM (PowerShell `[System.IO.File]::WriteAllText(..., $content, (New-Object System.Text.UTF8Encoding $false))` parce que le `>` redirect natif PS écrit en UTF-16 LE et casserait `db:check-types-fresh`). Les nouveaux Functions sont `finalize_recap_apply_snapshot: { Args: { p_recap_id: string; p_snapshot: Json }; Returns: Json }` et `process_recap_transactions: { Args: { p_group_id?: string; p_profile_id?: string; p_recap_id: string }; Returns: Json }`.

  **Tests** :
  - **Unit (non-gated, ~11 cas)** :
    - `lib/recap/__tests__/actions-salary.test.ts` (6 cas) : mock chainable de `supabaseServer.from('profiles').{select,update}.eq(...)` + `from('monthly_recaps').update.eq(...)` + `rpc(...)`. Profile happy (1 update + step), profile salaries.length>1 (rejet RecapActionError), profile profileId mismatch (rejet), group happy 3 members (3 updates + RPC recalc + step + `contributionsRecalculated=true`), group 1 outsider (rejet avec `extras.invalid=[id]`, **aucune mutation** — validation gate avant UPDATEs), group recalc fail (fail-soft, `contributionsRecalculated=false`, no throw).
    - `lib/recap/__tests__/actions-finalize.test.ts` (5 cas) : empty snapshot (apply_snapshot NOT called, mais process_transactions + completed_at toujours appelés), non-empty snapshot (apply_snapshot puis process_transactions en chaîne), apply_snapshot RPC error (fail-soft → snapshotApplied=null + continue), process_transactions RPC error (fail-soft → zero counts + continue), final UPDATE completion fail (throws — NOT fail-soft).

  - **Intégration (gated `SUPABASE_RECAP_TESTS=1`, 18 cas)** :
    - `app/api/monthly-recap/update-salaries/__tests__/route.integration.test.ts` (8 cas) : seed 4 users (3 in groupA: Alice/Bob/Carol + 1 outside: Dave avec group_id=null), `mockedAuth` mutable + `vi.mock('@/lib/api/with-auth')` injecte profile via `{ userId, profile: { id, group_id, first_name, last_name } }`. Profile happy 200/updated=1/step='final_recap'/`profiles.salary` updated en DB, profile length=2 → 400, profile profileId mismatch → 400, group 3 members happy → 200/updated=3/contributionsRecalculated=true/`profiles.salary` × 3 updated, group 1 outsider (Dave) → 400 + `invalid: [daveId]` + Dave's salary unchanged (sanity assert), not_initiator (group started by userB) → 403, step='manage_bilan' → 409 + currentStep, body vide → 400.
    - `app/api/monthly-recap/complete/__tests__/route.integration.test.ts` (10 cas) : seed 2 users + 1 group. Happy profile (snapshot {b1:20, b2:30} + 3 applied expenses + 2 non-applied + 2 applied incomes + 1 non-applied) → 200/`transactions={3,2,2,1}`/budgets `carryover_spent_amount` updated/carried rows `is_carried_over=true` + `carried_from_recap_id=recap.id`/applied DELETE'd, idempotency replay → 200 + `alreadyCompleted=true`, no_active_recap → 404, not_initiator → 403, step='salary_update' → 409, empty snapshot → snapshotApplied=null + still processes/completes, no transactions → all-zero counts + still completes, happy group → uses group_id filter (verify profile-scoped expense NOT touched), prior-carried-over row (`is_carried_over=true` pré-seedé) → counted nowhere + untouched, snapshot avec budget id inexistant → applied list n'inclut pas la clé bogus (filtre FOUND silent).

  - **Mocking pattern important** : pour les unit tests, le `vi.mock('@/lib/supabase-server')` retourne un objet chainable où `.from(table)` switch sur `table` et retourne un sous-objet avec `update()` ou `select()` retournant un objet avec `.eq` qui est lui-même un `vi.fn()` resolvable. Les spies sont exposés via `__mocks: SupabaseMockState` pour assertion + override per-test (`mockResolvedValueOnce`).

  **Conventions / leçons** :
  - **Idempotency endpoint = check DB row, pas state variable** : `complete` ne peut pas se reposer sur un compteur "a-déjà-tourné" parce que `getActiveRecap` est filtré (`completed_at IS NULL`). La disambiguation se fait par une query directe avec `.not('completed_at', 'is', null).maybeSingle()`. Pattern reproductible pour toute future API qui veut idempotency : (a) read current state, (b) si "stale" — query la "done state" explicitement, (c) si "done" → respond idempotent, (d) sinon → respond 404. Pas de side-effect dans la disambiguation (juste read).

  - **Fail-soft différencié par étape** : `apply_snapshot` et `process_transactions` sont fail-soft parce que le user a déjà vu l'écran 5 et cliqué "Retourne au dashboard" — bloquer en 500 le forcerait à re-enter et re-cliquer, friction inacceptable pour un cas-edge transient (network glitch RPC). Mais le mark-completed (`UPDATE monthly_recaps.completed_at`) DOIT throw parce que sans lui, la session next-tick re-entrerait le wizard. Trade-off documenté dans le file-header de `actions-finalize.ts`.

  - **Migration DEFAULT NULL pour codegen Supabase ergonomique** : avant l'ajout de `DEFAULT NULL` sur `p_profile_id` + `p_group_id`, le codegen marquait `process_recap_transactions: { Args: { p_group_id: string; p_profile_id: string; p_recap_id: string } }` — tous required. Côté TS, passer `null` était un type error. Solution : ajouter `DEFAULT NULL` SQL → codegen TS devient `?: string` (optional). Le runtime PG accepte `undefined` côté JS (omis du JSON payload, applique DEFAULT NULL). Pattern miroir `start_monthly_recap` (sprint 05).

  - **Régénération types depuis dev, pas prod, durant le sprint** : la migration n'est appliquée à prod qu'au sprint closeout (workflow `db push --dry-run` → confirm → `db push` → re-export baseline). Pendant le sprint, les types doivent refléter dev (sinon les calls `.rpc('finalize_recap_apply_snapshot', ...)` seraient un type error). Override via `--project-id ddehmjucyfgyppfkbddr` sur la CLI Supabase (le script `pnpm db:types` est hardcodé prod). Au sprint closeout après prod push, re-régénérer depuis prod pour aligner.

  - **`real_expenses` + `real_income_entries` ont les MÊMES 3 colonnes carryover** (`is_carried_over` bool default false, `carried_from_recap_id` uuid FK monthly_recaps, `applied_to_balance_at` timestamptz). Toute logique recap (sprint 08 + futurs) traite les 2 tables en miroir — facile d'oublier les revenus (la spec dit "dépenses ET revenus"). Test gated #1 et #7 verrouillent ce traitement symétrique.

  - **Endpoints recap totaux : 9** (start, status, transfer-surpluses-to-piggy, transform-remaining-surpluses-to-savings, refloat-from-piggy, refloat-from-savings, save-budget-snapshot, update-salaries, complete). Tous via `withAuthAndProfile` + `parseBody` Zod + même catch (`handleBadRequest` → `RecapActionError` → 500). Pattern stable cross-sprint, **9 routes en 1 répertoire**.

  **Files livrés (~1300 LOC nets, hors régénération types)** :
  - **Nouveaux** (8) : `supabase/migrations/20260526000000_create_recap_finalize_rpcs.sql`, `lib/recap/actions-salary.ts`, `lib/recap/actions-finalize.ts`, `app/api/monthly-recap/update-salaries/route.ts` + `__tests__/route.integration.test.ts`, `app/api/monthly-recap/complete/route.ts` + `__tests__/route.integration.test.ts`, `lib/recap/__tests__/actions-salary.test.ts`, `lib/recap/__tests__/actions-finalize.test.ts`. (9 files)
  - **Modifiés** (3) : `scripts/check-rpcs.mjs` (EXPECTED_RPCS 14→16), `lib/database.types.ts` (régénération depuis dev — Functions block étendu), `CLAUDE.md` (§5 description Monthly Recap V3 + §5.5 invariants 4 lignes mises à jour + §9 tests gated).

- ✅ **Sprint Dev-Seed-Scripts-CLI** (sprint 09/17 Monthly Recap V3, livré 2026-05-23). 27 scripts CLI Node sous `scripts/seed-recap/` (1 = 1 scénario QA), 1 module shared helpers (`_lib.mjs` ~440 LOC + 15 helpers), 1 reset pur (`_reset.mjs`), 1 README workflow + catalogue. **Pivot majeur architecture** : abandon de la page in-app `/dev/recap` + 3 routes `/api/debug/recap/{scenarios,seed,reset}` + module `lib/dev/*` (prévus dans le prompt original) → remplacés par 1 script CLI = 1 scénario qui seede la DB dev réelle (`ddehmjucyfgyppfkbddr`) avec les vrais users A/B dans un vrai groupe. Le user déroule ensuite l'UX réelle de bout en bout dans le navigateur (login → dashboard → wizard → retour). Aucune route serveur ni RPC ajoutée — la conformité aux états mid-flow (resume) / completed / locked-by-other passe par INSERT direct dans `monthly_recaps` (helper `seedRecapRow`), ce qui laisse `/api/monthly-recap/start` retourner naturellement `'resumed'` à l'ouverture.

  **Architecture installée** :

  **(1) `scripts/seed-recap/_lib.mjs`** (440 LOC) : client Supabase service-role **dev-only avec garde-fou anti-prod triple** :
  - URL hardcodée `https://ddehmjucyfgyppfkbddr.supabase.co` (ignore `NEXT_PUBLIC_SUPABASE_URL` même si pointé sur prod).
  - Env var dédiée `SUPABASE_DEV_SERVICE_ROLE_KEY` (DISTINCTE de `SUPABASE_SERVICE_ROLE_KEY` qui peut cibler prod selon la config user) — exit 1 immédiat si absente avec instructions de récupération sur le dashboard Supabase.
  - Inspection JWT payload du service-role key : décode la 2e segment base64url, refuse de tourner si la string contient `jzmppreybwabaeycvasz` (le service-role key embed son `ref` claim dans le payload non-signé).
  - Auto-chargement `.env.local` (loader inline ~25 LOC, pas de dotenv dep).

  **15 helpers** : `cleanupCurrentMonth({ profile, group })` (DELETE monthly_recaps du mois → DELETE real_expenses/income_entries du mois → DELETE estimated_budgets/incomes (recurring, non month-scope) → UPSERT piggy_bank A/G + bank_balances A à 0 → reset is_carried_over flags sur prior months) ; `ensureGroupMembership()` (SELECT A+B, throw avec SQL correctif si group_id ≠ GROUP_ID) ; `insertProfileBudgets/insertGroupBudgets(filter, [...])` → `Map<name, id>` ; `insertProfileExpenses/insertGroupExpenses(filter, budgetIdsByName, [...], { createdByUserId })` (computed `applied_to_balance_at` par défaut = expense_date ISO si `applied !== false`) ; idem incomes/realIncomes ; `setPiggy({ profile_id|group_id }, amount)` (UPDATE-or-INSERT idempotent) ; `setBank({ profile_id }, balance)` ; `setProfileSalary(userId, salary)` (UPDATE profiles + appel `calculate_group_contributions` RPC fail-soft si profile.group_id non-null) ; `seedRecapRow({ context, contextId, currentStep, startedByProfileId, refloatedFromPiggy, refloatedFromSavings, budgetSnapshotData, completedAt })` (DELETE row existante + INSERT propre — pas d'UPSERT propre possible à cause des partial unique indexes XOR profile/group) ; `printPostSeedInstructions({ scenarioKey, context, expectedUrl, expectedBehavior, expectedFigures, cookieHint })` (output formatté multi-lignes avec emoji + URL + warning cookies) ; `runScenario(name, asyncFn)` (wrapper try/catch + exit 1 sur erreur).

  **(2) `scripts/seed-recap/_reset.mjs`** (20 LOC) : `cleanupCurrentMonth()` + print confirmation. Wipe pur sans seeder de scénario — utile pour repartir d'un état `no_recap` propre.

  **(3) 27 scripts par scénario** : chacun ~30-60 LOC, structure type = `cleanupCurrentMonth()` → `setProfileSalary(...)` → `setPiggy/setBank(...)` → `insertProfileBudgets/insertGroupBudgets(...)` → `insertProfileExpenses/insertGroupExpenses(...)` → (optionnel) `seedRecapRow({...})` → `printPostSeedInstructions({...})`. Catégories :
  - **Positive (4)** : `fresh-no-budgets`, `happy-surplus-light` (200€), `happy-surplus-large` (800€), `surplus-with-existing-savings` (cumulated_savings préexistantes 50+100€).
  - **Déficit piggy-only (2)** : `deficit-tiny-piggy-covers` (-20€/piggy 100€), `deficit-large-piggy-exact` (-200€/piggy 200€).
  - **Déficit cascade savings (3)** : `deficit-medium-cascade-savings` (-150€ piggy 50€ pool 200€), `deficit-piggy-empty-savings-suffice`, `deficit-savings-pool-equal-deficit`.
  - **Déficit cascade full (3)** : `deficit-cascade-full` (-500€ piggy 100€ savings 100€ snapshot 300€), `deficit-cascade-savings-empty-budgets-only`, `deficit-cascade-extreme` (-2000€).
  - **Group (3)** : `group-positive-2-members`, `group-deficit-2-members`, `group-mixed-salaries` (A=3500€/B=1500€ contributions 70/30%).
  - **Resume mid-flow (5)** : `resume-at-summary`, `resume-at-manage-bilan-positive`, `resume-at-manage-bilan-negative-half` (piggy déjà débitée 50€, refloated_from_piggy=50, déficit restant 150€), `resume-at-salary-update` (group, `budget_snapshot_data` peuplé via Map<budgetId, amount> proportionnel sur estimated_amount), `resume-at-final-recap` (snapshot peuplé + refloats appliqués).
  - **Transactions mix (3)** : `transactions-mixed-validated` (6 applied/4 non + 2 incomes applied/1 non), `transactions-all-validated` (tous DELETE au complete), `transactions-all-non-validated` (tous flaggés carry-over avec badge "Reporté" dashboard suivant).
  - **Edge (4)** : `edge-empty-piggy-surplus-zero`, `edge-balance-exact-zero` (bilanSign='zero'), `edge-already-completed` (recap row `completed_at = now()` → /dashboard render direct), `edge-locked-by-other` (group, `started_by_profile_id = USER_B_ID` → A voit lock screen).

  **(4) `scripts/seed-recap/README.md`** (~6k chars) : pré-requis env, workflow (cleanup automatique à chaque seed, idempotency garantie), table 27 scénarios + UX attendue par script, troubleshooting (cookies httpOnly 5min, group membership, RPC failures), section "Pour ajouter un nouveau scénario".

  **(5) Docs** : nouvelle convention `.claude/conventions/user-questions.md` (~8k chars) qui formalise la règle "AskUserQuestion en vocabulaire métier" demandée par le user pendant le sprint planning (3 paires avant/après pour Monthly Recap, table de reformulations jargon→métier, paragraphe "Quand le jargon reste OK") ; mémoire `feedback_questions_business_language.md` (rappel cross-session) ; CLAUDE.md mis à jour (§3 commands +1 row `node scripts/seed-recap/<key>.mjs`, §5 paragraphe Monthly Recap V3 condensé pour intégrer le pointeur seed-recap, §6 nouvelle subsection "Interaction utilisateur" pointant vers user-questions.md, §5.5 invariants table trimée de ~340 chars pour libérer du budget sous le cap 39.5k).

  **Conventions / leçons** :
  - **Pivot in-app → CLI** : pour de l'outillage QA manuel, un script CLI bat une page in-app à plate couture quand les data sources sont réelles. La page in-app aurait dupliqué la logique d'orchestration ET ajouté 3 routes serveur (cleanup mental + tests + maintenance + risque sécurité prod) pour un ROI cosmétique. Le script CLI tourne en Node avec service-role direct, hardcoded sur dev, et le user déroule l'UX réelle dans le navigateur. Doc : règle ajoutée dans operational-rules.md §5 forbidden absolus.

  - **Garde-fou anti-prod multi-couches sur scripts à fort impact** : URL hardcodée + env var dédiée + JWT payload inspection. Chaque couche est défensive contre un type d'erreur user différent : URL hardcodée prévient le bug "j'ai pas vu que mon `.env.local` pointait sur prod" ; env var dédiée prévient "j'ai copié-collé le mauvais service_role" ; JWT payload check prévient "j'ai bien créé l'env var mais j'ai mis la clé prod par erreur dedans". Le coût d'implémentation = ~30 LOC dans `_lib.mjs`. Pattern reproductible pour tout script CLI qui write en DB.

  - **`seedRecapRow` direct insert > nouveau endpoint bypass** : pour seeder un état mid-flow / completed / locked-by-other, l'INSERT direct dans `monthly_recaps` (avec DELETE row existante préalable pour contourner les partial unique indexes XOR) suffit. Le `/api/monthly-recap/start` retournera naturellement `'resumed'` quand il trouvera la row et l'UI re-affichera le step. Aucun nouveau endpoint admin requis.

  - **`.env.local` auto-loader inline** (~25 LOC sans dotenv) : Node 20+ supporte `--env-file=.env.local` mais ajoute une friction CLI (le user doit le retaper à chaque commande). Un loader inline parse les `KEY=VALUE` du `.env.local` à l'import du module, en respectant les commentaires, lignes vides, et quotes optionnelles. Skip si la valeur est déjà set en process.env (priorité shell > .env.local).

  - **`ensureGroupMembership()` fail-fast avec SQL correctif inline** : plutôt que de planter mystérieusement plus loin (le seed essaie d'INSERT estimated_budgets sur group_id=GROUP_ID mais le RLS / la cascade trigger ne firent pas comme attendu), le helper SELECT A+B d'abord et throw avec l'UPDATE SQL exact à appliquer si group_id ≠ GROUP_ID. Réduit le debug-loop de 10 min à 30 sec.

  **Files livrés** :
  - **Nouveaux** (30) : `scripts/seed-recap/{_lib,_reset,README}.{mjs,md}` + 27 scripts par scénario + `.claude/conventions/user-questions.md`.
  - **Modifiés** (1) : `CLAUDE.md` (§3 + §5 + §5.5 + §6 nouvelle subsection).
  - **Mémoire** : `feedback_questions_business_language.md` (cross-session).

- ✅ **Sprint Wizard-Shell-Lock-Screen-V3** (sprint 10/17 Monthly Recap V3, livré 2026-05-23). Ouvre la Phase D (UI). Squelette navigationnel du wizard plein-écran + barre de progression + écran verrou groupe. 8 fichiers de runtime + 3 fichiers de tests (12 RTL cas) ; aucune modif DB, aucune nouvelle route serveur, aucune RPC, aucun test gated. Le wizard consomme `/api/monthly-recap/status` livré sprint 05 — il route sur `status.kind` puis `status.step` selon le contract de la discriminated union (`no_recap | in_progress | locked_by_other | completed`).

  **Architecture installée** :

  **(1) `hooks/useMonthlyRecap.ts`** (~25 LOC) : TanStack Query wrapper sur GET `/api/monthly-recap/status?context=...`. queryKey `['monthly-recap', 'status', context]`, staleTime 30s, refetchOnWindowFocus off. Retourne `{ status: RecapStatusKind, summary: RecapSummary | null }` typé. Throws `Error(body.error ?? 'fetch_status_failed')` sur `!res.ok` (TanStack Query catch + expose dans `error`). Le `signal` passé à fetch active l'AbortController natif de TanStack Query au unmount/refetch.

  **(2) `app/monthly-recap/page.tsx`** (~30 LOC) : server component avec `Suspense fallback={<RecapShell><p>Chargement…</p></RecapShell>}`. Parse `(await searchParams).context === 'group' ? 'group' : 'profile'` — défaut `profile`, valeurs invalides normalisées en `profile` (le proxy passe toujours `profile` ou `group` mais le defaulting est défensif). Mount `<RecapWizard context={...} />`. Pas de `'use client'` (server component qui awaite searchParams, pattern Next 16).

  **(3) `components/monthly-recap/RecapShell.tsx`** (~12 LOC) : layout wrapper `fixed inset-0 flex flex-col overflow-y-auto bg-gradient-to-br from-blue-50 to-indigo-100` + container `mx-auto w-full max-w-sm flex-1 px-4 py-6`. Pattern miroir `app/(dashboards)/layout.tsx:77` (même shell technique) mais avec un fond distinct (dégradé bleu-indigo = cérémonie recap, choix UX validé pendant le planning vs continuité visuelle dashboard ou blanc minimal). Pas de header global Popoth à masquer parce que `app/layout.tsx` mount uniquement `QueryProvider` + `AuthProvider`, sans nav globale.

  **(4) `components/monthly-recap/RecapProgressFrieze.tsx`** (~35 LOC) : barre de progression horizontale + label « Étape X sur 5 — <Nom> » au-dessus. 5 entries (welcome=Bienvenue, summary=Récap général, manage_bilan=Bilan du mois, salary_update=Salaire, final_recap=Final) — la 6e valeur RecapStep `'completed'` est traitée defensively (affiche « Étape 5 sur 5 — Final » barre 100 %). Width via `style={{ width: \`${pct}%\` }}` inline — Tailwind 4 ne supporte pas `w-${runtime}`JIT. Animation`transition-all duration-300`. Sémantique a11y `role="progressbar"`+`aria-valuenow/valuemin/valuemax`+`aria-label`. Choix « barre + label actif » validé par user pendant planning (vs cercles numérotés ou cercles + mini-labels — plus lisible sur iPhone SE 375 px).

  **(5) `components/monthly-recap/GroupLockScreen.tsx`** (~25 LOC) : écran verrou pour le membre groupe non-initiateur. Texte adapté selon `startedByName: string | null` — « Alice est en train de réaliser le récap mensuel du groupe » ou fallback générique « Un membre du groupe est en train ». Seule action = bouton « Se déconnecter » via `useLogoutAndRedirect`. **Correction du prompt sprint** : le prompt suggérait `const logout = useLogoutAndRedirect()` puis `<Button onClick={() => logout()}>` — c'est faux, le hook retourne `{ logoutAndRedirect }` (cf. [hooks/useAuth.ts:75-85](../../hooks/useAuth.ts)). Pattern correct = destructure `{ logoutAndRedirect }` + `onClick={() => void logoutAndRedirect()}`. Type strict `startedByName: string | null` (la status route retourne `null`, pas `undefined`, cf. [lib/recap/check-status.ts:24-39](../../lib/recap/check-status.ts)).

  **(6) `components/monthly-recap/RecapWizard.tsx`** (~110 LOC) : host + routeur kind/step. (a) Skeleton placeholder pendant `isLoading` (3 divs `animate-pulse rounded bg-blue-200/{60,40}`, pas de spinner). (b) Banner d'erreur si `error` (`text-red-700` + `error.message`). (c) Routing sur `status.kind` :
  - `'locked_by_other'` → `<GroupLockScreen startedByName={status.startedByName} />` PAS de frise (l'écran est terminal, frise non-pertinente).
  - `'completed'` → afficher « Récap déjà terminé, redirection… » + `useEffect(() => { if (kind === 'completed') router.replace(context === 'group' ? '/group-dashboard' : '/dashboard') }, [kind, context, router])`. Filet défensif pour la race condition entre mutation complete (dans 13) et refresh cookie proxy (proxy.ts:109-123 redirige déjà mais cookie cache 5 min pas encore posé au moment du render).
  - `'no_recap'` → `<RecapProgressFrieze currentStep="welcome" />` + `<WelcomeStep context={context} />`.
  - `'in_progress'` → frise sur `status.step` + routing fin par step : welcome/summary/manage_bilan (selon `summary.bilanSign` : negative → BilanNegative ; positive|zero → BilanPositive, spec section 4 « vert si bilan ≥ 0 »)/salary_update/final_recap/completed (filet identique kind=completed dans la branche in_progress).

  Filet défensif `if (!summary) return ErrorPlaceholder` pour le cas théorique `kind='in_progress' && summary===null` (ne devrait jamais arriver per contract endpoint sprint 05, mais évite un crash plutôt qu'un `summary!`).

  **(7) 6 step placeholders** `components/monthly-recap/steps/{Welcome,Summary,BilanPositive,BilanNegative,SalaryUpdate,FinalRecap}Step.tsx` (~10 LOC chacun) : stubs minimaux avec titre `<h1>` + ligne `[TODO sprint XX]` + une valeur dérivée du summary pour permettre le smoke test (ex: `bilan=X €` sur Summary, `déficit=X €` sur BilanNegative, `solde=X €` sur FinalRecap). Sprints 11-14 réécriront le contenu mais probablement pas la signature des props (`{ context }` ou `{ context, summary }`). Volontairement minces — éviter de "deviner" la forme des futurs écrans avant que la logique soit livrée.

  **Tests RTL (12 cas, env=jsdom)** :
  - `components/monthly-recap/__tests__/RecapWizard.test.tsx` (6 cas) : mock du hook `useMonthlyRecap` via `vi.mock('@/hooks/useMonthlyRecap', () => ({ useMonthlyRecap: vi.fn() }))` + mockReturnValue par cas (pattern repo : mocker le hook, pas TanStack Query — cf. `components/dashboard/__tests__/AddTransactionModal.test.tsx:1-48`). Mock `next/navigation` `useRouter` pour assert `router.replace`. Mock `useLogoutAndRedirect` pour éviter de toucher au AuthContext réel. Cas couverts : no_recap → Welcome + frise step 1 ; in_progress summary → Summary + frise step 2 ; in_progress manage_bilan bilanSign='positive' → BilanPositive ; in_progress manage_bilan bilanSign='negative' → BilanNegative ; locked_by_other → GroupLockScreen sans frise (assert via `queryByText(/Étape \d sur 5/).not.toBeInTheDocument()`) ; completed → message + `router.replace('/dashboard')` (via `waitFor` pour le useEffect).
  - `components/monthly-recap/__tests__/RecapProgressFrieze.test.tsx` (3 cas) : step='welcome' → label « Étape 1 sur 5 — Bienvenue » + `bar.style.width = '20%'` + `aria-valuenow="20"` ; step='salary_update' → 80 % ; step='completed' (defensive) → step 5/5 « Final » + 100 %.
  - `components/monthly-recap/__tests__/GroupLockScreen.test.tsx` (3 cas) : avec name → texte « Alice est en train » ; sans name (null) → texte fallback générique ; click bouton « Se déconnecter » → assert `logoutAndRedirect` mock appelé 1 ×.

  Compteur tests non-gated **447 → 459** (CLAUDE.md §5.5 invariant).

  **Conventions / leçons** :
  - **Mocker le data hook, pas TanStack Query** : ré-confirmation du pattern repo. Pour les composants qui consomment un useQuery wrappé (e.g. `useMonthlyRecap`), mocker directement le hook via `vi.mock('@/hooks/useMonthlyRecap', () => ({ useMonthlyRecap: vi.fn() }))` puis `useMonthlyRecapMock.mockReturnValue({ data, isLoading, error })`. Aucun besoin de QueryClient wrapper ni MSW. Le `signal` du queryFn n'a pas à être testé — TanStack Query le gère. Simpler et plus rapide que l'alternative.

  - **`router.replace` côté useEffect, pas inline render** : un `router.replace(...)` appelé inline dans le render déclenche un warning React `Cannot update a component while rendering a different component`. Le pattern correct est `useEffect(() => { if (cond) router.replace(...) }, [cond, ...])`. Le filet `kind === 'completed'` du wizard est implémenté ainsi avec `kind` extracted en variable pour éviter de re-fire l'effet à chaque re-render de `data` (`data?.status.kind ?? null` est stable).

  - **`fixed inset-0` pour le wizard plein-écran** : pattern miroir `app/(dashboards)/layout.tsx:77`. Pas de header global Popoth à masquer parce que `app/layout.tsx` mount uniquement les providers, pas de nav. Si une nav globale était ajoutée plus tard, il faudrait gate via pathname check ou via un route group dédié `app/(recap)/layout.tsx`.

  - **Style frise : barre + label actif > cercles numérotés** : choix UX validé avec user pendant planning. Plus lisible mobile 375 px (1 ligne label compact + 1 ligne barre vs 5 cercles + connecteurs qui serrent sur iPhone SE). Le label le plus long « Étape 3 sur 5 — Bilan du mois » fait ~30 caractères, tient sur 1 ligne à `text-sm` (~14 px). Pattern réutilisable pour tout futur wizard multi-étapes mobile-first.

  - **Type strict `startedByName: string | null`, pas `| undefined`** : le prompt sprint suggérait `string | undefined`. La status route retourne `string | null` (concaténation `first_name + last_name` trim, fallback null si vide — cf. [lib/recap/check-status.ts:218-221](../../lib/recap/check-status.ts)). Garder la cohérence avec le contract endpoint évite les casts inutiles.

  - **Pas de polling sur le lock screen** : décision KISS pour ce sprint. Si l'autre membre du groupe finit le recap pendant que l'user est sur le lock screen, l'user doit refresh manuellement. Le `staleTime: 30_000` + `refetchOnWindowFocus: false` du hook ne déclenche pas de re-fetch automatique. À reconsidérer si UX feedback (ajouter `refetchInterval: 30_000` scoped au lock state via un second `useQuery` conditionnel).

  - **Composants step placeholders** : volontairement minces (`<h1>` + 1 ligne TODO + 1 valeur). Pas de logique, pas de mutation, pas de bouton. Évite de "deviner" la signature des futurs écrans avant que la logique soit livrée — les sprints 11-14 écriront le vrai contenu en sachant ce dont ils ont besoin.

  - **Verify dev-target** : `pnpm verify` sans `$env:SUPABASE_PROJECT_REF` ciblait prod (`jzmppreybwabaeycvasz`) qui n'a pas encore la table `monthly_recaps` → `db:check-drift` faux-positif. Cibler dev (`$env:SUPABASE_PROJECT_REF = 'ddehmjucyfgyppfkbddr'` avant `pnpm verify`) pour passer green pendant le développement V3. Au sprint closeout final (sprint 17), push baseline prod + reverify sans override.

  **Files livrés** :
  - **Nouveaux** (11) : `app/monthly-recap/page.tsx`, `hooks/useMonthlyRecap.ts`, `components/monthly-recap/{RecapShell,RecapProgressFrieze,GroupLockScreen,RecapWizard}.tsx`, `components/monthly-recap/steps/{Welcome,Summary,BilanPositive,BilanNegative,SalaryUpdate,FinalRecap}Step.tsx`, `components/monthly-recap/__tests__/{RecapWizard,RecapProgressFrieze,GroupLockScreen}.test.tsx`.
  - **Modifiés** (3) : `CLAUDE.md` (§5 description Monthly Recap V3 + §5.5 tests 447 → 459 + §11 roadmap Part 20 entry), `.claude/reference/structure-repo.md` (nouveau dossier `components/monthly-recap/` + nouvelle ligne `app/monthly-recap/` + nouvelle ligne `hooks/useMonthlyRecap.ts`), `.claude/history/roadmap-detailed-20-salary-finalize.md` (cette entrée).
  - **Hors scope (à ne pas faire)** : aucune modif DB, aucune nouvelle route serveur, aucune RPC, aucun test gated. Les drawers indicatifs (surplus, économies) + step components réels viendront aux sprints 11-14. Animations slide entre steps : hors scope (les transitions naturelles via re-render TanStack après mutation suffiront sprints 11+).
