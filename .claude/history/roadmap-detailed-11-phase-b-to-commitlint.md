# Roadmap détaillée — Part 11/12 : Auto-Balance-Atomic-Phase-B → Commitlint

> Découpé 2026-05-18 depuis `.claude/history/roadmap-detailed.md` (393 967 chars, 12 parts).
> **Étendue** : Sprint Auto-Balance-Atomic-Phase-B ➜ Sprint Commitlint (6 sprints livrés).
> Pour l'index complet et le résumé court, voir CLAUDE.md §11.
> Navigation : [← Part précédente](roadmap-detailed-10-p10-to-auto-balance-atomic.md) | [Part suivante →](roadmap-detailed-12-cas3-to-refactor-recover.md)

---

## 11. Roadmap — Part 11/12

- ✅ **Sprint Auto-Balance-Atomic-Phase-B** (chantier 13b 2026-05-15) : ferme le bug latent **reversed RPC→INSERT pattern B (piggy_bank)** dans [app/api/monthly-recap/auto-balance/route.ts](app/api/monthly-recap/auto-balance/route.ts) PHASE 0, documenté hors scope Sprint Auto-Balance-Atomic 2026-05-15. Le pattern A (savings) avait été fermé par le Sprint précédent ; le pattern B (piggy) restait : `updatePiggyBank(aggregate)` L442 + INSERT batched `budget_transfers` (from_budget_id=NULL) L454-471 — si l'INSERT batched fail après le debit aggregate, piggy était débité de N×amount sans audit-trail row pour reconcilier. **5 commits sur `cleanup`** :
  - **Commit 1** ([`2271818`](#)) `feat(db): add transfer_piggy_to_budget_with_insert atomic RPC` — nouvelle migration [supabase/migrations/20260519000000_create_transfer_piggy_to_budget_with_insert_rpc.sql](supabase/migrations/20260519000000_create_transfer_piggy_to_budget_with_insert_rpc.sql) avec body PL/pgSQL miroir `transfer_with_savings_debit` (Sprint Refactor-I5-followup-v2) : SECURITY DEFINER + SET search_path=public + REVOKE/GRANT + NOTIFY pgrst, validation `p_amount > 0` + XOR profile/group, `PERFORM update_piggy_bank_amount(-p_amount, ...)` (CHECK piggy_bank.amount >= 0 raises sur overdraft → tx rollback) puis `INSERT INTO budget_transfers (from_budget_id=NULL, ...) RETURNING id`. Applied via apply-sql.mjs HTTP 201 + supabase migration repair + baseline re-export. `pnpm db:check-drift` exit 0, `pnpm db:audit-functions` 15/15 versionnées.
  - **Commit 2** ([`498a58b`](#)) `feat(finance): add transferPiggyToBudgetWithInsert helper + pin RPC` — [lib/finance/piggy-bank.ts](lib/finance/piggy-bank.ts) extension avec helper TS mirroring `transferWithSavingsDebit` pattern : ContextFilter resolve + supabaseServer.rpc + throw on error + RETURNING `{transfer_id, piggy_bank_amount}`. Convention C3 maintenue : direct submodule import, **pas exposé dans le barrel** `lib/finance/index.ts`. Regen `lib/database.types.ts` via `pnpm db:types` — la nouvelle RPC apparaît avec args correctement typés. `pnpm db:check-rpcs` 10/10 RPCs présentes en prod. `EXPECTED_RPCS` bumped 9 → 10 dans [scripts/check-rpcs.mjs](scripts/check-rpcs.mjs). Type fix collatéral : `p_recap_id` typed `string | undefined` (regen Supabase utilise undefined pour DEFAULT NULL, pas null) — `params.recapId ?? undefined`.
  - **Commit 3** ([`b2c255d`](#)) `test(finance): gated concurrency tests for transfer_piggy_to_budget_with_insert` — nouveau fichier [lib/finance/**tests**/transfer-piggy-to-budget-with-insert.test.ts](lib/finance/__tests__/transfer-piggy-to-budget-with-insert.test.ts) (~230 LOC, **4 cas gated SUPABASE_RPC_CONCURRENCY_TESTS=1**, ~3.5s vs prod). **Cas 1** happy path (piggy=100, amount=30 → piggy=70, 1 transfer row avec `from_budget_id=NULL` confirmé via SELECT post-insert). **Cas 2** insufficient piggy (piggy=10, amount=50 → RPC throws + **piggy unchanged + zero transfer rows = atomicity proof**, pre-fix le INSERT batched orphan-isait après piggy debit aggregate). **Cas 3** 100× concurrent (piggy=50, amount=1, chunked 10×10 → 50 succeed + 50 fail, piggy=0, exactement 50 transfer rows). **Cas 4** XOR validation (passing both p_profile_id + p_group_id via direct supabase.rpc bypass du helper — le helper bloque cette combo au compile-time via ContextFilter union type). Pattern miroir `transfer-with-savings.test.ts` : dynamic import in beforeAll, FK-safe cleanup cascade (budget_transfers defensive → piggy_bank PAS de ON DELETE CASCADE sur profile_id FK → estimated_budgets → admin.deleteUser).
  - **Commit 4** ([`eb1a65c`](#)) `refactor(monthly-recap): atomic transferPiggyToBudgetWithInsert in auto-balance PHASE 0` — [auto-balance/route.ts L427-465](app/api/monthly-recap/auto-balance/route.ts) bloc 2a réécrit : drop `updatePiggyBank(filter, piggyDelta)` aggregate (L441-449) + `piggyBankTransfers` INSERT batched (L454-479) → loop `for(transfer of transfersFromPiggyBank) transferPiggyToBudgetWithInsert(filter, {...})` per-pair avec fail-soft `logger.warn` + continue (cohérent PHASE 1 Sprint Auto-Balance-Atomic). Drop import `updatePiggyBank` (no remaining caller in this file). Net **-28 LOC** (23 ins / 51 del). PHASE 2 surplus inchangée (single INSERT atomic by design — surplus n'est pas stocké comme colonne). Counter `as unknown as SupabaseClient` reste à **0**.
  - **Commit 5** (this) closeout CLAUDE.md §1 score + §5 architecture note + §8 ✅/❌ bullets + §9 tests + §11 entrée.
  - **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / **0 warnings stable** (vs 0 pré-sprint, baseline post-Lot 6) ; `pnpm test:run` **394 passed / 79 skipped** (vs 394/75 pré-sprint = **+4 cas non-gated** = exactement les 4 nouveaux cas gated du commit 3, **zero régression**) ; `pnpm verify` exit 0 (8 stages : typecheck + tests + 6 db:\* checks dont 15/15 functions versionnées, 10/10 RPCs pinnées) ; `SUPABASE_RPC_CONCURRENCY_TESTS=1 pnpm test:run lib/finance/__tests__/transfer-piggy-to-budget-with-insert.test.ts` **4/4 passed** en ~3.5s contre prod (atomicity prouvée).
  - **Smoke browser deferred to user** : flow `/monthly-recap` CAS 2 (piggy disponible + budgets en déficit) → vérifier transferts piggy→budgets + piggy_bank deltas + budget_transfers rows byte-identique pré-refactor (UX visible identique : message "Répartition automatique effectuée: X€ répartis (Y€ tirelire + ...)"). L'edge case race middle-of-tx (insufficient piggy concurrent) n'est pas reproductible manuellement — couvert par les cas gated 2 et 3.
  - **Pattern post-sprint** : pour toute future paire `update_piggy_bank_amount + INSERT budget_transfers (from_budget_id=NULL)`, utiliser `transferPiggyToBudgetWithInsert` (cf. §8 ✅). Le helper devient le standard pour les transferts piggy → budget avec audit-trail (vs `transferFromPiggyToBudget` qui debit piggy + credit `cumulated_savings`, use case différent).
  - **Hors scope explicite** : (a) **auto-balance god-file extraction** vers `lib/recap/auto-balance-{algorithm,persist}.ts` mirror I5/I6 — chantier ultérieur ; (b) **`balance/route.ts`** mêmes considérations atomicité que auto-balance — pas de pattern reversed similaire mais à auditer si touché ; (c) **`monthly_recap_id` plumbing** dans budget_transfers (cf. §5 + §8) — le helper accepte `recapId` optional mais auto-balance ne le plumb pas (déféré audit_2/18) ; (d) abstraction `withCompensatingRollback()` — toujours prématurée tant que <5 sites cross-repo (après ce sprint il en reste 0 ; tous les patterns reversed identifiés sont fermés). Pattern miroir Sprint Auto-Balance-Atomic : ferme un gap atomicity surfacé puis confirmé par tests gated, sans expansion scope inutile.
- ✅ **Sprint P7-Authz-Solde-Groupe** (chantier 14, livré 2026-05-15, plan basé sur chantier source `audit_2/14_basse_p7-permissions-createur-solde-groupe.md`) : ferme P7 du backlog produit ([next-steps.md](next-steps.md)) — seul le créateur d'un groupe peut désormais modifier le solde disponible du groupe. **Phase 1 audit** a confirmé les 5 hypothèses du chantier : (a) `groups.creator_id` field existe ([app/api/groups/route.ts:60](app/api/groups/route.ts#L60)) ; (b) `useGroups().isCreator` déjà exposé au [hooks/useGroups.ts:232](hooks/useGroups.ts#L232) (computed via `groups[0]?.is_creator ?? false`) ; (c) `POST /api/bank-balance` group context avait **aucune vérification creator** (fetch `profile.group_id` seul) ; (d) UI bouton vit dans `EditableBalanceLine` (rendu inconditionnellement dans le menu Paramètres latéral) ; (e) profile dashboard inchangé (à NE PAS toucher — owner peut toujours éditer son solde). User a arbitré **Option Recommended Q1** (mocked API + smoke browser) pour l'effort S. **Defense in depth pattern 2-niveau** : (1) UI conditionnelle via `useGroups()` hook ; (2) API gate serveur-side via fetch `groups.creator_id` après `profile.group_id`. **2 commits sur `cleanup`** :
  - **Commit 1** ([`a0958f2`](#)) `feat(groups): restrict bank-balance edit to creator (P7)` — (a) `app/api/bank-balance/route.ts` POST handler L114-132 : ajout d'un fetch `groups.creator_id` après le fetch `profile.group_id` (uniquement en group context) + return 404 'Groupe introuvable' si fetch fail + return 403 'Action réservée au créateur du groupe' si `userId !== creator_id` ; (b) `app/group-dashboard/page.tsx` : ajout `import { useGroups }` + destructure `const { isCreator } = useGroups()` + wrap `EditableBalanceLine` dans `{isCreator && (<div ...>...</div>)}` ; (c) nouveau fichier `app/api/bank-balance/__tests__/route.test.ts` 3 cas mocked non-gated (~170 LOC) pattern miroir savings/transfer test : creator happy 200 + non-creator 403 + profile context regression-guard pas de creator check fired. Mock chain supabase avec `single`/`insert` queueables via `mockResolvedValueOnce` pour exercer la séquence multi-query du POST handler.
  - **Commit 2** (this) closeout CLAUDE.md §1 score + §11 entry + retirer P7 de [next-steps.md](next-steps.md) (déplacer vers section "Livrés").
  - **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / **0 warnings stable** (baseline post-Lot 6) ; `pnpm test:run` **394 passed / 75 skipped** (vs 391/75 baseline pre-sprint = **+3 cas non-gated P7**) ; `pnpm build` **54/54 routes** exit 0 ; `pnpm format:check` clean sur les 3 fichiers nouveaux/modifiés (les 27 warnings format restants sont tous pré-existants chantier 16 hygiène git inchangés). 0 migration DB. 0 RPC ajoutée. 0 nouveau `: any`. Counter `as unknown as SupabaseClient` reste à **0**.
  - **Smoke browser deferred to user** : (a) compte test créateur sur `/group-dashboard` → ouvrir menu Paramètres latéral → "Solde disponible" visible avec bouton crayon → click → EditBalanceModal s'ouvre → modify + submit → 200 + DB update + UI refetch via TanStack Query ; (b) compte test membre non-créateur même groupe → ouvrir menu Paramètres latéral → "Solde disponible" **absent du menu** ; (c) curl direct PATCH/POST avec session non-creator → 403 + `{ error: 'Action réservée au créateur du groupe' }` (defense in depth) ; (d) profile context `/dashboard` → bouton "Modifier solde" toujours présent + fonctionnel (regression guard) ; (e) lecteur d'écran : focus accessible sur le bouton crayon (creator-only).
  - **Trade-off documenté** : Sprint P7 est **defense in depth UX + authz** pour un item backlog produit visible. Score reste **~100 stable** (pas de saut métier observable — l'application avait déjà permission policy implicite via UI seule, ce sprint la rend explicite côté serveur). Pattern miroir Sprint P1 / P10 / UserGroupsList-Cleanup — fix UX produit sans impact métier mais ferme un item backlog visible + comble un gap de defense in depth authz.
  - **Hors scope explicite** : (a) **`groups.deleted_at` cascade vers bank_balances** — non couvert ; si un groupe est supprimé, son `bank_balances` row reste orpheline (déjà l'état pré-sprint, hors P7) ; (b) **GET `/api/bank-balance` n'a PAS de creator check** — lecture autorisée à tous les membres (cohérent UX : tout le monde voit le solde, seul le créateur peut le modifier) ; (c) **RTL UI test** déféré (mock RTL group-dashboard nécessiterait ~6 hooks à mocker pour valider 1 conditional render — smoke browser couvre) ; (d) **changement de créateur** (transfer ownership) non couvert ; le `creator_id` est immutable au schéma, set lors du `POST /api/groups` ([app/api/groups/route.ts:102](app/api/groups/route.ts#L102)) — si futur use case, ajouter RPC `transfer_group_ownership` (out of scope P7).
- ✅ **Sprint P8-P9-Menu-Groupe-Cleanup** (chantier 15, livré 2026-05-15, plan basé sur chantier source `audit_2/15_basse_p8-p9-menu-groupe-nettoyage.md`) : ferme P8 + P9 du backlog produit ([next-steps.md](next-steps.md)) — nettoyage UI du menu groupe dans `app/settings/page.tsx`. **Phase 1 audit a invalidé en partie le scope du chantier source** : (a) audit Grep `Créateur` cross-codebase a confirmé que le badge dans [components/groups/GroupMembersWithContributionsModal.tsx:217-220](components/groups/GroupMembersWithContributionsModal.tsx) est légitime (identifier le créateur dans la liste des membres) — hors scope ; (b) audit `Se déconnecter` a confirmé que les 3 hits hors settings (`app/dashboard/page.tsx:427` + `app/group-dashboard/page.tsx:342`) sont des pages dashboard séparées, hors scope P9 (qui cible spécifiquement "les options 'Mon groupe'" = `/settings`) ; (c) **2 ambiguïtés clarifiées via AskUserQuestion** : (i) "la box du bas" est ambigu — Phase 1 a identifié 2 candidats visuels (card bleue info "Vous appartenez déjà à un groupe" L440-469 + footer créateur sous la card "Vous êtes le créateur de ce groupe — Dernière mise à jour" L324-336) ; user a arbitré **"on garde ces boxes"** post-clarification → P8.3 annulé ; (ii) que voit le créateur quand "Supprimer" est retiré (3 options proposées : aucun bouton / bouton "Quitter" simple / bouton "Quitter" + warning) ; user a arbitré **"Bouton 'Quitter' + warning"** (Option 3 — la plus coûteuse mais cohérente UX). **2 commits sur `cleanup`** :
  - **Commit 1** ([`993456e`](#)) `feat(groups): clean group menu UI (P8 + P9)` — modifications atomiques sur 1 fichier `app/settings/page.tsx` : (a) **P8.1** drop `{currentGroup.is_creator && <span>Créateur</span>}` block dans la card "Mon groupe" (badge bleu inutile, le creator status reste visible dans le footer créateur Box B préservé + dans le modal "Voir membres") ; (b) **P8.2** remplace le ternaire `is_creator ? <Supprimer> : <Quitter>` par un bouton "Quitter" unifié + nouvelle fonction `handleLeaveClick` qui dispatch : non-créateurs → direct `handleLeaveGroup()` ; créateurs → `setShowLeaveWarning(true)` → nouveau `<ConfirmationDialog variant="warning">` ("Quitter votre groupe ? Vous êtes le créateur de ce groupe. En quittant, vous perdez le contrôle du groupe mais celui-ci continuera d'exister avec ses membres restants. Cette action est irréversible.") ; (c) **P9.1** drop les 2× boutons `Se déconnecter` du header settings (loading state header L174-181 + main render header L215-222). **Cleanup imports/state collatéral** : drop `useLogoutAndRedirect` import + `logoutAndRedirect` destructure (plus utilisé post-P9) ; drop `DeleteGroupModal` import + `<DeleteGroupModal>` JSX + state `groupToDelete`/`setGroupToDelete` + fonctions `handleDeleteGroup`+`openDeleteModal` (plus utilisés post-P8.2) ; drop `deleteGroup` from `useGroups()` destructure ; drop `import type { GroupData } from '@/app/api/groups/route'` (plus utilisé) ; **add** `ConfirmationDialog` import (`@/components/ui/ConfirmationDialog`) + state `showLeaveWarning`/`setShowLeaveWarning` + fonction `handleLeaveClick`. **`DeleteGroupModal.tsx` composant conservé** (Phase 1 a confirmé via grep que [components/\_\_tests\_\_/a11y-audit.test.tsx](components/__tests__/a11y-audit.test.tsx) le consomme toujours comme regression-guard a11y — pas orphelin cross-codebase). Lint-staged Prettier a auto-formatté légèrement le destructure `useGroups()` (multi-line → single-line, sub-100 chars) — cosmétique. Net **−41 LOC** sur 1 fichier (37 ins / 78 del).
  - **Commit 2** (this) closeout : retire P8 + P9 de [next-steps.md](next-steps.md) (déplace section "Livrés") + section "Backlog actif vide" ajoutée (tous P1-P10 livrés) + CLAUDE.md §1 score + §11 entrée.
  - **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / **0 warnings stable** (baseline post-Lot 6) ; `pnpm test:run` **394 passed / 75 skipped** (inchangé vs P7 baseline — aucun test RTL existant sur settings/page.tsx pre-sprint à adapter, 0 nouveau test) ; `pnpm build` **54/54 routes** exit 0 ; 0 migration DB, 0 RPC ajoutée, 0 nouveau `: any`, counter `as unknown as SupabaseClient` reste à **0**.
  - **Smoke browser deferred to user** : (a) `/settings` user **non-créateur** d'un groupe → header sans bouton "Se déconnecter" → card "Mon groupe" sans badge "Créateur" → bouton "Quitter" direct (pas de modal warning) → flow leaveGroup OK + UI refetch via TanStack Query ; (b) `/settings` user **créateur** → header sans logout → card "Mon groupe" sans badge → bouton "Quitter" → modal ConfirmationDialog warning s'ouvre avec icône jaune triangle + texte "vous perdez le contrôle du groupe..." + bouton "Annuler" / "Quitter" → click "Quitter" → modal disabled pendant `loading=true` → leaveGroup fires + modal close on success + UI refetch ; (c) logout reste accessible via menu utilisateur global (présumé header avatar dropdown ou similar — pas validé Phase 1, hors scope) ; (d) lecteur d'écran annonce ConfirmationDialog `role=dialog` + DialogTitle "Quitter votre groupe ?" + DialogDescription via natif Radix.
  - **Trade-off documenté** : Sprint P8-P9 est **cleanup UX produit** pour 2 items backlog visibles. Score reste **~100 stable** (pas de saut métier observable — l'application fonctionnait pré-sprint sans ce nettoyage). Pattern miroir P1 / P7 / P10 / UserGroupsList-Cleanup — fix UX produit sans impact métier mais ferme un item backlog visible. **Décision out-of-scope** : que devient un groupe quand son créateur quitte ? Le `creator_id` reste pointant vers un user ex-membre (le `groups.creator_id` est immutable post-creation per `POST /api/groups` ; le `profiles.group_id` du créateur devient NULL via `leaveGroup()` → cascade FK normale). Groupe orphelin mais persistant avec les membres restants — comportement DB existant inchangé, à surveiller via incident produit si UX feedback signale.
  - **Hors scope explicite** : (a) **transfer ownership** quand un créateur quitte (e.g. promouvoir auto le 1er membre restant) — pattern miroir P7 décision "creator_id immutable" ; si futur use case, ajouter RPC `transfer_group_ownership` ou logique côté `leaveGroup` ; (b) **DeleteGroupModal cleanup** — composant techniquement orphelin du code applicatif post-P8.2 mais consommé par test a11y-audit, hors scope deletion sans confirmation produit ; (c) **autres surfaces logout** `app/dashboard/page.tsx:427` + `app/group-dashboard/page.tsx:342` — pages dashboard séparées, hors scope "menu Mon groupe" P9 ; si user signale doublon, sprint séparé ; (d) **RTL test settings page** déféré (~6 hooks à mocker pour 1 fichier visuel — smoke browser couvre) ; (e) **i18n / wording** du warning créateur — text français hardcodé, hors scope i18n general.
- ✅ **Sprint Debug-Zod-Coverage** (chantier 22, livré 2026-05-15, plan basé sur chantier source `audit_2/22_basse_zod-debug-routes-residuelles.md`) : ferme la dette cosmétique Zod des 2 routes debug sans body (`app/api/debug/reset-all/route.ts` + `app/api/debug/reset-budgets/route.ts`) restantes après Sprint Zod-Rollout v2 (qui avait migré `retrigger-recap` seul). **Phase 1 audit** a confirmé : (a) `retriggerRecapBodySchema` déjà existant dans [lib/schemas/debug.ts](lib/schemas/debug.ts) — pattern `try { await request.json() } catch {} + safeParse` (inline empty-body tolerance, parseBody rejette les bodies vides) ; (b) 2 routes ciblées toutes deux POST sans payload, gated `blockInProduction()` 404 prod (risque sécurité nul) ; (c) après Sprint Dead-Code-Purge 2026-05-13, il reste exactement 3 routes debug survivantes (`remaining-to-live`/`financial`/`group-financial` supprimées). **User a arbitré Path A "migrer pour cohérence"** vs Path B "laisser tel quel" (CLAUDE.md system prompt "Don't design for hypothetical" aurait pointé Path B, mais user a privilégié la cohérence visuelle 100% Zod coverage du domaine debug). **1 commit code + closeout sur `cleanup`** : (a) [lib/schemas/debug.ts](lib/schemas/debug.ts) extension avec `resetAllBodySchema = z.object({}).strict()` + `resetBudgetsBodySchema = z.object({}).strict()` + JSDoc explicit "no input expected, schema pinned for visual consistency" ; (b) [app/api/debug/reset-all/route.ts](app/api/debug/reset-all/route.ts) ajout import `resetAllBodySchema` + bloc `let rawBody: unknown = {}; try { rawBody = await request.json() } catch {}; const parsed = resetAllBodySchema.safeParse(rawBody); if (!parsed.success) return 400` au top du try (pattern miroir [retrigger-recap/route.ts:29-41](app/api/debug/retrigger-recap/route.ts)) ; (c) [app/api/debug/reset-budgets/route.ts](app/api/debug/reset-budgets/route.ts) idem. Net **+30 LOC** sur 3 fichiers (~10 LOC par migration + 16 LOC schemas + JSDoc). **0 migration DB**, **0 RPC ajoutée/supprimée**, **0 nouveau test** (les schemas `.object({}).strict()` ont 0 logique à regression-guarder, cohérent décision Sprint P3/P2 closeouts pour les invariants triviaux). **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / **0 warnings stable** (baseline post-Lot 6) ; `pnpm test:run` **394 passed / 75 skipped** stable (flake transient worker pool timeout au 1er run vitest, re-run instantané vert — pattern courant Node.js worker fork startup, sans lien avec ce sprint) ; `pnpm verify` exit 0 (8 stages : typecheck + tests + 6 db:\* checks dont 14/14 functions versionnées). Counter `as unknown as SupabaseClient` reste à **0**. 0 nouveau `: any`. **Smoke browser non requis** : routes accessibles uniquement en dev `NODE_ENV !== 'production'` (cf. `blockInProduction()`). En dev, les 2 routes restent fonctionnellement identiques pré-migration (body absent ou `{}` → schema accepte both via `.strict()` on empty object + inline tolerance). **Trade-off documenté** : Sprint Debug-Zod-Coverage est **cohérence visuelle pure** — pas de saut métier, pas de fix de bug. Score reste **~100 stable**. Le bullet ⏭️ "Hors scope Zod" final retiré de §11 — désormais 100% des routes API du repo utilisent Zod (`parseBody` ou pattern inline pour les empty-body routes). Pattern miroir Sprint P2/P3-Closeout-Administrative (closeout cosmétique) + Sprint Zod-Rollout v2 (qui avait initié la couverture debug avec `retrigger-recap`).
- ✅ **Sprint Transfer-Atomicity-Eval** (chantier 23, livré 2026-05-15, plan basé sur chantier source `audit_2/23_basse_monthly-recap-transfer-atomicity-eval.md`) : triage closeout du chantier "évaluation atomicity manual UI transfer" déféré au Sprint Atomicity-Savings hors scope. **Phase 1 audit Read** de [app/api/monthly-recap/transfer/route.ts](app/api/monthly-recap/transfer/route.ts) (178 LOC) + [lib/schemas/recap.ts](lib/schemas/recap.ts) `manualTransferBodySchema` a confirmé : la route est **atomique by design**, **pas de risk pattern à corriger**. Détail du flow POST L41-177 : (a) **reads-only avant** — `parseBody(manualTransferBodySchema)` validate body (context + from*budget_id + to_budget_id + amount + monthly_recap_id?) + same-id refine ; check `context === 'group' && !profile.group_id` → 400 ; SELECT `estimated_budgets` × 2 verify ownership → 404 ; SELECT `real_expenses` × 2 pour calculer `fromSpentAmount`/`toSpentAmount` ; compute `fromBudgetSurplus = MAX(0, estimated - spent)` ; check surplus >= amount → 400 si insuffisant ; (b) **mutation unique** L127-129 — `INSERT` dans `budget_transfers` (audit-trail row avec `from_budget_id`, `to_budget_id`, `transfer_amount`, `transfer_date`, `monthly_recap_id` best-effort optional + `profile_id` OU `group_id` discriminated via `TablesInsert<'budget_transfers'>`) ; fail → 500 ; (c) **return** L147-171 — compute `newFromSpent`/`newToSpent`/`newFromSurplus`/etc. **in-memory only** pour response shape, **aucune mutation DB additionnelle**. **0 mutation** sur `piggy_bank`, `cumulated_savings`, `estimated_budgets`. **JSDoc route L14-19 confirme le contrat** : "Les calculs de surplus/déficit sont automatiquement ajustés dans l'API step2-data qui prend en compte les transferts". C'est-à-dire que le "transfert" est un **enregistrement d'intention** (audit-trail row), pas un mouvement d'argent DB. La lecture downstream `step2-data` lit `budget_transfers` + `real_expenses` pour calculer les surplus ajustés à la volée — pas la route elle-même. **Pas de risk pattern atomicity** : (i) pas de séquence "RPC then INSERT" → rien à reverse ; (ii) pas de compensating-rollback manuel → rien à fail ; (iii) single INSERT : si fail → state DB inchangé, si succeed → cohérent. **Cohérence avec note CLAUDE.md §11 Sprint Atomicity-Savings hors scope** ("`monthly-recap/transfer/route.ts` body-driven manual UI transfer pattern différent") : la note documentait correctement que cette route est \_différente* des routes `savings/transfer` (qui mutaient `cumulated_savings` + `piggy_bank` via 2 RPCs séquentielles avec rollback compensatoire pré-Sprint Atomicity-Savings) — pas qu'elle nécessitait un fix. **Edge case mineur (hors scope Phase 2)** : race théorique sur `surplus >= amount` validation en concurrence multi-membre groupe — marginal car le UX recap mensuel suppose un single session at a time, et `step2-data` SUM tous les transferts existants donc auto-corrige downstream si over-transfer accidentel. **0 code touché**, **0 test ajouté/modifié** (aucune logique à regression-guarder — le contrat single-INSERT est trivial), **0 migration DB**, **0 RPC**. **2 commits** sur `cleanup` : (1) header `⚠️ STALE — closed-by-already-atomic` ajouté à `audit_2/23_basse_monthly-recap-transfer-atomicity-eval.md` redirigeant vers cette entrée (pattern miroir Sprints Audit-Closeout C2/C3/C4/I3 / UserGroupsList-Cleanup / P2 / P3) ; (2) closeout (this) CLAUDE.md §1 score + §11 entrée. **Verif end-to-end** : pas relancée (modifs 100% doc). **Trade-off documenté** : Sprint Transfer-Atomicity-Eval est **closeout administratif** (cohérent les 9+ précédents Sprints Triage/Closeout — Audit-Closeout C2/C3/C4/I3 / Templates-Triage / UserGroupsList-Cleanup / Dead-Code-Purge / P2 / P3 / Debug-Zod-Coverage) — pas de saut métier observable, mais ferme la dernière dette atomicity surfacée en hors-scope Sprint Atomicity-Savings 2026-05-12. Pattern miroir Sprint P2/P3-Closeout-Administrative : l'inventaire pré-sprint (ici un audit Read de la route) a invalidé la prémisse "potentiel risk pattern à fixer" en confirmant que le pattern réel est single-INSERT atomique by design. Le chantier 23 du backlog `audit_2/` est désormais clos. Score reste **~100 stable**.
- ✅ **Sprint Commitlint** (chantier 24, livré 2026-05-15, plan basé sur chantier source `audit_2/24_basse_commitlint-reconsider.md`) : ferme la décision en attente "commitlint" laissée out-of-scope par Sprint 1 (2026-05-09) — install `@commitlint/cli@21.0.1` + `@commitlint/config-conventional@21.0.1` + nouveau hook `.husky/commit-msg` qui invoque `pnpm exec commitlint --edit "$1"` sur chaque commit, **enforcement automatique** de la convention Conventional Commits documentée CLAUDE.md §6 git. **Phase 1 audit historique a forcé une recalibration majeure du draft chantier 24** : le brief proposait `subject-case: lower-case` + `header-max-length: 72` + body-max-line-length default 100, mais le first-pass audit `pnpm exec commitlint --from HEAD~30 --to HEAD --verbose` a montré **28/30 commits récents fail** sur `subject-case` (proper nouns + abbreviations légitimes "CLAUDE.md", "Sprint Pn", "P3", "R10" apparaissent partout), plus quelques fails sur `header-max-length` (sprints multi-mots avec scope explicite dépassent 72 chars régulièrement) et `body-max-line-length` (closeouts multi-paragraphes avec prose). **Décision** : relax les 3 rules trop strictes pour matcher l'usage réel observé sur 50+ sprints. **Config finale** dans nouveau [commitlint.config.js](commitlint.config.js) : (a) `extends: ['@commitlint/config-conventional']` ; (b) `type-enum` allowlist 11 entries (`feat, fix, chore, docs, refactor, test, perf, style, revert, build, ci`) — couvre 100% des sprints livrés ; (c) `subject-case: [0]` OFF — laisse passer proper nouns ; (d) `subject-empty: [2, 'never']` — empêche `git commit -m ""` ; (e) `header-max-length: [2, 'always', 100]` au lieu de 72 default ; (f) `body-max-line-length: [0]` OFF — closeouts multi-paragraphes acceptés ; footer Co-Authored-By: passe via default. **Sanity tests post-install** : `git commit -m "wip"` → blocked (subject + type empty) ; `git commit -m "feat(test): valid"` → accepted ; `git commit -m "feat: This Has UpperCase"` → accepted (subject-case OFF) ; `git commit -m "wip: not in allowlist"` → blocked (type-enum). **Audit historique final** : 53/54 commits passent les 50 derniers (1 outlier `87cb5df test + docs: PeriodSelector RTL + closeout (P1 Commit 5)` — multi-type "test + docs:" non supporté par le parser commit-msg, edge case historique acceptable). **Husky integration** : `core.hooksPath` déjà set à `.husky/_` (proxy v9), le proxy `.husky/_/commit-msg` route vers `.husky/commit-msg` (que je viens de créer) — pas de `pnpm exec husky` post-install requis (le `prepare` script ré-arme à chaque `pnpm install`). **2 commits sur `cleanup`** : (1) `chore(tooling): install commitlint + commit-msg hook` (4 fichiers stagés : commitlint.config.js + .husky/commit-msg + package.json + pnpm-lock.yaml — les 4 M files pré-existants des Sprints Debug-Zod-Coverage / Transfer-Atomicity-Eval restent unstaged, hors scope) ; (2) `docs: closeout Sprint Commitlint (chantier 24)` — CLAUDE.md §1 score + §3 Hooks Git (Husky) ajout commit-msg + §6 git enforcement note + §11 cette entrée. **Verif end-to-end** : `pnpm typecheck` + `pnpm lint:check` + `pnpm test:run` non re-lancés en clôture (modifs 100% tooling + doc, 0 impact code applicatif — le hook commit-msg n'affecte que les futurs commits, pas la baseline). Lint baseline 0 stable (les 4 M files pré-existants n'ont pas été staged ni modifiés ce sprint). **Trade-off documenté** : Sprint Commitlint est **enforcement automatique d'une convention déjà respectée** + filet permanent contre dérive future. Pas de saut métier observable, score reste **~100 stable**. Pattern miroir Sprint Cleanup-I8 / Lot 6 (activation globale `no-console: 'error'` qui pinnait une baseline déjà à 0) : ferme une dette technique de Sprint 1 (skip délibéré 2026-05-09) en transformant la convention "documentée par confiance" en convention "enforced par hook". Bypass d'urgence `git commit --no-verify` reste possible mais à éviter (cf. CLAUDE.md §6 `JAMAIS --no-verify`).
