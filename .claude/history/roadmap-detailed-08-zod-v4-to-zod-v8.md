# Roadmap détaillée — Part 08/12 : Zod-Rollout v4 → Zod-Rollout v8

> Découpé 2026-05-18 depuis `.claude/history/roadmap-detailed.md` (393 967 chars, 12 parts).
> **Étendue** : Sprint Zod-Rollout v4 ➜ Sprint Zod-Rollout v8 (5 sprints livrés).
> Pour l'index complet et le résumé court, voir CLAUDE.md §11.
> Navigation : [← Part précédente](roadmap-detailed-07-audit-c2-to-zod-v3.md) | [Part suivante →](roadmap-detailed-09-zod-v9-to-tailwind-v4.md)

---

## 11. Roadmap — Part 08/12

- ✅ **Sprint Zod-Rollout v4** (plan dans `C:\Users\gille\.claude\plans\sprint-zod-rollout-v4-replicated-bee.md`) : closeout chirurgical post-v3 — ferme **2 forms auth orphelins** manqués par l'inventaire v3 + extrait un **composant réutilisable `<DecimalFormInput>`** + drop unused var. **Arbitrages user** (4 décisions toutes Recommended) : (Q1) Axe 1+2+3 full sweep, (Q2) append to `lib/schemas/auth.ts` (cohérent signupBodySchema/loginFormSchema/sessionActionBodySchema), (Q3) minimal Controller-only abstraction force shadcn `<Input>`, (Q4) 1 commit par axe. **3 commits code + 1 closeout** sur `cleanup`.
  - **Commit 1** ([`4453c70`](#)) `feat(forms): migrate auth password forms to react-hook-form + zodResolver` (Axe 1). 2 schemas ajoutés à [lib/schemas/auth.ts](lib/schemas/auth.ts) : `forgotPasswordFormSchema = z.object({ email: z.string().trim().email("Format d'email invalide") })` + `resetPasswordFormSchema` (z.object password min 6 + confirmPassword min 1 + `.refine(p === cp, path: ['confirmPassword'])` — mirror exact de signupBodySchema sans email). [app/forgot-password/page.tsx](app/forgot-password/page.tsx) : 4 useState (email/error/loading/success) → useForm + 2 useState (success + submittedEmail conservé pour le message UI post-soumission) + new serverError (Pattern F) ; `form.handleSubmit + noValidate + aria-invalid` + Supabase rate-limit detection préservée. [app/reset-password/page.tsx](app/reset-password/page.tsx) : **token validation state-machine UNTOUCHED** (useEffect L39-74, validatingToken + isValidToken + tokenError) ; 5 useState (password/confirmPassword/error/loading) → useForm + serverError ; **4 server-side error branches** (`session_not_found` / `New password should be different from the old password` / generic `password` keyword / fallback) **préservées verbatim dans les 2 paths** (if(updateError) + catch block — Supabase SDK peut throw OU return error). Renamed pre-existing token `error` state to `tokenError` to disambiguate from form serverError. Tests +7 cas dans [lib/schemas/\_\_tests\_\_/auth.test.ts](lib/schemas/__tests__/auth.test.ts) (3 forgotPasswordFormSchema + 4 resetPasswordFormSchema couvrant happy + empty + invalid-format + short + mismatch + empty confirm).

  - **Commit 2** ([`819392c`](#)) `refactor(ui): extract DecimalFormInput component + migrate 8 sites` (Axe 2). Nouveau composant [components/ui/DecimalFormInput.tsx](components/ui/DecimalFormInput.tsx) ~70 LOC : generic `<T extends FieldValues>` wrap Controller + shadcn `<Input>` ; props `control`/`name`/`id`/`placeholder`/`disabled`/`ariaInvalid`/`allowNegative`/`className`/`autoComplete` ; regex `allowNegative ? /^-?\d*[.,]?\d*$/ : /^\d*[.,]?\d*$/` ; comma→dot conversion sur onChange. **8 sites migrés** : (a) **3 sites shadcn déjà-conformes** (EditBalanceModal `allowNegative` + `pr-8` + suffix €, AddTransactionModal + EditTransactionModal `w-full` sans suffix, CreateGroupForm `placeholder="Ex: 2500"` + disabled) drop le Controller block in-place ; (b) **4 sites raw `<input>` dashboard** (AddIncomeDialog + EditIncomeDialog + AddBudgetDialog + EditBudgetDialog) migrés vers shadcn `<Input>` via le composant — layout préservé via `h-auto rounded-{xl,lg} px-{3,4} py-{2,3} pr-{8,12}` overrides, focus colors préservées via `className` avec `focus-visible:border-{green,orange}-500 focus-visible:ring-{green,orange}-500` (translation `focus:` → `focus-visible:` shadcn idiom), erreur red-border préservée via `cn()` conditional. Drop `Controller` import des 4 dashboard forms + `Input` import de EditBalanceModal (DecimalFormInput suffit). Net LOC delta : **−30 LOC** sur 9 fichiers (+70 composant + 8 sites raccourcis ≈ −100 = net −30). Modeste vs estimation audit ~−300 LOC du plan parce que j'ai préservé les cn() conditionals + focus colors plutôt que d'abandoner pour les shadcn defaults — trade-off conscient pour minimiser visual regression (rule "slight visual change accepted but minimize").

  - **Commit 3** ([`4db4979`](#)) `chore(monthly-recap): drop unused currentTotalDeficit var` (Axe 3). [components/monthly-recap/MonthlyRecapStep2.tsx:135-137](components/monthly-recap/MonthlyRecapStep2.tsx) `currentTotalDeficit` reducer computé mais jamais lu (grep cross-codebase 1 hit = la déclaration seule). Drop des 3 LOC ; les 2 sibling reducers `currentTotalSavings` + `currentTotalSurplus` restent consommés. **Lint baseline 182 → 181 warnings** (réduction attendue per plan).

  - **Patterns standardisés** : ajouté **Pattern H** au catalogue §6 — `<DecimalFormInput>` composant centralisant la logique Controller + regex décimal + comma→dot pour les forms à input décimal (cible 8 sites post-v4). Consumer-side reduce ~17 LOC → ~10 LOC. Si UX flake après merge, fallback inline pattern A reste possible.

  - **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / **181 warnings** (vs 182 baseline pre-sprint = −1 du Axe 3 cleanup) ; `pnpm test:run` **203 passed / 64 skipped** (vs 196/64 pré-v4 = **+7 cas non-gated** = 3 forgot + 4 reset password schemas) ; `pnpm format:check` 4 issues pré-existantes hors scope inchangées (doc2/AUDIT-RESOLUTIONS.md + next.config.js + .claude/settings.json + prompt-07-deep-dive-recap-algorithm-v7.md) ; `pnpm verify` exit 0 (8 stages : typecheck + test:run + 6 db:\_ checks). 0 migration DB. 0 nouveau test gated. 0 nouveau `: any`. Counter `as unknown as SupabaseClient` reste à 0. Net brut tous commits : **+209 / −287 = −78 LOC** sur 13 fichiers (auth: +209/−118 = +91 ; DecimalFormInput: +136/−166 = −30 ; currentTotalDeficit: −3).

  - **Smoke browser deferred to user** : (a) `/forgot-password` email vide → "Format d'email invalide" inline ; email valide → success state avec gradient + bullet points + back-to-login ; (b) `/reset-password?token=valid` password <6 → erreur inline sous password ; passwords ≠ → erreur inline sous confirmPassword ; valide → success + redirect /connexion ; `/reset-password?token=invalid` → invalid token state préservé ; (c) AddIncome/EditIncome préview cohérent avec total dynamique ; AddBudget/EditBudget balance refine vs revenus ; AddTransactionModal switch expense↔income propre, exceptional toggle, RAV blocking ; EditTransactionModal preserve transactionType ; EditBalance avec saisie négative -50 → accepté ; CreateGroup budget ≤ 0 → erreur. **Visual check spécifique** : les 4 dashboard forms (Add/Edit Income/Budget) ont migré de raw `<input>` à shadcn `<Input>` via le composant — vérifier que le focus vert/orange + erreur red border + suffixe € restent visibles et alignés.

  - **Hors scope** (documenté plan) : tests RTL des forms client → Sprint Zod-v5-Client-Testing dédié si surface suffisante. Sprint Tailwind-v4 / Supabase-Strict-Types / Chantier I6 / Lot 6 console-cleanup / OpenAPI / R10 audit → roadmappés §11.

  Livré 2026-05-13, 3 commits code + closeout sur `cleanup` (`4453c70` Axe 1 / `819392c` Axe 2 / `4db4979` Axe 3 / closeout). Score : ~99.99 → ~99.995/100 (consolidation — chantier Zod-client complètement clos, premier alignement complet validation client ↔ serveur via Zod ET factor commun pour la couche décimale).

- ✅ **Sprint Zod-Rollout v5** (plan dans `C:\Users\gille\.claude\plans\sprint-zod-rollout-v5-melodic-ullman.md`) : ferme le chantier Zod par sa couverture **côté tests RTL** + a11y. **Arbitrages user** (4 décisions Recommended) : (Q1) full sweep Axes 1+2+3+4+5+6, (Q2) mock-per-site inline aligned avec les 3 patterns Supabase mock existants, (Q3) bundle infra + Axe 2, (Q4) `__tests__/` dédié (pattern dominant 5 instances vs 1 co-located). **6 commits sur `cleanup`** :
  - **Commit 1** ([`47e3996`](#)) `test(client): add RTL infra + DecimalFormInput tests` — install jsdom@25 + `@testing-library/{react,user-event,jest-dom}` devDeps + `vitest.setup.ts` (jest-dom matchers + auto-cleanup) + **`test.projects` split** (env=node pour `*.test.ts` / env=jsdom pour `*.test.tsx`). Premier essai `environment: 'jsdom'` flat a causé une régression perf **x23** (3.75s → 86s sur 203 pure-unit tests) ; pivot vers projects config = 4s wall time stable. Drive-by `react`/`react-dom` pinned à `19.1.1` via `pnpm.overrides` (le `pnpm add` a bumpé react à 19.2.6 alors que react-dom restait 19.1.1, runtime mismatch = pattern précédent DX-Verify follow-up). 8 cas DecimalFormInput pure-unit (regex positive `12.50` / comma→dot / abc reject / double-dot reject / allowNegative true+false / aria-invalid propagation / undefined display).
  - **Commit 2** ([`a461e98`](#)) `test(auth): add RTL tests for 4 auth forms` — **21 cas** : forgot-password (5 — empty/invalid email block, happy success, rate-limit + generic Supabase error mapping) + reset-password (7 — token state-machine **4 sous-états** invalidating/invalid/success/form via `getSession` mock + password <6 + mismatch + happy + session_not_found + "different from old" mapping) + connexion (4 — empty + invalid + happy + serverError Pattern F) + inscription (5 — mismatch + happy + already-registered/weak-password/signup-disabled error mapping). vi.fn signature TS lesson : vitest 4.x drop `vi.fn<[args], Return>` tuple syntax, utiliser `(async (_x: T): Promise<R> => ...)` arrow types.
  - **Commit 3** ([`b6856d7`](#)) `test(client): profile/groups/income RTL` — **16 cas** : FirstTimeProfile (3, min 2 chars) + EditProfile (3, isDirty gate, key={id} remount Pattern C) + CreateGroupForm (4, Pattern A dual-type, useWatch live preview) + AddIncome (3) + EditIncome (3, key={id} Pattern C). AddIncomeDialog labels manquent `htmlFor` → tests utilisent `getByPlaceholderText` (a11y gap surfacé, hors scope Axe 5 ce sprint mais documenté).
  - **Commit 4** ([`8ec5906`](#)) `test(client): budget/transaction heavy RTL` — **19 cas** : AddBudget (4, factory refine Pattern D + useMemo rebuild on prop change) + EditBudget (4, delta calc + remount) + AddTransactionModal (5, **6-hook mock surface** useBudgets/Incomes/RealExpenses/RealIncomes/ProgressData/FinancialData + useRavValidation toggle + CustomDropdown mocké en `<select>` + RemainingToLivePreview/ExpenseBreakdownPreview en no-op + tests switch expense→income preserve description/amount + exceptional toggle hide FK + happy addExpense + RAV blocking serverError Pattern E + happy addIncome) + EditTransaction (3, transactionType prop fixe, updateExpense({id,...}) single-object signature) + EditBalance (3, allowNegative Pattern A + coerced value + serverError). **UUID lesson critique** : les ID style `'b-1'` rejetés silencieusement par `uuidSchema.nullable()` du discriminated union — addExpense never called, test timeout sans message clair. Fixtures migrées vers UUIDs valides (`'11111111-1111-4111-8111-111111111111'`).
  - **Commit 5** ([`17ef761`](#)) `a11y(forms): aria-describedby + role="alert" + drop unused dashboard var` — **Axe 5** : `aria-describedby` linkant 7 paires input↔error `<p>` sur les 4 auth forms (id-prefixes login-_/signup-_ pour éviter collision SPA) + `role="alert"` sur 10 serverError boxes (4 auth + FirstTimeProfile + EditProfile + CreateGroup + AddTransaction + EditTransaction + EditBalance). 2 RTL regression-guards ajoutés dans forgot-password.test.tsx (aria-describedby + role="alert"). **Axe 6** : drop unused `context` destructure dans `app/dashboard/page.tsx:41` (lint 181 → 180). Skipped : aria-describedby sur les non-auth forms (hors scope, pattern identique pour follow-up) + setFocus(name) sur invalid submit + format:check 4 pre-existing files (.claude/settings.json + doc2/AUDIT-RESOLUTIONS.md + next.config.js + prompt-recap-v7 — drafts user, non scope).
  - **Commit 6** (this) closeout CLAUDE.md §1 + §9 + §11.

  **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / **180 warnings** (vs 181 baseline pre-sprint = −1 Axe 6) ; `pnpm test:run` **267 passed / 64 skipped** (vs 203/64 pré-v5 = **+64 cas non-gated RTL**) ; pas de `pnpm verify` car aucun changement DB. Wall time tests ~10s (vs 3.75s baseline pure-unit only = ~3x slowdown attendu pour 15 .test.tsx files). 0 migration DB, 0 nouveau test gated, 0 nouveau `: any`, counter `as unknown as SupabaseClient` reste à **0**.

  **Patterns RTL standardisés** (à reprendre pour tout nouveau test client) : (a) mock-per-site inline `vi.mock('@/lib/supabase-client', ...)` + `vi.mock('@/hooks/useX', ...)` ; (b) `vi.fn(async (_x: T): Promise<R> => ...)` typed implementation pour les Supabase response shapes ; (c) `userEvent.setup()` + `await user.type/click` over `fireEvent` (fidélité keystroke) sauf pour DecimalFormInput où `fireEvent.change(input, { target: { value: '...' } })` est utilisé pour tester le filtre regex/comma→dot directement ; (d) `await waitFor(() => expect(...))` pour les transitions async (state machines, mutations) ; (e) `screen.getByLabelText` quand `htmlFor` link en place, sinon `getByPlaceholderText` ; (f) UUIDs valides pour les fixtures FK ; (g) mock CustomDropdown en `<select>` pour la testabilité ; (h) `role="alert"` + `aria-describedby` deviennent des contrats testables.

  **Smoke browser deferred to user** (RTL ne remplace pas l'UI) : (a) `/forgot-password` empty email → erreur + a11y announced ; (b) `/reset-password?token=valid` flow happy + invalid token state ; (c) `/dashboard` AddBudget refine balance inline ; (d) AddTransaction switch expense↔income propre + RAV blocking + exceptional toggle ; (e) `<DecimalFormInput>` saisie négative EditBalance ; (f) screen reader (VoiceOver/NVDA) annonce les serverError sur les 4 auth pages.

  **Hors scope explicite** (déjà roadmappés §11) : Playwright/E2E refusé (0 use case), MSW overkill, axe-core auto-audit (peut suivre v5-followup), Sprint Tailwind-v4/Supabase-Strict-Types/Chantier I6/Lot 6 console-cleanup/OpenAPI. Score : ~99.995 → **~99.997/100** (consolidation coverage client + a11y, **fin du chantier Zod 5-sprints** — schemas + RTL + a11y aligned côté client).

- ✅ **Sprint Zod-Rollout v6** ([prompt/prompt-07-deep-dive-zod-rollout-v6.md](prompt/prompt-07-deep-dive-zod-rollout-v6.md), plan dans `C:\Users\gille\.claude\plans\sprint-zod-rollout-v6-elegant-hopcroft.md`) : closeout a11y du chantier Zod-client en 5 commits code + closeout. **Arbitrages user** Phase 1 (4 décisions Recommended) : (Q1) Axe 1+2+3+4+5 full sweep, (Q2) setFocus sur les 14 forms (4 auth + 10 client), (Q3) skip `cleanOnBlur` doc-only, (Q4) 1 commit par axe. **5 commits sur `cleanup`** :
  - **Commit 1** ([`91b9ad9`](#)) `a11y(forms): aria-describedby + DecimalFormInput.ariaDescribedby + 2 htmlFor fix` — **Axe 1** : extension [components/ui/DecimalFormInput.tsx](components/ui/DecimalFormInput.tsx) avec `ariaDescribedby?: string` prop + propagation à `<Input aria-describedby={...}>` + **Risk #1 mitigation upfront** = `ref={field.ref}` + `onBlur={field.onBlur}` propagés au shadcn `<Input>` (sinon `form.setFocus` d'Axe 2 saute les champs DecimalFormInput silencieusement). Sur les **10 forms client** non-auth (FirstTime/EditProfile, CreateGroup, Add/EditIncome, Add/EditBudget, Add/EditTransaction, EditBalance) : `aria-describedby={fieldErrors.X ? '<id-prefix>-error' : undefined}` côté input + `id="<id-prefix>-error"` sur l'`<p>` d'erreur. **ID prefixes propres** : first-name-error, edit-first-name-error, group-name-error, group-budget-error, add-income-{name,amount}-error, edit-income-{name,amount}-error, add-budget-{name,amount}-error, edit-budget-{name,amount}-error, add-transaction-{description,amount,date,fk}-error, edit-transaction-{description,amount,date}-error, balance-error. **Drive-by htmlFor fix** : AddIncomeDialog L131-132 + L168-169 et AddBudgetDialog L146-147 + L183-184 utilisaient `<label className="...">` wrapper sans `htmlFor` — Phase 1 audit a flag (le label n'était associé au champ qu'implicitement). Ajout de `htmlFor="add-income-name"`/`"add-income-amount"`/`"add-budget-name"`/`"add-budget-amount"` + matching `id` sur les inputs. 11 fichiers / +142/−26 LOC. Lint 180 stable, tests 267/64 stable, typecheck OK.
  - **Commit 2** ([`12ef39f`](#)) `a11y(forms): focus first error field on invalid submit (14 forms)` — **Axe 2** : sur les 14 forms, wrap submit en `form.handleSubmit(onValidSubmit, onInvalidSubmit)` avec :

    ```tsx
    const onInvalidSubmit = (errors: FieldErrors<FormType>) => {
      const firstErrorKey = Object.keys(errors)[0]
      if (firstErrorKey) {
        form.setFocus(firstErrorKey as FieldPath<FormType>)
      }
    }
    ```

    **Discriminated union edge case** (AddTransaction + EditTransaction) : `Object.keys(errors)[0]` peut retourner une clé absente du type (e.g. `'expense_date'` côté income branch). Le cast permissif via `as FieldPath<FormType>` est fine — RHF résout le ref au runtime depuis la branche active. **DecimalFormInput dependency** : Risk #1 mitigation déjà en place via Commit 1 — `ref={field.ref}` propagé permet à setFocus de cibler le wrapped `<Input>` correctement. **AddTransactionModal** : interaction avec `useRavValidation.blocked` (Pattern E) — quand `ravValidation.blocked=true`, le `onValidSubmit` fire (pas onInvalid), donc setFocus n'est pas appelé ; le button est aussi `disabled={ravValidation.blocked}` côté UX. Acceptable. 14 fichiers / +192/−28 LOC. Prettier a reformaté les imports multi-ligne en single-line dans 2 fichiers (préservation logique). Lint 180 stable, tests 267/64 stable, typecheck OK.

  - **Commit 3** ([`00c037a`](#)) `test(client): RTL a11y regression-guards for aria-describedby + setFocus` — **Axe 3** : ajoute 3 cas non-gated dans AddBudgetDialog/EditIncomeDialog/AddTransactionModal qui regression-guardent le contrat Axe 1 + Axe 2 simultanément. Pattern miroir forgot-password.test.tsx L42-43+L77 (Sprint v5 Axe 5). Pour chaque test : submit invalide → `await waitFor(() => expect(input).toHaveAttribute('aria-describedby', 'X')` + `'aria-invalid', 'true'`) + `document.getElementById('X').toHaveTextContent(...)` + `expect(input).toHaveFocus()` (Axe 2). Pour AddTransaction la cible est `description` (premier failing field dans le discriminated union). 3 fichiers / +73/−1 LOC. Tests 267 → 270 (+3 non-gated). Lint 180 stable.
  - **Commit 4** ([`d4a28a0`](#)) `refactor(forms): migrate AddIncome+AddBudget name field to shadcn Input` — **Axe 4 bonus** : élimine la divergence raw `<input>` (Add\*) vs shadcn `<Input>` (Edit\*) sur le champ `name` du dashboard. Migration vers shadcn `<Input>` avec `className` overrides préservés (cn() conditionals focus colors green/orange + erreur red border + suffix € via parent wrapper). Adoption des `focus-visible:` idioms shadcn (au lieu de `focus:`). Drive-by : `disabled={isSubmitting}` ajouté (le raw input l'oubliait alors que le bouton submit l'avait). Note : import `Input` ajouté à `@/components/ui/input` dans les 2 fichiers. 2 fichiers / +12/−8 LOC. Tests 8/8 stable, lint 180 stable.
  - **Commit 5** ([`8e49482`](#)) `test(a11y): axe-core automated audit + fix close buttons aria-label` — **Axe 5** : nouveau fichier [components/\_\_tests\_\_/a11y-audit.test.tsx](components/__tests__/a11y-audit.test.tsx) avec 2 cas non-gated couvrant ConnexionPage + AddBudgetDialog via `axe(container).violations`. Install `jest-axe@10.0.0` + `@types/jest-axe@3.5.9` en devDeps. **Pivot du plan initial** : la matcher `toHaveNoViolations` jest-axe est incompatible vitest 4.x (utilise `this.utils` API Jest-spécifique → `TypeError: expectAssertion.call is not a function`). Workaround : utiliser `axe()` directement + `expect(results.violations).toEqual([])` — la diff vitest liste chaque rule fired avec helpUrl, signal suffisant pour diagnostic. **Vraie violation surfacée** au premier run : `button-name` critique sur le close X de AddBudgetDialog (svg-only, no accessible name). Fix appliqué à AddBudgetDialog + AddIncomeDialog (même pattern) : `type="button"` + `aria-label="Fermer"` + `aria-hidden="true"` sur l'`<svg>`. 6 fichiers / +385/−0 LOC. Tests 270 → 272 (+2 non-gated). Lint 180 stable.
  - **Commit 6** (this) closeout CLAUDE.md §1 + §6 + §9 + §11.

  **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / **180 warnings** stable (vs 180 pré-sprint) ; `pnpm test:run` **272 passed / 64 skipped** (vs 267/64 pré-v6 = **+5 cas non-gated** = +3 Axe 3 + +2 Axe 5) ; pas de `pnpm verify` car aucun changement DB. 0 migration DB, 0 nouveau test gated, 0 nouveau `: any`, counter `as unknown as SupabaseClient` reste à **0**.

  **Patterns v6 standardisés** (cf. §6) : (a) `aria-describedby={fieldErrors.X ? 'id-prefix-error' : undefined}` côté input + matching `id` sur l'`<p>` d'erreur — id-prefixes uniques par form pour éviter collisions SPA ; (b) `form.handleSubmit(onValid, onInvalid)` avec `onInvalid` qui appelle `form.setFocus(Object.keys(errors)[0])` ; (c) DecimalFormInput consumer pattern : passer `ariaDescribedby` en plus de `ariaInvalid` ; (d) Tests RTL : `expect(input).toHaveFocus()` pour pin setFocus + `document.getElementById('X').toHaveTextContent(...)` pour pin aria-describedby linkage ; (e) Tests axe-core : `expect(results.violations).toEqual([])` direct (pivot vitest 4.x).

  **Smoke browser deferred to user** : (a) `/dashboard` AddBudget submit empty → focus revient sur name input + lecteur d'écran annonce l'erreur via `aria-describedby` ; (b) `/connexion` email invalide → focus revient sur email automatiquement ; (c) `/reset-password?token=valid` password mismatch → focus sur confirmPassword ; (d) `/dashboard` AddIncome + AddBudget : layout pixel-perfect post shadcn migration (focus vert/orange + erreur red border + suffix € alignment) ; (e) close X buttons sur AddIncome+AddBudget : screen reader doit annoncer "Fermer" au focus.

  **Hors scope explicite** (roadmappés §11) : Playwright/E2E refusé v5, Sprint Tailwind-v4/Supabase-Strict-Types/Chantier I6/Lot 6 console-cleanup/OpenAPI séparés. axe-core extension aux 14 forms par opportunité PR-by-PR.

  Score : ~99.997 → **~99.998/100** (a11y client-side full coverage + setFocus 14 forms + axe-core automated audit + 1 vraie violation fixée — **chantier Zod-client complètement clos** sur 6 sprints).

- ✅ **Sprint Zod-Rollout v7** (plan dans `C:\Users\gille\.claude\plans\sprint-zod-rollout-v7-tranquil-sprout.md`) : extension consolidation a11y post-v6 sur les 2 gaps explicitement laissés en "extension par opportunité PR-by-PR". 3 commits code + closeout sur `cleanup`. **Arbitrages user** Phase 3 (4 décisions Recommended) : (Q1) **Full sweep 9 fichiers** au lieu de 7 du draft ; (Q2) skip francisation shadcn `<DialogContent>` close `<span className="sr-only">Close</span>` (déjà a11y-clean en fr-FR locale via screen reader translation) ; (Q3) Axe 2 full set 5 surfaces (3 auth + 2 modals) ; (Q4) skip Axe 3 format:check des 4 fichiers stale pré-existants (drafts user + tracked stale préservés délibérément).
  - **Phase 1 audit a corrigé le draft plan +5/−3** : trouvé en grep `M6 18L18 6M6 6l12 12` cross-codebase **12 fichiers**, narrowés à **9 candidats actifs** après dédoublonnage des 2 fixés v6 (AddIncome/AddBudget) et exclusion des shadcn `<DialogContent>` callers (EditBalanceModal + EditProfileDialog + FirstTimeProfileDialog — confirmed clean via Read direct sur [components/ui/dialog.tsx:66](components/ui/dialog.tsx) `<span className="sr-only">Close</span>`). Added vs draft : SavingsDistributionDrawer (2 sites L228 drawer principal + L435 nested transfer modal), PlanningDrawer, GroupMembersWithContributionsModal, GroupMembersModal, DeleteGroupModal.
  - **Commit 1** ([`baa6b2b`](#)) `a11y(forms): aria-label + aria-hidden on raw close X buttons (4 files)` — **Axe 1.A** : 4 fichiers utilisant `<button onClick>` raw avec close X SVG (5 sites total) : EditBudgetDialog, EditIncomeDialog, PlanningDrawer, SavingsDistributionDrawer (2 sites). Diff par site : `+type="button" +aria-label="Fermer"` sur le `<button>` + `+aria-hidden="true"` sur le `<svg>`. Pattern miroir v6 Axe 5 (commit `8e49482`). 4 fichiers / +15 LOC. Lint 180 stable, tests 272/64 stable. Lint-staged Prettier + ESLint --fix appliqués automatiquement au commit (zero diff).
  - **Commit 2** ([`a46c6a7`](#)) `a11y(forms): aria-label + aria-hidden on shadcn close X buttons (5 files)` — **Axe 1.B** : 5 fichiers utilisant `<Button variant="ghost">` shadcn : AddTransactionModal, EditTransactionModal, GroupMembersWithContributionsModal, GroupMembersModal, DeleteGroupModal. Diff par site : `+aria-label="Fermer"` prop sur le `<Button>` (shadcn propage à l'underlying `<button>` via Radix Slot) + `+aria-hidden="true"` sur le `<svg>`. `type="button"` non requis — shadcn Button défaut déjà `type="button"`. Drive-by sur Add/EditTransactionModal : pre-existing Prettier collapse de l'import react-hook-form multi-line → single-line (printWidth 100 satisfait), bundled cosmetic. 5 fichiers / +54 / −21 LOC. Lint-staged Prettier a re-collapsed les multi-line `<Button>` blocks → single-line (sous 100 chars). Lint 180 stable.
  - **Commit 3** ([`7235d5b`](#)) `test(a11y): extend axe-core audit to 7 surfaces (3 auth + 2 client modals)` — **Axe 2** : extension [components/\_\_tests\_\_/a11y-audit.test.tsx](components/__tests__/a11y-audit.test.tsx) de 2 → 7 cas. **5 nouvelles surfaces** : ForgotPasswordPage (mock `resetPasswordForEmail` light), ResetPasswordPage (mock `getSession` valid + `updateUser` ; `await waitFor(() => screen.getByLabelText(/nouveau mot de passe/i))` pour le mount useEffect form), InscriptionPage (mock `signUp` light), AddIncomeDialog (controlled component pas de mock requis), AddTransactionModal (**6-hook mock surface** copy depuis [components/dashboard/\_\_tests\_\_/AddTransactionModal.test.tsx](components/dashboard/__tests__/AddTransactionModal.test.tsx) — useBudgets/Incomes/RealExpenses/RealIncomes/ProgressData/FinancialData + useRavValidation + 2 child components no-op + CustomDropdown stub). **Mocks consolidés** : un seul `vi.mock('@/lib/supabase-client', ...)` avec 4 vi.fn (resetPasswordForEmail/getSession/updateUser/signUp) pour les 3 auth pages. **Drive-by mock fix** : CustomDropdown mock `<select>` gain `aria-label={placeholder ?? 'Sélection'}` car axe-core flag `select-name` critical sans accessible name — test-side only, n'affecte pas la prod CustomDropdown (qui a son propre Combobox pattern avec role/aria). Tests **272 → 277** (+5 non-gated). 1 fichier / +175 / −4 LOC.
  - **Commit 4** (this) closeout CLAUDE.md §1 + §9 + §11.
  - **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / **180 warnings stable** ; `pnpm test:run` **277 passed / 64 skipped** (vs 272/64 pré-v7 = +5 axe-core extensions) ; pas de `pnpm verify` car aucun changement DB. 0 migration DB, 0 nouveau test gated, 0 nouveau `: any`, counter `as unknown as SupabaseClient` reste à **0**. Negative greps clean : `Grep "aria-label=\"Fermer\"" components/` retourne désormais **≥11 hits** (2 v6 + 9 v7 + ServiceWorkerRegistration contexte différent). Positive grep : `Grep "axe(container)" components/__tests__/a11y-audit.test.tsx` → 7 hits.
  - **Smoke browser deferred to user** : (a) `/dashboard` ouvrir EditBudget/EditIncome → close X annonce "Fermer" au focus screen reader (VoiceOver/NVDA) ; (b) `/dashboard` PlanningDrawer (icône calendrier) → close X "Fermer" ; (c) SavingsDistributionDrawer → 2 close X (drawer principal + nested transfer modal) ; (d) AddTransaction + EditTransaction close X via shadcn ghost button ; (e) `/groups` GroupMembers + GroupMembersWithContributions + DeleteGroup close X ; (f) Lighthouse a11y score doit rester ≥95.
  - **Trade-off documenté** : v7 ne fait pas avancer le score métier ni la dette technique — pure consolidation a11y. Pattern à reprendre uniquement si signalement screen reader, audit RGAA/WCAG externe, ou Lighthouse a11y <100.
  - **Hors scope explicite** : Axe 3 format:check des 4 fichiers stale (skip user decision) ; francisation shadcn `<DialogContent>` close "Close" → "Fermer" (skip — sr-only fonctionne en fr-FR locale moderne) ; other axe rules (heading hierarchy, color contrast, alt text) — fix as-they-come PR-by-PR. Roadmappés §11 inchangés : Sprint Tailwind-v4 / Supabase-Strict-Types / Chantier I6 / Lot 6 console-cleanup / OpenAPI.
  - Livré 2026-05-13, 3 commits code + closeout sur `cleanup` (`baa6b2b` Axe 1.A / `a46c6a7` Axe 1.B / `7235d5b` Axe 2 / closeout). Score : ~99.998 → **~99.998/100 stable** (consolidation a11y, pas de saut métier).

- ✅ **Sprint Zod-Rollout v8** ([prompt/prompt-07-deep-dive-zod-rollout-v8.md](prompt/prompt-07-deep-dive-zod-rollout-v8.md), plan dans `C:\Users\gille\.claude\plans\sprint-zod-rollout-v8-crystalline-nygaard.md`) : migration **structurale** des modals — 11 modals migrés sur 11 fichiers de raw `<div className="fixed inset-0 ...">` vers `<Dialog>` + `<DialogContent>` Radix via le wrapper shadcn existant ([components/ui/dialog.tsx](components/ui/dialog.tsx) + `@radix-ui/react-dialog@1.1.15`). Native focus trap + Esc-to-close + return-focus + role=dialog + aria-modal acquis sur 11 surfaces. **Arbitrages user Phase 1+3** (5 décisions) : (Q1) **Option A** Full Radix migration via wrapper existant (0 nouveau dep) ; (Q2) **scope élargi mid-sprint** = inclure les 2 drawers (au lieu de défer v9) via heavy `<DialogContent>` className override ; (Q3) **inclure ConfirmationDialog** (+1 surface trouvée Phase 1) ; (Q4) **extension a11y-audit.test.tsx** uniquement (2 cas focus-trap regression-guards Esc-close, pas de nouveau fichier RTL pour les 7 modals sans test) ; (Q5) **Path B DELETE** dead GroupMembersModal (175 LOC, 0 consumer cross-codebase — 6ᵉ application du pattern miroir Lot 5b status-test / Lot 5c testSupabaseConnection / Atomicity-Savings v2 / Dead-Code-Purge ×3). Final scope = **11 modals** (10 centered + 2 drawers - 1 dead).
  - **Commit 1** PoC ([`8289419`](#)) Edit{Budget,Income}Dialog — valide le pattern (`<Dialog open={isOpen} onOpenChange={handleOpenChange}><DialogContent hideCloseButton className="overflow-hidden rounded-2xl border-0 p-0 shadow-xl sm:max-w-md sm:rounded-2xl">`) contre les tests RTL existants (EditBudgetDialog.test.tsx + EditIncomeDialog.test.tsx 8/8 pass). `isOpen?: boolean = true` (optional default true) pour préserver les tests sans changement et le legacy parent pattern `{isOpen && editing && <Modal />}` dans PlanningDrawer. Custom close X v7 préservé via `hideCloseButton={true}`. `<h2>` wrappé en `<DialogTitle asChild>`. `handleOpenChange` gate sur `!isSubmitting`.
  - **Commit 2** ([`c1084ca`](#)) Add{Budget,Income}Dialog pattern miroir Commit 1. `isOpen: boolean` reste required (parents passent explicitement). Drop `if (!isOpen) return null` early-return — Radix gate via `open={isOpen}`. `handleOpenChange` délègue à `handleClose` (form.reset + onClose). Tests 16/16 (4 Add/Edit Budget/Income files).
  - **Commit 3** ([`63a9e32`](#)) {Add,Edit}TransactionModal — 6-hook mock surfaces préservées dans tests existants. `handleOpenChange` délègue à `handleClose` (qui gate déjà sur `!isSubmitting`). Drop `key={editingTransaction.id}` du parent [app/dashboard/page.tsx](app/dashboard/page.tsx) — Radix unmount-on-close + parent onClose nulling editingTransaction remplacent le force-reset Sprint 1.5. `EditTransactionModal` `if (!transaction) return null` préservé (mirror test `renders nothing when transaction prop is null`). 9/9 tests pass (5 Add + 4 Edit).
  - **Commit 4** ([`45530f0`](#)) Groups + ConfirmationDialog + **DELETE GroupMembersModal**. (a) `GroupMembersWithContributionsModal.tsx` — `sm:max-w-2xl` width preserved via className override + `<h2>` in `<DialogTitle asChild>`. (b) `DeleteGroupModal.tsx` — drop `<Card>` wrapper (DialogContent provides bg + shadow + rounded), `handleOpenChange` guards on `!isDeleting`. (c) `ConfirmationDialog.tsx` — `<h3>` in `<DialogTitle asChild>` + `<p>` in `<DialogDescription asChild>` for proper a11y title/description linkage. `handleOpenChange` guards on `!loading`. Behavior change accepted : ConfirmationDialog now closes on outside click + Esc (was not the case before). (d) **DELETE** components/groups/GroupMembersModal.tsx (175 LOC, 0 consumer cross-codebase via Phase 1 audit grep).
  - **Commit 5** ([`653ebe0`](#)) Drawers — PlanningDrawer + SavingsDistributionDrawer via **heavy className override** sur `<DialogContent>` : fullscreen (`inset-0 h-screen w-screen max-h-screen max-w-none translate-x-0 translate-y-0 sm:inset-0 sm:max-w-none sm:translate-x-0`) + drop chrome (`rounded-none border-0 p-0 shadow-none sm:rounded-none gap-0`) + flex-col bg-white + Tailwind slide-from-bottom animations (`data-[state=open]:slide-in-from-bottom data-[state=closed]:slide-out-to-bottom data-[state=*]:zoom-{in,out}-100 duration-300`). `tailwind-merge` via `cn()` résout les conflits avec centered-modal defaults. Lazy-loaded child modals (Add/Edit Budget/Income, ConfirmationDialog) déjà Radix ; Radix natively supporte nested via Portal stacking. SavingsDistributionDrawer nested transfer modal migré en parallèle (centered pattern, `Dialog open={isTransferModalOpen && !!selectedFromBudget}` + `handleTransferModalOpenChange` gate on `!isProcessing`). 3 `selectedFromBudget.X` → `selectedFromBudget?.X` parce que conditional render wrapper droppé (TS narrowing limit).
  - **Commit 6** (this) focus-trap RTL regression-guards + closeout. [components/\_\_tests\_\_/a11y-audit.test.tsx](components/__tests__/a11y-audit.test.tsx) étendu avec 2 cas non-gated : EditBudgetDialog Esc-close + AddTransactionModal Esc-close (pattern `userEvent.keyboard('{Escape}')` + `expect(onClose).toHaveBeenCalled()`). Pin le contrat Radix Dialog → onOpenChange → onClose. Si une future migration revient à raw `<div>`, ces tests catch.
  - **Verif end-to-end** : `pnpm typecheck` exit 0 à chaque commit ; `pnpm lint:check` 0 errors / **180 warnings stable** (vs 180 pré-sprint) ; `pnpm test:run` **279 passed / 64 skipped** (vs 277/64 pré-v8 = +2 focus-trap regression-guards). 0 migration DB. 0 nouveau test gated. 0 nouveau `: any`. Counter `as unknown as SupabaseClient` reste à **0**. Positive grep `Grep "from '@/components/ui/dialog'" components/` retourne désormais ≥15 hits (3 pre-v8 + 11 nouveaux + 1 wrapper file). Negative grep `Grep "class[Nn]ame=\"fixed inset-0" components/dashboard/ components/groups/ components/ui/ConfirmationDialog.tsx` → 0 hit (12 fichiers migrés ou supprimés).
  - **Smoke browser deferred to user** (RTL ne remplace pas l'UI keyboard tests complets) : (a) `/dashboard` ouvrir Add Budget / Edit Budget / Add Income / Edit Income → Tab cycle borné au modal, Esc close, focus revient sur bouton trigger ; (b) AddTransaction submit invalide → focus sur description (Pattern setFocus v6 préservé) + Esc close ; (c) EditTransaction modifier amount, switch entre transactions → defaultValues correctement réinitialisés (parent perd `key={id}`, Radix gère unmount) ; (d) `/groups` GroupMembers + Delete Group confirm → Tab cycle, Esc close ; (e) `/dashboard` PlanningDrawer → slide-in bottom-up → ouvrir AddBudget enfant → Tab confiné enfant → Esc ferme enfant only → Esc ferme drawer ; (f) SavingsDistributionDrawer → cliquer Transférer → nested transfer modal centered ouvre → Tab confiné nested → Esc ferme nested only → drawer reste ouvert ; (g) Lecteur d'écran : modal opening annonce `role="dialog"` + lecture DialogTitle + background announcements muted.
  - **Trade-offs documentés** : (1) backdrop slightly darker (`bg-black/80` Radix vs `bg-black bg-opacity-50` actuel) ; (2) animation Tailwind slide-from-bottom (via `tailwindcss-animate`) vs translate-y custom — visuellement équivalent ; (3) pas de drag-to-dismiss (vaul aurait fourni, déferré si signalement) ; (4) outside-click closes drawers (Radix default) — cohérent autres modals v8 ; (5) v8 ne fait pas avancer le score métier — pure consolidation a11y keyboard-only + structural Radix migration.
  - **Pattern miroir** : v6 a fixé les attributs HTML des boutons (close X aria-label) ; v7 a étendu sur 9 fichiers / 11 sites + axe-core 2→7 surfaces ; v8 ferme la **structure** des 11 modals (focus trap + Esc + return focus + role/aria-modal nativement). Ensemble v6+v7+v8 closent le chantier a11y modal complet — rien de déféré post-v8.
  - **Hors scope explicite** : Sprint Tailwind-v4 / Supabase-Strict-Types / Chantier I6 / Lot 6 console-cleanup / OpenAPI / Error boundaries / Sentry / i18n — roadmap §11 inchangée.
  - Livré 2026-05-14, 5 commits code + closeout sur `cleanup` (`8289419` PoC / `c1084ca` Add forms / `63a9e32` Transactions + drop key / `45530f0` Groups + Confirm + DELETE dead / `653ebe0` Drawers + nested / closeout). Score : ~99.998 → **~99.999/100** (chantier a11y modal complètement clos, gain keyboard-only users + structural Radix migration).
