# Précédents Sprint — chronologie résumée (Part 2)

> Suite de [sprint-chronology.md](sprint-chronology.md) (Part 1 saturée 2026-05-22, cap 39.5k atteint au sprint Fix-Auth-Flicker-And-Recap-Reentry-Gate). Split chronologique préemptif au sprint 11 Monthly Recap V3.
>
> Append-only : 1 nouvelle ligne par sprint installant un pattern réutilisable. Granularité différente des `roadmap-detailed-NN-*.md` (verbatim closeouts). Ici : 1 row = 1 invariant/pattern actionable.

## Sprints installant un pattern réutilisable

| Sprint                            | Date       | Pattern installé                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Référence §11 |
| --------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| Sprint Screens-Welcome-Summary-V3 | 2026-05-23 | Endpoint générique `POST /api/monthly-recap/advance-step` (body `{ context, fromStep, toStep }`) + helper pure `executeAdvanceStep` (UPDATE race-safe `WHERE current_step = fromStep` + check 0 rows ⇒ `stale_step`) + 2 mutations TanStack `useStartRecap`/`useAdvanceStep` (idempotence via discriminant RPC `'resumed'`). 3 composants UI réutilisables : `<BilanBlock>` (3 variantes), `<SurplusDetailDrawer>` (orange) + `<SavingsDetailDrawer>` (violet) drawers indicatifs lecture-seule. Helper `formatEuro` (2 décimales, [lib/format-currency.ts](../../lib/format-currency.ts)) cohabite avec `formatCurrency` (0 décimales). Color theming SummaryStep accent `bank/neutral/budget/savings` (border-l-4 + amount text, discret). Règle re-entrée wizard "retour EXACT step" persistée via mémoire `feedback_recap_exact_reentry`. Routes API 37 → 38. Tests 459 → 505 non-gated + 158 → 164 gated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | CLAUDE.md §11 |
| Sprint Fix-Recap-Bilan-Formula    | 2026-05-24 | **Correction critique formule** : `bilan = ravEffectif - ravEstime` (SOUSTRACTION, pas addition). Bug introduit sprint 04 dans `computeRecapSummary` ([lib/recap/calculations.ts](../../lib/recap/calculations.ts) ligne 51), silencieux côté tests (re-engineered autour de la mauvaise formule) mais visible au smoke UI sprint 11. Pattern : pour formule métier critique, écrire au moins 1 cas test EN VALEURS PROVENANT DE LA SPEC (pas dérivé via `inputProducingExpected`). Cascade 6 fichiers spec mis à jour (types comment, tests, 2 fichiers prompt-spec). History part-18 laissée verbatim per size-policy §7. Bundled avec color theming Summary screen (orange budget / violet savings / sky-blue solde / slate RAVs — accents border-l-4 discrets) + headers drawers re-teintés (orange + violet pleins, icônes circulaires colorées). Tests 505 stables (4 cas calc re-engineered, intention préservée).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | CLAUDE.md §11 |
| Sprint Screen-Bilan-Positive-V3   | 2026-05-24 | Écran 3A du wizard (cas `bilanSign ∈ {'positive', 'zero'}`). 2 mutations TanStack ajoutées dans `useMonthlyRecap` : `useTransferSurplusesToPiggy` (response inclut `summary` fresh → `setQueryData` zéro round-trip) + `useTransformRemainingSurplusesToSavings` (response sans summary → `invalidateQueries`, le serveur avance `current_step → 'salary_update'` lui-même). `<SurplusSelectionDrawer>` interactif multi-sélect tap-row (`<button aria-pressed>` plein-largeur, sans dep `@radix-ui/react-checkbox`, mobile-friendlier qu'une case 20px) + selection reset via **adjust-state-on-prop-change pattern** (`if (prop !== prev) { setPrev(prop); setX(...) }` — évite `react-hooks/set-state-in-effect`). UX rules : **bouton trigger persistant à côté du drawer** (vs state-machine Yes/No qui trappait le user si fermeture par mégarde) + **`finally` close-drawer-on-error** (Radix Dialog `aria-hidden` sur siblings → alert invisible derrière drawer si `setDrawerOpen(false)` seulement on success). Charte couleur étendue : `violet` = destination tirelire (bouton Répartir, drawer header + selected rows + amounts, cohérent avec SavingsDetailDrawer), `green` = transformation indicative. `tabular-nums` pour alignement vertical des colonnes financières. Tests 505 → 529 non-gated. Routes API inchangées (endpoints transfer/transform livrés sprint 06).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | CLAUDE.md §11 |
| Sprint Screen-Bilan-Negative-V3   | 2026-05-24 | Écran 3B du wizard (cas `bilanSign === 'negative'`). **Cascade séquentielle** 3 lignes (tirelire → économies des budgets → équilibrage proportionnel snapshot), 1 seule active à la fois ; gating logique ordre `done > empty > deficitCovered (unneeded) > locked > active` — un déficit comblé en amont neutralise les lignes suivantes (état `unneeded` au lieu de `locked`). **Pas de page change** post-mutation : 3 nouveaux hooks `useRefloatFromPiggy` + `useRefloatFromSavings` + `useSaveBudgetSnapshot` updatent le cache via `setQueryData` (piggy/savings patchent `summary` + `recap.refloated*` ; snapshot patche `recap.snapshotData` mais ne mirror PAS l'auto-advance server-side de `current_step → salary_update` — le bouton Continuer en bas drive l'advance final). Pas de bascule positive (la tirelire qui couvre le déficit + residual reste sur cet écran avec récap "X utilisée, il reste Y"). Snackbar succès inline `fixed bottom z-[60] animate-in slide-in-from-bottom-4` auto-dismiss 3s + `role="status"`. **Pure helper `computeDeficitRemaining` extrait** dans [lib/recap/deficit-math.ts](../../lib/recap/deficit-math.ts) (client-safe ; le module `actions-negative.ts` importe `supabaseServer` donc la barrel `@/lib/recap` ne peut pas être consommée en value-import côté browser — leçon : **toujours importer la valeur depuis le module pur direct, garder la barrel pour `import type` only**). Status endpoint étendu avec sibling `recap: { id, currentStep, refloatedFromPiggy, refloatedFromSavings, snapshotData } \| null` pour exposer les trackers à l'UI (recalcul live du déficit côté client, survit aux refreshs). `BudgetSummary.carryoverSpentAmount` ajouté (SELECT load-summary + propagation calc). Charte UI **tirelire = violet, économies = violet (même famille), budgets = orange, deficit = red, surplus/succès = green, locked/empty/unneeded = `bg-white`** (contraste vs fond bleu gradient page wizard). Preview lists en layout **2-lignes** par budget (nom + delta sur la 1re, before → after sur la 2e) — évite la troncature sur mobile 430px. 4 scripts dev User B (`profile-b-deficit-{piggy-bascule,savings-cover,cascade,snapshot-only}.mjs`) + helper `_profile-b-lib.mjs` (cleanupForB + printForB). Tests 529 → 566 non-gated (29 RTL nouveaux sur 4 composants + 9 pure deficit-math + fixture updates). Routes API inchangées (endpoints refloat livrés sprint 07).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | CLAUDE.md §11 |
| Sprint Screens-Salary-Final-V3    | 2026-05-25 | Écrans 4 (salary update optionnel) + 5 (final recap) du wizard Monthly Recap V3 — closing the wizard UI loop. 3 nouveaux composants (`SalaryUpdateStep` Yes/No + form profile OR group, `GroupMemberSalaryForm` qui consomme `useGroupContributions` au lieu d'un nouvel endpoint, `FinalRecapStep` avec 3 cas pos/nég/cascade + breakdown par source en charte couleur sprint 13). 2 nouvelles mutations `useUpdateSalaries` + `useCompleteRecap` (cette dernière idempotente — `{ alreadyCompleted: true }` traité comme succès). **`useMonthlyRecap` accepte `{ enabled }`** : pattern conditional fetch propre via TanStack Query — utilisé par `RecapWizard` pour peek le statut du recap groupe seulement quand `context === 'profile'` ET `profile.group_id != null`. **Chain redirect profile→group** sur écran 5 : si le user finit son recap perso et que son groupe n'a pas encore commencé son propre recap, le bouton devient "Aller au recap du groupe « X »" et la redirection post-finalize cible `/monthly-recap?context=group` au lieu de `/dashboard` — close le gap du proxy gating qui ne checke que le contexte navigué. **`useAdvanceStep.onError` invalidate sur `stale_step`** : fix de la race entre auto-advance server-side (snapshot save sprint 13, update-salaries sprint 14) et l'advance explicite client — sans ce filet, BilanNegativeStep restait stuck sur step 3B jusqu'à refresh manuel. **`RecapShell` accepte `headerLabel`** : pill centrée nuances de gris (`border-gray-300 bg-gray-50 text-gray-700 rounded-full`) au top de toutes les branches de la shell, identifie "Recap de \<prénom\>" (profile) ou "Recap du groupe « \<name\> »" (group). Première itération en teal — refusée par user pour ne pas multiplier les couleurs sémantiques. **`RecapRedirecting` helper** : Loader2 centré orange + copy, remplace le précédent texte mono-ligne sur les branches `status.kind === 'completed'` et `step === 'completed'`. **Explicit `calculate_group_contributions` pour profile context** : `lib/recap/actions-salary.ts` n'attend plus le trigger DB `profiles_contribution_recalc` — appel RPC explicite dès que `args.profile.group_id` non null (fix smoke-test où le trigger ne propageait pas). **bg-white sur les inputs salary** : override le default `bg-transparent` du shadcn Input qui blendait avec le fond du parent. **Lift `salaryUpdated` state au wizard** : flag transient propagé via callback à SalaryUpdateStep + prop à FinalRecapStep — refresh perd le flag (trade-off accepté, pas de tracking serveur). Tests 566 → 599 non-gated (+33 cas : 9 SalaryUpdate + 6 GroupMemberSalaryForm + 12 FinalRecap + 6 RecapWizard redirect targets). Routes API inchangées (les 3 endpoints `/update-salaries`, `/advance-step`, `/complete` étaient livrés sprint 08). 1 nouveau seed script `chain-profile-done-group-pending.mjs` pour smoke-tester le chain redirect. | CLAUDE.md §11 |
| Sprint Carry-Over-UI-V3           | 2026-05-25 | Sprint 15/17 Monthly Recap V3 — ferme le gap dashboard pour les transactions reportées (carried-over) par `process_recap_transactions` au mois précédent. 3 RPCs atomiques composites ([20260527000000_create_carry_over_rpcs.sql](../../supabase/migrations/20260527000000_create_carry_over_rpcs.sql)) : `toggle_carry_over_and_apply{,_income}(id, p_validate)` **bidirectionnel** (flip is_carried_over + applied_to_balance_at + bank_balance delta, `carried_from_recap_id` PRÉSERVÉ comme mémoire pour le retour arrière), `delete_carried_expense_to_piggy(id)` (DELETE row + crédit tirelire en 1 tx, inline `INSERT INTO piggy_bank … EXCEPTION WHEN unique_violation` pour fresh accounts). EXPECTED_RPCS 16 → 19, functions versionnées 23/23 → 26/26. 2 routes API nouvelles `POST /api/finance/{expenses,income}/real/toggle-carry-applied` (routes 38 → 40). **Auto-détection serveur DELETE** (Option A choisie user) : le handler DELETE lit `is_carried_over` et route vers `delete_carried_expense_to_piggy` si true, sinon procédure normale — le client envoie un DELETE classique, pas de signal. **Filter `is_carried_over=false` sur 6 sites de calcul** (financial-data, income-compensation, budget-savings-detail, expenses-progress, income-progress, budgets-estimated) — règle stricte : filtrer sur CALCULS, JAMAIS sur GET listing UI. UI `TransactionListItem` : **badge gris "Mois précédent"** (`bg-gray-100 text-gray-700 border-gray-200`) — gris choisi vs ambre du prompt initial pour éviter conflit avec orange réservé aux budgets, cohérent avec pill RecapShell sprint 14. Background `bg-gray-50` quand carried. **Long-press bidirectionnel** routé selon `carried_from_recap_id` (carry-over) vs comportement classique. **Dropdown adapté** : "Valider et appliquer au solde" / "Dévalider (remettre en attente)" + "Supprimer (renvoyer en tirelire)" pour expense carry-over. **"Modifier" ABSENT pour state A** (follow-up user feedback : "carry-overs ne peuvent qu'être validés ou supprimés"). **Défense en profondeur API** : PUT `/expenses/real` + PUT `/income/real` retournent 409 `cannot-edit-carried-transaction` si `is_carried_over=true` (pattern miroir guard 409 DELETE applied). Optional `onToggleCarryApplied` prop pour compat tests RTL legacy. Hooks `useRealExpenses`/`useRealIncomes` étendus : interface + `toggleCarryAppliedMutation` (optimistic flip is_carried_over + applied_to_balance_at). Snackbar wording distinct "validée"/"dévalidée — retour en attente" vs "appliqué"/"retiré du solde". Tests 599 → 611 non-gated (+12 RTL) ; 164 → 177 gated (+8 RPC carry-over + 3 financial-data filter + 2 misc). 2 commits incrémentaux.                                                                                                                                                                                                                      | CLAUDE.md §11 |

| Sprint Read-Only-Virtual-Rows-V3 | 2026-05-28 | Sprint 16/17 Monthly Recap V3 — virtual rows read-only pour salaire perso et contribution groupe dans la liste des revenus estimés du drawer Planification. `FinancialData.meta.readOnlyIncomes: ReadOnlyIncome[]` populé côté serveur dans `_loadFinancialData` (sans toucher `totalEstimatedIncome` qui alimente `ravEstime` du recap). Itération all-members (commit `6d14b4f`) : `getGroupFinancialData(groupId)` expose 1 ligne par membre triée par prénom (`Contribution de <prénom>`). **Salary-pool ceiling fix** (commit `5f8bfa8`) : `meta.groupSalaryTotal` débloque le bootstrap d'un groupe vide (la validation budget passe de `sum(contributions)` circulaire à `sum(salaries) + group estimated_incomes`). Tests 611 → 618 non-gated + 177 → 181 gated. | CLAUDE.md §11 |
| Sprint Contribution-Expense-V3 | 2026-05-28 | Dépense virtuelle "Contribution au groupe XXX" auto-managée par triggers DB sur le dashboard perso de chaque membre. **4 nouvelles migrations** : (1) colonnes `real_expenses.contribution_id` (FK group_contributions CASCADE) + `last_applied_amount` ; (2) trigger `sync_contribution_real_expense` AFTER INSERT/UPDATE on group_contributions (UPSERT row miroir avec `is_exceptional=true`) + trigger `credit_balance_on_contribution_delete` BEFORE DELETE on real_expenses (restitution solde quand quitte le groupe) ; (3) toggle RPC V2 (3 branches : apply standard, apply drift re-validate, un-apply) + `last_applied_amount` snapshot tracked ; (4) trigger v2 auto-devalidate (commit `31a8ed5`) — change contribution alors qu'applied → crédite solde + nullify applied_at + PRÉSERVE last_applied pour le delta UI. **UI** : `TransactionListItem` mode contribution row — catégorie gris, kebab masqué, bloc warning in-card orange avec 2 messages (jamais validée ou drift `vous devez ajouter\|retirer X€ au groupe avant de valider cette dépense`). **`TransactionTabsComponent`** : float-to-top via useMemo. **Backend** : PUT/DELETE 409 guards `cannot-edit-contribution-row` / `cannot-delete-contribution-row`. **`invalidateFinancialRefreshes` extended à 7 keys** (commit `1aabbd8`) : ajout `['real-expenses']` + `['bank-balance']` pour propager les mutations group → perso. Functions versionnées 26 → 28. Tests 618 → 626 non-gated + 181 → 187 gated (+5 trigger + 7 RTL + 1 query-client). 6 commits incrémentaux. | CLAUDE.md §11 |
| Sprint Salary-Edit-Gating | 2026-05-25 | **Édition du salaire restreinte à 2 voies** : (1) fin du recap mensuel via wizard sprint 08 V3 (`POST /api/monthly-recap/update-salaries`, inchangé) ; (2) planificateur vierge (aucune ligne dans `estimated_budgets`/`_incomes`/`real_expenses`/`real_income_entries`, carry-overs inclus). Hors ces 2 cas, le champ salaire de `ProfileSettingsCard` est read-only avec helper "Modifiable à la fin de ton recap mensuel, ou quand ton planificateur est complètement vide" + icône Lucide `Lock`. Scope variable selon `profile.group_id` : solo = check perso seul, groupe = check perso ET groupe ensemble (évite la recalcul de contribution sans notification du co-équipier). **Helper [lib/finance/planner-emptiness.ts](../../lib/finance/planner-emptiness.ts)** : `isPlannerEmpty(scope)` (4 SELECT COUNT en parallèle, `.or('profile_id.eq.X,group_id.eq.Y')` pour le scope group) + `canEditSalary(profile)` → `{ editable, reason }`. **Endpoint nouveau** : `GET /api/profile/salary-editability` via `withAuthAndProfile` (routes API 40 → 41). **Server-side enforcement** PUT `/api/profile` : 409 `cannot-edit-salary-when-planner-not-empty` si `body.salary` change et planner non-vierge ; condition au changement effectif uniquement, autres champs (prénom/nom/avatar) toujours acceptés. **Hook `useSalaryEditability`** consomme l'endpoint via `useQuery`. **`invalidateFinancialRefreshes` extended à 8 keys** : ajout `['salary-editability']` → toute mutation budget/income/expense refetch la décision serveur sans plumbing dans les hooks consommateurs. **Pattern installé** : "feature-gating server-driven via GET helper + cross-domain invalidation TanStack". Tests 626 → 631 non-gated (+5 RTL ProfileSettingsCard) ; 187 → 198 gated (+11 planner-emptiness). 0 migration DB. 1 commit. | CLAUDE.md §11 |
| Sprint Fix-Recap-Welcome-Skip | 2026-05-31 | **Bug "étape sautée" du wizard Monthly Recap V3** : la RPC `start_monthly_recap` ([20260525000000_create_recap_start_rpc.sql](../../supabase/migrations/20260525000000_create_recap_start_rpc.sql) ligne 72) hardcodait `current_step = 'summary'` à l'INSERT initial → mismatch avec l'attente client `welcome → complete_month` → `executeAdvanceStep` renvoyait `stale_step` 409 → swallow du Sprint Flicker-Fix masquait l'erreur → wizard re-fetch status `'summary'` → écran `CompleteMonthStep` jamais affiché. Bug introduit Sprint Complete-Month-Step (2026-05-29) qui a inséré `'welcome'` puis `'complete_month'` server-persisted sans rebaser ligne 72. Fix **in-place** (branche `monthly_recap` encore feature, non shippée prod) : literal `'summary'` → `'welcome'` + re-application via `apply-sql.mjs` (idempotent grâce au `DROP FUNCTION IF EXISTS` + `CREATE OR REPLACE` ligne 35-37). Test intégration ligne 173 mis à jour. **Pattern installé** : "step initial RPC = `RECAP_STEP_ORDER[0]` ('welcome'), JAMAIS un step ultérieur hardcodé — sinon le swallow `stale_step` masque le mismatch et l'étape suivante est silencieusement sautée". 0 nouveau test ni nouveau fichier — 1 SQL + 1 assert TS. | CLAUDE.md §11 |

Pour la chronologie complète des 124 sprints, voir CLAUDE.md §11 (index des 26 parts `.claude/history/roadmap-detailed-NN-*.md`).
