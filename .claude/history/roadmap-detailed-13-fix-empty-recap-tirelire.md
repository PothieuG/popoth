# Roadmap détaillée — Part 13/13 : Fix-Empty-Recap-Tirelire + Group-Budget-Auto-Sync + Hygiene-Next-16-Migration + Lazy-Mount-GroupManagementPanel + GroupMembersModal-Show-Budget-Share-Pct + Drawer-Slide-Fix-And-Header-Harmonize

> Créée 2026-05-19 — Part 12 ayant déjà franchi le plafond 38k (cf. [@.claude/guardrails/size-policy.md](../guardrails/size-policy.md)), le sprint suivant ouvre une nouvelle part.
> **Étendue** : Sprint Fix-Empty-Recap-Tirelire + Sprint Group-Budget-Auto-Sync + Sprint Hygiene-Next-16-Migration + Sprint Lazy-Mount-GroupManagementPanel + Sprint GroupMembersModal-Show-Budget-Share-Pct + Sprint Drawer-Slide-Fix-And-Header-Harmonize (6 sprints livrés).
> Pour l'index complet et le résumé court, voir CLAUDE.md §11.
> Navigation : [← Part précédente](roadmap-detailed-12-cas3-to-refactor-recover.md) | (pas de partie suivante)

---

## 11. Roadmap — Part 13/13

- ✅ **Sprint Fix-Empty-Recap-Tirelire** (livré 2026-05-19, déclenché par bug report user "0 dans mes valeurs, je ne peux pas continuer") : ferme un crash bloquant le monthly-recap Step 1 pour tout utilisateur sans ligne `piggy_bank` (nouveau compte, jamais accumulé de tirelire). **Erreur reportée user** : `Erreur lors du rééquilibrage: Erreur récupération tirelire: Cannot coerce the result to a single JSON object` au clic "Continuer" Step 1. **Root cause** : [lib/recap/step1-persist.ts:148](../../lib/recap/step1-persist.ts) `loadSnapshot` utilisait `.single()` sur `piggy_bank` qui RAISE `PGRST116` quand 0 rows existent. Le `if (piggyBankError) throw` propage l'erreur côté `handleStep1Next` (`MonthlyRecapFlow.tsx`) → alert frontend, navigation Step 2 bloquée. **Même la lecture corrigée ne suffit pas** : la RPC `update_piggy_bank_amount` ([supabase/migrations/20260506000000_create_finance_rpcs.sql:42-44](../../supabase/migrations/20260506000000_create_finance_rpcs.sql)) RAISE `'piggy_bank row not found for the given context'` quand son UPDATE affecte 0 rows. Sprint Refactor-I5 (2026-05-11) avait extrait la logique algorithme/persist mais préservé verbatim la lecture `.single()` héritée de la route god-file 740 LOC originale — le pattern n'avait jamais été stressé par un compte fresh sans piggy en local dev (les tests gated `SUPABASE_FINANCE_TESTS=1` créent toujours une ligne piggy via `resetUserFinancialState` fixture).

  **Pattern installé** : helper `ensurePiggyBankRow(filter)` idempotent (INSERT `amount=0`, ignore PG unique_violation `23505` via partial unique indexes `idx_piggy_bank_profile_id_unique` + `idx_piggy_bank_group_id_unique` cf. [supabase/migrations/20260508000000_add_piggy_bank_indexes.sql](../../supabase/migrations/20260508000000_add_piggy_bank_indexes.sql)). Appelé AVANT chaque `updatePiggyBank()` RPC dans `applyDecision`. Race-safe car la partial unique index empêche les doublons sous concurrence ; le 23505 catch est cosmétique en single-user mais correct sous double-invocation (fast retry network, devtools curl).

  **5 fichiers modifiés** (1 commit unique sur `cleanup`) :
  - [lib/finance/piggy-bank.ts](../../lib/finance/piggy-bank.ts) — nouveau export `ensurePiggyBankRow(filter: ContextFilter): Promise<void>` (+26 LOC). Import `Database` type-only pour `PiggyBankInsert`. Build payload conditionnel via `resolveContextIds` narrowing : `profile_id !== undefined ? { profile_id, group_id: null, amount: 0 } : { profile_id: null, group_id: group_id!, amount: 0 }` pour satisfaire la contrainte `piggy_bank_owner_exclusive_check` (XOR profile_id/group_id, cf. [supabase/migrations/20260508000001_add_piggy_bank_constraints.sql](../../supabase/migrations/20260508000001_add_piggy_bank_constraints.sql)). Pattern miroir [app/api/monthly-recap/accumulate-piggy-bank/route.ts](../../app/api/monthly-recap/accumulate-piggy-bank/route.ts) qui faisait déjà du INSERT-if-missing manuel.
  - [lib/recap/step1-persist.ts](../../lib/recap/step1-persist.ts) — 3 modifs : (1) `import { ensurePiggyBankRow, updatePiggyBank } from '@/lib/finance/piggy-bank'`, (2) `loadSnapshot` ligne 148 `.single()` → `.maybeSingle()` + commentaire JSDoc 4 lignes expliquant le contrat `{data:null, error:null}` quand pas de row, (3) 2 sites d'appel `ensurePiggyBankRow(filter)` dans `applyDecision` : avant la boucle CAS 1 si au moins une op `step==='1.1' && type==='excedent_to_piggy_bank'` (guard via `decision.operations.some(...)`), et avant l'UPDATE 2.4.1 dans le `try { await updatePiggyBank(filter, newDifference) }` CAS 2.
  - [app/api/monthly-recap/step1-data/route.ts](../../app/api/monthly-recap/step1-data/route.ts) + [app/api/monthly-recap/balance/route.ts](../../app/api/monthly-recap/balance/route.ts) — `.single()` → `.maybeSingle()` cosmétique. Pre-fix l'erreur était déjà swallow par `const { data: piggyBank } = ...` (destructuring sans `error:` binding) mais Supabase loggait l'erreur en interne. Post-fix : aucune erreur émise quand pas de row, défaut `piggyBank?.amount || 0` continue de fonctionner identiquement. La route `balance/` est dead code depuis Sprint Refactor-I5 (UI appelle `process-step1` à la place), mais alignée pour cohérence.
  - [lib/recap/**tests**/step1-persist.test.ts](../../lib/recap/__tests__/step1-persist.test.ts) — 3 modifs : (1) chain mock étendu avec `chain.maybeSingle = single` (réutilise le même `vi.fn()` que `chain.single` pour back-compat avec les 6 cas `mockResolvedValueOnce` existants), (2) `vi.mock('@/lib/finance/piggy-bank')` étendu avec `ensurePiggyBankRow: vi.fn(async () => undefined)`, (3) 2 nouveaux assertions : régression test `loadSnapshot` "piggy_bank row absent (maybeSingle returns null/null) → piggyBank defaults to 0, no throw" + assertion ordre via `mock.invocationCallOrder` sur le test existant "1.1 piggy push" garantissant `ensurePiggyBankRow` fire AVANT `updatePiggyBank`. Type inline étendu `{ updatePiggyBank: ..., ensurePiggyBankRow: ReturnType<typeof vi.fn> }` pour le `await import` cast.

  **Verif end-to-end** : `pnpm typecheck` exit 0 (1 erreur transitoire `noUncheckedIndexedAccess` sur `mock.invocationCallOrder[0]` corrigée via narrowing `if (x === undefined) throw`) ; `pnpm lint:check` 0 errors / 0 warnings stable ; `pnpm format:check` exit 0 ; `pnpm test:run` **494 passed / 89 skipped** (+1 vs 493 baseline pre-sprint). 0 migration DB. 0 nouvelle RPC (`ensurePiggyBankRow` est un INSERT direct via supabaseServer, pas de SECURITY DEFINER fn). 0 nouveau `: any`. Counter `as unknown as SupabaseClient` reste à 0. Counter `declare global` reste à 0.

  **Smoke browser deferred to user** : flow `/monthly-recap` Step 1 sur compte test fresh (jamais utilisé, 0 budget / 0 income / 0 piggy) → clic "Continuer" doit naviguer Step 2 sans erreur. Vérifier aussi compte avec piggy déjà créée pour confirmer non-régression (CAS 1 excédent → piggy + 1 = piggy avant + N).

  **Pattern à retenir** [@.claude/conventions/operational-rules.md](../conventions/operational-rules.md) §5 : pour toute table hybride à 1-row-par-owner (`piggy_bank`, `bank_balances`), `.single()` est un piège — utiliser `.maybeSingle()` + helper `ensure<Table>Row()` avant tout RPC UPDATE qui RAISE sur 0 rows. Le test gated `SUPABASE_FINANCE_TESTS=1` n'a pas surfacé ce bug car ses fixtures créent toujours une ligne piggy. **Améliorer la coverage** (déféré sprint ultérieur `Recap-Bootstrap-Tests`) : ajouter un test gated qui supprime explicitement la ligne piggy via fixture `cleanupPiggyBankRow(userId)` avant de lancer `process-step1`, vérifie le 200 + `case='excedent'` + ligne piggy créée a posteriori avec `amount=0` ou `amount=excedent`.

  **Trade-off** : Sprint chirurgical 1-commit déclenché par bug user. Pas de saut métier observable côté UX au-delà de "le flow ne crash plus pour les nouveaux comptes". Ferme une dette héritée de la god-file pré-Refactor-I5 (le `.single()` était déjà là avant l'extraction, juste invisible derrière le god-file 740 LOC). Score reste **~100 stable**.

- ✅ **Sprint Group-Budget-Auto-Sync** (livré 2026-05-19, déclenché par bug report user "ma contribution ne bouge pas quand le groupe crée un budget") : ferme un bug où la contribution salariale d'un membre de groupe n'était jamais recalculée à la création/modification/suppression d'un item de budget de groupe (`estimated_budgets` avec `group_id IS NOT NULL`). **Root cause double** : (1) le trigger `trigger_group_budget_change` ne fire que sur UPDATE de `groups.monthly_budget_estimate`, jamais déclenché par un changement dans `estimated_budgets` ; (2) `groups.monthly_budget_estimate` était un nombre saisi manuellement au `CreateGroupForm` + éditable via `PUT /api/groups/[id]`, déconnecté de la somme réelle des items du groupe. L'intention métier utilisateur est que **le budget total du groupe = SUM(estimated_budgets WHERE group_id = X)** — la contribution doit suivre ce total automatiquement.

  **Décision architecturale** : `groups.monthly_budget_estimate` reste en DB (lu par `calculate_group_contributions` + affiché par l'UI) mais devient un **mirror auto-maintenu** par un nouveau trigger sur `estimated_budgets`. La saisie manuelle est retirée côté API (POST /api/groups + PUT /api/groups/[id]) et côté UI (CreateGroupForm). Le hook custom `useGroupContributions` (useState + AbortController, dette tech connue depuis Sprint 1.5 cf. doc2/features/group-contributions.md §11) est migré vers TanStack Query — la cascade UI passe par `invalidateFinancialRefreshes` (étendu de 3 → 4 query keys) sans plumbing manuel cross-hook.

  **Cascade DB** : INSERT/UPDATE/DELETE sur `estimated_budgets` (avec `group_id IS NOT NULL`) → `estimated_budgets_sync_group_budget` (nouveau) recompute SUM + UPDATE `groups.monthly_budget_estimate` (guard `IS DISTINCT FROM` évite no-op) → `groups_budget_contribution_recalc` (existant) PERFORM `calculate_group_contributions(group_id)` → UPSERT toutes les rows `group_contributions` du groupe. Pas de boucle (3 tables distinctes : estimated_budgets → groups → group_contributions). 1 cascade-test gated `SUPABASE_TRIGGER_TESTS=1` ajouté (Case 6 dans [lib/**tests**/trigger-behavior.test.ts](../../lib/__tests__/trigger-behavior.test.ts)) : INSERT 600€ → assert groups.monthly_budget_estimate=600 + contributions 200/400 (salaires 1000/2000) ; UPDATE 900 → 300/600 ; DELETE → 0/0.

  **17 fichiers modifiés** (6 commits Conventional sur `cleanup`) :
  - **DB (Phase 1)** : [supabase/migrations/20260520000000_auto_sync_group_budget.sql](../../supabase/migrations/20260520000000_auto_sync_group_budget.sql) — nouvelle fonction `sync_group_monthly_budget_estimate()` `SECURITY DEFINER SET search_path = public` + trigger `estimated_budgets_sync_group_budget` (FOR EACH ROW AFTER INSERT/UPDATE/DELETE, WHEN guard sur NEW.group_id IS NOT NULL OR OLD.group_id IS NOT NULL) + backfill idempotent `UPDATE groups SET monthly_budget_estimate = (SELECT COALESCE(SUM(estimated_amount), 0) FROM estimated_budgets WHERE group_id = g.id)`. Appliquée via `apply-sql.mjs` + repair. [scripts/check-trigger-functions.mjs](../../scripts/check-trigger-functions.mjs) étendu 4→5 fonctions attendues.
  - **API (Phase 2)** : [lib/schemas/groups.ts](../../lib/schemas/groups.ts) — `createGroupBodySchema` + `updateGroupBodySchema` + `createGroupFormSchema` retirent `monthly_budget_estimate`. Imports `moneyFormSchema`/`moneySchema` retirés (inutiles). [app/api/groups/route.ts](../../app/api/groups/route.ts) POST ne parse plus le champ + insert hardcodé `monthly_budget_estimate: 0` (le trigger update au 1er `estimated_budget`). [app/api/groups/[id]/route.ts](../../app/api/groups/[id]/route.ts) PUT ne modifie plus le champ — seul `name` mutable.
  - **Frontend (Phase 3)** : [hooks/useGroupContributions.ts](../../hooks/useGroupContributions.ts) refactor complet `useState → useQuery + useMutation` (queryKey `['group-contributions']`). API publique préservée byte-identique (contributions, groupInfo, isLoading, error, isRecalculating, fetchContributions=refetch alias, recalculateContributions, getUserContribution, getGroupStats, hasContributions, hasGroup, totalMembers, isOperationInProgress, resetContributions=no-op, cleanup=no-op). AbortController custom remplacé par signal natif de TanStack. [lib/query-client.ts](../../lib/query-client.ts) `invalidateFinancialRefreshes` 3→4 keys + comment. [hooks/useGroups.ts:90](../../hooks/useGroups.ts) retire la branche `if ('monthly_budget_estimate' in updates)` devenue obsolète (UpdateGroupRequest type ne contient plus le champ).
  - **UI (Phase 4)** : [components/groups/CreateGroupForm.tsx](../../components/groups/CreateGroupForm.tsx) retire le champ "Budget mensuel" + simplifie signature `onSubmit(name)` (au lieu de `onSubmit(name, budget)`) + helper text adapté ("Le budget se met à jour automatiquement à mesure que vous créez des items de budget"). [components/settings/GroupManagementPanel.tsx:45](../../components/settings/GroupManagementPanel.tsx) `handleCreateGroup(name)` au lieu de `handleCreateGroup(name, budget)`. [components/profile/ProfileSettingsCard.tsx](../../components/profile/ProfileSettingsCard.tsx) retire le `useEffect` mount qui call `fetchContributions()` (TanStack auto-fetch via useQuery). [components/contributions/UserContributionCard.tsx](../../components/contributions/UserContributionCard.tsx) idem. [components/groups/GroupMembersWithContributionsModal.tsx](../../components/groups/GroupMembersWithContributionsModal.tsx) conserve son `useEffect` sur `isOpen` (rafraîchit la cache à l'ouverture du modal — `fetchContributions` est désormais un alias `refetch`).
  - **Tests (Phase 5)** : [lib/**tests**/query-client.test.ts](../../lib/__tests__/query-client.test.ts) bump 3→4 invalidations asserties + nouveau cas 'group-contributions'. [components/groups/**tests**/CreateGroupForm.test.tsx](../../components/groups/__tests__/CreateGroupForm.test.tsx) retire l'interaction sur le champ budget + le cas "budget=0 invalide" (no longer applicable) + adapte la signature `onSubmit('Famille Test')`. [lib/schemas/**tests**/groups.test.ts](../../lib/schemas/__tests__/groups.test.ts) adapte aux schemas sans monthly_budget_estimate + ajoute 2 tests "ignore le champ si passé par un client legacy" (Zod strip-by-default). [lib/**tests**/trigger-behavior.test.ts](../../lib/__tests__/trigger-behavior.test.ts) ajoute Case 6 gated `SUPABASE_TRIGGER_TESTS=1` (cf. ci-dessus).
  - **Doc (Phase 6)** : [doc2/features/group-contributions.md](../../doc2/features/group-contributions.md) refonte des §2 Glossaire (auto-syncé), §3.1 cycle de vie (étape 4 réécrite), §6.4 table triggers (5 lignes au lieu de 4), §6.5 diagramme flux (nouvelle cascade), §7.3 table routes (PUT budget retiré + POST/PUT/DELETE estimated_budgets ajouté), §8.2 hook description, §11 roadmap (dette ✅ DONE).

  **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / 0 warnings stable ; `pnpm format:check` exit 0 (Prettier --write sur 2 fichiers post-Edit) ; `pnpm test:run` **494 passed / 90 skipped** (+1 gated vs 89 baseline pre-sprint — le nouveau Case 6 du trigger-behavior.test.ts). `pnpm db:check-functions` étendu 4→5 fonctions et exit 0 post-migration. Counter `as unknown as SupabaseClient` reste à 0. Counter `declare global` reste à 0. Counter `: any` reste à 0.

  **Smoke browser deferred to user** : (1) créer un nouveau groupe → vérifier qu'il n'y a plus de champ "Budget mensuel" + contribution affichée 0 € dans ProfileSettingsCard ; (2) créer un item de budget groupe 500€ → contribution se met à jour sans F5 ; (3) modifier 500→800 → live update ; (4) supprimer → retour 0.

  **Pattern à retenir** [@.claude/conventions/operational-rules.md](../conventions/operational-rules.md) §5 : pour toute colonne mirror auto-syncée par trigger (`groups.monthly_budget_estimate` ← `SUM(estimated_budgets)`), ne PAS réintroduire de saisie manuelle dans les schemas/forms/routes — créer un précédent ❌ explicite. Use trigger cascade (table A → table B → table C) plutôt que cross-mutation TanStack invalidation manuelle dès qu'un workflow est purement déterministe.

  **Trade-off** : Sprint multi-phase (6 commits) déclenché par bug user. Ferme deux dettes en parallèle (le bug + la dette TanStack du hook). Réduit la complexité côté UI (1 champ form en moins, 1 useEffect mount en moins ×2 composants). DB cascade plus longue (transaction de mutation `estimated_budgets` fire désormais 2 triggers en cascade au lieu de 0 — à monitorer sous concurrence si bench régression future). Score reste **~100 stable**.

- ✅ **Sprint Hygiene-Next-16-Migration** (livré 2026-05-20) : trois cleanups groupés en un seul sprint chore, déclenchés par 2 warnings dev Next 16 + 1 incident toolchain user-reported pendant le sprint.

  **(1) Migration `middleware.ts` → `proxy.ts`** (Next 16 file convention) — `middleware.ts` est déprécié en Next 16.2.6, warning dev "The 'middleware' file convention is deprecated. Please use 'proxy' instead." Le runtime change aussi par contrat Next : `proxy.ts` tourne sur **nodejs** (non-configurable), `middleware.ts` tournait sur edge par défaut. Le projet Popoth n'utilise aucune API edge-only critique (`jose` JWT pur JS, `lib/recap/check-status.ts` Edge-safe par design) → switch sans regret. Étapes : `git mv middleware.ts proxy.ts` (préserve historique git), rename function default-export `middleware` → `proxy` ([proxy.ts:23](../../proxy.ts)), maj 4 log prefixes `[Middleware]` → `[Proxy]` (debug + error sites dans proxy.ts), maj `eslint.config.mjs:45` files override block `'middleware.ts'` → `'proxy.ts'`, maj 3 cross-refs documentaires (`app/monthly-recap/page.tsx:32` + `app/api/monthly-recap/status/route.ts:11` + `.claude/reference/structure-repo.md:72`). `pnpm typecheck` + `pnpm lint:check` exit 0. Pas de tests à ajouter (proxy.ts est testé en E2E via le flow auth user — pas de gated tests dans le repo pour ce path).

  **(2) Cleanup `experimental.optimizePackageImports`** — [next.config.js](../../next.config.js) listait `experimental: { optimizePackageImports: ['lucide-react', '@radix-ui/react-slot'] }`, ce qui faisait sortir un warning dev "Experiments (use with caution): optimizePackageImport". Vérif doc Next 16.2.6 ([optimizePackageImports reference](https://nextjs.org/docs/app/api-reference/config/next-config-js/optimizePackageImports)) : (a) `lucide-react` figure dans la **default optimized list** de Next 16 (aux côtés de date-fns, lodash-es, ramda, antd, @mui/material, etc.) → la lister explicitement est redondant, le warning sort sans aucun gain ; (b) `@radix-ui/react-slot` n'expose que **5 petits exports** (`Root`, `Slot`, `Slottable`, `createSlot`, `createSlottable` — vérifié via `node -e "console.log(Object.keys(require('@radix-ui/react-slot')))"`) — l'optimisation `optimizePackageImports` cible les packages avec **des centaines d'exports** (lucide-react ~1500), gain négligeable sur 5. Décision : suppression du bloc `experimental` entier (l'objet `experimental: {}` vide aurait pu rester techniquement, mais aucune autre option n'y vit → bloc supprimé en totalité). Zéro régression perf (lucide-react reste optimisé via default-list), warning éteint, config plus minimaliste.

  **(3) Revert `packageManager` pnpm@11.1.3 → pnpm@9.15.5 (incident toolchain)** — user a lancé `pnpm self-update` (sans target version) pendant la session pour rafraîchir son install, en pensant que la commande ne ferait que ré-installer la même version. Or `pnpm self-update` lit la dernière version pnpm publiée et fait **2 choses simultanément** : (a) update silencieusement `package.json` line 95 `"packageManager": "pnpm@9.15.5"` → `"packageManager": "pnpm@11.1.3"` (le pin du projet, qui correspondait à CLAUDE.md §2 stack), (b) tente d'installer pnpm 11.1.3 dans `~/AppData/Local/pnpm/.tools/@pnpm+win-x64/11.1.3/`. L'install (b) a échoué partiellement : `pnpm.exe` (98MB) posé dans le bon dossier, mais les **shims `bin/pnpm` + `bin/pnpm.CMD` + `bin/pnpm.ps1` non créés** → toute commande `pnpm` subséquente faisait `spawnSync C:\Users\gille\AppData\Local\pnpm\.tools\@pnpm+win-x64\11.1.3\bin\pnpm ENOENT`. État du dossier `.tools/@pnpm+win-x64/` post-incident : 4 versions installées (9.5.0, 9.15.5, 10.10.0, 11.1.3 cassé). Fix : revert [package.json:95](../../package.json) à `"packageManager": "pnpm@9.15.5"` (matches CLAUDE.md §2 pin + dossier `.tools/@pnpm+win-x64/9.15.5/` intact avec 54MB pnpm.exe + tous shims présents). Verif : `pnpm --version` → `9.15.5`. Cleanup secondaire : `Remove-Item -Recurse -Force .tools/@pnpm+win-x64/11.1.3/` (98MB libérés).

  **Bonus cleanup** : `.next/dev/` (cache HMR webpack — Next 16 sépare `.next/` pour build et `.next/dev/` pour dev) supprimé après l'arrêt du dev server. Symptôme post-rename middleware→proxy : "Internal Next.js error: Router action dispatched before initialization." dans la console browser, stack trace entièrement dans les internes Next (`app-next-dev.js`, `webpack.js`), aucun code applicatif. Hypothèse : router manifest du HMR encore référence à l'ancien chunk middleware émis avant le rename, race d'initialisation au boot router client. Confirmed fix par cache clean.

  **5 fichiers modifiés** (1 commit unique sur `cleanup`) :
  - [proxy.ts](../../proxy.ts) — renommé depuis `middleware.ts` via `git mv` (historique git préservé), function rename + 4 log prefix renames + 2 comment renames.
  - [next.config.js](../../next.config.js) — bloc `experimental.optimizePackageImports: [...]` supprimé (3 lignes).
  - [package.json](../../package.json) — line 95 `packageManager` revert à `pnpm@9.15.5`.
  - [eslint.config.mjs](../../eslint.config.mjs) — line 45 files override `'middleware.ts'` → `'proxy.ts'`.
  - 3 comment cross-refs : [app/monthly-recap/page.tsx](../../app/monthly-recap/page.tsx), [app/api/monthly-recap/status/route.ts](../../app/api/monthly-recap/status/route.ts), [.claude/reference/structure-repo.md](../reference/structure-repo.md).

  **Patterns à retenir** [@.claude/conventions/operational-rules.md](../conventions/operational-rules.md) §5 Forbidden absolus : (a) ❌ NE PAS réintroduire `middleware.ts` — file convention `proxy.ts` Next 16+, runtime nodejs non-configurable (cf. doc Next upgrading-v16 §"middleware to proxy") ; (b) ❌ NE PAS lancer `pnpm self-update` sans target version explicite — bumpe silencieusement le pin `packageManager` ; pattern correct = `pnpm self-update <version>` ou edit `package.json` manuellement + `pnpm install`. Une 3e leçon implicite : (c) toujours `git mv` plutôt que `mv` + `git add` pour préserver l'historique git lors d'un rename de file convention (Git détecte automatiquement le rename via similarity index sans hint, mais `git mv` rend l'intent explicit).

  **Trade-off** : Sprint chore 1-commit, zéro impact métier observable côté UX au-delà des 2 warnings dev éteints + flux dev plus propre. Documente 2 nouvelles règles ❌ pérennes (file convention + `pnpm self-update`) qui sauvent les futurs sprints d'une régression silencieuse. Score reste **~100 stable**.

- ✅ **Sprint Lazy-Mount-GroupManagementPanel** (livré 2026-05-20, déclenché par user "j'ai 3 fetchs NS_BINDING_ABORTED au mount dashboard sur Firefox") : ferme un fetch `/api/groups` spéculatif systématique au mount du dashboard. `<SettingsDrawer>` rendait toujours `<GroupManagementPanel>` même drawer fermé (juste translaté off-screen via `-translate-x-full` pour préserver l'animation de slide entre vues main ↔ group-management) → le hook `useGroups()` du panel firait son `useQuery` à chaque mount du dashboard, alors que la donnée n'était jamais consommée tant que l'utilisateur n'ouvrait pas le drawer ET ne cliquait pas "Gestion du groupe".

  **Diagnostic** : (1) En dev React Strict Mode (Next.js 16 défaut), le double-mount provoquait visiblement 3 `NS_BINDING_ABORTED` Firefox sur `/api/profile` + `/api/groups/contributions` + `/api/groups` — les 3 `useQuery` qui passent `signal` à leur `queryFn`. Strict Mode mount → `queryFn` start → unmount → `AbortController.abort()` propagé via `signal` → fetch annulé → remount → 2e fetch passe (200). (2) `useFinancialData` ([hooks/useFinancialData.ts:51](../../hooks/useFinancialData.ts)) et `useBankBalance` ne passent pas `signal` à leur `fetch(url, { method, credentials })` — leurs fetchs double-firent silencieusement en dev mais retournent 200 chacun. Pas dans la liste des aborts user, juste invisibles. (3) Le `credentials: "omit"` dans le copy-as-fetch Firefox des 2 premières requêtes est une bizarrerie d'affichage sur requêtes coupées avant que les headers soient complètement écrits (les `Sec-Fetch-*` manquent aussi). La 3e (`/api/groups`) coupée plus tard a tous ses headers + `credentials: "include"`. Le code envoie bien `credentials: 'include'` partout, vérifié via `Grep credentials:` sur 31 sites dans `hooks/`. (4) Les 2 autres aborts (`/api/profile` + `/api/groups/contributions`) viennent de hooks consommés directement par la page dashboard ([useProfile](../../hooks/useProfile.ts) + [useGroupContributions](../../hooks/useGroupContributions.ts)) → vrai usage métier, impossible à dropper. Restent normaux en dev Strict Mode, disparaissent en prod (Strict Mode ne double-mount plus).

  **Fix appliqué** : lazy-mount `<GroupManagementPanel>` via flag `hasBeenOpened` dans [components/settings/SettingsDrawer.tsx](../../components/settings/SettingsDrawer.tsx). Initialement tenté en `useEffect(() => { if (isOpen) setHasBeenOpened(true) }, [isOpen])` → ESLint flat config rule `react-hooks/set-state-in-effect` (introduite par React 19 / `eslint-plugin-react-hooks` shipped with React 19) refuse : "Calling setState synchronously within an effect can trigger cascading renders. Avoid calling setState() directly within an effect." Pivot vers le pattern "adjust state during render" documenté par React docs (`react.dev/learn/you-might-not-need-an-effect#adjusting-some-state-when-a-prop-changes`) :

  ```tsx
  const [hasBeenOpened, setHasBeenOpened] = useState(false)
  if (isOpen && !hasBeenOpened) {
    setHasBeenOpened(true)
  }
  // ...
  <div className={...}>
    {hasBeenOpened && <GroupManagementPanel onBack={() => setView('main')} onClose={onClose} />}
  </div>
  ```

  React détecte le `setState` synchrone dans le body de render, bail-out le commit du render en cours, schedule un re-render → un seul commit visible. Le panel monte à la 1re ouverture du drawer (n'importe quelle view), reste monté ensuite pour préserver l'animation de slide entre views et éviter les re-mounts/re-fetches sur ouvertures subséquentes. Le hook `useGroups()` du panel ne fire donc plus jamais avant la 1re ouverture du drawer.

  **1 fichier modifié** (1 commit unique sur `cleanup`) : [components/settings/SettingsDrawer.tsx](../../components/settings/SettingsDrawer.tsx) — +9 LOC (state + conditional render + commentaire pattern) / -1 LOC (remove unconditional panel mount). Aucun test à modifier — pas de `SettingsDrawer*.test.*` ni `GroupManagementPanel*.test.*` dans le repo (composants drawer-orchestration peu testés directement, couverts via les pages dashboard / group-dashboard en E2E manuels).

  **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / 0 warnings stable (après pivot du `useEffect` vers "adjust state during render") ; `pnpm format:check` exit 0 ; `pnpm test:run` **494 passed / 90 skipped** (baseline identique pre-sprint, aucun test SettingsDrawer-adjacent). 0 migration DB. 0 nouvelle RPC. 0 nouveau `: any`. Counter `as unknown as SupabaseClient` reste à 0. Counter `declare global` reste à 0.

  **Smoke browser deferred to user** : (1) login → arriver sur dashboard → ouvrir Firefox DevTools Network → vérifier qu'il n'y a plus de fetch `/api/groups` du tout (les 2 aborts `/api/profile` + `/api/groups/contributions` restent en dev Strict Mode, normaux et inéluctables sans dropper Strict Mode dev) ; (2) ouvrir le drawer paramètres → vérifier que `/api/groups` fire à ce moment (200, attendu, panel mount) ; (3) naviguer vers "Gestion du groupe" → vérifier que la liste est instantanée (donnée déjà cached) ; (4) fermer + rouvrir drawer plusieurs fois → vérifier que `/api/groups` ne re-fire pas (panel reste monté grâce à `hasBeenOpened` qui ne revient jamais à false, cache TanStack chaud).

  **Patterns à retenir** [@.claude/conventions/operational-rules.md](../conventions/operational-rules.md) §5 Modals & UI : ❌ NE PAS toujours-mounter un composant lourd (qui fait des `useQuery` / `fetch`) à l'intérieur d'un drawer/modal masqué par défaut via CSS (translate, opacity, display visible-mais-pas-vu) — lazy-mount via state flag `hasBeenOpened` + pattern "adjust state during render" React 19 (PAS `useEffect` car bloqué par ESLint `react-hooks/set-state-in-effect`). Le coût d'un fetch HTTP spéculatif n'est pas négligeable au cold start (Next.js dev compile la route `/api/*` à la 1re requête → 200-500ms en dev). Pattern miroir applicable à tout panel "slot" d'un drawer (modals d'édition, panels de réglages, sub-views horizontales).

- ✅ **Sprint GroupMembersModal-Show-Budget-Share-Pct** (livré 2026-05-20, déclenché par user "le pourcentage affiché est le pourcentage du salaire alloué, alors que j'aimerais que ça soit le pourcentage de contribution que le user apporte au budget du groupe") : aligne la sémantique du % affiché par membre dans `<GroupMembersWithContributionsModal>` (drawer Paramètres → "Gestion du groupe" → "Voir les membres") sur la **part du budget du groupe** plutôt que la **part du salaire personnel mobilisé**. Cohérent avec le pattern de disambiguation déjà installé dans le navbar par le commit `2759b7a` (Sprint feat(ui) 2026-05-19) qui affiche les **deux** percentages en clair sur une 3e ligne (`<X%> de votre salaire · <Y%> du budget`).

  **Pre-fix** : le modal lisait `contribution.contribution_percentage` de la table `group_contributions`. La fonction PG `calculate_group_contributions` ([supabase/migrations/20260512000000_capture_trigger_functions.sql:99-102](../../supabase/migrations/20260512000000_capture_trigger_functions.sql)) stocke cette colonne comme `contribution_amount / member.salary * 100` (% du salaire perso mobilisé) — pas comme part du budget. Label affiché "Pourcentage" sans qualifier ambigu.

  **Post-fix** : calcul on-the-fly côté client : `group.monthly_budget_estimate > 0 ? (contribution.contribution_amount / group.monthly_budget_estimate) * 100 : 0` avec guard division-par-zéro (un groupe sans aucun `estimated_budget` a `monthly_budget_estimate=0` post-Sprint Group-Budget-Auto-Sync). Label "Pourcentage" → "Part du budget". Mathématiquement équivalent à `(member.salary / total_salaries) * 100` puisque `contribution_amount = (member.salary / total_salaries) * group_budget` (formule de la PG fn), mais le code lit le `contribution_amount` déjà calculé pour rester aligné avec ce que le serveur a stocké (single source of truth).

  **Décision architecturale : DB column inchangée** — `group_contributions.contribution_percentage` continue de stocker la sémantique "% du salaire perso". Raisons : (a) `<UserContributionCard>` ([components/contributions/UserContributionCard.tsx:132-134](../../components/contributions/UserContributionCard.tsx)) affiche cette valeur sous le label explicite "de votre salaire" — switcher le sens de la colonne casserait cette UX surface ; (b) le commit 2759b7a expose **les deux** percentages côté navbar, ce qui prouve que les deux sémantiques ont leur usage légitime. Conclusion : pas de migration DB, pas d'API change, pas de Zod schema change — l'alternate derivation se calcule là où elle est nécessaire.

  **1 fichier modifié** (1 commit unique sur `cleanup`) : [components/groups/GroupMembersWithContributionsModal.tsx](../../components/groups/GroupMembersWithContributionsModal.tsx) — bloc "Contribution Information" : `formatPercentage(contribution.contribution_percentage)` → calcul inline avec guard + label "Pourcentage" → "Part du budget". Le helper `formatPercentage` (Intl.NumberFormat `style: 'percent'` qui divise l'input par 100) reste utilisé tel quel.

  **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / 0 warnings stable ; `pnpm format:check` exit 0 ; `pnpm test:run` baseline identique (aucun test RTL ne couvrait ce site — le modal n'a pas de test file). 0 migration DB. 0 nouvelle RPC. 0 nouveau `: any`. Counter `as unknown as SupabaseClient` reste à 0. Counter `declare global` reste à 0.

  **Smoke browser deferred to user** : (1) ouvrir le drawer Paramètres → "Gestion du groupe" → "Voir les membres" → vérifier que chaque carte membre affiche "Part du budget: X%" cohérent avec la 3e ligne du navbar (`Y% du budget` pour le user courant) ; (2) cas-limite groupe sans budget (jamais créé d'`estimated_budget`) → afficher 0% sans crash NaN.

  **Pattern à retenir** : pour toute colonne DB nommée de façon ambiguë (`contribution_percentage` aurait pu signifier "% du salaire" OU "% du budget"), si deux UI surfaces ont besoin de sémantiques différentes, calculer l'alternate **client-side** plutôt que de muter la signification de la colonne. Le label UI doit être explicite ("Part du budget", "de votre salaire") plutôt qu'un mot générique ("Pourcentage"). Pas de nouvelle règle ❌ ajoutée à [operational-rules.md](../conventions/operational-rules.md) — cohérent avec le précédent navbar 2759b7a.

  **Trade-off** : Sprint chirurgical 1-commit 1-fichier déclenché par feedback user post-Sprint Lazy-Mount-GroupManagementPanel (même session). Aucun impact métier au-delà de l'alignement de sémantique modal ↔ navbar. Score reste **~100 stable**.

  **Trade-off** : Sprint chirurgical 1-commit + 1-fichier déclenché par observation user. Bénéfice mesurable en prod (1 fetch en moins par mount dashboard) + dev plus propre (réduit le bruit `NS_BINDING_ABORTED` de 3 → 2 au mount). N'affecte pas le runtime Strict Mode lui-même (les 2 autres aborts restent — ils viennent de hooks consommés directement par la page dashboard, impossible à dropper sans tuer le fetch métier réel). Score reste **~100 stable**.

- ✅ **Sprint Drawer-Slide-Fix-And-Header-Harmonize** (livré 2026-05-20, déclenché par user "les drawer ne viennent pas du bas de l'écran, mais ressemble plus à l'ouverture d'une modal dans son animation") : ferme un bug visuel latent depuis Sprint Zod-Rollout v8 (2026-05-14) où les 2 drawers du dashboard (`<SavingsDistributionDrawer>` + `<PlanningDrawer>`) **animaient en diagonale depuis le coin haut-gauche avec zoom 95% → 100%** au lieu de slider depuis le bas. Bonus : harmonise le header `<PlanningDrawer>` sur le template `<SavingsDistributionDrawer>` en conservant la couleur bleue.

  **Root cause (Tailwind v4 alphabetical-sort gotcha)** : [components/ui/dialog.tsx:52](../../components/ui/dialog.tsx) `<DialogContent>` de base déclare `slide-in-from-top-[48%]` + `slide-in-from-left-1/2` + `zoom-in-95`. [components/ui/drawer-content-classes.ts](../../components/ui/drawer-content-classes.ts) tente d'overrider avec `slide-in-from-bottom` + `zoom-in-100`. Mais ces utilitaires `tw-animate-css` posent **tous le même CSS custom property** (`--tw-enter-translate-{x,y}`, `--tw-enter-scale`). Tailwind v4 émet les utilités triées alphabétiquement, la cascade prend la **dernière** assignation : `top` bat `bottom` sur Y, `slide-in-from-left-1/2` reste seul sur X, `zoom-in-95` bat `zoom-in-100` (lexico char-par-char `1`<`9`). Composé : modal centrée qui s'ouvre. Le bug a survécu 6 jours parce que tests RTL ne couvrent que focus trap / Esc — jsdom ne tourne pas les keyframe animations.

  **Fix animation** : `!` postfix Tailwind v4 sur les utilitaires d'override → `slide-in-from-bottom!` / `slide-out-to-bottom!` / `zoom-in-100!` / `zoom-out-100!`. Plus neutralisation X via arbitrary `[--tw-enter-translate-x:0]!` / `[--tw-exit-translate-x:0]!`. Composé : enter va de `translate3d(0, 100%, 0) scale(1)` à `translate3d(0, 0, 0) scale(1)` = vraie animation depuis le bas.

  **Header template (SavingsDistributionDrawer)** : icône wrapper `h-10 w-10 rounded-full bg-{color}-600` + svg `h-5 w-5 text-white` ; titre `text-xl font-bold` ; bg `bg-{color}-50/30` ; padding `px-4 py-4` ; close `<ModalCloseX variant="circle" className="h-10 w-10" svgClassName="h-5 w-5 text-gray-600" />`. Appliqué à `<PlanningDrawer>` avec `bg-blue-{50/30,600}` (vs purple savings). Le drag handle décoratif est **supprimé** : savings n'en a pas, et l'affordance est trompeuse sur un drawer fullscreen sans drag-to-dismiss réel (`vaul`).

  **2 fichiers modifiés** (2 commits sur `cleanup`) : [components/ui/drawer-content-classes.ts](../../components/ui/drawer-content-classes.ts) — `!` postfix + arbitrary CSS vars + JSDoc explicatif ; [components/dashboard/PlanningDrawer.tsx:333-369](../../components/dashboard/PlanningDrawer.tsx) — header restyle (h-8→h-10, h-4→h-5, text-lg→text-xl, 80→30, py-3→py-4) + drag handle deleted.

  **Verif** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0/0 ; `pnpm test:run` 494/90 baseline (2 tests `expectEscClose` continuent de passer — assertion sur focus trap + titles, pas l'animation). Counters `: any` / `as unknown as SupabaseClient` / `declare global` à 0 stable.

  **Pattern à retenir (nouvelle règle ❌ ajoutée à [operational-rules.md](../conventions/operational-rules.md) §5 Modals & UI)** : pour tout override d'utilitaires Tailwind v4 qui ciblent la **même CSS custom property** (typiquement `--tw-enter-*` / `--tw-exit-*` de `tw-animate-css` : `slide-in-from-*`, `slide-out-to-*`, `zoom-in/out-*`, `fade-in/out-*`, `spin-in/out-*`, `blur-in/out-*`), utiliser **systématiquement** le `!` postfix sur les classes de l'override. Sinon l'ordre alphabétique de tri en source CSS triomphe de l'ordre HTML d'application. Score reste **~100 stable**.
