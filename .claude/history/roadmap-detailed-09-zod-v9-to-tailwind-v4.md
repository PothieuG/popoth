# Roadmap détaillée — Part 09/12 : Zod-Rollout v9 → Tailwind-v4

> Découpé 2026-05-18 depuis `.claude/history/roadmap-detailed.md` (393 967 chars, 12 parts).
> **Étendue** : Sprint Zod-Rollout v9 ➜ Sprint Tailwind-v4 (5 sprints livrés).
> Pour l'index complet et le résumé court, voir CLAUDE.md §11.
> Navigation : [← Part précédente](roadmap-detailed-08-zod-v4-to-zod-v8.md) | [Part suivante →](roadmap-detailed-10-p10-to-auto-balance-atomic.md)

---

## 11. Roadmap — Part 09/12

- ✅ **Sprint Zod-Rollout v9** ([prompt/prompt-07-deep-dive-zod-rollout-v9.md](prompt/prompt-07-deep-dive-zod-rollout-v9.md), plan dans `C:\Users\gille\.claude\plans\sprint-zod-rollout-v9-mighty-wreath.md`) : consolidation post-v8 en 3 commits code + closeout. **Arbitrages user Phase 1** (2 décisions Recommended) : (Q1) `DRAWER_CONTENT_CLASSES` dans nouveau fichier dédié (séparation of concerns, idiom shadcn) plutôt qu'export from dialog.tsx ; (Q2) nested-stacking test full path avec fallback stub si flaky (préserve le réalisme du test, fallback documenté en cas de flake).
  - **Commit 1** ([`f4bf846`](#)) `test(a11y): extend focus-trap regression-guards to 10 v8 modals` — étend [components/**tests**/a11y-audit.test.tsx](components/__tests__/a11y-audit.test.tsx) le `describe('Radix Dialog focus-trap + Esc-to-close (regression-guard)')` block de 2 → 12 cas (+10 nouveaux). Surfaces ajoutées : AddBudgetDialog, AddIncomeDialog, EditIncomeDialog, EditTransactionModal (heading vs button text disambiguated via `getByRole('heading', { level: 2, name: /Modifier la dépense/i })` parce que "Modifier la dépense" apparaît dans le `<h2>` + le submit `<Button>`), ConfirmationDialog, DeleteGroupModal, GroupMembersWithContributionsModal, PlanningDrawer (drawer), SavingsDistributionDrawer (drawer), + **nested-stacking PlanningDrawer + lazy AddBudget child** (click "Ajouter un budget" → wait `next/dynamic` ssr:false → Esc ferme child via Radix Portal stacking → second Esc ferme drawer parent). **6 nouveaux mocks file-level** : useBudgetProgress, useIncomeProgress, useProfile, useGroupMembers, useGroupContributions, `@tanstack/react-query` (mock `useQuery: () => ({ data: undefined, isLoading: false, error: null })` pour SavingsDistributionDrawer dont la fetch refait dans queryFn n'a pas de fetch polyfill jsdom). **Extension des 4 mocks existants** useBudgets/useIncomes/useRealExpenses/useRealIncomes avec les méthodes destructurées par PlanningDrawer + EditTransactionModal (refreshBudgets/refreshIncomes/totalBudgets/totalIncomes/addBudget/updateBudget/deleteBudget/updateExpense/updateIncome). 7 nouveaux imports + 281 LOC ajoutés. Tests 279 → 289 (+10 non-gated). Lint baseline 180 stable. Le test nested-stacking a pass au premier run — pas de flake `next/dynamic` jsdom timing.
  - **Commit 2** ([`2862810`](#)) `refactor(ui): extract DRAWER_CONTENT_CLASSES constant` — création [components/ui/drawer-content-classes.ts](components/ui/drawer-content-classes.ts) (24 LOC : `cn()` call avec les ~12 utility classes Tailwind du drawer override + JSDoc trigger pour migration vaul si 3ᵉ drawer surface ou drag-to-dismiss signalement). Consommateurs : PlanningDrawer.tsx L327-341 + SavingsDistributionDrawer.tsx L207-221 — `className={cn(...)}` remplacé par `className={DRAWER_CONTENT_CLASSES}` + 2 imports ajoutés. **Drive-by Prettier cleanup** lint-staged-applied : SavingsDistributionDrawer.tsx body interne post-v8 avait une indentation pending-fix (nested transfer Dialog body indenté +2 spaces de trop) — auto-cleaned au commit. Visual + animation behavior identique pré-v9. Tests 19/19 stable, lint 180 stable, typecheck OK.
  - **Commit 3** ([`1574eee`](#)) `refactor(savings): drop selectedFromBudget?.X workarounds in nested modal` — SavingsDistributionDrawer.tsx (1) `open={isTransferModalOpen && !!selectedFromBudget}` → `open={isTransferModalOpen}` (single source de vérité — child render guard préserve l'invariant), (2) wrap le DialogContent interne dans `{selectedFromBudget && (<>...</>)}` (TS narrow propre maintenant), (3) drop `?.` sur L477/L480/L503 → accès direct `selectedFromBudget.name` / `.cumulated_savings`. **L97 préservé** (out of scope v9 — `availableBudgets.filter((budget) => budget.id !== selectedFromBudget?.id)` pre-existe v8, runs dans la source-budget dropdown filter AVANT qu'un fromBudget soit sélectionné). Re-indent +4 spaces sur le block toggle destination + dropdown conditional + validation error + footer pour matcher le nouveau wrapper niveau. Net diff `162 insertions / 159 deletions` (delta réel : +1 wrapper ligne + 3 spaces moins par ligne). Tests 19/19 stable, lint 180 stable, typecheck OK. Negative grep `Grep "selectedFromBudget\?\." components/dashboard/SavingsDistributionDrawer.tsx` → 1 hit (L98, expected).
  - **Verif end-to-end** : `pnpm typecheck` exit 0 à chaque commit ; `pnpm lint:check` 0 errors / **180 warnings stable** ; `pnpm test:run` **289 passed / 64 skipped** (vs 279/64 pré-v9 = +10 non-gated). 0 migration DB. 0 nouveau dep. 0 nouveau `: any`. Counter `as unknown as SupabaseClient` reste à **0**. Negative greps : `Grep "selectedFromBudget\?\." components/dashboard/SavingsDistributionDrawer.tsx` → 1 hit (L98, hors scope) ; `Grep "inset-0 left-0 top-0 h-screen" components/dashboard/` → 0 hit (extracted) ; `Grep "DRAWER_CONTENT_CLASSES" components/` → 3 hits (1 declaration + 2 consumers).
  - **Smoke browser deferred to user** : (a) `/dashboard` PlanningDrawer + SavingsDistributionDrawer → visual parity (slide-from-bottom, fullscreen, animation) avec v8 — identique sauf code interne extrait dans constante. (b) `/dashboard` SavingsDistribution → ouvrir transfer modal depuis un budget tile → contenu rendered (plus de `undefined` visible — théoriquement jamais survenu en pratique mais defensive). (c) Lecteur d'écran : annonce `role="dialog"` à chaque ouverture, focus trap conservé, Esc ferme correctement (child d'abord puis drawer parent pour nested cases).
  - **Pattern v9 → standard** : pour tout nouveau drawer fullscreen bottom-up, importer `DRAWER_CONTENT_CLASSES` de `@/components/ui/drawer-content-classes` plutôt que dupliquer l'override className inline. Pour tout nouveau modal Radix migré post-v8, ajouter un cas focus-trap regression-guard dans `a11y-audit.test.tsx` pour pin le contrat `userEvent.keyboard('{Escape}')` → `onClose called`. Si le contenu nested du modal dépend d'un prop optionnel qui peut être null mais ne l'est jamais en pratique, préférer le pattern `{selectedX && (<>...</>)}` wrapper plutôt que `selectedX?.X` chains — TS narrow propre + contenu skippé si invariant violé.
  - **Hors scope explicite** : Sprint Tailwind-v4 / Supabase-Strict-Types / Chantier I6 / Lot 6 console-cleanup / OpenAPI / Error boundaries / Sentry / i18n / vaul drag-to-dismiss / Storybook — roadmap §11 inchangée. Pre-existing dirty working tree (~80 fichiers M+D docs/→doc2/ + prompts/→prompt/ reorg) préservé (hors scope v9).
  - Livré 2026-05-14, 3 commits code + closeout sur `cleanup` (`f4bf846` Axe 1 RTL coverage / `2862810` Axe 2.A drawer DRY / `1574eee` Axe 2.B selectedFromBudget cleanup / closeout). Score : ~99.999 → **~99.999/100 stable** (pure consolidation post-v8 : couverture test exhaustive + DRY drawer + TS narrowing propre — pas de saut métier mais clôture nette des 2 gaps surfacés v8 closeout).

- ✅ **Sprint Zod-Rollout v10** (plan dans `C:\Users\gille\.claude\plans\sprint-zod-rollout-v10-effervescent-sparkle.md`) : consolidation finale post-v8/v9 — ferme la DRY violation modal close X (11 sites dans 10 fichiers depuis v8) + la duplication du focus-trap test boilerplate (10 cas sur 12 dans a11y-audit.test.tsx depuis v9). **Arbitrages user Phase 1+3** (3 décisions Recommended) : (Q1) **Option α** Préserver les variations via props (`className?` override + `disabled?` + `onClose` callback) plutôt que standardiser uniformément — 4 surprises Phase 1 (h-10 w-10 drawer principal SavingsDistribution, p-1 vs p-2 DeleteGroup, onClick complexe nested transfer, no-disabled GroupMembers) passent en props caller-side. Zero visual change pré-v10. (Q2) **EditTransactionModal reste hors `expectEscClose()`** (heading level 2 spécial = 1 cas sur 12 acceptable) — helper signature simple `(element, onClose, titleText: string | RegExp)`. (Q3) **Full sweep Axe 1 + Axe 2** : 3 commits code + closeout. **Phase 1 audit Explore** a confirmé exactement les 11 sites attendus (10 fichiers, dont SavingsDistributionDrawer a 2 sites — drawer principal + nested transfer) + 12 cas focus-trap dans `a11y-audit.test.tsx` + **trouvaille technique critique** : shadcn `<Button variant="ghost">` a une règle CSS `[&_svg]:size-4` qui override le svg className `h-5 w-5` des 4 sites ghost — le rendered visual est donc `h-4 w-4` (pas h-5 w-5 comme le className laisserait penser). Donc le composant `<ModalCloseX>` ghost variant doit aussi rendre svg à `h-4 w-4` par default pour zéro visual change.
  - **Commit 1** ([`859fe80`](#)) `feat(ui): add ModalCloseX component + unit tests` — [components/ui/modal-close-x.tsx](components/ui/modal-close-x.tsx) (70 LOC + JSDoc). API : `onClose: () => void` (required) + `disabled?: boolean = false` + `variant: 'circle' | 'ghost'` (required, pas de default — force choix explicite) + `className?: string` (override pour h-10 w-10 drawer / p-1 DeleteGroup) + `svgClassName?: string` (override SVG size pour SavingsDistribution drawer principal h-10 w-10 + svg h-5 w-5) + `ariaLabel?: string` (default `"Fermer"`, i18n future-proof). Variantes internes : (a) `circle` = raw `<button>` avec `h-8 w-8 rounded-full bg-gray-100 hover:bg-gray-200` + SVG h-4 w-4 text-gray-600 ; (b) `ghost` = raw `<button>` avec `h-8 w-8 rounded-md p-2 hover:bg-accent hover:text-accent-foreground` + SVG h-4 w-4 text-gray-600 (raw button pour les 2 variants — pas de dépendance shadcn `<Button>`, garde l'implémentation simple). Click handler `() => !disabled && onClose()` + native `disabled` attribute = defense-in-depth. Sub-component privé `<CloseSvg>` pour centraliser le path `d="M6 18L18 6M6 6l12 12"` + `strokeWidth="2"` + `viewBox="0 0 24 24"` + `aria-hidden="true"`. **Tests** [components/ui/\_\_tests\_\_/ModalCloseX.test.tsx](components/ui/__tests__/ModalCloseX.test.tsx) (4 cas non-gated) : rendu variant circle h-8 w-8 + rounded-full + bg-gray-100 + SVG default h-4 w-4 / rendu variant ghost rounded-md + hover:bg-accent (different from circle) / click fires onClose 1× + click NOT fired quand disabled / svgClassName override appliqué + className override h-10 w-10 préservé. 128 insertions sur 2 fichiers.
  - **Commit 2** ([`e7c8312`](#)) `refactor(ui): migrate 11 modal close X sites to ModalCloseX` — migration de 11 sites dans 10 fichiers (chaque inline 12-15 LOC `<button>+<svg>+<path>` ou `<Button variant="ghost">+<svg>+<path>` remplacé par `<ModalCloseX ... />` 3-5 LOC). **7 sites circle** : AddBudgetDialog L135-149 + AddIncomeDialog L120-140 + EditBudgetDialog L151-171 + EditIncomeDialog L133-153 + PlanningDrawer L361-381 (tous `onClose={handleClose}` ou `onClose={onClose}` sans disabled) + **SavingsDistributionDrawer drawer principal L236-256** (`onClose={onClose}` + `className="h-10 w-10"` + `svgClassName="h-5 w-5 text-gray-600"` — la combination h-10 button + h-5 svg unique vs autres circle) + **SavingsDistributionDrawer nested transfer L445-465** (`onClose={() => setIsTransferModalOpen(false)}` + `disabled={isProcessing}` — replacing l'ancien complex handler `onClick={() => !isProcessing && setIsTransferModalOpen(false)}` par un pattern uniforme disabled-attribute + simple onClose callback). **4 sites ghost** : AddTransactionModal L296-318 (`disabled={isSubmitting}`) + EditTransactionModal L242-264 (`disabled={isSubmitting}`) + GroupMembersWithContributionsModal L112-127 (no disabled) + DeleteGroupModal L94-116 (`disabled={isDeleting}` + `className="p-1"`). Import `{ ModalCloseX } from '@/components/ui/modal-close-x'` ajouté dans les 10 fichiers (après l'import `{ Dialog, DialogContent, DialogTitle } from '@/components/ui/dialog'`). Imports `Button` shadcn préservés (toujours utilisés ailleurs dans les 4 fichiers ghost — submit/cancel buttons). **Net −239 LOC sur 10 fichiers** (mieux que l'estimation plan −120). 12 tests focus-trap dans `a11y-audit.test.tsx` restent verts (Esc-close path inchangé — Radix Dialog gère le keyboard, le close X est juste un bouton onClick).
  - **Commit 3** ([`4734636`](#)) `test(a11y): extract expectEscClose helper + migrate 10 focus-trap cases` — [components/\_\_tests\_\_/a11y-helpers.ts](components/__tests__/a11y-helpers.ts) (36 LOC + JSDoc). Helper signature `(element: ReactElement, onClose: vi.Mock, titleText: string | RegExp): Promise<void>` qui centralise le 5-line boilerplate (`userEvent.setup()` + `render(element)` + `await waitFor(() => expect(screen.getByText(titleText)).toBeInTheDocument())` + `await user.keyboard('{Escape}')` + `expect(onClose).toHaveBeenCalled()`). Limitation explicite : ne supporte que `getByText` pour mount assertion — tests utilisant `getByRole('heading', { level: 2 })` ou flow stacking complexe doivent rester manuels. **10 des 12 cas focus-trap migrés** : EditBudgetDialog + AddTransactionModal + AddBudgetDialog + AddIncomeDialog + EditIncomeDialog + ConfirmationDialog + DeleteGroupModal + GroupMembersWithContributionsModal + PlanningDrawer + SavingsDistributionDrawer. **2 cas restent manuels par design** : (a) cas 6 EditTransactionModal utilise `getByRole('heading', { level: 2, name: /Modifier la dépense/i })` pour disambiguïser H2 vs submit Button qui partagent le texte ; (b) cas 12 nested-stacking PlanningDrawer + AddBudget child (flow complexe 2 Esc consécutifs + assertions intermédiaires entre les deux). Net +88 LOC / -73 LOC = **+15 LOC** (helper 36 + 10 cas avant→après ~-21 = +15). a11y-audit.test.tsx : 19/19 tests passants (7 axe-core + 12 focus-trap, identique pré-v10).
  - **Verif end-to-end** : `pnpm typecheck` exit 0 à chaque commit ; `pnpm lint:check` 0 errors / **180 warnings stable** (vs 180 pré-v10 = 0 delta) ; `pnpm test:run` **293 passed / 64 skipped** (vs 289/64 pré-v10 = **+4 tests** = les 4 ModalCloseX unit tests, focus-trap tests inchangés en count parce que helper migration ne change pas le nombre de cas, juste leur compacité). Pas de `pnpm verify` car aucun changement DB. 0 migration DB. 0 nouveau dep. 0 nouveau `: any`. Counter `as unknown as SupabaseClient` reste à **0**. **Negative greps clean** : `Grep "M6 18L18 6M6 6l12 12" components/` retourne **2 hits** = 1 dans modal-close-x.tsx (SVG centralisé) + 1 dans components/ui/icons.tsx (icone X out of scope, non-modal) ; `Grep "aria-label=\"Fermer\"" components/` retourne **2 hits** = 1 dans modal-close-x.tsx (centralisé) + 1 dans components/ServiceWorkerRegistration.tsx (PWA install banner, non-modal).
  - **Smoke browser deferred to user** : (a) Visual parity check — ouvrir les 10 modals depuis l'UI : `/dashboard` Add/Edit Budget/Income via tile click ; AddTransaction + EditTransaction ; PlanningDrawer + SavingsDistributionDrawer depuis FinancialIndicators/SavingsDrawer ; `/groups` GroupMembers + DeleteGroup. Close X visuellement identique pré-v10 (h-8 w-8 sur la plupart, h-10 w-10 sur le drawer SavingsDistribution principal, p-2 sur 3 ghost, p-1 sur DeleteGroup). (b) Keyboard a11y : Tab cycle borné au modal, Esc close, lecteur d'écran annonce "Fermer" au focus du close X. (c) Disabled states honored : pendant `isSubmitting`/`isDeleting`/`isProcessing`, le close X affiche `disabled:cursor-not-allowed disabled:opacity-50` et ne répond pas au click. (d) Lighthouse a11y score ≥95.
  - **Pattern v10 → standard du repo** : pour tout futur modal Radix-migré post-v8, utiliser `<ModalCloseX onClose={handleClose} variant="circle"|"ghost" disabled={isSubmitting} />` au lieu d'un raw button + SVG inline (15 LOC → 3 LOC). Pour tout nouveau focus-trap regression-guard test, utiliser `await expectEscClose(<NewModal isOpen onClose={onClose} ... />, onClose, '<Title>')` au lieu du squelette 10-line. Si le modal a un title-duplication (H2 + Button submit), test reste manuel avec `getByRole('heading', { level: 2 })`.
  - **Trade-off documenté** : v10 ne fait pas avancer le score métier — pure consolidation finale. (a) DRY violation close X fermée (1 site de vérité pour le SVG path + a11y attributes sur 11 sites). (b) Test helper DRY (1 site de vérité pour le focus-trap pattern sur 10 cas). (c) Pattern à reprendre pour tout nouveau modal migré Radix dans le futur. **Chantier a11y modal completely closed** sur 3 sprints v8+v9+v10.
  - **Hors scope explicite** : `<ModalDrawer>` shadcn-style wrapper pour les 2 drawers (pas justifié, 2 sites < threshold CLAUDE.md "Three similar lines is better than premature abstraction") ; Standardisation visuelle des 4 variations h-10→h-8 / p-1→p-2 / gain disabled GroupMembers / conversion handler nested (Option β refusée par user au Phase 1) ; Tab cycle / outside-click / return-focus tests (Radix les fournit nativement) ; EditTransactionModal cas 6 dans helper via overload (refusé Phase 1) ; Nested-stacking cas 12 (flow complexe reste manuel). Sprints roadmap §11 inchangés : Tailwind v4 / Supabase-Strict-Types / Chantier I6 / Lot 6 console-cleanup / OpenAPI / Storybook / Error boundaries / Sentry / i18n / vaul drag-to-dismiss.
  - Livré 2026-05-14, 3 commits code + closeout sur `cleanup` (`859fe80` ModalCloseX composant + tests / `e7c8312` migration 11 sites / `4734636` helper + migration 10 cas / closeout). Score : ~99.999 → **~99.999/100 stable** (pure consolidation finale du chantier modal v8/v9/v10 — DRY close X + test helper, pas de saut métier mais clôture nette des 2 dernières duplications surfacées v9 closeout).

- ✅ **Sprint Supabase-Strict-Types** (livré 2026-05-14, 3 commits code + closeout sur `cleanup`) : ferme le bloqueur installé au Sprint DX-Verify follow-up 2026-05-07 (hémorragie revert wave Dependabot) qui pinned `@supabase/supabase-js` à `^2.57.4` via ignore Dependabot pour éviter `RejectExcessProperties` strict typing introduit en `2.105+`. **Phase 1 audit a invalidé en partie le prompt source** (rédigé 2026-05-07, stale post-Sprint Refactor-I5/I6 2026-05-11/14) : (a) les line numbers L:133/L:556/L:588/L:182/L:154 sont décalés (Sprint Refactor-Architecture-v4 a wrappé en `withAuthAndProfile`) ; (b) le pattern résiduel **n'est pas `[x: string]: any`** (0 hit cross-codebase grâce à Sprint Lint-Baseline-Cleanup Phase 4.2 qui a installé `Database['public']['Tables'][...]['Insert']` partout) mais les **computed keys `[ownerField]: contextId`** où `ownerField: 'profile_id' | 'group_id'` est dynamic — TypeScript widening l'objet vers `{ [x: string]: string | number | null | undefined }` qui collisione avec `RejectExcessProperties = { [x: string]: never }` ; (c) `complete/route.ts` ABSENT du scope (Sprint Refactor-I6 2026-05-14 l'a extrait en thin handler 59 LOC → `lib/recap/complete-persist.ts` utilise déjà `as MonthlyRecapInsert` strict-friendly) ; (d) `recover/route.ts` 7 sites déjà strict via `TablesInsert<>` (Sprint Lint-Baseline-Cleanup) ; (e) `lib/recap/{step1,complete}-persist.ts` déjà strict via helpers typés (`buildTransferPayload`, `as MonthlyRecapInsert`). **Scope évolué** : 4 fichiers `app/api/monthly-recap/*` (5 sites cassés) + **1 site BONUS** `app/api/profile/route.ts:168` PUT (pattern différent — `Record<string, unknown>` au lieu de computed key). Aucun `pnpm.overrides` à modifier (jamais forcé Supabase). **Pattern uniforme** appliqué (cohérent recover/route.ts Sprint Lint-Baseline-Cleanup) : narrowing if/else explicit `context === 'profile' ? { profile_id: contextId, ... } : { group_id: contextId, ... }` typé via `TablesInsert<'X'>` / `TablesUpdate<'X'>` plutôt qu'un helper function `withOwner()` (cargo cult abstraction pour 5 sites — CLAUDE.md "Three similar lines is better than premature abstraction"). Pour les arrays via `.map()` (auto-balance L567 + L602), narrow dans le callback avec spread base ; pour le single object update-step L87 + transfer L114, narrow via `{ ...base, profile_id: contextId } : { ...base, group_id: contextId }` ; pour le single object accumulate-piggy-bank L85, narrow direct sans spread (2 champs only).
  - **Commit 1** (TBD-sha) `refactor(api): type strict 6 inserts/updates via TablesInsert/TablesUpdate` — 5 sites monthly-recap (accumulate-piggy-bank/auto-balance×2/transfer/update-step) + 1 site BONUS profile (PUT). Tous les sites ajoutent `import type { TablesInsert | TablesUpdate } from '@/lib/database.types'`. Préserve verbatim la logique métier (conditional sur `context`, `from_budget_id: null` pour tirelire, `monthly_recap_id: monthly_recap_id || null`, transfer_reason multilingual selon `transfer.source === 'savings'`, computed `transferDate` extracté en const dans transfer route pour partager entre base et conditional spread). Drive-by : `new Date().toISOString().split('T')[0]!` (avec `!` pour résoudre `noUncheckedIndexedAccess`) — préserve la sémantique runtime.
  - **Commit 2** (TBD-sha) `chore(deps): upgrade @supabase/supabase-js to 2.105.4 + lift Dependabot ignore` — `pnpm install @supabase/supabase-js@^2.105.3` résolu vers 2.105.4 ; package.json L38 + pnpm-lock.yaml updated. `.github/dependabot.yml` : 8 lignes de l'ignore rule `@supabase/supabase-js >=2.105.0` retirées (les 2 autres ignore rules `tailwindcss semver-major` + `eslint-config-next >=16.0.0` préservées). 17 packages adjacents installés (transitives `@supabase/auth-js` etc.) sans breakage runtime.
  - **Commit 3** (this) closeout CLAUDE.md §1 score + §2 stack version + §11 roadmap.
  - **Verif end-to-end** : `pnpm typecheck` exit 0 sur Supabase 2.105.4 (preuve que les 6 sites compilent strict avec `RejectExcessProperties`) ; `pnpm lint:check` 0 errors / **115 warnings stable** (vs 115 pré-sprint = 0 delta) ; `pnpm test:run` **343 passed / 69 skipped** (zéro régression vs baseline) ; `pnpm build` 52/52 routes exit 0 ; `pnpm verify` exit 0 (8 stages dont 13/13 functions versionnées) ; gated tests `SUPABASE_API_TESTS=1 + SUPABASE_RECAP_TESTS=1` lib/**tests**/api-regressions.test.ts + app/api/monthly-recap/process-step1/**tests**/ : **12/12 passed**. Counter `as unknown as SupabaseClient` reste à **0**. Counter `: any` ajouté à **0**.
  - **Trouvaille latérale CRITIQUE PRÉ-EXISTANTE** (hors scope) : `app/api/monthly-recap/complete/__tests__/route.integration.test.ts:444` CAS 3 mixed deficit + surplus échoue — `expensesAfter` length 1 attendu 0 (cleanup step pas complet sur ce cas). **Vérifié pré-existant** via `git stash` mes changes + downgrade Supabase 2.57.4 → toujours fail. Aucun lien avec ce sprint ; cassé probablement par un sprint ultérieur à Refactor-I6 (introduit avec les 5 caract tests gated 2026-05-14 → un sprint a depuis cassé l'invariant cleanup pour ce cas spécifique sans s'en rendre compte parce que CAS 3 utilise la branche `carry_forward` qui exerce un path différent de CAS 1 carry_forward simple). À investiguer dans un sprint dédié (lecture détaillée de `lib/recap/complete-persist.ts` cleanup DELETE step).
  - **Smoke browser deferred to user** : (a) `/monthly-recap` flow complet sur compte test (initialize → step1-data → process-step1 → update-step → step2-data → accumulate-piggy-bank → auto-balance OU transfer manual → complete) — vérifier toutes les routes touchées retournent 200 + DB rows correctes byte-identique pré-upgrade ; (b) `/settings` EditProfile (PUT /api/profile) — modifier first_name / last_name / salary individuellement — vérifier que skip-undefined fonctionne (partial updates) ; (c) `/dashboard` SavingsDistribution → transferts inter-budgets (POST /api/monthly-recap/transfer) — exercice du narrowing computed key sur context profile et group.
  - **Pattern → standard** : pour tout futur Supabase `.insert(...)` ou `.update(...)` avec un computed key dynamic `[ownerField]: contextId`, déclarer `TablesInsert<'X'>` / `TablesUpdate<'X'>` + narrowing if/else explicit `context === 'profile' ? { profile_id: ... } : { group_id: ... }`. Pour les `.map()` callbacks, narrow dans le callback avec spread base. Pour les `Record<string, unknown>` accumulators d'update partiel, typer explicitement avec `TablesUpdate<>`.
  - **Hors scope explicite** : investigation flake CAS 3 complete cleanup (sprint dédié) ; Sprint Tailwind-v4 (déféré 2026-05-07 ; ignore rule `tailwindcss semver-major` reste en place) ; eslint-config-next 16+ (toujours ignored, pinned à 15.0.0) ; Lot 6 console-cleanup final sweep ; OpenAPI / R10 audit ; balance/auto-balance/recover thin handler extraction (chantier I6 follow-up).
  - **Trade-off** : Sprint Supabase-Strict-Types est consolidation tooling, score reste **~99.999/100 stable** (pas de saut métier observable, mais le repo est désormais up-to-date avec Supabase major + bloqueur Sprint DX-Verify follow-up fermé après 1 semaine d'attente).
- ✅ **Sprint UserGroupsList-Cleanup** (livré 2026-05-14, plan implicite via `audit_2/03_haute_user-groups-list-view-members-leave.md` tagué STALE closed-by-deletion) : closeout d'un prompt issu de l'audit `audit_2/` qui demandait l'implémentation de 2 handlers `onClick` vides (L107 view members + L132 leave group) dans [components/groups/UserGroupsList.tsx](components/groups/UserGroupsList.tsx). **Phase 1 audit Explore × 1 cross-codebase a invalidé la prémisse du prompt** : `Grep "UserGroupsList" --glob '**/*.{ts,tsx}'` retourne uniquement le fichier lui-même (0 consumer applicatif). Les 5 hits totaux sont : 1 fichier source + 4 docs `audit_2/`. **Pas de `app/groups/` directory**, pas de dynamic import via `next/dynamic`. Vérification croisée des 4 autres composants `components/groups/*` confirme que `CreateGroupForm` + `GroupSearchList` + `DeleteGroupModal` + `GroupMembersWithContributionsModal` sont tous consommés par `app/settings/page.tsx` — seul `UserGroupsList` est isolé. **Trouvaille critique** : [app/settings/page.tsx:253-337](app/settings/page.tsx) rend déjà **inline** une UI quasi-identique (mêmes boutons "Voir membres" / "Supprimer" / "Quitter" / badge "Créateur" / mêmes handlers `setShowMembersModal(true)` / `openDeleteModal` / `handleLeaveGroup` tous fonctionnels) sur `currentGroup` singular — UserGroupsList était une variante prévue pour un futur multi-groups support qui n'a jamais matérialisé ([hooks/useGroups.ts:231](hooks/useGroups.ts#L231) `currentGroup: groups.length > 0 ? groups[0] : null` confirme l'app limite à 1 groupe par utilisateur). **User a délégué le choix Path A vs Path B vs Path C** ("Je te laisse choisir ce qui est le plus approprié pour mon application") → arbitré **Path B DELETE** per CLAUDE.md system prompt "Don't design for hypothetical future requirements" + "Don't add features beyond what the task requires" + 6 précédents codebase Path B (Lot 5b status-test / Lot 5c testSupabaseConnection / Atomicity-Savings v2 handlePiggyBankAction / Sprint Dead-Code-Purge ×3 / Audit-Closeout I3 monthly-recap-calculations). **7ᵉ application** du pattern. **Commit 1** ([TBD-sha](#)) `refactor(groups): drop unused UserGroupsList (dead code, 0 consumer)` — `git rm components/groups/UserGroupsList.tsx` (−157 LOC) + header `⚠️ STALE — closed-by-deletion (Path B) 2026-05-14` ajouté à `audit_2/03_haute_user-groups-list-view-members-leave.md` redirigeant vers cette entrée + recovery instructions. **Commit 2** (this) closeout CLAUDE.md §1 score paragraph étendu + §11 nouvelle entrée. **Verif end-to-end** : `pnpm typecheck` exit 0 (pas de cache stale Next.js parce que UserGroupsList n'était pas indexé par typegen — pas un route, juste un composant) ; `pnpm lint:check` 0 errors / **115 warnings stable** (vs 115 pré-sprint, baseline post-Refactor-I6 / Supabase-Strict-Types) ; `pnpm test:run` **343 passed / 69 skipped** (zéro régression vs baseline — 0 test n'exerçait UserGroupsList). **0 nouveau test** ajouté (Path B = pas d'implémentation à regression-guarder). **0 migration DB**. **0 RPC ajoutée/supprimée**. **0 modif ESLint config** (le glob `components/**` Lot 5 couvrait déjà UserGroupsList — la deletion ne change pas la config). **Smoke browser non requis** : le code supprimé n'avait aucun consumer UI. `/settings` continue de fonctionner identique via inline rendering + 4 modals déjà branchés. **Recovery path** : `git show <sha>:components/groups/UserGroupsList.tsx > components/groups/UserGroupsList.tsx` puis brancher dans un nouveau `app/groups/page.tsx` ou inliner le rendering dans une page existante. **Trade-off** : Sprint UserGroupsList-Cleanup est pure consolidation cleanup — pas de saut métier observable, mais ferme une dette latente (~157 LOC dead code + 2 TODO publics qui auraient continué à apparaître dans tout grep "TODO" + audit_2/03 prompt qui aurait continué à pop dans les inventaires). Score : ~99.999 → **~99.999/100 stable** (Path B closed-by-deletion ne fait jamais avancer le score métier, conformément aux 6 précédents).
- ✅ **Sprint Tailwind-v4** (livré 2026-05-14, plan dans `C:\Users\gille\.claude\plans\sprint-tailwind-v4-...md` — branche dédiée `sprint-tailwind-v4`) : ferme le bloqueur Sprint DX-Verify follow-up 2026-05-07 (hémorragie revert wave Dependabot, ignore rule `tailwindcss: update-types: ["version-update:semver-major"]` installée Sprint Stabilize-Deps S1). Migration `tailwindcss` 3.4.17 → 4.3.0 via `npx @tailwindcss/upgrade@4.3.0` tool. **Phase 1 audit a confirmé 3 findings critiques** : (a) `tailwindcss-animate` jamais installé (vérifié `pnpm ls` retourne nothing + 0 `@keyframes enter`/`exit` dans built CSS) — les classes `animate-in`/`fade-in-0`/`zoom-in-95`/`slide-in-from-*` utilisées dans [components/ui/dialog.tsx](components/ui/dialog.tsx) (shadcn wrapper) et [components/ui/drawer-content-classes.ts](components/ui/drawer-content-classes.ts) Sprint Zod-Rollout v9 étaient **dead code depuis Sprint Zod-Rollout v8** (Radix Dialog migration) — modals + drawers snap open/close sans animation ; (b) `0` plugin Tailwind dans `tailwind.config.ts` (`plugins: []`) — chantier beaucoup plus simple que le plan source ne le laissait craindre ; (c) `tailwind-merge@3.3.1` déjà v4-compatible + `prettier-plugin-tailwindcss@0.8.0` déjà latest (pas besoin de bumper, le tool a re-déclaré le bump mais le ^0.8.0 satisfait déjà v4). User a arbitré **Option β** pour les animations : install `tw-animate-css@1.4.0` (successeur officiel v4 de tailwindcss-animate) + `@import 'tw-animate-css'` dans globals.css. **3 commits sur `cleanup`** :
  - **Commit 1** ([70770c2](#)) `chore(deps): upgrade tailwindcss 3.4.17 → 4.3.0 via @tailwindcss/upgrade` — 54 fichiers / +415/-767 LOC nets. Tool migration mécanique : `tailwind.config.ts` SUPPRIMÉ (config CSS-first dans globals.css `@theme {}` block), `app/globals.css` `@tailwind base/components/utilities` → `@import 'tailwindcss'` + nouveau `@theme {}` block (CSS variables shadcn préservées) + v3-compat shim border-color (`border-gray-200, currentcolor`), `postcss.config.js` `tailwindcss + autoprefixer` → `@tailwindcss/postcss` only (autoprefixer retiré, v4 l'inclut nativement), `package.json` tailwindcss `^3.4.1` → `^4.3.0` + ajout `@tailwindcss/postcss` devDep + retrait `autoprefixer` devDep. 49 fichiers .tsx renames mécaniques : `outline-none` → `outline-hidden`, `shadow` → `shadow-sm`, `shadow-sm` → `shadow-xs` (shadow scale renamed v4). Pre-commit hook lint-staged a appliqué Prettier sur staged.
  - **Commit 2** ([e2a8e0e](#)) `chore(format): re-order Tailwind classes per v4 plugin convention` — 24 fichiers .tsx / +120/-125 LOC. Le `prettier-plugin-tailwindcss@0.8.0` lit la nouvelle config CSS-first via globals.css `@theme {}` et applique son ordering algorithm v4 : theme/color classes (`bg-background`, `text-muted-foreground`, `ring-offset-background`, `font-roboto`) déplacées en tête, `leading-tight` placé après `text-base/text-lg` avant `font-black/bold`. Le 2e passage est nécessaire parce que le plugin a un module cache : pendant lint-staged du Commit 1, il lisait encore l'ancien `tailwind.config.ts` (qui venait d'être supprimé) ; après le commit, il charge la nouvelle config CSS-first et produit le bon ordre v4. Pré-existants dirty (3 `__tests__/*.test.tsx` + 12 .ts/.md/.json/.css + next.config.js) restaurés via `git restore` pour rester chirurgical (chantier 16 hygiène git hors scope).
  - **Commit 3** ([b5c0537](#)) `feat(ui): restore modal/drawer animations via tw-animate-css` — 3 fichiers / +10 LOC. Install `tw-animate-css@1.4.0` devDep + `@import 'tw-animate-css'` dans globals.css. **Verif build** : built CSS contient désormais `@keyframes enter` + `@keyframes exit` (vs juste `pulse`/`spin` pré-fix) qui alimentent fade/zoom/slide animations sur 12 modals Radix migrés v8 + 2 drawers (PlanningDrawer + SavingsDistributionDrawer). Smoke browser à faire user-side pour valider visuellement.
  - **Commit 4** (this) closeout : (a) `.github/dependabot.yml` ignore rule `tailwindcss semver-major` retirée (commentaire mis à jour pointant vers ce sprint) ; (b) CLAUDE.md §1 score + §2 stack `Tailwind 3` → `Tailwind 4` + §11 nouvelle entrée + retrait entry ⏭️ ligne 832 conservée comme historical marker (convention miroir Sprint Supabase-Strict-Types qui garde ⏭️ ligne 833 même après ✅ livraison).
  - **Verif end-to-end** : `pnpm typecheck` exit 0 ; `pnpm lint:check` 0 errors / **115 warnings stable** (vs 115 pré-sprint = 0 delta — tous les `no-console` pré-existants, aucun nouveau) ; `pnpm test:run` **343 passed / 69 skipped** (vs 343/69 pré-sprint = zéro régression) ; `pnpm build` 52/52 routes exit 0 (vs 52 pré-sprint = stable). Working tree restauré aux pré-existants dirty post-sprint (3 test .tsx + 12 .ts/.md + next.config.js + audit_2/+doc2/ untracked) via `git stash pop` final pour rétablir l'état pré-sprint (le stash `sprint-tailwind-v4: stash pre-upgrade working tree` capturait les 7 M tracked + 14 D tracked + 24+ untracked existants).
  - **Smoke browser deferred to user** : (a) **animations** : ouvrir chaque modal (4 dashboard Add/Edit Budget/Income + 2 dashboard Add/EditTransaction + 3 groups GroupMembersWithContributions/DeleteGroup/ConfirmationDialog + 3 profile EditBalance/EditProfile/FirstTimeProfile = 12 modals total) → vérifier fade-in + zoom-in à l'ouverture + fade-out + zoom-out à la fermeture ; ouvrir 2 drawers (PlanningDrawer via FinancialIndicators bouton calendrier + SavingsDistributionDrawer via SavingsDrawer) → vérifier slide-in-from-bottom animation 300ms ; (b) **visuel** : visiter les 9 pages racine + dashboard (`/`, `/connexion`, `/inscription`, `/forgot-password`, `/reset-password`, `/dashboard`, `/group-dashboard`, `/monthly-recap`, `/settings`) → vérifier shadcn theme tokens (`bg-background`, `text-foreground`, `text-muted-foreground`, etc.) rendent correctement + focus colors (green-500/orange-500/etc.) intacts + suffixe € positionné correctement sur les inputs DecimalFormInput.
  - **Pattern v4 → standard du repo** : pour tout nouveau Tailwind utility class, vérifier dans la doc v4 (https://tailwindcss.com/docs) — les renames v3→v4 sont nombreux (shadow-sm/xs, outline-hidden, ring-1/2, etc.). Pour toute classe d'animation `animate-in`/`fade-*`/`zoom-*`/`slide-*`, elles sont fournies par `tw-animate-css` (installé v4-followup). Si une 3e drawer fullscreen bottom-up surface, importer `DRAWER_CONTENT_CLASSES` depuis [components/ui/drawer-content-classes.ts](components/ui/drawer-content-classes.ts) (Sprint Zod-Rollout v9 pattern). Si un signalement drag-to-dismiss arrive, envisager migration vers `vaul` (lib drawer dédiée, JSDoc trigger documenté dans drawer-content-classes.ts).
  - **Hors scope explicite** : (a) chantier 16 hygiène git working tree (24+ untracked + 7 M tracked + 14 D tracked + 27 prompts/.md dirty) — reste préservé via stash, non-touché ce sprint ; (b) Sprint Tailwind-v4-followup éventuel pour migrer le `.dark` mode si jamais déployé (CSS variables `.dark { ... }` dans globals.css sont préservées mais inactives — aucun `darkMode: 'class'` config) ; (c) shadcn/ui CLI re-init (non requis post-v4, theme tokens préservés via CSS variables) ; (d) **Smoke browser non-automatisé** — RTL ne teste pas l'animation visuelle, axe-core ne teste pas la perception. User valide manuellement.
  - **Trade-off** : Sprint Tailwind-v4 est **consolidation infra** + **fix latent animation bug**, score reste **~99.999/100 stable** (pas de saut métier observable, mais le repo est désormais sur Tailwind v4 + animations modal/drawer fonctionnelles + bloqueur Dependabot fermé après 7 jours d'attente). Pattern miroir Sprint Supabase-Strict-Types qui fermait son propre bloqueur DX-Verify follow-up le même jour.
